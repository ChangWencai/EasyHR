---
phase: quick-merge-attendance-compliance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/views/layout/AppLayout.vue
  - frontend/src/router/index.ts
  - frontend/src/views/attendance/AttendanceMonthly.vue
  - frontend/src/views/attendance/ComplianceOvertime.vue
  - frontend/src/views/attendance/ComplianceLeave.vue
  - frontend/src/views/attendance/ComplianceAnomaly.vue
  - frontend/src/components/attendance/ComplianceStatCard.vue
  - frontend/src/components/attendance/ComplianceTable.vue
  - frontend/src/components.d.ts
autonomous: true
must_haves:
  truths:
    - "合规报表菜单已消失，所有功能入口在考勤管理子菜单下"
    - "路由 /compliance/* 全部迁移到 /attendance/*，旧路径不可访问"
    - "出勤月报页面包含增强统计列（迟到/早退/年假/病假/事假/异常标记）和部门筛选"
    - "合规报表的三个页面（加班统计/请假合规/出勤异常）在新路径正常加载"
  artifacts:
    - path: "frontend/src/views/layout/AppLayout.vue"
      provides: "合并后的侧边栏菜单（考勤管理包含7个菜单项）"
      contains: "/attendance/overtime"
    - path: "frontend/src/router/index.ts"
      provides: "整合后的路由（/attendance/overtime, /attendance/leave, /attendance/anomaly）"
      contains: "attendance/overtime"
    - path: "frontend/src/views/attendance/AttendanceMonthly.vue"
      provides: "增强版出勤月报（合并 ComplianceMonthly 功能）"
      contains: "selectedDepts"
  key_links:
    - from: "frontend/src/router/index.ts"
      to: "frontend/src/views/attendance/ComplianceOvertime.vue"
      via: "dynamic import"
      pattern: "import.*ComplianceOvertime"
    - from: "frontend/src/views/attendance/ComplianceOvertime.vue"
      to: "frontend/src/components/attendance/ComplianceStatCard.vue"
      via: "component import"
      pattern: "import.*ComplianceStatCard.*attendance"
---

<objective>
将独立的「合规报表」一级菜单合并到「考勤管理」下，并将「出勤月报」和「月度考勤汇总」合并为一个增强版月报页面。

Purpose: 简化导航结构，减少一级菜单数量，将考勤相关功能集中管理，用户体验更直观。
Output: 合并后的菜单、路由、增强版月报页面、迁移后的合规报表视图和组件文件。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@frontend/src/views/layout/AppLayout.vue
@frontend/src/router/index.ts
@frontend/src/views/attendance/AttendanceMonthly.vue
@frontend/src/views/compliance/ComplianceMonthly.vue

<interfaces>
<!-- Key contracts the executor needs -->

From frontend/src/api/attendance.ts:
- attendanceApi.getMonthlyReport(params) -- existing AttendanceMonthly API
- attendanceApi.getComplianceMonthly(params) -- ComplianceMonthly API (takes dept_ids)
- attendanceApi.exportComplianceMonthly(params) -- enhanced export with dept filter
- attendanceApi.getDailyRecords(params) -- daily records for drawer
- attendanceApi.exportMonthlyExcel(params) -- basic monthly export
- type MonthlyReportItem -- current monthly report row type
- type MonthlyComplianceItem -- compliance monthly row type (has late_count, early_leave_count, annual_leave_days, sick_leave_days, personal_leave_days, is_anomaly)
- type ComplianceMonthlyStats -- stats type for compliance monthly
- type DailyRecord -- daily record type for drawer

From frontend/src/api/department.ts:
- departmentApi.list() -- returns { id: number; name: string }[]

From frontend/src/components/compliance/ComplianceStatCard.vue:
- Props: { value: string | number; label: string; icon: string; iconClass?: string }

From frontend/src/components/compliance/ComplianceTable.vue:
- Props: { data: any[]; total: number; page: number; pageSize: number; rowClassName?: fn }
- Emits: { 'page-change': [page: number] }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 迁移合规报表文件并更新 import 路径</name>
  <files>
    frontend/src/views/attendance/ComplianceOvertime.vue,
    frontend/src/views/attendance/ComplianceLeave.vue,
    frontend/src/views/attendance/ComplianceAnomaly.vue,
    frontend/src/components/attendance/ComplianceStatCard.vue,
    frontend/src/components/attendance/ComplianceTable.vue
  </files>
  <action>
1. 将 4 个合规报表视图文件复制到 attendance 目录：
   - cp frontend/src/views/compliance/ComplianceOvertime.vue frontend/src/views/attendance/ComplianceOvertime.vue
   - cp frontend/src/views/compliance/ComplianceLeave.vue frontend/src/views/attendance/ComplianceLeave.vue
   - cp frontend/src/views/compliance/ComplianceAnomaly.vue frontend/src/views/attendance/ComplianceAnomaly.vue

2. 将 2 个合规报表组件文件复制到 attendance 组件目录：
   - cp frontend/src/components/compliance/ComplianceStatCard.vue frontend/src/components/attendance/ComplianceStatCard.vue
   - cp frontend/src/components/compliance/ComplianceTable.vue frontend/src/components/attendance/ComplianceTable.vue

3. 更新 3 个视图文件中的 import 路径（从 @/components/compliance/ 改为 @/components/attendance/）：
   - frontend/src/views/attendance/ComplianceOvertime.vue: `import ComplianceStatCard from '@/components/attendance/ComplianceStatCard.vue'`
   - frontend/src/views/attendance/ComplianceLeave.vue: `import ComplianceStatCard from '@/components/attendance/ComplianceStatCard.vue'`
   - frontend/src/views/attendance/ComplianceAnomaly.vue: `import ComplianceStatCard from '@/components/attendance/ComplianceStatCard.vue'`

4. 删除旧的合规报表目录：
   - rm -rf frontend/src/views/compliance/
   - rm -rf frontend/src/components/compliance/

5. 更新 frontend/src/components.d.ts 中两行引用路径：
   - `ComplianceStatCard: typeof import('./components/compliance/ComplianceStatCard.vue')['default']` 改为 `'./components/attendance/ComplianceStatCard.vue'`
   - `ComplianceTable: typeof import('./components/compliance/ComplianceTable.vue')['default']` 改为 `'./components/attendance/ComplianceTable.vue'`
  </action>
  <verify>
    <automated>cd /Users/wencai/github/EasyHR/frontend && grep -c "components/compliance" src/views/attendance/ComplianceOvertime.vue src/views/attendance/ComplianceLeave.vue src/views/attendance/ComplianceAnomaly.vue src/components.d.ts 2>&1 | grep -v ":0$" || echo "All imports updated"</automated>
  </verify>
  <done>合规报表的 3 个视图文件和 2 个组件文件已迁移到 attendance 目录，import 路径已更新，旧的 compliance 目录已删除。</done>
</task>

<task type="auto">
  <name>Task 2: 更新路由和菜单</name>
  <files>
    frontend/src/router/index.ts,
    frontend/src/views/layout/AppLayout.vue
  </files>
  <action>
**router/index.ts 修改：**

1. 将 4 个 /compliance/* 路由迁移到 /attendance/* 下：
   - `/compliance/overtime` -> `/attendance/overtime`，name 改为 `attendance-overtime`，component import 路径改为 `@/views/attendance/ComplianceOvertime.vue`
   - `/compliance/leave` -> `/attendance/leave`，name 改为 `attendance-leave`，component import 路径改为 `@/views/attendance/ComplianceLeave.vue`
   - `/compliance/anomaly` -> `/attendance/anomaly`，name 改为 `attendance-anomaly`，component import 路径改为 `@/views/attendance/ComplianceAnomaly.vue`
   - `/compliance/monthly` 路由**删除**（功能合并到 /attendance/monthly）

2. 删除路由守卫中的 `/compliance` 检查（第 278 行 `to.path.startsWith('/compliance') ||`）

3. 路由迁移后的考勤路由区域应为：
```typescript
// 考勤管理
{ path: '/attendance/rule', name: 'attendance-rule', component: () => import('@/views/attendance/AttendanceRule.vue') },
{ path: '/attendance/clock-live', name: 'attendance-clock-live', component: () => import('@/views/attendance/ClockLive.vue') },
{ path: '/attendance/approval', name: 'attendance-approval', component: () => import('@/views/attendance/AttendanceApproval.vue') },
{ path: '/attendance/monthly', name: 'attendance-monthly', component: () => import('@/views/attendance/AttendanceMonthly.vue') },
{ path: '/attendance/overtime', name: 'attendance-overtime', component: () => import('@/views/attendance/ComplianceOvertime.vue') },
{ path: '/attendance/leave', name: 'attendance-leave', component: () => import('@/views/attendance/ComplianceLeave.vue') },
{ path: '/attendance/anomaly', name: 'attendance-anomaly', component: () => import('@/views/attendance/ComplianceAnomaly.vue') },
```

**AppLayout.vue 修改：**

4. 桌面端侧边栏（约第 57-75 行）：
   - 删除合规报表子菜单（`<el-sub-menu index="/compliance">` 整个块，第 66-75 行）
   - 在考勤管理子菜单中增加 5 个菜单项：
```html
<el-sub-menu index="/attendance">
  <template #title>
    <el-icon><Clock /></el-icon>
    <span>考勤管理</span>
  </template>
  <el-menu-item index="/attendance/rule">打卡规则</el-menu-item>
  <el-menu-item index="/attendance/clock-live">今日实况</el-menu-item>
  <el-menu-item index="/attendance/approval">审批管理</el-menu-item>
  <el-menu-item index="/attendance/overtime">加班统计</el-menu-item>
  <el-menu-item index="/attendance/leave">请假合规</el-menu-item>
  <el-menu-item index="/attendance/anomaly">出勤异常</el-menu-item>
  <el-menu-item index="/attendance/monthly">出勤月报</el-menu-item>
</el-sub-menu>
```

5. 移动端抽屉菜单（约第 178-190 行）：同样合并，删除合规报表子菜单，将菜单项加入考勤管理子菜单。

6. 更新 pageTitleMap：
   - 删除所有 `/compliance/*` 映射
   - 添加新映射：
```
'/attendance/approval': '审批管理',
'/attendance/overtime': '加班统计',
'/attendance/leave': '请假合规',
'/attendance/anomaly': '出勤异常',
'/attendance/monthly': '出勤月报',
```

7. 删除 Document icon import（第 232 行），因为合规报表子菜单是唯一使用 Document icon 的地方。
  </action>
  <verify>
    <automated>cd /Users/wencai/github/EasyHR/frontend && grep -c "/compliance" src/router/index.ts src/views/layout/AppLayout.vue 2>&1 | grep -v ":0$" || echo "All /compliance references removed"</automated>
  </verify>
  <done>路由 /compliance/* 已迁移到 /attendance/*，合规报表子菜单已删除并合并到考勤管理，Document icon import 已清理，pageTitleMap 已更新。</done>
</task>

<task type="auto">
  <name>Task 3: 增强出勤月报页面（合并 ComplianceMonthly 功能）</name>
  <files>
    frontend/src/views/attendance/AttendanceMonthly.vue
  </files>
  <action>
在现有 AttendanceMonthly.vue 基础上，增强以下功能：

1. **添加部门筛选器**：在 header-actions 中（月份选择器和导出按钮之间）添加 el-select 多选部门筛选：
```html
<el-select
  v-model="selectedDepts"
  multiple
  collapse-tags
  collapse-tags-tooltip
  placeholder="选择部门"
  clearable
  class="dept-select"
  @change="load(1)"
>
  <el-option v-for="d in deptOptions" :key="d.id" :label="d.name" :value="d.id" />
</el-select>
```
   - 添加 `selectedDepts` ref（`ref<number[]>([])`）
   - 添加 `deptOptions` ref（`ref<{ id: number; name: string }[]>([])`）
   - 在 onMounted 中调用 `departmentApi.list()` 加载部门选项
   - import `departmentApi` from `@/api/department`

2. **增强统计表格列**：在统计视图的 el-table 中，在现有"缺勤"列之后、"出勤率"列之前，添加以下列（来自 ComplianceMonthly）：
   - 迟到(次)：`row.late_count`，值 > 0 时添加 `metric-value--warning` 样式
   - 早退(次)：`row.early_leave_count`
   - 年假(天)：`row.annual_leave_days`
   - 病假(天)：`row.sick_leave_days`，值 > 0 时添加 `metric-value--danger` 样式
   - 事假(天)：`row.personal_leave_days`，值 > 0 时添加 `metric-value--warning` 样式
   - 异常标记：使用 el-tag（`row.is_anomaly ? 'danger' : 'success'`）

3. **更新数据加载逻辑**：将 load 函数改为调用 `attendanceApi.getComplianceMonthly`（代替原来的 `getMonthlyReport`），传入 `dept_ids` 参数：
```typescript
const deptIds = selectedDepts.value.length ? selectedDepts.value.join(',') : undefined
const { data } = await attendanceApi.getComplianceMonthly({
  year_month: selectedMonth.value,
  dept_ids: deptIds,
  page: p,
  page_size: pageSize,
})
```
   - 更新 list 类型为 `MonthlyComplianceItem[]`
   - 更新 stats 类型为 `ComplianceMonthlyStats`
   - import `MonthlyComplianceItem, ComplianceMonthlyStats` from `@/api/attendance`

4. **更新导出逻辑**：将 `handleExport` 改为调用 `attendanceApi.exportComplianceMonthly`（代替 `exportMonthlyExcel`），传入 `dept_ids`：
```typescript
const deptIds = selectedDepts.value.length ? selectedDepts.value.join(',') : undefined
const blob = await attendanceApi.exportComplianceMonthly({
  year_month: selectedMonth.value,
  dept_ids: deptIds,
}) as unknown as Blob
```
   - 导出文件名改为 `月度考勤汇总_${selectedMonth.value}.xlsx`

5. **添加异常行高亮样式**：在 el-table 上添加 `:row-class-name="getRowClassName"`，实现 `getRowClassName` 函数：
```typescript
function getRowClassName({ row }: { row: MonthlyComplianceItem }) {
  return row.is_anomaly ? 'anomaly-row' : ''
}
```
   - 添加非 scoped 样式块（与 ComplianceMonthly 一致）：
```scss
<style lang="scss">
.anomaly-row td {
  background-color: rgba(239, 68, 68, 0.04) !important;
}
</style>
```

6. **添加 dept-select CSS**：
```scss
.dept-select { width: 200px; }
```

7. **格子视图保持不变**：格子视图和打卡详情抽屉不做修改。

8. **保持视图切换 Tab**（统计视图 / 格子视图）不变。
  </action>
  <verify>
    <automated>cd /Users/wencai/github/EasyHR/frontend && grep -c "selectedDepts\|deptOptions\|getComplianceMonthly\|exportComplianceMonthly\|anomaly-row\|late_count\|annual_leave_days" src/views/attendance/AttendanceMonthly.vue</automated>
  </verify>
  <done>出勤月报页面已增强：包含部门筛选器、迟到/早退/年假/病假/事假列、异常行高亮、增强版 Excel 导出。格子视图和打卡详情抽屉保持不变。</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx vue-tsc --noEmit` -- TypeScript 编译无错误
2. 访问 /attendance/overtime、/attendance/leave、/attendance/anomaly 页面正常加载
3. 访问 /attendance/monthly 显示增强版月报（含部门筛选和更多列）
4. 访问 /compliance/* 应重定向或 404（旧路径已不可访问）
5. 侧边栏只有一个考勤管理子菜单，包含 7 个菜单项
</verification>

<success_criteria>
- 合规报表一级菜单已从侧边栏移除
- 考勤管理子菜单包含 7 个菜单项（打卡规则、今日实况、审批管理、加班统计、请假合规、出勤异常、出勤月报）
- 路由 /compliance/* 已全部迁移到 /attendance/* 下
- 出勤月报页面包含部门筛选、增强统计列（迟到/早退/年假/病假/事假/异常标记）
- 旧的 compliance 目录已完全删除
- TypeScript 编译通过
</success_criteria>

<output>
After completion, create `.planning/quick/260425-001-merge-attendance-compliance/260425-001-SUMMARY.md`
</output>
