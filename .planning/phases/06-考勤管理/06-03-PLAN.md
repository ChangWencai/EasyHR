---
phase: "06"
plan: "03"
type: execute
wave: 2
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - "internal/attendance/model.go"
  - "internal/attendance/approval_service.go"
  - "internal/attendance/dto.go"
  - "internal/attendance/repository.go"
  - "internal/attendance/service.go"
  - "internal/attendance/handler.go"
  - "frontend/src/views/attendance/AttendanceApproval.vue"
  - "frontend/src/components/attendance/ApprovalApplyDialog.vue"
  - "frontend/src/components/attendance/LeaveDurationCalc.vue"
  - "frontend/src/components/attendance/ApprovalTypeTag.vue"
  - "frontend/src/api/attendance.ts"
autonomous: true
requirements:
  - ATT-09
  - ATT-10
  - ATT-11
  - ATT-12
  - ATT-13
  - ATT-14
  - ATT-15
  - ATT-16
  - ATT-17
user_setup:
  - service: qmuntal/stateless
    why: "审批流状态机核心依赖"
    env_vars:
      - name: GOPRIVATE
        source: "go env GOPRIVATE (无需用户操作)"
    dashboard_config:
      - task: "确认已添加到 go.mod"
        location: "go.mod"
go_deps:
  - name: qmuntal/stateless
    version: v1.8.0
    reason: "审批流状态机，11种审批类型统一管理 per D-01"
must_haves:
  truths:
    - "员工可提交11种审批申请（补卡/调班/出差/外出/7种请假）"
    - "审批状态机管理：draft → pending → approved / rejected / cancelled / timeout"
    - "管理员可同意或驳回申请，审批列表显示待办条数"
    - "请假时长按 0.5h 取整显示（D-07），存储精确到 0.01h（D-08）"
  artifacts:
    - path: "internal/attendance/model.go"
      provides: "Approval 模型（审批记录）"
      contains: "Approval"
    - path: "internal/attendance/approval_service.go"
      provides: "审批流状态机服务（qmuntal/stateless）"
      exports: "ApprovalService, NewApprovalService, Approve, Reject, Cancel"
    - path: "internal/attendance/handler.go"
      provides: "Approval CRUD API"
      exports: "GET /api/v1/attendance/approvals, POST /api/v1/attendance/approvals, PUT /api/v1/attendance/approvals/:id/approve, PUT /api/v1/attendance/approvals/:id/reject, PUT /api/v1/attendance/approvals/:id/cancel, GET /api/v1/attendance/approvals/pending-count"
    - path: "frontend/src/views/attendance/AttendanceApproval.vue"
      provides: "考勤审批页面（3-Tab + 行内操作）"
    - path: "frontend/src/components/attendance/ApprovalApplyDialog.vue"
      provides: "新建申请弹窗（11种类型 + 时长自动计算）"
  key_links:
    - from: "internal/attendance/approval_service.go"
      to: "internal/attendance/repository.go"
      via: "ApprovalRepository"
      pattern: "repo\\."
    - from: "frontend/src/views/attendance/AttendanceApproval.vue"
      to: "frontend/src/api/attendance.ts"
      via: "listApprovals|approveApproval|cancelApproval"
      pattern: "attendanceApi\\.(listApprovals|approve|cancel)"
    - from: "frontend/src/components/attendance/ApprovalApplyDialog.vue"
      to: "frontend/src/api/attendance.ts"
      via: "createApproval"
      pattern: "attendanceApi\\.createApproval"
---

<objective>
完成考勤审批流功能。员工可提交11种审批申请（补卡/调班/出差/外出/7种请假），使用 qmuntal/stateless v1.8.0 状态机统一管理审批状态（draft → pending → approved/rejected/cancelled/timeout）。管理员可同意/驳回申请，审批列表显示待办条数。请假时长自动计算，按 0.5h 取整显示。

**Scope:** ATT-09~ATT-17, D-01, D-02, D-07, D-08
**Purpose:** 考勤数据来源之一，审批通过后更新 ClockRecord 和 AttendanceMonthly
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-考勤管理/06-CONTEXT.md
@.planning/phases/06-考勤管理/06-UI-SPEC.md

## 关键决策（D-XX）

- **D-01:** 使用 `qmuntal/stateless` v1.8.0 状态机库
- **D-02:** 状态转换：draft → pending → approved / rejected / cancelled / timeout
- **D-03:** 守卫条件：仅审批人在 pending 状态下可操作 approve/reject；申请人可取消自己的申请
- **D-07:** 加班时长按 0.5h（半小时）取整
- **D-08:** 存储精确到 0.01h，显示时四舍五入到 0.5h

## qmuntal/stateless 状态机模式

```go
import "github.com/qmuntal/stateless"
import "github.com/qmuntal/stateless/tuple"

// 状态定义
const (
    StatusDraft     = "draft"
    StatusPending   = "pending"
    StatusApproved  = "approved"
    StatusRejected  = "rejected"
    StatusCancelled = "cancelled"
    StatusTimeout   = "timeout"
)

// 触发器定义
const (
    TriggerSubmit  = "submit"
    TriggerApprove = "approve"
    TriggerReject  = "reject"
    TriggerCancel  = "cancel"
    TriggerTimeout = "timeout"
)

// 审批类型（11种）
const (
    ApprovalTypeMakeup      = "makeup"       // 补卡申请
    ApprovalTypeShiftSwap   = "shift_swap"   // 调班申请
    ApprovalTypeBusinessTrip = "business_trip" // 出差申请
    ApprovalTypeOutside     = "outside"      // 外出申请
    ApprovalTypePersonalLeave = "personal_leave" // 事假
    ApprovalTypeSickLeave    = "sick_leave"   // 病假
    ApprovalTypePTO         = "PTO"          // 调休假
    ApprovalTypeAnnualLeave = "annual_leave" // 年假
    ApprovalTypeMarriageLeave = "marriage_leave" // 婚假
    ApprovalTypeMaternityLeave = "maternity_leave" // 产假
    ApprovalTypePaternityLeave = "paternity_leave" // 陪产假
    ApprovalTypeOvertime    = "overtime"     // 加班申请
)
```

## 参考模式

From `internal/finance/service_expense.go` — 审批流字段结构：
```go
// 审批记录基础字段（可复用）
EmployeeID     int64
ApprovalType   string
StartTime      time.Time
EndTime        time.Time
Duration       float64
Reason         string
Status         string
ApproverID     *int64
ApprovedAt     *time.Time
RejectedAt     *time.Time
RejectedNote   string
Attachments    string // JSON 数组
```

From `frontend/src/views/employee/OffboardingList.vue` — 审批列表行内按钮：
```vue
<el-popconfirm :title="确认同意？" @confirm="handleApprove(row.id)">
  <template #reference>
    <el-button size="small" type="primary">同意</el-button>
  </template>
</el-popconfirm>
<el-button size="small" type="danger" @click="openRejectDialog(row)">驳回</el-button>
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: 添加 Approval 模型 + qmuntal/stateless 状态机服务</name>
  <files>
    - internal/attendance/model.go
    - internal/attendance/approval_service.go
    - internal/attendance/repository.go
    - internal/attendance/dto.go
  </files>
  <action>
## 添加 Go 依赖

```bash
cd /Users/wencai/github/EasyHR && go get github.com/qmuntal/stateless@v1.8.0
```

## backend/internal/attendance/model.go

在文件底部添加：

```go
// Approval 审批记录（请假/加班/补卡/调班/出差/外出）
type Approval struct {
    model.BaseModel
    EmployeeID     int64      `gorm:"column:employee_id;not null;index;comment:申请员工ID"`
    ApprovalType   string     `gorm:"column:approval_type;type:varchar(30);not null;index;comment:审批类型"`
    StartTime      time.Time  `gorm:"column:start_time;not null;comment:开始时间"`
    EndTime        time.Time  `gorm:"column:end_time;not null;comment:结束时间"`
    Duration       float64    `gorm:"column:duration;comment:时长（小时）精确到0.01 D-08"`
    Reason         string     `gorm:"column:reason;type:varchar(500);comment:事由"`
    LeaveType      string     `gorm:"column:leave_type;type:varchar(30);comment:请假类型（7种请假类型）"`
    Status         string     `gorm:"column:status;type:varchar(20);not null;default:draft;index;comment:状态 D-02"`
    ApproverID     *int64     `gorm:"column:approver_id;comment:审批人ID"`
    ApprovedAt     *time.Time `gorm:"column:approved_at;comment:审批通过时间"`
    RejectedAt     *time.Time `gorm:"column:rejected_at;comment:审批驳回时间"`
    RejectedNote   string     `gorm:"column:rejected_note;type:varchar(500);comment:驳回原因"`
    CancelledAt    *time.Time `gorm:"column:cancelled_at;comment:撤回时间"`
    ExpiredAt      *time.Time `gorm:"column:expired_at;comment:过期时间"`
    Attachments    string     `gorm:"column:attachments;type:text;comment:附件URLs JSON数组"`
    CCUserIDs      string     `gorm:"column:cc_user_ids;type:text;comment:抄送人ID列表 JSON"`
}

func (Approval) TableName() string { return "attendance_approvals" }

// 审批状态常量
const (
    ApprovalStatusDraft     = "draft"
    ApprovalStatusPending   = "pending"
    ApprovalStatusApproved  = "approved"
    ApprovalStatusRejected  = "rejected"
    ApprovalStatusCancelled = "cancelled"
    ApprovalStatusTimeout   = "timeout"
)

// 审批类型常量
const (
    ApprovalTypeMakeup       = "makeup"
    ApprovalTypeShiftSwap   = "shift_swap"
    ApprovalTypeBusinessTrip = "business_trip"
    ApprovalTypeOutside     = "outside"
    ApprovalTypePersonalLeave = "personal_leave"
    ApprovalTypeSickLeave    = "sick_leave"
    ApprovalTypePTO         = "PTO"
    ApprovalTypeAnnualLeave = "annual_leave"
    ApprovalTypeMarriageLeave = "marriage_leave"
    ApprovalTypeMaternityLeave = "maternity_leave"
    ApprovalTypePaternityLeave = "paternity_leave"
    ApprovalTypeOvertime    = "overtime"
)

// 审批类型名称映射（用于前端展示）
var ApprovalTypeNameMap = map[string]string{
    ApprovalTypeMakeup:       "补卡申请",
    ApprovalTypeShiftSwap:   "调班申请",
    ApprovalTypeBusinessTrip: "出差申请",
    ApprovalTypeOutside:     "外出申请",
    ApprovalTypePersonalLeave: "事假",
    ApprovalTypeSickLeave:    "病假",
    ApprovalTypePTO:         "调休",
    ApprovalTypeAnnualLeave: "年假",
    ApprovalTypeMarriageLeave: "婚假",
    ApprovalTypeMaternityLeave: "产假",
    ApprovalTypePaternityLeave: "陪产假",
    ApprovalTypeOvertime:    "加班申请",
}

// AutoMigrateTables 补充 Approval
func AutoMigrateTables(db *gorm.DB) error {
    return db.AutoMigrate(&AttendanceRule{}, &Shift{}, &Schedule{}, &ClockRecord{}, &Approval{})
}
```

## backend/internal/attendance/dto.go

在文件底部添加：

```go
// === Approval DTO ===

type ApprovalResponse struct {
    ID           int64    `json:"id"`
    EmployeeID   int64    `json:"employee_id"`
    EmployeeName string  `json:"employee_name"`
    ApprovalType string  `json:"approval_type"`
    TypeName     string  `json:"type_name"`
    StartTime    string  `json:"start_time"`
    EndTime      string  `json:"end_time"`
    Duration     float64 `json:"duration"`
    LeaveType    string  `json:"leave_type,omitempty"`
    Reason       string  `json:"reason"`
    Status       string  `json:"status"`
    ApproverID   *int64  `json:"approver_id,omitempty"`
    ApproverName string  `json:"approver_name,omitempty"`
    ApprovedAt   string  `json:"approved_at,omitempty"`
    RejectedAt   string  `json:"rejected_at,omitempty"`
    RejectedNote string  `json:"rejected_note,omitempty"`
    CancelledAt  string  `json:"cancelled_at,omitempty"`
    Attachments  []string `json:"attachments"`
    CreatedAt    string  `json:"created_at"`
}

type ApprovalListResponse struct {
    List  []ApprovalResponse `json:"list"`
    Total int64             `json:"total"`
    Page  int               `json:"page"`
}

type PendingCountResponse struct {
    PendingCount int64 `json:"pending_count"`
}

type CreateApprovalRequest struct {
    ApprovalType string   `json:"approval_type" binding:"required"`
    StartTime    string   `json:"start_time" binding:"required"`
    EndTime      string   `json:"end_time" binding:"required"`
    Reason       string   `json:"reason"`
    LeaveType    string   `json:"leave_type"`    // 7种请假类型
    Attachments  []string `json:"attachments"`   // 附件 URL 列表
    CCUserIDs    []int64  `json:"cc_user_ids"`   // 抄送人
}

type RejectApprovalRequest struct {
    Note string `json:"note"` // 驳回原因（选填）
}
```

## backend/internal/attendance/repository.go

在文件底部添加：

```go
// --- Approval ---

func (r *AttendanceRepository) CreateApproval(approval *Approval) error {
    return r.db.Create(approval).Error
}

func (r *AttendanceRepository) GetApproval(orgID int64, id int64) (*Approval, error) {
    var approval Approval
    err := r.db.Scopes(r.orgScope(orgID)).First(&approval, id).Error
    if err == gorm.ErrRecordNotFound {
        return nil, nil
    }
    return &approval, err
}

func (r *AttendanceRepository) ListApprovals(orgID int64, status, approvalType string, employeeID *int64, page, pageSize int) ([]Approval, int64, error) {
    query := r.db.Scopes(r.orgScope(orgID))
    if status != "" {
        query = query.Where("status = ?", status)
    }
    if approvalType != "" {
        query = query.Where("approval_type = ?", approvalType)
    }
    if employeeID != nil {
        query = query.Where("employee_id = ?", *employeeID)
    }
    var total int64
    query.Model(&Approval{}).Count(&total)
    var approvals []Approval
    offset := (page - 1) * pageSize
    err := query.Order("created_at DESC").Offset(offset).Limit(pageSize).Find(&approvals).Error
    return approvals, total, err
}

func (r *AttendanceRepository) ListPendingApprovals(orgID int64, approverID int64) ([]Approval, error) {
    var approvals []Approval
    err := r.db.Scopes(r.orgScope(orgID)).
        Where("status = ?", ApprovalStatusPending).
        Order("created_at ASC").
        Find(&approvals).Error
    return approvals, err
}

func (r *AttendanceRepository) CountPendingApprovals(orgID int64) (int64, error) {
    var count int64
    err := r.db.Scopes(r.orgScope(orgID)).
        Model(&Approval{}).
        Where("status = ?", ApprovalStatusPending).
        Count(&count).Error
    return count, err
}

func (r *AttendanceRepository) UpdateApproval(approval *Approval) error {
    return r.db.Save(approval).Error
}

// ListApprovalsByEmployeeMonth 聚合假勤统计数据（供 Plan 02 假勤统计使用）
func (r *AttendanceRepository) ListApprovalsByEmployeeMonth(orgID int64, employeeID int64, yearMonth string) ([]Approval, error) {
    startDate := yearMonth + "-01"
    endDate := yearMonth + "-31"
    var approvals []Approval
    err := r.db.Scopes(r.orgScope(orgID)).
        Where("employee_id = ? AND status IN (?, ?) AND start_time >= ? AND start_time <= ?",
            employeeID, ApprovalStatusApproved, ApprovalStatusPending,
            startDate, endDate).
        Find(&approvals).Error
    return approvals, err
}
```

## backend/internal/attendance/approval_service.go

创建文件，实现 qmuntal/stateless 状态机：

```go
package attendance

import (
    "encoding/json"
    "fmt"
    "time"

    "github.com/qmuntal/stateless"
)

// ApprovalStateMachine 基于 qmuntal/stateless 的审批状态机
// 状态转换：draft → pending → approved / rejected / cancelled / timeout
type ApprovalStateMachine struct {
    sm       *stateless.StateMachine
    approval *Approval
}

// NewApprovalStateMachine 创建审批状态机
func NewApprovalStateMachine(approval *Approval) *ApprovalStateMachine {
    sm := stateless.NewStateMachine(approval.Status)

    // 触发器
    const (
        TriggerSubmit  = "submit"
        TriggerApprove = "approve"
        TriggerReject  = "reject"
        TriggerCancel  = "cancel"
        TriggerTimeout = "timeout"
    )

    // 状态转换规则 per D-02
    sm.Configure(ApprovalStatusDraft).
        Permit(TriggerSubmit, ApprovalStatusPending)

    sm.Configure(ApprovalStatusPending).
        Permit(TriggerApprove, ApprovalStatusApproved).
        Permit(TriggerReject, ApprovalStatusRejected).
        Permit(TriggerCancel, ApprovalStatusCancelled).
        Permit(TriggerTimeout, ApprovalStatusTimeout)

    // 终态不可转换
    sm.Configure(ApprovalStatusApproved)
    sm.Configure(ApprovalStatusRejected)
    sm.Configure(ApprovalStatusCancelled)
    sm.Configure(ApprovalStatusTimeout)

    return &ApprovalStateMachine{sm: sm, approval: approval}
}

// CanApprove 检查当前用户是否可以审批
func (asm *ApprovalStateMachine) CanApprove(approverID int64) bool {
    if asm.approval.Status != ApprovalStatusPending {
        return false
    }
    // 守卫条件 D-03：仅审批人在 pending 状态下可审批
    // 当前实现：允许 ADMIN/OWNER 角色审批（具体 approverID 逻辑由调用方判断）
    return true
}

// CanCancel 检查申请人是否可以取消
func (asm *ApprovalStateMachine) CanCancel(requesterID int64) bool {
    if asm.approval.Status != ApprovalStatusPending {
        return false
    }
    // 守卫条件 D-03：申请人可取消自己的申请
    return asm.approval.EmployeeID == requesterID
}

// Submit 提交申请
func (asm *ApprovalStateMachine) Submit() error {
    if !asm.sm.CanFire("submit") {
        return fmt.Errorf("当前状态不允许提交")
    }
    asm.sm.Fire("submit")
    asm.approval.Status = asm.sm.MustState()
    return nil
}

// Approve 审批通过
func (asm *ApprovalStateMachine) Approve(approverID int64) error {
    if !asm.sm.CanFire("approve") {
        return fmt.Errorf("当前状态不允许审批")
    }
    now := time.Now()
    asm.approval.Status = ApprovalStatusApproved
    asm.approval.ApproverID = &approverID
    asm.approval.ApprovedAt = &now
    asm.sm.SetState(ApprovalStatusApproved)
    return nil
}

// Reject 审批驳回
func (asm *ApprovalStateMachine) Reject(approverID int64, note string) error {
    if !asm.sm.CanFire("reject") {
        return fmt.Errorf("当前状态不允许驳回")
    }
    now := time.Now()
    asm.approval.Status = ApprovalStatusRejected
    asm.approval.ApproverID = &approverID
    asm.approval.RejectedAt = &now
    asm.approval.RejectedNote = note
    asm.sm.SetState(ApprovalStatusRejected)
    return nil
}

// Cancel 撤回申请
func (asm *ApprovalStateMachine) Cancel(requesterID int64) error {
    if !asm.CanCancel(requesterID) {
        return fmt.Errorf("无权撤回此申请")
    }
    now := time.Now()
    asm.approval.Status = ApprovalStatusCancelled
    asm.approval.CancelledAt = &now
    asm.sm.SetState(ApprovalStatusCancelled)
    return nil
}

// ApprovalService 审批流服务
type ApprovalService struct {
    repo *AttendanceRepository
}

// NewApprovalService 创建审批服务
func NewApprovalService(repo *AttendanceRepository) *ApprovalService {
    return &ApprovalService{repo: repo}
}

// CalculateDuration 计算审批时长（小时），存储精确到 0.01h（D-08）
func (s *ApprovalService) CalculateDuration(startTime, endTime time.Time, leaveType string) float64 {
    duration := endTime.Sub(startTime).Hours()
    if duration < 0 {
        return 0
    }
    // 存储精确到 0.01h
    return float64(int(duration*100)) / 100
}

// RoundDurationForDisplay 显示时四舍五入到 0.5h（D-07）
func RoundDurationForDisplay(duration float64) float64 {
    return float64(int(duration/0.5+0.5)) * 0.5
}

// CreateApproval 创建审批申请
func (s *ApprovalService) CreateApproval(ctx context.Context, orgID, employeeID int64, req *CreateApprovalRequest) (*ApprovalResponse, error) {
    startTime, err := time.Parse(time.RFC3339, req.StartTime)
    if err != nil {
        return nil, fmt.Errorf("开始时间格式错误: %w", err)
    }
    endTime, err := time.Parse(time.RFC3339, req.EndTime)
    if err != nil {
        return nil, fmt.Errorf("结束时间格式错误: %w", err)
    }
    if endTime.Before(startTime) {
        return nil, fmt.Errorf("结束时间不能早于开始时间")
    }

    duration := s.CalculateDuration(startTime, endTime, req.LeaveType)
    attachmentsJSON, _ := json.Marshal(req.Attachments)
    ccJSON, _ := json.Marshal(req.CCUserIDs)

    approval := &Approval{
        BaseModel: model.BaseModel{OrgID: orgID, CreatedBy: employeeID, UpdatedBy: employeeID},
        EmployeeID:   employeeID,
        ApprovalType: req.ApprovalType,
        StartTime:    startTime,
        EndTime:      endTime,
        Duration:     duration,
        Reason:       req.Reason,
        LeaveType:   req.LeaveType,
        Status:      ApprovalStatusDraft,
        Attachments: string(attachmentsJSON),
        CCUserIDs:   string(ccJSON),
    }

    // 状态机提交
    sm := NewApprovalStateMachine(approval)
    if err := sm.Submit(); err != nil {
        return nil, fmt.Errorf("提交申请失败: %w", err)
    }

    if err := s.repo.CreateApproval(approval); err != nil {
        return nil, fmt.Errorf("创建申请记录失败: %w", err)
    }

    return toApprovalResponse(approval), nil
}

// Approve 审批通过
func (s *ApprovalService) Approve(ctx context.Context, orgID, approverID, approvalID int64) (*ApprovalResponse, error) {
    approval, err := s.repo.GetApproval(orgID, approvalID)
    if err != nil {
        return nil, fmt.Errorf("获取申请失败: %w", err)
    }
    if approval == nil {
        return nil, fmt.Errorf("申请不存在")
    }

    sm := NewApprovalStateMachine(approval)
    if err := sm.Approve(approverID); err != nil {
        return nil, fmt.Errorf("审批失败: %w", err)
    }

    approval.UpdatedBy = approverID
    if err := s.repo.UpdateApproval(approval); err != nil {
        return nil, fmt.Errorf("更新申请状态失败: %w", err)
    }

    return toApprovalResponse(approval), nil
}

// Reject 审批驳回
func (s *ApprovalService) Reject(ctx context.Context, orgID, approverID, approvalID int64, note string) (*ApprovalResponse, error) {
    approval, err := s.repo.GetApproval(orgID, approvalID)
    if err != nil {
        return nil, fmt.Errorf("获取申请失败: %w", err)
    }
    if approval == nil {
        return nil, fmt.Errorf("申请不存在")
    }

    sm := NewApprovalStateMachine(approval)
    if err := sm.Reject(approverID, note); err != nil {
        return nil, fmt.Errorf("驳回失败: %w", err)
    }

    approval.UpdatedBy = approverID
    if err := s.repo.UpdateApproval(approval); err != nil {
        return nil, fmt.Errorf("更新申请状态失败: %w", err)
    }

    return toApprovalResponse(approval), nil
}

// Cancel 撤回申请
func (s *ApprovalService) Cancel(ctx context.Context, orgID, requesterID, approvalID int64) (*ApprovalResponse, error) {
    approval, err := s.repo.GetApproval(orgID, approvalID)
    if err != nil {
        return nil, fmt.Errorf("获取申请失败: %w", err)
    }
    if approval == nil {
        return nil, fmt.Errorf("申请不存在")
    }

    sm := NewApprovalStateMachine(approval)
    if err := sm.Cancel(requesterID); err != nil {
        return nil, fmt.Errorf("撤回失败: %w", err)
    }

    approval.UpdatedBy = requesterID
    if err := s.repo.UpdateApproval(approval); err != nil {
        return nil, fmt.Errorf("更新申请状态失败: %w", err)
    }

    return toApprovalResponse(approval), nil
}

// ListApprovals 查询审批列表
func (s *ApprovalService) ListApprovals(ctx context.Context, orgID int64, status, approvalType string, employeeID *int64, page, pageSize int) (*ApprovalListResponse, error) {
    approvals, total, err := s.repo.ListApprovals(orgID, status, approvalType, employeeID, page, pageSize)
    if err != nil {
        return nil, fmt.Errorf("查询申请列表失败: %w", err)
    }
    list := make([]ApprovalResponse, len(approvals))
    for i, a := range approvals {
        list[i] = *toApprovalResponse(&a)
    }
    return &ApprovalListResponse{List: list, Total: total, Page: page}, nil
}

// GetPendingCount 获取待审批条数
func (s *ApprovalService) GetPendingCount(ctx context.Context, orgID int64) (int64, error) {
    return s.repo.CountPendingApprovals(orgID)
}

func toApprovalResponse(a *Approval) *ApprovalResponse {
    var attachments []string
    json.Unmarshal([]byte(a.Attachments), &attachments)
    var typeName string
    if name, ok := ApprovalTypeNameMap[a.ApprovalType]; ok {
        typeName = name
    } else {
        typeName = a.ApprovalType
    }
    resp := &ApprovalResponse{
        ID:           a.ID,
        EmployeeID:   a.EmployeeID,
        ApprovalType: a.ApprovalType,
        TypeName:     typeName,
        StartTime:    a.StartTime.Format(time.RFC3339),
        EndTime:      a.EndTime.Format(time.RFC3339),
        Duration:     a.Duration,
        LeaveType:    a.LeaveType,
        Reason:       a.Reason,
        Status:       a.Status,
        ApproverID:   a.ApproverID,
        ApprovedAt:   formatTimePtr(a.ApprovedAt),
        RejectedAt:   formatTimePtr(a.RejectedAt),
        RejectedNote: a.RejectedNote,
        CancelledAt:  formatTimePtr(a.CancelledAt),
        Attachments:  attachments,
        CreatedAt:    a.CreatedAt.Format("2006-01-02 15:04:05"),
    }
    return resp
}

func formatTimePtr(t *time.Time) string {
    if t == nil {
        return ""
    }
    return t.Format("2006-01-02 15:04:05")
}
```

**Important:** 在 `cmd/server/main.go` 中注册 ApprovalService：
```go
// 在 attendance 模块初始化部分
approvalSvc := attendance.NewApprovalService(attendanceRepo)
attendance.RegisterApprovalRoutes(apiV1, approvalSvc)
```
  </action>
  <verify>
    <automated>cd /Users/wencai/github/EasyHR && go get github.com/qmuntal/stateless@v1.8.0 && go build ./internal/attendance/...</automated>
  </verify>
  <done>
    qmuntal/stateless 状态机服务完成，Approval 模型和 Repository 层完成
  </done>
</task>

<task type="auto">
  <name>Task 2: 注册 Approval Handler 路由 + 更新 Service</name>
  <files>
    - internal/attendance/handler.go
    - internal/attendance/service.go
  </files>
  <action>
## backend/internal/attendance/handler.go

在 `RegisterRoutes` 函数中补充路由：

```go
// 审批流（使用独立的 ApprovalService）
func RegisterApprovalRoutes(r *gin.RouterGroup, approvalSvc *ApprovalService) {
    g := r.Group("/attendance")
    {
        // 获取待审批条数（用于侧边栏 Badge）
        g.GET("/approvals/pending-count", middleware.RequireAuth, getPendingCountHandler(approvalSvc))
        // 审批列表
        g.GET("/approvals", middleware.RequireAuth, listApprovalsHandler(approvalSvc))
        // 创建申请
        g.POST("/approvals", middleware.RequireAuth, createApprovalHandler(approvalSvc))
        // 同意申请
        g.PUT("/approvals/:id/approve", middleware.RequireAuth, approveApprovalHandler(approvalSvc))
        // 驳回申请
        g.PUT("/approvals/:id/reject", middleware.RequireAuth, rejectApprovalHandler(approvalSvc))
        // 撤回申请
        g.PUT("/approvals/:id/cancel", middleware.RequireAuth, cancelApprovalHandler(approvalSvc))
    }
}
```

添加 handler 函数：

```go
func getPendingCountHandler(svc *ApprovalService) gin.HandlerFunc {
    return func(c *gin.Context) {
        count, err := svc.GetPendingCount(c.Request.Context(), getOrgID(c))
        if err != nil {
            c.JSON(http.StatusInternalServerError, gin.H{"error": err.Error()})
            return
        }
        c.JSON(http.StatusOK, gin.H{"data": PendingCountResponse{PendingCount: count}})
    }
}

func listApprovalsHandler(svc *ApprovalService) gin.HandlerFunc {
    return func(c *gin.Context) {
        status := c.Query("status")
        approvalType := c.Query("approval_type")
        employeeIDStr := c.Query("employee_id")
        page, _ := strconv.Atoi(c.DefaultQuery("page", "1"))
        pageSize, _ := strconv.Atoi(c.DefaultQuery("page_size", "20"))

        var employeeID *int64
        if employeeIDStr != "" {
            if id, err := strconv.ParseInt(employeeIDStr, 10, 64); err == nil {
                employeeID = &id
            }
        }

        result, err := svc.ListApprovals(c.Request.Context(), getOrgID(c), status, approvalType, employeeID, page, pageSize)
        if err != nil {
            c.JSON(http.StatusInternalServerError, gin.H{"error": err.Error()})
            return
        }
        c.JSON(http.StatusOK, gin.H{"data": result})
    }
}

func createApprovalHandler(svc *ApprovalService) gin.HandlerFunc {
    return func(c *gin.Context) {
        var req CreateApprovalRequest
        if err := c.ShouldBindJSON(&req); err != nil {
            c.JSON(http.StatusBadRequest, gin.H{"error": "参数错误: " + err.Error()})
            return
        }
        result, err := svc.CreateApproval(c.Request.Context(), getOrgID(c), getUserID(c), &req)
        if err != nil {
            c.JSON(http.StatusInternalServerError, gin.H{"error": err.Error()})
            return
        }
        c.JSON(http.StatusCreated, gin.H{"data": result})
    }
}

func approveApprovalHandler(svc *ApprovalService) gin.HandlerFunc {
    return func(c *gin.Context) {
        id, _ := strconv.ParseInt(c.Param("id"), 10, 64)
        result, err := svc.Approve(c.Request.Context(), getOrgID(c), getUserID(c), id)
        if err != nil {
            c.JSON(http.StatusInternalServerError, gin.H{"error": err.Error()})
            return
        }
        c.JSON(http.StatusOK, gin.H{"data": result})
    }
}

func rejectApprovalHandler(svc *ApprovalService) gin.HandlerFunc {
    return func(c *gin.Context) {
        id, _ := strconv.ParseInt(c.Param("id"), 10, 64)
        var req RejectApprovalRequest
        c.ShouldBindJSON(&req) // note 为选填
        result, err := svc.Reject(c.Request.Context(), getOrgID(c), getUserID(c), id, req.Note)
        if err != nil {
            c.JSON(http.StatusInternalServerError, gin.H{"error": err.Error()})
            return
        }
        c.JSON(http.StatusOK, gin.H{"data": result})
    }
}

func cancelApprovalHandler(svc *ApprovalService) gin.HandlerFunc {
    return func(c *gin.Context) {
        id, _ := strconv.ParseInt(c.Param("id"), 10, 64)
        result, err := svc.Cancel(c.Request.Context(), getOrgID(c), getUserID(c), id)
        if err != nil {
            c.JSON(http.StatusInternalServerError, gin.H{"error": err.Error()})
            return
        }
        c.JSON(http.StatusOK, gin.H{"data": result})
    }
}
```

## backend/internal/attendance/service.go

在 `GetLeaveStats` 中补充真实数据聚合（调用 Approval 表）：

找到 `GetLeaveStats` 函数，替换为：

```go
// GetLeaveStats 获取员工假勤统计数据（从 Approval 表聚合 per ATT-07）
func (s *AttendanceService) GetLeaveStats(ctx context.Context, orgID int64, employeeID int64, yearMonth string) (*LeaveStatsResponse, error) {
    approvals, err := s.repo.ListApprovalsByEmployeeMonth(orgID, employeeID, yearMonth)
    if err != nil {
        return nil, fmt.Errorf("获取假勤数据失败: %w", err)
    }

    var leaveDays, pendingDays, approvedDays float64
    var businessDays, outsideDays float64
    var makeupCount, shiftSwapCount int
    var overtimeHours float64

    for _, a := range approvals {
        hours := a.Duration
        days := hours / 8.0 // 按每天8小时折算

        switch a.ApprovalType {
        case ApprovalTypePersonalLeave, ApprovalTypeSickLeave, ApprovalTypePTO,
            ApprovalTypeAnnualLeave, ApprovalTypeMarriageLeave,
            ApprovalTypeMaternityLeave, ApprovalTypePaternityLeave:
            leaveDays += days
            if a.Status == ApprovalStatusApproved {
                approvedDays += days
            } else if a.Status == ApprovalStatusPending {
                pendingDays += days
            }
        case ApprovalTypeBusinessTrip:
            businessDays += days
        case ApprovalTypeOutside:
            outsideDays += days
        case ApprovalTypeMakeup:
            makeupCount++
        case ApprovalTypeShiftSwap:
            shiftSwapCount++
        case ApprovalTypeOvertime:
            overtimeHours += hours
        }
    }

    return &LeaveStatsResponse{
        EmployeeID:     employeeID,
        YearMonth:       yearMonth,
        LeaveDays:       roundHalf(hoursToDays(leaveDays)),
        BusinessDays:    roundHalf(hoursToDays(businessDays)),
        OutsideDays:     roundHalf(hoursToDays(outsideDays)),
        MakeupCount:     makeupCount,
        ShiftSwapCount:  shiftSwapCount,
        OvertimeHours:   roundHalfFloat64(overtimeHours), // D-07: 0.5h 取整
        PendingDays:     roundHalf(hoursToDays(pendingDays)),
        ApprovedDays:    roundHalf(hoursToDays(approvedDays)),
    }, nil
}

func hoursToDays(hours float64) float64 {
    return hours / 8.0
}

func roundHalf(hours float64) float64 {
    return float64(int(hours/0.5+0.5)) * 0.5
}

func roundHalfFloat64(hours float64) float64 {
    return float64(int(hours/0.5+0.5)) * 0.5
}
```
  </action>
  <verify>
    <automated>cd /Users/wencai/github/EasyHR && go build ./internal/attendance/...</automated>
  </verify>
  <done>
    Approval Handler 路由注册，GetLeaveStats 真实数据聚合完成
  </done>
</task>

<task type="auto">
  <name>Task 3: 创建前端考勤审批页面</name>
  <files>
    - frontend/src/views/attendance/AttendanceApproval.vue
    - frontend/src/components/attendance/ApprovalApplyDialog.vue
    - frontend/src/components/attendance/LeaveDurationCalc.vue
    - frontend/src/components/attendance/ApprovalTypeTag.vue
    - frontend/src/api/attendance.ts
  </files>
  <action>
## frontend/src/api/attendance.ts 补充

在文件末尾添加：

```typescript
export interface Approval {
  id: number
  employee_id: number
  employee_name: string
  approval_type: string
  type_name: string
  start_time: string
  end_time: string
  duration: number // 精确到0.01h，显示时四舍五入到0.5h
  leave_type: string
  reason: string
  status: 'draft' | 'pending' | 'approved' | 'rejected' | 'cancelled' | 'timeout'
  approver_id: number | null
  approver_name: string
  approved_at: string
  rejected_at: string
  rejected_note: string
  cancelled_at: string
  attachments: string[]
  created_at: string
}

export interface ApprovalListResponse {
  list: Approval[]
  total: number
  page: number
}

export interface PendingCountResponse {
  pending_count: number
}

export const attendanceApi = {
  // ... (Plan 01, 02 已定义)

  // 审批
  getPendingCount: () => request.get<PendingCountResponse>('/attendance/approvals/pending-count'),

  listApprovals: (params: {
    status?: string
    approval_type?: string
    employee_id?: number
    page?: number
    page_size?: number
  }) => request.get<ApprovalListResponse>('/attendance/approvals', { params }),

  createApproval: (data: {
    approval_type: string
    start_time: string
    end_time: string
    reason?: string
    leave_type?: string
    attachments?: string[]
    cc_user_ids?: number[]
  }) => request.post<Approval>('/attendance/approvals', data),

  approveApproval: (id: number) =>
    request.put<Approval>(`/attendance/approvals/${id}/approve`),

  rejectApproval: (id: number, note?: string) =>
    request.put<Approval>(`/attendance/approvals/${id}/reject`, { note }),

  cancelApproval: (id: number) =>
    request.put<Approval>(`/attendance/approvals/${id}/cancel`),
}
```

## frontend/src/components/attendance/ApprovalTypeTag.vue

创建文件：

```vue
<template>
  <el-tag :type="tagType" size="small">
    {{ label }}
  </el-tag>
</template>

<script setup lang="ts">
import { computed } from 'vue'

const props = defineProps<{
  type: string
}>()

// 审批类型名称（与后端 ApprovalTypeNameMap 一致）
const typeNameMap: Record<string, string> = {
  makeup: '补卡申请',
  shift_swap: '调班申请',
  business_trip: '出差申请',
  outside: '外出申请',
  personal_leave: '事假',
  sick_leave: '病假',
  PTO: '调休',
  annual_leave: '年假',
  marriage_leave: '婚假',
  maternity_leave: '产假',
  paternity_leave: '陪产假',
  overtime: '加班申请',
}

// 不同审批类型颜色
const typeColorMap: Record<string, string> = {
  makeup: 'primary',
  shift_swap: 'warning',
  business_trip: 'success',
  outside: 'success',
  personal_leave: 'danger',
  sick_leave: 'warning',
  PTO: 'primary',
  annual_leave: 'primary',
  marriage_leave: 'success',
  maternity_leave: 'success',
  paternity_leave: 'success',
  overtime: 'warning',
}

const label = computed(() => typeNameMap[props.type] || props.type)
const tagType = computed(() => (typeColorMap[props.type] as any) || 'info')
</script>
```

## frontend/src/components/attendance/LeaveDurationCalc.vue

创建文件：

```vue
<template>
  <div class="leave-duration-calc">
    <el-date-picker
      v-model="dateRange"
      type="daterange"
      range-separator="至"
      start-placeholder="开始日期"
      end-placeholder="结束日期"
      format="YYYY-MM-DD"
      value-format="YYYY-MM-DD"
      :clearable="false"
      style="width: 100%"
      @change="calculateDuration"
    />
    <div class="duration-result" v-if="result !== null">
      <span class="duration-label">计算时长:</span>
      <span class="duration-value">{{ result }} 天</span>
      <span class="duration-hours">（{{ hours }} 小时）</span>
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref, computed, watch } from 'vue'

const props = defineProps<{
  leaveType?: string
}>()

const emit = defineEmits<{
  (e: 'update:duration', value: number): void
  (e: 'update:startTime', value: string): void
  (e: 'update:endTime', value: string): void
}>()

const dateRange = ref<[string, string] | null>(null)
const hours = ref(0)

const result = computed(() => {
  if (hours.value === 0) return null
  return (hours.value / 8).toFixed(1)
})

function calculateDuration() {
  if (!dateRange.value || dateRange.value.length !== 2) {
    hours.value = 0
    emit('update:duration', 0)
    return
  }
  const [start, end] = dateRange.value
  const startDate = new Date(start)
  const endDate = new Date(end)
  const diffMs = endDate.getTime() - startDate.getTime()
  const diffHours = diffMs / (1000 * 60 * 60) + 8 // 含首尾日 + 默认8小时
  hours.value = Math.max(0, diffHours)
  emit('update:startTime', `${start}T09:00:00+08:00`)
  emit('update:endTime', `${end}T18:00:00+08:00`)
  emit('update:duration', hours.value)
}

// 7种请假类型说明
const leaveTypeHints: Record<string, string> = {
  personal_leave: '事假无薪',
  sick_leave: '病假按工龄系数',
  PTO: '调休带薪',
  annual_leave: '年假带薪',
  marriage_leave: '婚假带薪',
  maternity_leave: '产假带薪',
  paternity_leave: '陪产假带薪',
}
</script>

<style scoped lang="scss">
.leave-duration-calc {
  display: flex;
  flex-direction: column;
  gap: 8px;
}
.duration-result {
  display: flex;
  align-items: baseline;
  gap: 6px;
  padding: 8px 12px;
  background: #f0f7ff;
  border-radius: 4px;
}
.duration-label {
  font-size: 12px;
  color: #8c8c8c;
}
.duration-value {
  font-size: 18px;
  font-weight: 700;
  color: #1677ff;
}
.duration-hours {
  font-size: 12px;
  color: #8c8c8c;
}
</style>
```

## frontend/src/components/attendance/ApprovalApplyDialog.vue

创建文件：

```vue
<template>
  <el-dialog v-model="visible" title="新建申请" width="520px" destroy-on-close>
    <el-form label-width="100px" @submit.prevent="handleSubmit">
      <el-form-item label="申请类型" required>
        <el-select v-model="form.approval_type" placeholder="请选择申请类型" style="width: 100%" @change="onTypeChange">
          <el-option v-for="opt in approvalTypeOptions" :key="opt.value" :label="opt.label" :value="opt.value" />
        </el-select>
      </el-form-item>

      <!-- 请假类型选择（仅7种请假类型显示） -->
      <el-form-item v-if="showLeaveType" label="请假类型" required>
        <el-select v-model="form.leave_type" placeholder="请选择请假类型" style="width: 100%">
          <el-option label="事假（无薪）" value="personal_leave" />
          <el-option label="病假（按工龄系数）" value="sick_leave" />
          <el-option label="调休假（带薪）" value="PTO" />
          <el-option label="年假（带薪）" value="annual_leave" />
          <el-option label="婚假（带薪）" value="marriage_leave" />
          <el-option label="产假（带薪）" value="maternity_leave" />
          <el-option label="陪产假（带薪）" value="paternity_leave" />
        </el-select>
        <div v-if="form.leave_type" class="leave-hint">
          {{ leaveTypeHint }}
        </div>
      </el-form-item>

      <!-- 时间选择 -->
      <el-form-item label="开始时间" required>
        <el-date-picker
          v-model="form.start_time"
          type="datetime"
          format="YYYY-MM-DD HH:mm"
          value-format="YYYY-MM-DDTHH:mm:ss"
          placeholder="选择开始时间"
          style="width: 100%"
          @change="calculateDuration"
        />
      </el-form-item>

      <el-form-item label="结束时间" required>
        <el-date-picker
          v-model="form.end_time"
          type="datetime"
          format="YYYY-MM-DD HH:mm"
          value-format="YYYY-MM-DDTHH:mm:ss"
          placeholder="选择结束时间"
          style="width: 100%"
          @change="calculateDuration"
        />
      </el-form-item>

      <!-- 时长自动计算显示 -->
      <el-form-item v-if="form.start_time && form.end_time" label="计算时长">
        <div class="duration-display">
          <span class="duration-value">{{ displayDuration }} 小时</span>
          <span class="duration-note">（存储精确到0.01h，显示四舍五入到0.5h）</span>
        </div>
      </el-form-item>

      <el-form-item label="事由">
        <el-input
          v-model="form.reason"
          type="textarea"
          :rows="3"
          placeholder="请输入申请事由"
          maxlength="500"
          show-word-limit
        />
      </el-form-item>

      <!-- 附件上传（ATT-17） -->
      <el-form-item label="附件">
        <el-upload
          :auto-upload="false"
          :limit="9"
          accept="image/*"
          list-type="picture-card"
          v-model:file-list="fileList"
        >
          <el-icon><Plus /></el-icon>
        </el-upload>
        <div class="upload-tip">支持拍照上传，最多9张（ATT-17）</div>
      </el-form-item>
    </el-form>

    <template #footer>
      <el-button @click="visible = false">取消</el-button>
      <el-button type="primary" :loading="submitting" @click="handleSubmit">提交申请</el-button>
    </template>
  </el-dialog>
</template>

<script setup lang="ts">
import { ref, computed, watch } from 'vue'
import { Plus } from '@element-plus/icons-vue'
import { attendanceApi } from '@/api/attendance'
import { ElMessage } from 'element-plus'
import type { UploadFile } from 'element-plus'

const visible = defineModel<boolean>({ default: false })

const submitting = ref(false)
const fileList = ref<UploadFile[]>([])

const form = ref({
  approval_type: '',
  start_time: '',
  end_time: '',
  reason: '',
  leave_type: '',
})

const approvalTypeOptions = [
  { label: '补卡申请', value: 'makeup' },
  { label: '调班申请', value: 'shift_swap' },
  { label: '出差申请', value: 'business_trip' },
  { label: '外出申请', value: 'outside' },
  { label: '事假', value: 'personal_leave' },
  { label: '病假', value: 'sick_leave' },
  { label: '调休假', value: 'PTO' },
  { label: '年假', value: 'annual_leave' },
  { label: '婚假', value: 'marriage_leave' },
  { label: '产假', value: 'maternity_leave' },
  { label: '陪产假', value: 'paternity_leave' },
  { label: '加班申请', value: 'overtime' },
]

const leaveTypeHints: Record<string, string> = {
  personal_leave: '事假无薪，请知悉',
  sick_leave: '病假工资按工龄系数计算（6个月医疗期内/超6个月）',
  PTO: '调休带薪',
  annual_leave: '年假带薪',
  marriage_leave: '婚假带薪',
  maternity_leave: '产假带薪',
  paternity_leave: '陪产假带薪',
}

const leaveTypeHint = computed(() => leaveTypeHints[form.value.leave_type] || '')

const showLeaveType = computed(() =>
  ['personal_leave', 'sick_leave', 'PTO', 'annual_leave', 'marriage_leave', 'maternity_leave', 'paternity_leave'].includes(form.value.approval_type)
)

const displayDuration = computed(() => {
  if (!form.value.start_time || !form.value.end_time) return 0
  const start = new Date(form.value.start_time).getTime()
  const end = new Date(form.value.end_time).getTime()
  if (isNaN(start) || isNaN(end) || end <= start) return 0
  const hours = (end - start) / (1000 * 60 * 60)
  // D-07: 显示时四舍五入到 0.5h
  return Math.round(hours / 0.5) * 0.5
})

function onTypeChange() {
  if (!showLeaveType.value) {
    form.value.leave_type = ''
  }
}

function calculateDuration() {
  // 时长由 displayDuration 计算属性自动处理
}

function resetForm() {
  form.value = { approval_type: '', start_time: '', end_time: '', reason: '', leave_type: '' }
  fileList.value = []
}

async function handleSubmit() {
  if (!form.value.approval_type || !form.value.start_time || !form.value.end_time) {
    ElMessage.warning('请填写必填字段')
    return
  }
  submitting.value = true
  try {
    const attachments = fileList.value.map(f => (f as any).url || '').filter(Boolean)
    await attendanceApi.createApproval({
      approval_type: form.value.approval_type,
      start_time: form.value.start_time,
      end_time: form.value.end_time,
      reason: form.value.reason,
      leave_type: form.value.leave_type || undefined,
      attachments: attachments.length > 0 ? attachments : undefined,
    })
    ElMessage.success('申请已提交，等待审批')
    visible.value = false
    resetForm()
  } catch {
    ElMessage.error('提交失败，请稍后重试')
  } finally {
    submitting.value = false
  }
}

watch(visible, (val) => {
  if (val) resetForm()
})
</script>

<style scoped lang="scss">
.leave-hint {
  font-size: 12px;
  color: #8c8c8c;
  margin-top: 4px;
}
.duration-display {
  display: flex;
  align-items: baseline;
  gap: 8px;
}
.duration-value {
  font-size: 18px;
  font-weight: 700;
  color: #1677ff;
}
.duration-note {
  font-size: 12px;
  color: #8c8c8c;
}
.upload-tip {
  font-size: 12px;
  color: #8c8c8c;
  margin-top: 4px;
}
</style>
```

## frontend/src/views/attendance/AttendanceApproval.vue

创建文件：

```vue
<template>
  <div class="attendance-approval">
    <el-card>
      <template #header>
        <div class="header">
          <span>考勤审批</span>
          <el-button type="primary" @click="openApplyDialog">+ 新建申请</el-button>
        </div>
      </template>

      <el-tabs v-model="activeTab" @tab-change="load(1)">
        <el-tab-pane label="全部" name="all" />
        <el-tab-pane name="pending">
          <template #label>
            <span>待我审批</span>
            <el-badge v-if="pendingCount > 0" :value="pendingCount" :max="99" style="margin-left: 4px" />
          </template>
        </el-tab-pane>
        <el-tab-pane label="我发起的" name="my" />
      </el-tabs>

      <el-table :data="list" stripe v-loading="loading">
        <el-table-column prop="employee_name" label="申请人" min-width="90" />
        <el-table-column label="类型" min-width="110">
          <template #default="{ row }">
            <ApprovalTypeTag :type="row.approval_type" />
          </template>
        </el-table-column>
        <el-table-column label="时段" min-width="200">
          <template #default="{ row }">
            {{ formatTimeRange(row.start_time, row.end_time) }}
          </template>
        </el-table-column>
        <el-table-column label="时长" min-width="80">
          <template #default="{ row }">
            {{ formatDuration(row.duration) }} <!-- D-07: 0.5h 取整显示 -->
          </template>
        </el-table-column>
        <el-table-column label="状态" min-width="90">
          <template #default="{ row }">
            <el-tag :type="statusTagType(row.status)" size="small">
              {{ statusName(row.status) }}
            </el-tag>
          </template>
        </el-table-column>
        <el-table-column label="操作" width="200" fixed="right">
          <template #default="{ row }">
            <!-- pending 状态: 同意 + 驳回 -->
            <template v-if="row.status === 'pending' && activeTab !== 'my'">
              <el-popconfirm
                :title="`确认同意 ${row.employee_name || ''} 的申请？`"
                @confirm="handleApprove(row.id)"
              >
                <template #reference>
                  <el-button size="small" type="primary" :loading="approvingId === row.id">同意</el-button>
                </template>
              </el-popconfirm>
              <el-button size="small" type="danger" @click="openRejectDialog(row)">驳回</el-button>
            </template>
            <!-- 我发起的 pending: 撤销 -->
            <template v-if="row.status === 'pending' && activeTab === 'my'">
              <el-popconfirm
                title="确认撤回此申请？"
                @confirm="handleCancel(row.id)"
              >
                <template #reference>
                  <el-button size="small" :loading="cancellingId === row.id">撤销</el-button>
                </template>
              </el-popconfirm>
            </template>
            <!-- 非 pending 且非我发起: 无操作 -->
          </template>
        </el-table-column>
      </el-table>

      <el-pagination
        class="mt-4"
        layout="total,prev,pager,next"
        :total="total"
        :page="page"
        :page-size="pageSize"
        @current-change="load"
      />
    </el-card>

    <!-- 驳回弹窗 -->
    <el-dialog v-model="rejectDialogVisible" title="驳回申请" width="440px" destroy-on-close>
      <el-form>
        <el-form-item label="驳回原因（选填）">
          <el-input
            v-model="rejectNote"
            type="textarea"
            :rows="3"
            placeholder="请输入驳回原因"
            maxlength="500"
            show-word-limit
          />
        </el-form-item>
      </el-form>
      <template #footer>
        <el-button @click="rejectDialogVisible = false">取消</el-button>
        <el-button type="danger" :loading="rejecting" @click="handleReject">确认驳回</el-button>
      </template>
    </el-dialog>

    <!-- 新建申请弹窗 -->
    <ApprovalApplyDialog v-model="applyDialogVisible" @submitted="load(1)" />
  </div>
</template>

<script setup lang="ts">
import { ref, onMounted, computed } from 'vue'
import { attendanceApi, type Approval } from '@/api/attendance'
import { ElMessage } from 'element-plus'
import ApprovalTypeTag from '@/components/attendance/ApprovalTypeTag.vue'
import ApprovalApplyDialog from '@/components/attendance/ApprovalApplyDialog.vue'

const activeTab = ref('all')
const loading = ref(false)
const list = ref<Approval[]>([])
const total = ref(0)
const page = ref(1)
const pageSize = ref(20)
const pendingCount = ref(0)

// 审批操作
const approvingId = ref<number | null>(null)
const cancellingId = ref<number | null>(null)
const rejectDialogVisible = ref(false)
const rejectNote = ref('')
const rejectingRowId = ref<number | null>(null)
const rejecting = ref(false)

// 新建申请弹窗
const applyDialogVisible = ref(false)

const statusNameMap: Record<string, string> = {
  draft: '草稿',
  pending: '待审批',
  approved: '已通过',
  rejected: '已驳回',
  cancelled: '已撤回',
  timeout: '已超时',
}

const statusTagTypeMap: Record<string, string> = {
  pending: 'warning',
  approved: 'success',
  rejected: 'danger',
  cancelled: 'info',
  timeout: 'info',
}

function statusName(status: string) { return statusNameMap[status] || status }
function statusTagType(status: string) { return (statusTagTypeMap[status] as any) || 'info' }

function formatTimeRange(start: string, end: string): string {
  if (!start || !end) return '--'
  const s = start.replace('T', ' ').slice(0, 16)
  const e = end.replace('T', ' ').slice(0, 16)
  return `${s} ~ ${e}`
}

// D-07: 显示时四舍五入到 0.5h
function formatDuration(hours: number): string {
  if (!hours || hours <= 0) return '--'
  const rounded = Math.round(hours / 0.5) * 0.5
  if (rounded >= 8) return `${(rounded / 8).toFixed(1)} 天`
  return `${rounded} 小时`
}

async function loadPendingCount() {
  try {
    const res = await attendanceApi.getPendingCount()
    pendingCount.value = res.data?.pending_count || 0
  } catch {
    // ignore
  }
}

async function load(p = 1) {
  page.value = p
  loading.value = true
  try {
    const params: any = { page: p, page_size: pageSize.value }
    if (activeTab.value === 'pending') {
      params.status = 'pending'
    } else if (activeTab.value === 'my') {
      // TODO: 需要当前用户 employee_id（从 auth store 获取）
    }
    const res = await attendanceApi.listApprovals(params)
    list.value = res.data?.list || []
    total.value = res.data?.total || 0
  } catch {
    ElMessage.error('加载审批列表失败，请稍后重试')
  } finally {
    loading.value = false
  }
}

function openRejectDialog(row: Approval) {
  rejectingRowId.value = row.id
  rejectNote.value = ''
  rejectDialogVisible.value = true
}

async function handleApprove(id: number) {
  if (approvingId.value) return
  approvingId.value = id
  try {
    await attendanceApi.approveApproval(id)
    ElMessage.success('审批已通过')
    load(page.value)
    loadPendingCount()
  } catch {
    ElMessage.error('操作失败')
  } finally {
    approvingId.value = null
  }
}

async function handleReject() {
  if (rejectingRowId.value === null) return
  rejecting.value = true
  try {
    await attendanceApi.rejectApproval(rejectingRowId.value, rejectNote.value || undefined)
    ElMessage.success('申请已驳回')
    rejectDialogVisible.value = false
    load(page.value)
    loadPendingCount()
  } catch {
    ElMessage.error('操作失败')
  } finally {
    rejecting.value = false
  }
}

async function handleCancel(id: number) {
  if (cancellingId.value) return
  cancellingId.value = id
  try {
    await attendanceApi.cancelApproval(id)
    ElMessage.success('申请已撤回')
    load(page.value)
    loadPendingCount()
  } catch {
    ElMessage.error('操作失败')
  } finally {
    cancellingId.value = null
  }
}

function openApplyDialog() {
  applyDialogVisible.value = true
}

onMounted(() => {
  load()
  loadPendingCount()
})
</script>

<style scoped lang="scss">
.attendance-approval {
  padding: 20px 24px;
  width: 100%;
  box-sizing: border-box;
}
.header {
  display: flex;
  align-items: center;
  justify-content: space-between;
}
.mt-4 {
  margin-top: 16px;
}
</style>
```

**Important:** 在侧边栏的考勤审批菜单项添加 Badge 占位（后续 Phase 09 可扩展为实时待办数量）。在 `frontend/src/views/layout/AppLayout.vue` 的考勤审批菜单项添加：
```vue
<el-menu-item index="/attendance/approval">
  考勤审批
  <el-badge v-if="false" /> <!-- pendingCount 后续扩展 -->
</el-menu-item>
```
  </action>
  <verify>
    <automated>cd /Users/wencai/github/EasyHR && go build ./internal/attendance/... && cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    AttendanceApproval.vue 页面完整，11种审批类型 + 状态机 + 待办 Badge 功能实现
  </done>
</task>

</tasks>

<verification>
## Phase 06 Plan 3 整体验证

### Backend 编译 + 依赖
```bash
cd /Users/wencai/github/EasyHR && go get github.com/qmuntal/stateless@v1.8.0 && go build ./...
```

### Frontend TypeScript
```bash
cd /Users/wencai/github/EasyHR/frontend && npx tsc --noEmit
```

### API 测试
```bash
# 创建申请
curl -X POST -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"approval_type":"personal_leave","start_time":"2026-04-20T09:00:00Z","end_time":"2026-04-22T18:00:00Z","reason":"家中有事"}' \
  http://localhost:8080/api/v1/attendance/approvals

# 查询审批列表
curl -H "Authorization: Bearer $TOKEN" \
  "http://localhost:8080/api/v1/attendance/approvals?status=pending"

# 获取待审批条数
curl -H "Authorization: Bearer $TOKEN" \
  http://localhost:8080/api/v1/attendance/approvals/pending-count

# 审批通过
curl -X PUT -H "Authorization: Bearer $TOKEN" \
  http://localhost:8080/api/v1/attendance/approvals/1/approve

# 审批驳回
curl -X PUT -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"note":"材料不足"}' \
  http://localhost:8080/api/v1/attendance/approvals/1/reject

# 撤回
curl -X PUT -H "Authorization: Bearer $TOKEN" \
  http://localhost:8080/api/v1/attendance/approvals/1/cancel
```

### 手动验证步骤
1. 访问考勤审批页 -> 点击"新建申请" -> 选择11种类型之一 -> 填写时间 -> 时长自动计算 -> 提交
2. 提交后列表显示待审批 -> 点击"同意" -> 状态变为已通过
3. 新建申请 -> 点击"驳回" -> 输入驳回原因 -> 状态变为已驳回
4. "我发起的" Tab -> pending 状态申请显示"撤销"按钮 -> 点击撤销 -> 状态变为已撤回
5. "待我审批" Tab Badge 显示待审批条数
</verification>

<success_criteria>
- [ ] qmuntal/stateless 状态机管理 draft → pending → approved/rejected/cancelled/timeout
- [ ] 11种审批类型均可创建申请
- [ ] 请假时长按 0.5h 取整显示（D-07），存储精确到 0.01h（D-08）
- [ ] 审批同意/驳回/撤回功能正常
- [ ] 待审批 Badge 显示条数
- [ ] 附件上传支持拍照（ATT-17）
- [ ] `go build ./...` 编译通过
</success_criteria>

<output>
After completion, create `.planning/phases/06-考勤管理/06-03-SUMMARY.md`
</output>
