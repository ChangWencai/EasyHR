---
phase: "06"
plan: "04"
type: execute
wave: 3
depends_on:
  - "06-01"
  - "06-02"
  - "06-03"
files_modified:
  - "internal/attendance/model.go"
  - "internal/attendance/repository.go"
  - "internal/attendance/service.go"
  - "internal/attendance/handler.go"
  - "frontend/src/views/attendance/AttendanceMonthly.vue"
  - "frontend/src/components/attendance/GridCalendar.vue"
  - "frontend/src/api/attendance.ts"
autonomous: true
requirements:
  - ATT-18
  - ATT-19
  - ATT-20
  - ATT-21
user_setup: []
must_haves:
  truths:
    - "管理员可查看出勤月报（默认最近一个月，可选定年月）"
    - "出勤月报显示每个员工的实际出勤、应出勤、加班时长"
    - "管理员可查看员工每日打卡记录详情"
    - "出勤月报支持 Excel 格式导出（含每日打卡记录）"
  artifacts:
    - path: "internal/attendance/model.go"
      provides: "AttendanceMonthly 模型（预计算月报）"
      contains: "AttendanceMonthly"
    - path: "internal/attendance/handler.go"
      provides: "出勤月报 API + Excel 导出 API"
      exports: "GET /api/v1/attendance/monthly, GET /api/v1/attendance/monthly/export, GET /api/v1/attendance/daily-records"
    - path: "frontend/src/views/attendance/AttendanceMonthly.vue"
      provides: "出勤月报页面（统计视图 + 格子视图切换）"
    - path: "frontend/src/components/attendance/GridCalendar.vue"
      provides: "格子矩阵日历组件（节假日列底色 #FFF1F0）"
  key_links:
    - from: "internal/attendance/handler.go"
      to: "internal/attendance/service.go"
      via: "GetMonthlyReport|ExportMonthlyExcel"
      pattern: "GetMonthlyReport|ExportMonthlyExcel"
    - from: "frontend/src/views/attendance/AttendanceMonthly.vue"
      to: "frontend/src/api/attendance.ts"
      via: "getMonthlyReport|exportMonthlyExcel"
      pattern: "attendanceApi\\.(getMonthlyReport|exportMonthlyExcel)"
---

<objective>
完成出勤月报功能。管理员可查看出勤月报（统计视图默认 + 格子视图可选），显示每个员工的实际出勤/应出勤/加班时长，点击员工展开当月每日打卡详情，支持 Excel 格式导出（含每日打卡记录）。后端新增 AttendanceMonthly 预计算模型和 Excel 导出接口。

**Scope:** ATT-18~ATT-21, D-04, D-05, D-06, D-07
**Purpose:** 月度考勤数据汇总，为薪资计算（Phase 7）提供接口
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-考勤管理/06-CONTEXT.md
@.planning/phases/06-考勤管理/06-UI-SPEC.md

## 关键决策（D-XX）

- **D-04:** 默认视图：出勤率统计行 + 应/实/加班时长统计卡片（与员工/薪资看板一致）
- **D-05:** 可选视图：格子矩阵表（员工行 × 日期列，横屏滚动）
- **D-06:** 视图切换：顶部切换按钮，默认折叠到统计行视图
- **D-07:** 加班时长按 0.5h（半小时）取整

## Backend 关键接口

From `internal/attendance/model.go` (新建):
```go
// AttendanceMonthly 出勤月报（预计算，禁止 UPDATE 历史 per ATT-19）
type AttendanceMonthly struct {
    model.BaseModel
    EmployeeID     int64     `gorm:"column:employee_id;not null;index:idx_monthly_emp_month,unique;comment:员工ID"`
    YearMonth      string    `gorm:"column:year_month;type:varchar(7);not null;index:idx_monthly_emp_month,unique;comment:年月 YYYY-MM"`
    ActualDays     float64   `gorm:"column:actual_days;comment:实际出勤天数"`
    RequiredDays   float64   `gorm:"column:required_days;comment:应出勤天数"`
    OvertimeHours  float64   `gorm:"column:overtime_hours;comment:加班时长（小时）D-07"`
    AbsentDays     float64   `gorm:"column:absent_days;comment:缺勤天数"`
    LeaveDays      float64   `gorm:"column:leave_days;comment:请假天数"`
    BusinessDays   float64   `gorm:"column:business_days;comment:出差天数"`
    Status         string    `gorm:"column:status;type:varchar(20);comment:状态 computed/final"`
}
```

## 参考模式

From `internal/employee/service.go` — Excel 导出模式：
```go
// ExportExcel 使用 excelize 生成 Excel
func (s *EmployeeService) ExportExcel(...) error {
    f := excelize.NewFile()
    // ... 填充数据
    return f.SaveAs(filename)
}
```

From `frontend/src/views/employee/EmployeeDashboard.vue` — 统计卡片模式（复用）：
```vue
<div class="stats-grid">
  <div class="stat-card">
    <div class="stat-value">{{ data?.active_count ?? 0 }}</div>
    <div class="stat-label">在职人数</div>
  </div>
</div>
```

From `06-UI-SPEC.md` — 格子矩阵表节假日列底色：
```
格子内符号：√=正常，迟到=迟到，缺=缺勤，假=请假，出=出差，加=加班，--=休息
节假日列底色 #FFF1F0
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: 添加 AttendanceMonthly 模型 + 月报聚合 Service</name>
  <files>
    - internal/attendance/model.go
    - internal/attendance/repository.go
    - internal/attendance/service.go
  </files>
  <action>
## backend/internal/attendance/model.go

在文件底部添加：

```go
// AttendanceMonthly 出勤月报（预计算，禁止 UPDATE 历史 per ATT-19）
// 月报预计算后禁止修改，只能通过补卡/请假等审批通过后追加新记录
type AttendanceMonthly struct {
    model.BaseModel
    EmployeeID    int64     `gorm:"column:employee_id;not null;index:idx_monthly_emp_month,unique:composite,priority:1;comment:员工ID"`
    YearMonth     string    `gorm:"column:year_month;type:varchar(7);not null;index:idx_monthly_emp_month,unique:composite,priority:2;comment:年月 YYYY-MM"`
    ActualDays    float64   `gorm:"column:actual_days;default:0;comment:实际出勤天数"`
    RequiredDays  float64   `gorm:"column:required_days;default:0;comment:应出勤天数"`
    OvertimeHours float64   `gorm:"column:overtime_hours;default:0;comment:加班时长（小时）D-07"`
    AbsentDays    float64   `gorm:"column:absent_days;default:0;comment:缺勤天数"`
    LeaveDays     float64   `gorm:"column:leave_days;default:0;comment:请假天数（含已审批）"`
    BusinessDays  float64   `gorm:"column:business_days;default:0;comment:出差天数"`
    LateCount     int       `gorm:"column:late_count;default:0;comment:迟到次数"`
    EarlyLeaveCount int     `gorm:"column:early_leave_count;default:0;comment:早退次数"`
    Note          string    `gorm:"column:note;type:varchar(500);comment:备注"`
}

func (AttendanceMonthly) TableName() string { return "attendance_monthly" }

// AutoMigrateTables 补充 AttendanceMonthly
func AutoMigrateTables(db *gorm.DB) error {
    return db.AutoMigrate(
        &AttendanceRule{},
        &Shift{},
        &Schedule{},
        &ClockRecord{},
        &Approval{},
        &AttendanceMonthly{},
    )
}
```

## backend/internal/attendance/repository.go

在文件底部添加：

```go
// --- AttendanceMonthly ---

func (r *AttendanceRepository) GetAttendanceMonthly(orgID int64, employeeID int64, yearMonth string) (*AttendanceMonthly, error) {
    var m AttendanceMonthly
    err := r.db.Scopes(r.orgScope(orgID)).
        Where("employee_id = ? AND year_month = ?", employeeID, yearMonth).
        First(&m).Error
    if err == gorm.ErrRecordNotFound {
        return nil, nil
    }
    return &m, err
}

func (r *AttendanceRepository) UpsertAttendanceMonthly(m *AttendanceMonthly) error {
    return r.db.Save(m).Error
}

func (r *AttendanceRepository) ListAttendanceMonthly(orgID int64, yearMonth string, departmentID *int64, page, pageSize int) ([]AttendanceMonthly, int64, error) {
    query := r.db.Scopes(r.orgScope(orgID)).
        Where("year_month = ?", yearMonth)
    if departmentID != nil {
        // JOIN employee 表按 department_id 筛选（需要 employeeRepo，暂时用子查询）
        // TODO: 后续通过 department filter
    }
    var total int64
    query.Model(&AttendanceMonthly{}).Count(&total)
    offset := (page - 1) * pageSize
    var list []AttendanceMonthly
    err := query.Order("employee_id ASC").Offset(offset).Limit(pageSize).Find(&list).Error
    return list, total, err
}

// GetDailyClockRecords 获取员工某月的每日打卡记录（供月报和 GridCalendar 使用）
func (r *AttendanceRepository) GetDailyClockRecords(orgID int64, employeeID int64, yearMonth string) (map[string]map[string]string, error) {
    // 返回 map[date][clockType] = HH:mm
    startDate := yearMonth + "-01"
    records, err := r.GetClockRecordsByEmployee(orgID, employeeID, startDate, yearMonth+"-31")
    if err != nil {
        return nil, err
    }
    result := make(map[string]map[string]string)
    for _, r := range records {
        dateStr := r.WorkDate.Format("2006-01-02")
        if result[dateStr] == nil {
            result[dateStr] = make(map[string]string)
        }
        // 每个班次最多保留最新一条（in/out）
        if existing, ok := result[dateStr][r.ClockType]; !ok {
            result[dateStr][r.ClockType] = r.ClockTime.Format("15:04")
        }
    }
    return result, nil
}
```

## backend/internal/attendance/service.go

在文件底部添加：

```go
// === 出勤月报 ===

// MonthlyReportItem 月报行
type MonthlyReportItem struct {
    EmployeeID      int64   `json:"employee_id"`
    EmployeeName    string  `json:"employee_name"`
    DepartmentName  string  `json:"department_name"`
    ActualDays      float64 `json:"actual_days"`
    RequiredDays    float64 `json:"required_days"`
    OvertimeHours   float64 `json:"overtime_hours"`  // D-07: 显示时 0.5h 取整
    AbsentDays      float64 `json:"absent_days"`
    LeaveDays       float64 `json:"leave_days"`
    BusinessDays    float64 `json:"business_days"`
    AttendanceRate  float64 `json:"attendance_rate"` // 百分比
    LateCount       int     `json:"late_count"`
}

type MonthlyReportResponse struct {
    YearMonth string               `json:"year_month"`
    Stats     MonthlyStats        `json:"stats"`
    List      []MonthlyReportItem `json:"list"`
    Total     int64                `json:"total"`
    Page      int                  `json:"page"`
    PageSize  int                  `json:"page_size"`
}

// MonthlyStats 月度汇总统计（4张卡片 per D-04）
type MonthlyStats struct {
    TotalActualDays  float64 `json:"total_actual_days"`
    TotalRequiredDays float64 `json:"total_required_days"`
    TotalOvertimeHours float64 `json:"total_overtime_hours"`
    TotalAbsentDays float64   `json:"total_absent_days"`
}

// DailyRecord 每日打卡详情
type DailyRecord struct {
    Date      string `json:"date"`       // YYYY-MM-DD
    ClockIn   string `json:"clock_in"`  // HH:mm 或 --
    ClockOut  string `json:"clock_out"` // HH:mm 或 --
    Status    string `json:"status"`    // normal/late/absent/no_schedule
    IsHoliday bool   `json:"is_holiday"`
    IsWeekend bool   `json:"is_weekend"`
    Symbol    string `json:"symbol"`    // √/迟到/缺/假/出/加/--
}

type DailyRecordsResponse struct {
    EmployeeID int64          `json:"employee_id"`
    YearMonth  string         `json:"year_month"`
    Records    []DailyRecord  `json:"records"`
}

// GetMonthlyReport 获取出勤月报
func (s *AttendanceService) GetMonthlyReport(ctx context.Context, orgID int64, yearMonth string, page, pageSize int) (*MonthlyReportResponse, error) {
    // 1. 获取当月所有员工考勤数据（从 AttendanceMonthly 或实时聚合）
    reports, total, err := s.repo.ListAttendanceMonthly(orgID, yearMonth, nil, page, pageSize)
    if err != nil {
        return nil, fmt.Errorf("获取月报数据失败: %w", err)
    }

    // 2. 聚合统计
    var totalActual, totalRequired, totalOvertime, totalAbsent float64
    items := make([]MonthlyReportItem, len(reports))
    for i, r := range reports {
        // D-07: 加班时长显示时四舍五入到 0.5h
        overtimeDisplay := roundHalfFloat64(r.OvertimeHours)
        rate := 0.0
        if r.RequiredDays > 0 {
            rate = float64(int(r.ActualDays/r.RequiredDays*100*10)) / 10
        }
        items[i] = MonthlyReportItem{
            EmployeeID:     r.EmployeeID,
            ActualDays:     r.ActualDays,
            RequiredDays:   r.RequiredDays,
            OvertimeHours:  overtimeDisplay,
            AbsentDays:     r.AbsentDays,
            LeaveDays:      r.LeaveDays,
            BusinessDays:   r.BusinessDays,
            AttendanceRate: rate,
            LateCount:      r.LateCount,
        }
        totalActual += r.ActualDays
        totalRequired += r.RequiredDays
        totalOvertime += r.OvertimeHours
        totalAbsent += r.AbsentDays
    }

    return &MonthlyReportResponse{
        YearMonth: yearMonth,
        Stats: MonthlyStats{
            TotalActualDays:    roundHalf(totalActual),
            TotalRequiredDays: roundHalf(totalRequired),
            TotalOvertimeHours: roundHalfFloat64(totalOvertime), // D-07
            TotalAbsentDays:    roundHalf(totalAbsent),
        },
        List:     items,
        Total:    total,
        Page:     page,
        PageSize: pageSize,
    }, nil
}

// GetDailyRecords 获取员工每日打卡详情（ATT-20）
func (s *AttendanceService) GetDailyRecords(ctx context.Context, orgID int64, employeeID int64, yearMonth string) (*DailyRecordsResponse, error) {
    // 1. 获取考勤规则判断节假日
    rule, err := s.repo.GetRule(orgID)
    if err != nil {
        return nil, fmt.Errorf("获取考勤规则失败: %w", err)
    }
    ruleEngine := NewRuleEngine(rule)

    // 2. 获取打卡记录
    clockMap, err := s.repo.GetDailyClockRecords(orgID, employeeID, yearMonth)
    if err != nil {
        return nil, fmt.Errorf("获取打卡记录失败: %w", err)
    }

    // 3. 获取当月所有天数
    year, month, _ := time.Now().Date()
    if yearMonth != "" {
        parsed, _ := time.Parse("2006-01", yearMonth)
        year, month, _ = parsed.Date()
    }
    daysInMonth := time.Date(year, month+1, 0, 0, 0, 0, 0, time.UTC).Day()

    records := make([]DailyRecord, daysInMonth)
    for day := 1; day <= daysInMonth; day++ {
        date := time.Date(year, month, day, 0, 0, 0, 0, time.UTC)
        dateStr := date.Format("2006-01-02")
        weekday := date.Weekday()
        isWeekend := weekday == time.Saturday || weekday == time.Sunday
        isHoliday := ruleEngine.IsHoliday(date)

        dayClocks := clockMap[dateStr]
        clockIn := dayClocks["in"]
        clockOut := dayClocks["out"]

        status := "normal"
        symbol := "√"
        if clockIn == "" && clockOut == "" {
            if !isWeekend && !isHoliday {
                status = "absent"
                symbol = "缺"
            } else {
                status = "no_schedule"
                symbol = "--"
            }
        } else if clockIn != "" && rule != nil && rule.WorkStart != "" {
            if clockIn > rule.WorkStart {
                status = "late"
                symbol = "迟到"
            }
        }

        records[day-1] = DailyRecord{
            Date:      dateStr,
            ClockIn:   clockIn,
            ClockOut:  clockOut,
            Status:    status,
            IsHoliday: isHoliday,
            IsWeekend: isWeekend,
            Symbol:    symbol,
        }
    }

    return &DailyRecordsResponse{
        EmployeeID: employeeID,
        YearMonth:  yearMonth,
        Records:    records,
    }, nil
}

// ExportMonthlyExcel 导出出勤月报 Excel（ATT-21）
func (s *AttendanceService) ExportMonthlyExcel(ctx context.Context, orgID int64, yearMonth string) ([]byte, string, error) {
    // 1. 获取月报数据
    report, err := s.GetMonthlyReport(ctx, orgID, yearMonth, 1, 1000)
    if err != nil {
        return nil, "", fmt.Errorf("获取月报数据失败: %w", err)
    }

    // 2. 使用 excelize 生成 Excel
    f := excelize.NewFile()
    sheet := "出勤月报"
    index, _ := f.NewSheet(sheet)
    f.SetActiveSheet(index)
    f.DeleteSheet("Sheet1")

    // 表头
    headers := []string{"姓名", "部门", "实际出勤(天)", "应出勤(天)", "加班时长(小时)", "缺勤(天)", "请假(天)", "出差(天)", "出勤率(%)"}
    for i, h := range headers {
        cell, _ := excelize.CoordinatesToCellName(i+1, 1)
        f.SetCellValue(sheet, cell, h)
        f.SetCellStyle(sheet, cell, cell, titleStyle(f))
    }

    // 数据行
    for rowIdx, item := range report.List {
        row := rowIdx + 2
        values := []interface{}{
            item.EmployeeName,
            item.DepartmentName,
            item.ActualDays,
            item.RequiredDays,
            item.OvertimeHours,
            item.AbsentDays,
            item.LeaveDays,
            item.BusinessDays,
            fmt.Sprintf("%.1f%%", item.AttendanceRate),
        }
        for colIdx, val := range values {
            cell, _ := excelize.CoordinatesToCellName(colIdx+1, row)
            f.SetCellValue(sheet, cell, val)
        }
    }

    // 汇总行
    summaryRow := len(report.List) + 2
    f.SetCellValue(sheet, fmt.Sprintf("A%d", summaryRow), "合计")
    f.SetCellValue(sheet, fmt.Sprintf("C%d", summaryRow), report.Stats.TotalActualDays)
    f.SetCellValue(sheet, fmt.Sprintf("D%d", summaryRow), report.Stats.TotalRequiredDays)
    f.SetCellValue(sheet, fmt.Sprintf("E%d", summaryRow), roundHalfFloat64(report.Stats.TotalOvertimeHours))
    f.SetCellValue(sheet, fmt.Sprintf("F%d", summaryRow), report.Stats.TotalAbsentDays)

    // 保存
    buf, err := f.WriteToBuffer()
    if err != nil {
        return nil, "", fmt.Errorf("生成 Excel 失败: %w", err)
    }
    filename := fmt.Sprintf("出勤月报_%s.xlsx", yearMonth)
    return buf.Bytes(), filename, nil
}

func titleStyle(f *excelize.File) int {
    style, _ := f.NewStyle(&excelize.Style{
        Font:      &excelize.Font{Bold: true},
        Fill:      excelize.Fill{Type: "pattern", Color: []string{"#E6E6E6"}, Pattern: 1},
        Alignment: &excelize.Alignment{Horizontal: "center"},
    })
    return style
}
```
  </action>
  <verify>
    <automated>cd /Users/wencai/github/EasyHR && go build ./internal/attendance/...</automated>
  </verify>
  <done>
    AttendanceMonthly 模型完成，月报聚合 Service 和 Excel 导出完成
  </done>
</task>

<task type="auto">
  <name>Task 2: 注册月报 Handler 路由</name>
  <files>
    - internal/attendance/handler.go
  </files>
  <action>
## backend/internal/attendance/handler.go

在 `RegisterRoutes` 函数中补充路由：

```go
// 出勤月报
g.GET("/monthly", middleware.RequireAuth, getMonthlyReportHandler(svc))
g.GET("/monthly/export", middleware.RequireAuth, exportMonthlyExcelHandler(svc))
g.GET("/daily-records", middleware.RequireAuth, getDailyRecordsHandler(svc))
```

添加 handler 函数：

```go
func getMonthlyReportHandler(svc *AttendanceService) gin.HandlerFunc {
    return func(c *gin.Context) {
        yearMonth := c.DefaultQuery("year_month", time.Now().Format("2006-01"))
        page, _ := strconv.Atoi(c.DefaultQuery("page", "1"))
        pageSize, _ := strconv.Atoi(c.DefaultQuery("page_size", "20"))
        if page < 1 {
            page = 1
        }
        if pageSize < 1 || pageSize > 100 {
            pageSize = 20
        }
        result, err := svc.GetMonthlyReport(c.Request.Context(), getOrgID(c), yearMonth, page, pageSize)
        if err != nil {
            c.JSON(http.StatusInternalServerError, gin.H{"error": err.Error()})
            return
        }
        c.JSON(http.StatusOK, gin.H{"data": result})
    }
}

func exportMonthlyExcelHandler(svc *AttendanceService) gin.HandlerFunc {
    return func(c *gin.Context) {
        yearMonth := c.DefaultQuery("year_month", time.Now().Format("2006-01"))
        data, filename, err := svc.ExportMonthlyExcel(c.Request.Context(), getOrgID(c), yearMonth)
        if err != nil {
            c.JSON(http.StatusInternalServerError, gin.H{"error": err.Error()})
            return
        }
        c.Header("Content-Type", "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet")
        c.Header("Content-Disposition", fmt.Sprintf(`attachment; filename*=UTF-8''%s`, url.QueryEscape(filename)))
        c.Data(http.StatusOK, "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet", data)
    }
}

func getDailyRecordsHandler(svc *AttendanceService) gin.HandlerFunc {
    return func(c *gin.Context) {
        employeeIDStr := c.Query("employee_id")
        yearMonth := c.DefaultQuery("year_month", time.Now().Format("2006-01"))
        if employeeIDStr == "" {
            c.JSON(http.StatusBadRequest, gin.H{"error": "employee_id required"})
            return
        }
        employeeID, _ := strconv.ParseInt(employeeIDStr, 10, 64)
        result, err := svc.GetDailyRecords(c.Request.Context(), getOrgID(c), employeeID, yearMonth)
        if err != nil {
            c.JSON(http.StatusInternalServerError, gin.H{"error": err.Error()})
            return
        }
        c.JSON(http.StatusOK, gin.H{"data": result})
    }
}
```

在 handler.go 顶部添加 import：
```go
import (
    "net/url"
    ...
)
```
  </action>
  <verify>
    <automated>cd /Users/wencai/github/EasyHR && go build ./internal/attendance/...</automated>
  </verify>
  <done>
    月报 Handler 路由注册完成
  </done>
</task>

<task type="auto">
  <name>Task 3: 创建前端出勤月报页面</name>
  <files>
    - frontend/src/views/attendance/AttendanceMonthly.vue
    - frontend/src/components/attendance/GridCalendar.vue
    - frontend/src/api/attendance.ts
  </files>
  <action>
## frontend/src/api/attendance.ts 补充

在文件末尾添加：

```typescript
export interface MonthlyReportItem {
  employee_id: number
  employee_name: string
  department_name: string
  actual_days: number
  required_days: number
  overtime_hours: number // D-07: 显示时 0.5h 取整
  absent_days: number
  leave_days: number
  business_days: number
  attendance_rate: number
  late_count: number
}

export interface MonthlyStats {
  total_actual_days: number
  total_required_days: number
  total_overtime_hours: number
  total_absent_days: number
}

export interface MonthlyReportResponse {
  year_month: string
  stats: MonthlyStats
  list: MonthlyReportItem[]
  total: number
  page: number
  page_size: number
}

export interface DailyRecord {
  date: string
  clock_in: string
  clock_out: string
  status: string
  is_holiday: boolean
  is_weekend: boolean
  symbol: string
}

export interface DailyRecordsResponse {
  employee_id: number
  year_month: string
  records: DailyRecord[]
}

export const attendanceApi = {
  // ... (Plan 01-03 已定义)

  // 出勤月报
  getMonthlyReport: (params: { year_month: string; page?: number; page_size?: number }) =>
    request.get<MonthlyReportResponse>('/attendance/monthly', { params }),

  exportMonthlyExcel: (params: { year_month: string }) =>
    request.get('/attendance/monthly/export', { params, responseType: 'blob' }),

  getDailyRecords: (params: { employee_id: number; year_month: string }) =>
    request.get<DailyRecordsResponse>('/attendance/daily-records', { params }),
}
```

## frontend/src/components/attendance/GridCalendar.vue

创建文件：

```vue
<template>
  <div class="grid-calendar">
    <el-table
      :data="tableData"
      border
      :size="tableSize"
      :scroll-x="true"
      :scroll-y="false"
      :cell-class-name="getCellClass"
    >
      <!-- 固定姓名列 -->
      <el-table-column prop="employee_name" label="姓名" min-width="100" fixed="left" />

      <!-- 动态日期列 -->
      <el-table-column
        v-for="day in days"
        :key="day.date"
        :label="day.label"
        width="50"
        min-width="50"
      >
        <template #default="{ row }">
          <span :class="getSymbolClass(row.symbols[day.date])">
            {{ row.symbols[day.date] || '' }}
          </span>
        </template>
      </el-table-column>

      <!-- 汇总列 -->
      <el-table-column label="实际出勤" width="80" fixed="right">
        <template #default="{ row }">
          {{ row.actual_days }}天
        </template>
      </el-table-column>
      <el-table-column label="应出勤" width="80" fixed="right">
        <template #default="{ row }">
          {{ row.required_days }}天
        </template>
      </el-table-column>
      <el-table-column label="加班" width="80" fixed="right">
        <template #default="{ row }">
          {{ row.overtime_hours }}h
        </template>
      </el-table-column>
    </el-table>
  </div>
</template>

<script setup lang="ts">
import { computed } from 'vue'
import type { DailyRecord } from '@/api/attendance'

const props = defineProps<{
  records: Map<number, DailyRecord[]> // employeeID -> records
  employeeIds: number[]
  yearMonth: string
  holidays: string[] // holiday date strings YYYY-MM-DD
  tableSize?: 'default' | 'small' | 'large'
}>()

const days = computed(() => {
  const [year, month] = props.yearMonth.split('-').map(Number)
  const daysInMonth = new Date(year, month, 0).getDate()
  const result = []
  const weekdayNames = ['日', '一', '二', '三', '四', '五', '六']
  for (let d = 1; d <= daysInMonth; d++) {
    const date = new Date(year, month - 1, d)
    const dateStr = `${year}-${String(month).padStart(2, '0')}-${String(d).padStart(2, '0')}`
    const isHoliday = props.holidays.includes(dateStr)
    const isWeekend = date.getDay() === 0 || date.getDay() === 6
    result.push({
      date: dateStr,
      label: `${d}日`,
      isHoliday,
      isWeekend,
    })
  }
  return result
})

// 构建表格数据
const tableData = computed(() => {
  return props.employeeIds.map(empId => {
    const records = props.records.get(empId) || []
    const recordMap = new Map(records.map(r => [r.date, r]))
    const symbols: Record<string, string> = {}
    for (const day of days.value) {
      const record = recordMap.get(day.date)
      if (record) {
        symbols[day.date] = record.symbol
      } else if (day.isWeekend || day.isHoliday) {
        symbols[day.date] = '--'
      }
    }
    // TODO: 从 props 获取员工汇总数据
    return {
      employee_id: empId,
      employee_name: '', // 从 employee store 获取
      symbols,
      actual_days: 0,
      required_days: 0,
      overtime_hours: 0,
    }
  })
})

function getSymbolClass(symbol?: string): string {
  if (!symbol) return ''
  if (symbol === '√') return 'symbol-normal'
  if (symbol === '迟到') return 'symbol-late'
  if (symbol === '缺') return 'symbol-absent'
  return 'symbol-other'
}

function getCellClass({ column }: any): string {
  // 节假日列底色 #FFF1F0
  const day = days.value.find(d => d.label === column.label)
  if (day?.isHoliday) return 'holiday-cell'
  return ''
}
</script>

<style scoped lang="scss">
.grid-calendar {
  overflow-x: auto;
}

.symbol-normal {
  color: #52c41a;
  font-weight: 700;
}

.symbol-late {
  color: #fa8c16;
  font-weight: 700;
}

.symbol-absent {
  color: #ff4d4f;
  font-weight: 700;
}

.symbol-other {
  color: #8c8c8c;
}

:deep(.holiday-cell) {
  background-color: #fff1f0 !important;
}
</style>
```

## frontend/src/views/attendance/AttendanceMonthly.vue

创建文件：

```vue
<template>
  <div class="attendance-monthly">
    <el-card>
      <template #header>
        <div class="header">
          <span>出勤月报</span>
          <div class="header-actions">
            <el-date-picker
              v-model="selectedMonth"
              type="month"
              format="YYYY-MM"
              value-format="YYYY-MM"
              placeholder="选择月份"
              style="width: 140px"
              @change="load(1)"
            />
            <el-button type="primary" @click="handleExport">导出月报</el-button>
          </div>
        </div>
      </template>

      <!-- 视图切换按钮 -->
      <div class="view-toggle">
        <el-button-group>
          <el-button :type="viewMode === 'stat' ? 'primary' : 'default'" @click="viewMode = 'stat'">
            统计视图
          </el-button>
          <el-button :type="viewMode === 'grid' ? 'primary' : 'default'" @click="viewMode = 'grid'">
            格子视图
          </el-button>
        </el-button-group>
      </div>

      <!-- 统计视图（默认 per D-04） -->
      <div v-if="viewMode === 'stat'" v-loading="loading">
        <!-- 4张统计卡片 -->
        <AttendanceStatsCard
          :actual="stats.total_actual_days"
          :required="stats.total_required_days"
          :overtime="stats.total_overtime_hours"
          :absent="stats.total_absent_days"
        />

        <!-- 员工出勤明细表格 -->
        <el-table :data="list" stripe @row-click="handleRowExpand">
          <el-table-column prop="employee_name" label="姓名" min-width="90" />
          <el-table-column prop="department_name" label="部门" min-width="100" />
          <el-table-column label="实际出勤" min-width="100">
            <template #default="{ row }">{{ row.actual_days }} 天</template>
          </el-table-column>
          <el-table-column label="应出勤" min-width="100">
            <template #default="{ row }">{{ row.required_days }} 天</template>
          </el-table-column>
          <el-table-column label="加班时长" min-width="100">
            <template #default="{ row }">{{ row.overtime_hours }} 小时</template>
          </el-table-column>
          <el-table-column label="缺勤" min-width="80">
            <template #default="{ row }">{{ row.absent_days }} 天</template>
          </el-table-column>
          <el-table-column label="出勤率" min-width="90">
            <template #default="{ row }">
              <span :style="{ color: getRateColor(row.attendance_rate) }">
                {{ row.attendance_rate.toFixed(1) }}%
              </span>
            </template>
          </el-table-column>
          <el-table-column label="查看打卡详情" width="130" fixed="right">
            <template #default="{ row }">
              <el-button size="small" type="primary" link @click.stop="viewDailyRecords(row)">
                查看打卡详情
              </el-button>
            </template>
          </el-table-column>

          <!-- 展开行：每日打卡详情 -->
          <el-table-column type="expand" width="1">
            <template #default="{ row: expandedRow }">
              <div class="daily-records-panel" v-if="expandedRow.employee_id === expandedEmployeeId">
                <div class="daily-records-header">
                  {{ expandedRow.employee_name }} - {{ selectedMonth }} 打卡记录
                </div>
                <div class="daily-records-grid">
                  <div
                    v-for="record in dailyRecords"
                    :key="record.date"
                    class="daily-record-item"
                    :class="{
                      'is-holiday': record.is_holiday,
                      'is-weekend': record.is_weekend,
                    }"
                  >
                    <div class="record-date">{{ formatDate(record.date) }}</div>
                    <div class="record-detail">
                      <ClockStatusTag v-if="record.clock_in" :status="record.status" />
                      <span v-if="record.clock_in" class="clock-time">
                        {{ record.clock_in }} 上班
                      </span>
                      <span v-if="record.clock_out" class="clock-time">
                        {{ record.clock_out }} 下班
                      </span>
                      <span v-if="!record.clock_in && !record.clock_out" class="no-record">
                        {{ record.symbol }}
                      </span>
                    </div>
                  </div>
                </div>
              </div>
            </template>
          </el-table-column>
        </el-table>

        <el-pagination
          class="mt-4"
          layout="total,prev,pager,next"
          :total="total"
          :page="page"
          :page-size="pageSize"
          @current-change="load"
        />
      </div>

      <!-- 格子视图（可选 per D-05） -->
      <div v-if="viewMode === 'grid'" v-loading="loading" class="grid-view">
        <GridCalendar
          :records="gridRecords"
          :employee-ids="employeeIds"
          :year-month="selectedMonth"
          :holidays="holidays"
        />
      </div>
    </el-card>

    <!-- 打卡详情抽屉 -->
    <el-drawer
      v-model="drawerVisible"
      :title="`${drawerEmployeeName} - ${selectedMonth} 打卡详情`"
      size="600px"
      direction="rtl"
    >
      <div v-if="dailyRecords.length === 0" class="empty-state">
        暂无打卡数据
      </div>
      <div v-else class="drawer-records">
        <div
          v-for="record in dailyRecords"
          :key="record.date"
          class="drawer-record-item"
          :class="{
            'is-holiday': record.is_holiday,
            'is-weekend': record.is_weekend,
          }"
        >
          <span class="record-date">{{ formatDate(record.date) }}</span>
          <span class="record-symbol">{{ record.symbol }}</span>
          <template v-if="record.clock_in">
            <span class="record-clock">上班 {{ record.clock_in }}</span>
            <span v-if="record.clock_out" class="record-clock">下班 {{ record.clock_out }}</span>
          </template>
          <el-tag v-if="record.clock_in" :type="record.status === 'late' ? 'warning' : 'success'" size="small">
            {{ record.status === 'late' ? '迟到' : '正常' }}
          </el-tag>
          <span v-else class="no-record">休息/缺勤</span>
        </div>
      </div>
    </el-drawer>
  </div>
</template>

<script setup lang="ts">
import { ref, computed, onMounted } from 'vue'
import { attendanceApi, type MonthlyReportItem, type DailyRecord } from '@/api/attendance'
import { ElMessage } from 'element-plus'
import AttendanceStatsCard from '@/components/attendance/AttendanceStatsCard.vue'
import ClockStatusTag from '@/components/attendance/ClockStatusTag.vue'
import GridCalendar from '@/components/attendance/GridCalendar.vue'

const loading = ref(false)
const viewMode = ref<'stat' | 'grid'>('stat')
const selectedMonth = ref(new Date().toISOString().slice(0, 7))
const list = ref<MonthlyReportItem[]>([])
const total = ref(0)
const page = ref(1)
const pageSize = ref(20)
const stats = ref({
  total_actual_days: 0,
  total_required_days: 0,
  total_overtime_hours: 0,
  total_absent_days: 0,
})
const holidays = ref<string[]>([])

// 展开行相关
const expandedEmployeeId = ref<number | null>(null)
const dailyRecords = ref<DailyRecord[]>([])

// 打卡详情抽屉
const drawerVisible = ref(false)
const drawerEmployeeName = ref('')

// 格子视图数据
const gridRecords = computed(() => {
  // Map<employeeID, DailyRecord[]>
  const map = new Map<number, DailyRecord[]>()
  for (const item of list.value) {
    // TODO: 需要按员工加载每日记录（优化：批量加载）
  }
  return map
})

const employeeIds = computed(() => list.value.map(item => item.employee_id))

function getRateColor(rate: number): string {
  if (rate >= 95) return '#52C41A'
  if (rate >= 80) return '#FA8C16'
  return '#FF4D4F'
}

function formatDate(dateStr: string): string {
  const [, month, day] = dateStr.split('-')
  const weekdayNames = ['日', '一', '二', '三', '四', '五', '六']
  const date = new Date(dateStr)
  return `${parseInt(month)}月${parseInt(day)}日周${weekdayNames[date.getDay()]}`
}

async function load(p = 1) {
  page.value = p
  loading.value = true
  try {
    const res = await attendanceApi.getMonthlyReport({
      year_month: selectedMonth.value,
      page: p,
      page_size: pageSize.value,
    })
    const data = res.data
    list.value = data?.list || []
    total.value = data?.total || 0
    stats.value = data?.stats || {
      total_actual_days: 0,
      total_required_days: 0,
      total_overtime_hours: 0,
      total_absent_days: 0,
    }
  } catch {
    ElMessage.error('加载月报数据失败，请刷新页面重试')
  } finally {
    loading.value = false
  }
}

async function handleRowExpand(row: MonthlyReportItem) {
  if (expandedEmployeeId.value === row.employee_id) {
    expandedEmployeeId.value = null
    return
  }
  expandedEmployeeId.value = row.employee_id
  await loadDailyRecords(row.employee_id)
}

async function viewDailyRecords(row: MonthlyReportItem) {
  drawerEmployeeName.value = row.employee_name || '员工'
  await loadDailyRecords(row.employee_id)
  drawerVisible.value = true
}

async function loadDailyRecords(employeeId: number) {
  try {
    const res = await attendanceApi.getDailyRecords({
      employee_id: employeeId,
      year_month: selectedMonth.value,
    })
    dailyRecords.value = res.data?.records || []
  } catch {
    dailyRecords.value = []
  }
}

async function handleExport() {
  try {
    const blob = await attendanceApi.exportMonthlyExcel({ year_month: selectedMonth.value })
    const url = URL.createObjectURL(blob as unknown as Blob)
    const a = document.createElement('a')
    a.href = url
    a.download = `出勤月报_${selectedMonth.value}.xlsx`
    a.click()
    URL.revokeObjectURL(url)
    ElMessage.success('月报导出成功')
  } catch {
    ElMessage.error('导出失败，请稍后重试')
  }
}

onMounted(() => load())
</script>

<style scoped lang="scss">
.attendance-monthly {
  padding: 20px 24px;
  width: 100%;
  box-sizing: border-box;
}

.header {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.header-actions {
  display: flex;
  gap: 8px;
}

.view-toggle {
  margin-bottom: 16px;
}

.mt-4 {
  margin-top: 16px;
}

.daily-records-panel {
  padding: 12px 16px;
  background: #fafafa;
}

.daily-records-header {
  font-size: 14px;
  font-weight: 600;
  color: #1a1a1a;
  margin-bottom: 12px;
}

.daily-records-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(80px, 1fr));
  gap: 4px;
}

.daily-record-item {
  text-align: center;
  padding: 6px 4px;
  border-radius: 4px;
  background: #fff;
  border: 1px solid #f0f0f0;

  &.is-holiday {
    background: #fff1f0;
    border-color: #ffccc7;
  }

  &.is-weekend {
    background: #f5f5f5;
  }
}

.record-date {
  font-size: 11px;
  color: #8c8c8c;
  display: block;
}

.record-detail {
  margin-top: 2px;
}

.record-symbol {
  font-size: 14px;
  font-weight: 700;
}

.clock-time {
  font-size: 10px;
  color: #52c41a;
  display: block;
}

.no-record {
  font-size: 11px;
  color: #bfbfbf;
}

.grid-view {
  overflow-x: auto;
}

// 抽屉打卡记录
.drawer-records {
  padding: 0 16px;
}

.drawer-record-item {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 8px 0;
  border-bottom: 1px solid #f0f0f0;

  &.is-holiday {
    background: #fff1f0;
    padding: 8px 12px;
    margin: 0 -12px;
  }

  &.is-weekend {
    background: #fafafa;
  }
}

.record-date {
  min-width: 80px;
  font-size: 13px;
}

.record-symbol {
  min-width: 40px;
  font-weight: 700;
}

.record-clock {
  font-size: 13px;
  color: #52c41a;
}

.empty-state {
  text-align: center;
  color: #8c8c8c;
  padding: 40px;
}
</style>
```
  </action>
  <verify>
    <automated>cd /Users/wencai/github/EasyHR && go build ./internal/attendance/... && cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    AttendanceMonthly.vue 页面完整，统计视图 + 格子视图 + Excel 导出功能实现
  </done>
</task>

</tasks>

<verification>
## Phase 06 Plan 4 整体验证

### Backend 编译
```bash
cd /Users/wencai/github/EasyHR && go build ./internal/attendance/...
```

### Frontend TypeScript
```bash
cd /Users/wencai/github/EasyHR/frontend && npx tsc --noEmit
```

### API 测试
```bash
# 获取出勤月报
curl -H "Authorization: Bearer $TOKEN" \
  "http://localhost:8080/api/v1/attendance/monthly?year_month=2026-04"

# 导出 Excel
curl -H "Authorization: Bearer $TOKEN" \
  "http://localhost:8080/api/v1/attendance/monthly/export?year_month=2026-04" \
  -o attendance_monthly.xlsx

# 获取每日打卡详情
curl -H "Authorization: Bearer $TOKEN" \
  "http://localhost:8080/api/v1/attendance/daily-records?employee_id=1&year_month=2026-04"
```

### 手动验证步骤
1. 访问出勤月报页 -> 统计卡片行显示（实际出勤/应出勤/加班时长/缺勤天数）
2. 切换月份选择器 -> 数据相应更新
3. 点击员工行展开 -> 显示当月每日打卡详情（日期/打卡时间/状态）
4. 点击"查看打卡详情" -> 右侧 Drawer 显示打卡记录
5. 切换到"格子视图" -> 显示格子矩阵表，节假日列底色 #FFF1F0
6. 点击"导出月报" -> 浏览器下载 Excel 文件
7. Excel 包含所有员工出勤数据和合计行
</verification>

<success_criteria>
- [ ] 出勤月报页面可访问，4张统计卡片正常显示
- [ ] 统计视图默认展示，点击员工行展开每日打卡详情
- [ ] 格子视图显示格子矩阵表，节假日列底色 #FFF1F0
- [ ] 月份选择器切换正常
- [ ] Excel 导出下载正常（含每日打卡记录）
- [ ] `go build ./...` 编译通过
</success_criteria>

<output>
After completion, create `.planning/phases/06-考勤管理/06-04-SUMMARY.md`
</output>
