---
phase: "09"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "frontend/src/views/home/HomeView.vue"
  - "frontend/src/api/dashboard.ts"
  - "internal/dashboard/model.go"
  - "internal/dashboard/repository.go"
  - "internal/dashboard/service.go"
  - "internal/dashboard/handler.go"
  - "internal/dashboard/router.go"
autonomous: true
requirements:
  - "TODO-19"
  - "TODO-20"
  - "TODO-09"
  - "TODO-10"
user_setup: []
must_haves:
  truths:
    - "管理员可在首页看到全部事项完成率环形图和限时任务完成率环形图"
    - "管理员可在首页轮播图区域看到1-3张轮播图，点击跳转对应链接"
    - "管理员可在首页快捷入口看到新增的3个入口（新入职/调薪/考勤）"
    - "环形图数据来自后端API，包含已完成数和待办数"
  artifacts:
    - path: "frontend/src/views/home/components/TodoRingChart.vue"
      provides: "ECharts环形图组件，radius=['40%','70%']"
      min_lines: 50
    - path: "frontend/src/views/home/components/HomeCarousel.vue"
      provides: "轮播图组件，4秒自动切换，淡入淡出"
      min_lines: 60
    - path: "frontend/src/views/home/HomeView.vue"
      provides: "环形图+轮播图+快捷入口更新"
      exports: "环形图占位、CarouselBanner插入、gridItems扩展"
    - path: "internal/dashboard/model.go"
      provides: "GetTodoStatsResponse/GetTimeLimitedStatsResponse类型"
      exports: "RingChartStats结构体"
    - path: "internal/dashboard/repository.go"
      provides: "GetTodoRingStats/GetTimeLimitedRingStats查询"
      exports: "DashboardRepository方法扩展"
    - path: "internal/dashboard/service.go"
      provides: "GetTodoStats/GetTimeLimitedStats聚合方法"
      exports: "DashboardService方法扩展"
    - path: "internal/dashboard/handler.go"
      provides: "GetTodoStats/GetTimeLimitedStats端点"
      exports: "GET /dashboard/todo-stats, GET /dashboard/time-limited-stats"
    - path: "internal/dashboard/router.go"
      provides: "新端点路由注册"
      exports: "/dashboard/todo-stats, /dashboard/time-limited-stats"
  key_links:
    - from: "TodoRingChart.vue"
      to: "/api/v1/dashboard/todo-stats"
      via: "fetch onMounted"
      pattern: "fetchDashboard.*todo-stats"
    - from: "HomeView.vue"
      to: "HomeCarousel.vue"
      via: "import and template usage"
      pattern: "HomeCarousel.*carouselItems"
---

<objective>
在 HomeView.vue 顶部实现两张完成率环形图（全部事项 / 限时任务），集成轮播图组件和快捷入口扩展，并完成后端 GetTodoStats / GetTimeLimitedStats API 供环形图数据使用。

Purpose: 为管理员提供一眼可知的完成率概览（D-09-01/D-09-02），同时展示轮播公告和快捷入口。
Output:
- TodoRingChart.vue 组件（ECharts pie, radius=['40%','70%']）
- HomeCarousel.vue 组件（淡入淡出，4秒切换）
- HomeView.vue 更新（环形图 + 轮播图 + 快捷入口 +3）
- 后端 GetTodoStats / GetTimeLimitedStats API（环形图数据）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@internal/dashboard/model.go
@internal/dashboard/repository.go
@internal/dashboard/service.go
@internal/dashboard/handler.go
@internal/dashboard/router.go
@frontend/src/views/home/HomeView.vue
@frontend/src/views/employee/OrgChart.vue
@frontend/src/api/dashboard.ts

From OrgChart.vue — ECharts setup pattern:
```typescript
import VChart from 'vue-echarts'
import { use } from 'echarts/core'
import { PieChart } from 'echarts/charts'
import { TooltipComponent, LegendComponent } from 'echarts/components'
import { CanvasRenderer } from 'echarts/renderers'
use([PieChart, TooltipComponent, LegendComponent, CanvasRenderer])
```

From HomeView.vue — gridItems structure:
```typescript
const gridItems = [
  { path: '/employee', label: '员工管理', icon: User, color: '#1677ff', bg: '#e6f4ff' },
  // existing 6 items
]
// New 3 per D-09-08:
  { path: '/employee/create', label: '新入职', icon: UserAdd, color: '#1677ff', bg: '#e6f4ff' },
  { path: '/tool/salary', label: '调薪', icon: TrendCharts, color: '#52c41a', bg: '#f6ffed' },
  { path: '/attendance/clock-live', label: '考勤', icon: Clock, color: '#722ed1', bg: '#f9f0ff' },
```

From HomeView.vue — shortcuts-grid:
```css
.shortcuts-grid {
  grid-template-columns: repeat(6, 1fr);
  /* 扩展为 repeat(9, 1fr) per D-09-08 */
}
```

From socialinsurance/scheduler.go — redisLocker pattern:
```go
if rdb != nil {
    locker := newRedisLocker(rdb, "easyhr:todo:")
    opts = append(opts, gocron.WithDistributedLocker(locker))
}
```

Color palette per UI-SPEC:
- Brand: #4F6EF7, Hover: #6B84F9, Light: #EEF1FF
- Danger: #FF5630, DangerLight: #FFF1ED
- Warning: #FFAB00, WarningLight: #FFF8E6
- Success: #36B37E, SuccessLight: #E8F8EF
- Gray/terminated: #8C8C8C
- Text: #172B4D (primary), #5E6C84 (secondary), #97A0AF (tertiary)
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From internal/dashboard/model.go:
```go
type TodoType string
const (
    TodoSocialInsurance TodoType = "social_insurance"
    TodoTax            TodoType = "tax"
    TodoEmployee       TodoType = "employee"
    TodoContract       TodoType = "contract"
    TodoExpense        TodoType = "expense"
    TodoVoucher        TodoType = "voucher"
)

type TodoItem struct {
    Type     TodoType `json:"type"`
    Title    string   `json:"title"`
    Count    int      `json:"count"`
    Deadline string   `json:"deadline,omitempty"`
    Priority int      `json:"priority"`
}
```

From frontend/src/api/dashboard.ts:
```typescript
export interface TodoItem {
  type: string
  title: string
  count: number
  deadline?: string
  priority: number
}
```

From internal/dashboard/handler.go:
```go
func (h *Handler) GetDashboard(c *gin.Context) {
    orgIDVal, exists := c.Get("org_id")
    // ... extract orgID, call h.svc.GetDashboard(...)
}
```

From internal/dashboard/router.go:
```go
func RegisterRouter(rg *gin.RouterGroup, authMiddleware gin.HandlerFunc, db *gorm.DB) {
    repo := NewRepository(db)
    svc := NewService(repo)
    handler := NewHandler(svc)
    rg.Use(authMiddleware)
    rg.GET("", handler.GetDashboard)
    rg.GET("/employee-dashboard", handler.GetEmployeeDashboard)
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 后端环形图统计 API（GetTodoStats / GetTimeLimitedStats）</name>
  <files>internal/dashboard/model.go, internal/dashboard/repository.go, internal/dashboard/service.go, internal/dashboard/handler.go, internal/dashboard/router.go</files>
  <read_first>internal/dashboard/model.go, internal/dashboard/repository.go, internal/dashboard/service.go, internal/dashboard/handler.go, internal/dashboard/router.go</read_first>
  <action>
Wave 0: 在 internal/dashboard/model.go 中新增以下结构体（不修改现有代码）：

```go
// RingChartStats holds ring chart data for todo completion stats.
type RingChartStats struct {
    Completed     int     `json:"completed"`     // 已完成数
    Pending       int     `json:"pending"`       // 待办数
    Total         int     `json:"total"`         // 总数
    Percent       float64 `json:"percent"`       // 完成率百分比，0-100
}

// GetTodoStatsResponse 全事项完成率响应
type GetTodoStatsResponse struct {
    Stats RingChartStats `json:"stats"`
}

// GetTimeLimitedStatsResponse 限时任务完成率响应
type GetTimeLimitedStatsResponse struct {
    Stats RingChartStats `json:"stats"`
}
```

Wave 0: 在 internal/dashboard/repository.go 的 DashboardRepositoryImpl 中新增两个查询方法：

```go
// GetTodoRingStats returns completed/pending counts for all todos.
// Only counts status IN ('pending','completed'). Skips terminated.
func (r *DashboardRepositoryImpl) GetTodoRingStats(ctx context.Context, orgID int64) (completed, pending int, err error) {
    var completedCount, pendingCount int64

    // TODO: Check if todo_items table exists via r.db.Migrator().HasTable.
    // If table doesn't exist, return (0, 0, nil) gracefully.

    if err := r.db.Model(&TodoItemRecord{}).
        Scopes(middleware.TenantScope(orgID)).
        Where("status = ?", "completed").
        Count(&completedCount).Error; err != nil {
        return 0, 0, fmt.Errorf("count completed todos: %w", err)
    }

    if err := r.db.Model(&TodoItemRecord{}).
        Scopes(middleware.TenantScope(orgID)).
        Where("status = ?", "pending").
        Count(&pendingCount).Error; err != nil {
        return 0, 0, fmt.Errorf("count pending todos: %w", err)
    }

    return int(completedCount), int(pendingCount), nil
}

// GetTimeLimitedRingStats returns completed/pending counts for time-limited todos only.
func (r *DashboardRepositoryImpl) GetTimeLimitedRingStats(ctx context.Context, orgID int64) (completed, pending int, err error) {
    var completedCount, pendingCount int64

    if err := r.db.Model(&TodoItemRecord{}).
        Scopes(middleware.TenantScope(orgID)).
        Where("is_time_limited = ? AND status = ?", true, "completed").
        Count(&completedCount).Error; err != nil {
        return 0, 0, fmt.Errorf("count completed time-limited todos: %w", err)
    }

    if err := r.db.Model(&TodoItemRecord{}).
        Scopes(middleware.TenantScope(orgID)).
        Where("is_time_limited = ? AND status = ?", true, "pending").
        Count(&pendingCount).Error; err != nil {
        return 0, 0, fmt.Errorf("count pending time-limited todos: %w", err)
    }

    return int(completedCount), int(pendingCount), nil
}
```

Wave 0: 在 internal/dashboard/repository.go 末尾追加 TodoItemRecord 占位类型（用于 GORM 查询）：

```go
// TodoItemRecord mirrors todo.TodoItem for repository queries.
// Defined here to avoid circular import; actual model in internal/todo/model.go.
type TodoItemRecord struct {
    ID             uint
    OrgID         uint
    Status         string `gorm:"column:status"`
    IsTimeLimited bool   `gorm:"column:is_time_limited"`
}

func (TodoItemRecord) TableName() string { return "todo_items" }
```

Wave 0: 在 internal/dashboard/repository.go 的 DashboardRepository 接口中新增两个方法签名：

```go
GetTodoRingStats(ctx context.Context, orgID int64) (completed, pending int, err error)
GetTimeLimitedRingStats(ctx context.Context, orgID int64) (completed, pending int, err error)
```

Wave 1: 在 internal/dashboard/service.go 中扩展 DashboardService：

在 ServiceInterface 中新增方法签名：
```go
GetTodoStats(ctx context.Context, orgID int64) (*GetTodoStatsResponse, error)
GetTimeLimitedStats(ctx context.Context, orgID int64) (*GetTimeLimitedStatsResponse, error)
```

在 DashboardService struct 已有，不需要改动 struct 定义。

新增两个方法：
```go
// GetTodoStats returns ring chart stats for all todos.
func (s *DashboardService) GetTodoStats(ctx context.Context, orgID int64) (*GetTodoStatsResponse, error) {
    completed, pending, err := s.repo.GetTodoRingStats(ctx, orgID)
    if err != nil {
        return nil, fmt.Errorf("get todo ring stats: %w", err)
    }
    total := completed + pending
    percent := 0.0
    if total > 0 {
        percent = math.Round(float64(completed)*100/float64(total)*100) / 100
    }
    return &GetTodoStatsResponse{
        Stats: RingChartStats{
            Completed: completed,
            Pending:   pending,
            Total:     total,
            Percent:   percent,
        },
    }, nil
}

// GetTimeLimitedStats returns ring chart stats for time-limited todos only.
func (s *DashboardService) GetTimeLimitedStats(ctx context.Context, orgID int64) (*GetTimeLimitedStatsResponse, error) {
    completed, pending, err := s.repo.GetTimeLimitedRingStats(ctx, orgID)
    if err != nil {
        return nil, fmt.Errorf("get time-limited ring stats: %w", err)
    }
    total := completed + pending
    percent := 0.0
    if total > 0 {
        percent = math.Round(float64(completed)*100/float64(total)*100) / 100
    }
    return &GetTimeLimitedStatsResponse{
        Stats: RingChartStats{
            Completed: completed,
            Pending:   pending,
            Total:     total,
            Percent:   percent,
        },
    }, nil
}
```

确保 import 中添加 "math" 包（如果不存在）。

Wave 1: 在 internal/dashboard/handler.go 中新增两个 Handler 方法：

```go
// GetTodoStats handles GET /api/v1/dashboard/todo-stats.
func (h *Handler) GetTodoStats(c *gin.Context) {
    orgIDVal, exists := c.Get("org_id")
    if !exists {
        response.Error(c, http.StatusUnauthorized, 40100, "missing org_id in context")
        return
    }
    orgID, ok := orgIDVal.(int64)
    if !ok {
        response.Error(c, http.StatusUnauthorized, 40100, "invalid org_id type")
        return
    }

    result, err := h.svc.GetTodoStats(c.Request.Context(), orgID)
    if err != nil {
        response.Error(c, http.StatusInternalServerError, 50000, "failed to get todo stats: "+err.Error())
        return
    }

    response.Success(c, result)
}

// GetTimeLimitedStats handles GET /api/v1/dashboard/time-limited-stats.
func (h *Handler) GetTimeLimitedStats(c *gin.Context) {
    orgIDVal, exists := c.Get("org_id")
    if !exists {
        response.Error(c, http.StatusUnauthorized, 40100, "missing org_id in context")
        return
    }
    orgID, ok := orgIDVal.(int64)
    if !ok {
        response.Error(c, http.StatusUnauthorized, 40100, "invalid org_id type")
        return
    }

    result, err := h.svc.GetTimeLimitedStats(c.Request.Context(), orgID)
    if err != nil {
        response.Error(c, http.StatusInternalServerError, 50000, "failed to get time-limited stats: "+err.Error())
        return
    }

    response.Success(c, result)
}
```

Wave 1: 在 internal/dashboard/router.go 中新增路由注册：

在 RegisterRouter 函数的 rg.Use(authMiddleware) 之后添加：
```go
rg.GET("/todo-stats", handler.GetTodoStats)
rg.GET("/time-limited-stats", handler.GetTimeLimitedStats)
```

CRITICAL: 需要确保 internal/todo/model.go 中的 todo_items 表在 dashboard repo 查询前存在。在 repository.go 的两个方法开头添加表存在检查：
```go
if !r.db.Migrator().HasTable(&TodoItemRecord{}) {
    return 0, 0, nil
}
```
  </action>
  <verify>
    <automated>grep -n "GetTodoStats\|GetTimeLimitedStats\|RingChartStats" internal/dashboard/model.go internal/dashboard/service.go internal/dashboard/handler.go internal/dashboard/router.go | head -30</automated>
  </verify>
  <done>
    GET /api/v1/dashboard/todo-stats 返回 {stats:{completed,pending,total,percent}}
    GET /api/v1/dashboard/time-limited-stats 返回 {stats:{completed,pending,total,percent}}
    两个 API 都从 org_id 租户隔离提取数据
  </done>
  <acceptance_criteria>
    - [ ] internal/dashboard/model.go contains RingChartStats, GetTodoStatsResponse, GetTimeLimitedStatsResponse
    - [ ] internal/dashboard/repository.go contains GetTodoRingStats() and GetTimeLimitedRingStats() methods on DashboardRepositoryImpl
    - [ ] internal/dashboard/repository.go has TodoItemRecord struct and TableName() returning "todo_items"
    - [ ] internal/dashboard/service.go contains GetTodoStats() and GetTimeLimitedStats() methods
    - [ ] internal/dashboard/handler.go contains GetTodoStats handler and GetTimeLimitedStats handler
    - [ ] internal/dashboard/router.go registers /todo-stats and /time-limited-stats routes
    - [ ] Both stats methods use integer math, avoid float division by zero (total=0 returns percent=0)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 前端环形图组件 + HomeView 更新</name>
  <files>frontend/src/views/home/components/TodoRingChart.vue, frontend/src/views/home/HomeView.vue, frontend/src/api/dashboard.ts</files>
  <read_first>frontend/src/views/home/HomeView.vue, frontend/src/views/employee/OrgChart.vue, frontend/src/api/dashboard.ts</read_first>
  <action>
Wave 0: 创建 frontend/src/views/home/components/ 目录（如果不存在）：

创建 frontend/src/views/home/components/TodoRingChart.vue：

```vue
<template>
  <div class="ring-chart-wrapper">
    <div v-if="loading" class="ring-loading">
      <el-icon class="is-loading" size="20"><Loading /></el-icon>
    </div>
    <div v-else-if="!hasData" class="ring-empty">暂无数据</div>
    <div v-else class="ring-content">
      <v-chart :option="option" autoresize style="height: 200px" />
      <div class="ring-center-label">
        <div class="center-percent">{{ stats.percent }}%</div>
        <div class="center-count">{{ stats.completed }}/{{ stats.total }}</div>
      </div>
    </div>
  </div>
</template>

<script setup lang="ts">
import { computed, ref, onMounted } from 'vue'
import VChart from 'vue-echarts'
import { use } from 'echarts/core'
import { PieChart } from 'echarts/charts'
import { TooltipComponent, LegendComponent } from 'echarts/components'
import { CanvasRenderer } from 'echarts/renderers'
import { Loading } from '@element-plus/icons-vue'
import { fetchTodoStats } from '@/api/dashboard'

use([PieChart, TooltipComponent, LegendComponent, CanvasRenderer])

interface Props {
  /** 'all': 全部事项, 'time-limited': 限时任务' */
  type: 'all' | 'time-limited'
}

const props = defineProps<Props>()
const loading = ref(false)
const stats = ref({ completed: 0, pending: 0, total: 0, percent: 0 })

const hasData = computed(() => stats.value.total > 0)

const option = computed(() => ({
  tooltip: { trigger: 'item', formatter: '{b}: {c} ({d}%)' },
  series: [{
    type: 'pie',
    radius: ['40%', '70%'],
    center: ['50%', '50%'],
    avoidLabelOverlap: false,
    label: { show: false },
    emphasis: {
      label: { show: false },
    },
    data: [
      {
        value: stats.value.completed,
        name: '已完成',
        itemStyle: { color: '#4F6EF7' },
      },
      {
        value: stats.value.pending,
        name: '待办',
        itemStyle: { color: '#E8ECF0' },
      },
    ],
  }],
}))

async function load() {
  loading.value = true
  try {
    const endpoint = props.type === 'all' ? 'all' : 'time-limited'
    const data = await fetchTodoRingStats(endpoint)
    stats.value = data.stats
  } finally {
    loading.value = false
  }
}

onMounted(load)
</script>

<style scoped lang="scss">
.ring-chart-wrapper {
  display: flex;
  flex-direction: column;
  align-items: center;
  position: relative;
  min-height: 200px;
}

.ring-content {
  position: relative;
  width: 100%;
}

.ring-center-label {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  text-align: center;
  pointer-events: none;
}

.center-percent {
  font-size: 22px;
  font-weight: 700;
  color: #172B4D;
  line-height: 1.2;
}

.center-count {
  font-size: 13px;
  color: #97A0AF;
  margin-top: 2px;
}

.ring-loading,
.ring-empty {
  height: 200px;
  display: flex;
  align-items: center;
  justify-content: center;
  color: #97A0AF;
  font-size: 13px;
}
</style>
```

Wave 1: 更新 frontend/src/api/dashboard.ts，追加环形图API函数：

在文件末尾追加：

```typescript
export interface RingChartStats {
  completed: number
  pending: number
  total: number
  percent: number
}

export interface RingChartResponse {
  stats: RingChartStats
}

export function fetchTodoRingStats(type: 'all' | 'time-limited'): Promise<RingChartResponse> {
  const endpoint = type === 'all' ? '/dashboard/todo-stats' : '/dashboard/time-limited-stats'
  return request.get(endpoint).then((res) => res.data)
}
```

Wave 1: 更新 frontend/src/views/home/HomeView.vue：

在 <script setup> 中：
1. 新增 import: `import TodoRingChart from './components/TodoRingChart.vue'`
2. 从 @element-plus/icons-vue 新增 import: `UserAdd, TrendCharts`（如果不存在）
3. 在 gridItems 数组中追加3个新入口（保留原有6个）：
```typescript
{ path: '/employee/create', label: '新入职', icon: UserAdd, color: '#1677ff', bg: '#e6f4ff' },
{ path: '/tool/salary', label: '调薪', icon: TrendCharts, color: '#52c41a', bg: '#f6ffed' },
{ path: '/attendance/clock-live', label: '考勤', icon: Clock, color: '#722ed1', bg: '#f9f0ff' },
```

在 <template> 中，在待办事项区域之前（page-header 之后）新增环形图卡片区域：

```html
<!-- 完成率环形图 -->
<div class="ring-chart-card section">
  <div class="ring-chart-row">
    <div class="ring-chart-item">
      <div class="ring-chart-label">全部事项完成率</div>
      <TodoRingChart type="all" />
    </div>
    <div class="ring-chart-item">
      <div class="ring-chart-label">限时任务完成率</div>
      <TodoRingChart type="time-limited" />
    </div>
  </div>
</div>
```

同时在 <style> 中新增环形图相关样式（在 .section 之后）：

```scss
// 环形图卡片
.ring-chart-card {
  // same as .section background/radius/padding
}

.ring-chart-row {
  display: flex;
  gap: 16px;
}

.ring-chart-item {
  flex: 1;
  text-align: center;
}

.ring-chart-label {
  font-size: 14px;
  font-weight: 600;
  color: #172B4D;
  margin-bottom: 8px;
}
```

将 shortcuts-grid 的 grid-template-columns 从 repeat(6, 1fr) 改为 repeat(9, 1fr)：

```scss
.shortcuts-grid {
  display: grid;
  grid-template-columns: repeat(9, 1fr);
  gap: 8px;
  margin-top: 12px;
}
```

在响应式断点中：
- 1200px 以下: shortcuts-grid 改为 repeat(6, 1fr)
- 900px 以下: shortcuts-grid 改为 repeat(3, 1fr)
- 768px 以下: shortcuts-grid 改为 repeat(2, 1fr)

  </action>
  <verify>
    <automated>grep -n "TodoRingChart\|ring-chart\|fetchTodoRingStats\|UserAdd\|TrendCharts" frontend/src/views/home/HomeView.vue frontend/src/api/dashboard.ts | head -20</automated>
  </verify>
  <done>
    HomeView.vue 页面标题下方展示两张并排环形图
    快捷入口从6个扩展到9个，新增新入职/调薪/考勤三个入口
    点击快捷入口跳转到对应功能页
  </done>
  <acceptance_criteria>
    - [ ] frontend/src/views/home/components/TodoRingChart.vue exists with ECharts pie chart (radius=['40%','70%'])
    - [ ] TodoRingChart uses fetchTodoRingStats from api/dashboard.ts
    - [ ] HomeView.vue template contains TodoRingChart component usage for both "all" and "time-limited"
    - [ ] gridItems array in HomeView.vue contains all 9 shortcuts (6 existing + 3 new)
    - [ ] shortcuts-grid CSS uses grid-template-columns: repeat(9, 1fr)
    - [ ] ring-chart-card section appears between page-header and todo-section in template
    - [ ] TodoRingChart.vue shows "暂无数据" when total=0, percentage and count otherwise
    - [ ] fetchTodoRingStats function added to frontend/src/api/dashboard.ts
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: 轮播图 HomeCarousel 组件</name>
  <files>frontend/src/views/home/components/HomeCarousel.vue, frontend/src/api/carousel.ts</files>
  <read_first>frontend/src/api/dashboard.ts</read_first>
  <action>
Wave 0: 创建轮播图 API 客户端 frontend/src/api/carousel.ts：

```typescript
import request from '@/api/request'

export interface CarouselItem {
  id: number
  image_url: string
  link_url: string
  sort_order: number
  active: boolean
  start_at: string
  end_at: string
}

export function fetchCarousels(): Promise<{ data: CarouselItem[] }> {
  return request.get('/carousels').then((res) => res.data)
}
```

Wave 1: 创建轮播图组件 frontend/src/views/home/components/HomeCarousel.vue：

```vue
<template>
  <div v-if="carousels.length > 0" class="home-carousel">
    <el-carousel
      :interval="4000"
      trigger="click"
      indicator-position="outside"
      type="card"
      height="160px"
      arrow="never"
      @change="onChange"
    >
      <el-carousel-item v-for="item in carousels" :key="item.id">
        <a :href="item.link_url || 'javascript:void(0)'" class="carousel-link" target="_blank">
          <img :src="item.image_url" :alt="'carousel-' + item.id" class="carousel-image" />
        </a>
      </el-carousel-item>
    </el-carousel>
  </div>
</template>

<script setup lang="ts">
import { ref, onMounted } from 'vue'
import { fetchCarousels, type CarouselItem } from '@/api/carousel'

const carousels = ref<CarouselItem[]>([])

async function load() {
  try {
    const res = await fetchCarousels()
    // Filter only active carousels based on current time
    const now = new Date()
    carousels.value = (res.data || []).filter((item: CarouselItem) => {
      if (!item.active) return false
      const start = item.start_at ? new Date(item.start_at) : null
      const end = item.end_at ? new Date(item.end_at) : null
      if (start && now < start) return false
      if (end && now > end) return false
      return true
    })
  } catch {
    carousels.value = []
  }
}

function onChange(_index: number) {
  // No-op: carousel auto-advances
}

onMounted(load)
</script>

<style scoped lang="scss">
.home-carousel {
  margin-bottom: 16px;
  border-radius: 8px;
  overflow: hidden;
}

.carousel-link {
  display: block;
  width: 100%;
  height: 100%;
  text-decoration: none;
}

.carousel-image {
  width: 100%;
  height: 100%;
  object-fit: cover;
  border-radius: 8px;
}

:deep(.el-carousel__item--card) {
  border-radius: 8px;
}

:deep(.el-carousel__item) {
  background-color: #f5f7fa;
}

:deep(.el-carousel__indicator--horizontal) {
  padding: 4px 4px;
}

:deep(.el-carousel__button) {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background-color: rgba(79, 110, 247, 0.3);
}

:deep(.el-carousel__indicator.is-active .el-carousel__button) {
  background-color: #4F6EF7;
}
</style>
```

Wave 1: 更新 HomeView.vue，在环形图卡片区域下方插入轮播图组件：

在 <script setup> 中新增 import：
```typescript
import HomeCarousel from './components/HomeCarousel.vue'
```

在 <template> 的 ring-chart-card 之后，todo-section 之前新增：
```html
<!-- 轮播图公告 -->
<HomeCarousel />
```

同时将 HomeView.vue 的待办事项 section 全宽布局改为：

在轮播图之后，shortcuts-grid 之前，确保 DOM 顺序为：
1. ring-chart-card
2. HomeCarousel
3. todo-section
4. bottom-row (shortcuts + overview)
  </action>
  <verify>
    <automated>grep -n "HomeCarousel\|fetchCarousels\|carousel" frontend/src/views/home/HomeView.vue frontend/src/api/carousel.ts | head -20</automated>
  </verify>
  <done>
    首页展示1-3张轮播图，淡入淡出动画，4秒自动切换
    轮播图点击跳转到配置的 link_url
    轮播图在当前时间不在 start_at/end_at 区间内时自动隐藏
    无轮播图时不显示轮播区域
  </done>
  <acceptance_criteria>
    - [ ] frontend/src/api/carousel.ts exists with fetchCarousels() function and CarouselItem interface
    - [ ] frontend/src/views/home/components/HomeCarousel.vue exists with el-carousel
    - [ ] el-carousel uses :interval="4000" for 4-second auto-advance
    - [ ] HomeCarousel filters active carousels by current time vs start_at/end_at
    - [ ] HomeView.vue imports and uses HomeCarousel component in template
    - [ ] Carousel images use object-fit: cover for consistent display
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> API | Ring chart stats API returns org-scoped data only |
| API -> database | GORM queries use TenantScope(org_id) for all stats |
| Carousel link_url | External URLs open in new tab, no user input executed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-01 | Information Disclosure | GetTodoStats API | mitigate | org_id from JWT context, GORM TenantScope, table exists check returns zeros |
| T-9-03 | XSS | Carousel link_url | mitigate | link_url used as href target="_blank", not rendered as innerHTML |
</threat_model>

<verification>
After Plan 01, verify:
1. `grep -n "GetTodoStats\|GetTimeLimitedStats" internal/dashboard/ -r` shows all 4 files modified
2. `grep -n "TodoRingChart\|HomeCarousel" frontend/src/views/home/HomeView.vue` shows component imports and usage
3. Backend builds: `cd /Users/wencai/github/EasyHR && go build ./internal/dashboard/...`
4. Frontend builds: `cd frontend && npm run build` (or type-check: npx vue-tsc --noEmit)
</verification>

<success_criteria>
- 管理员在首页看到两张并排环形图（全部事项 / 限时任务），蓝色系配色，radius=['40%','70%']
- 环形图显示完成率百分比和 已完成/总数 数字，total=0时显示"暂无数据"
- 首页在环形图下方展示1-3张轮播图，淡入淡出，4秒切换，点击跳转 link_url
- 快捷入口从6个扩展到9个，新增 新入职（/employee/create）、调薪（/tool/salary）、考勤（/attendance/clock-live）
- GET /api/v1/dashboard/todo-stats 返回 {stats:{completed,pending,total,percent}}
- GET /api/v1/dashboard/time-limited-stats 返回 {stats:{completed,pending,total,percent}}
- 两个 API 均正确使用 org_id 租户隔离
</success_criteria>

<output>
After completion, create `.planning/phases/09-待办中心/09-01-PLAN-SUMMARY.md`
</output>
