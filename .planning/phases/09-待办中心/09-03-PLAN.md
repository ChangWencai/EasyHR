---
phase: "09"
plan: "03"
type: "execute"
wave: 2
depends_on:
  - "09-01"
  - "09-02"
files_modified:
  - "internal/todo/handler.go"
  - "internal/todo/router.go"
  - "internal/todo/scheduler.go"
  - "internal/todo/repository.go"
  - "internal/employee/service.go"
  - "internal/employee/contract_service.go"
  - "internal/upload/handler.go"
  - "internal/upload/router.go"
  - "frontend/src/views/todo/TodoListView.vue"
  - "frontend/src/views/todo/InviteFillPage.vue"
  - "frontend/src/views/todo/CarouselManagePage.vue"
  - "frontend/src/api/carousel.ts"
  - "frontend/src/router/index.ts"
autonomous: true
requirements:
  - "TODO-04"
  - "TODO-05"
  - "TODO-11"
  - "TODO-12"
  - "TODO-13"
  - "TODO-14"
  - "TODO-15"
  - "TODO-16"
  - "TODO-17"
  - "TODO-18"
user_setup: []
must_haves:
  truths:
    - "管理员可邀请协办（生成 Token 链接），协办人通过链接无需登录即可填写"
    - "管理员可终止待办任务（状态变为 terminated，不软删除）"
    - "asynq 每日凌晨 02:00 扫描限时任务更新 urgency_status"
    - "asynq 每日 08:00 检查轮播图激活状态"
    - "系统自动生成7种限时任务（合同新签/续签/个税/社保缴费/增减员/社保基数调整/公积金基数调整）"
    - "限时任务 urgency_status 按 D-09-04 规则（1-7天超期=超时，15天+=失效）"
    - "管理员可在轮播图管理页面上传/编辑/删除轮播图（最多3张）"
    - "管理员可启用/停用轮播图，调整排序"
  artifacts:
    - path: "internal/todo/scheduler.go"
      provides: "asynq定时任务（urgency_status扫描 + 轮播图激活 + 7种限时任务生成）"
      exports: "ProcessUrgencyStatus, ProcessCarouselActivation, ProcessTaskGeneration"
    - path: "internal/todo/handler.go"
      provides: "InviteTodo/TerminateTodo/SubmitInvite端点"
      exports: "POST /todos/:id/invite, PUT /todos/:id/terminate, POST /todos/invite/:token/submit (public)"
    - path: "internal/todo/router.go"
      provides: "协办和终止路由 + 公开提交路由"
      exports: "/todos/:id/invite, /todos/:id/terminate, /todos/invite/:token/submit (public)"
    - path: "internal/employee/service.go"
      provides: "合同签署时触发待办创建"
      exports: "CreateContract时调用todoService.CreateTodo"
    - path: "internal/employee/contract_service.go"
      provides: "合同续签提醒触发待办创建"
      exports: "CheckContractRenewal时创建待办"
    - path: "frontend/src/views/todo/TodoListView.vue"
      provides: "协办邀请/终止操作入口"
      exports: "actions列：邀请协办、终止任务"
    - path: "frontend/src/views/todo/InviteFillPage.vue"
      provides: "协办填写页（Token验证+表单）"
      exports: "/todo/:id/invite?token=xxx"
    - path: "frontend/src/views/todo/CarouselManagePage.vue"
      provides: "轮播图管理页面（上传/编辑/删除/排序/启用停用）"
      exports: "/carousel/manage"
    - path: "frontend/src/api/carousel.ts"
      provides: "轮播图CRUD API客户端"
      exports: "createCarousel, updateCarousel, deleteCarousel, listAllCarousels, uploadImage"
    - path: "internal/upload/handler.go"
      provides: "图片上传OSS端点"
      exports: "POST /upload/image"
  key_links:
    - from: "scheduler.go"
      to: "todo_items table"
      via: "gocron cron 02:00"
      pattern: "ProcessUrgencyStatus.*UPDATE"
    - from: "scheduler.go"
      to: "carousel_items table"
      via: "gocron cron 08:00"
      pattern: "ProcessCarouselActivation.*UPDATE"
    - from: "TodoListView.vue"
      to: "/api/v1/todos/:id/invite"
      via: "invite action"
      pattern: "api/todo.*inviteTodo"
    - from: "InviteFillPage.vue"
      to: "/api/v1/todos/invite/:token/verify"
      via: "token verify on mount"
      pattern: "verifyInviteToken"
    - from: "CarouselManagePage.vue"
      to: "/api/v1/carousels"
      via: "CRUD API calls"
      pattern: "createCarousel.*updateCarousel.*deleteCarousel"
---

<objective>
实现协办邀请（Token 链接）和终止任务功能，以及 gocron 定时任务调度器（urgency_status 扫描 + 轮播图激活 + 7种限时任务生成）和各模块限时任务触发逻辑。

FIX (checker iteration 3): D-09-06 requires admin to manage 1-3 carousel images — Plan 02+03 were missing CreateCarousel/UpdateCarousel/DeleteCarousel endpoints and CarouselManagePage.vue. Added in this revision.

Purpose: 完成待办中心的协作和管理功能，以及自动化限时任务生成和状态管理。
Output:
- internal/todo/scheduler.go（gocron 调度器）
- internal/todo handler.go 扩展（InviteTodo/TerminateTodo/SubmitInvite）
- internal/todo router.go 扩展（协办和终止路由 + 公开提交路由）
- internal/todo repository.go 扩展（ScanUrgencyStatus等）
- internal/employee/service.go + contract_service.go（限时任务触发）
- internal/upload/handler.go（图片 OSS 上传端点）
- frontend TodoListView.vue 扩展（协办/终止操作）
- frontend InviteFillPage.vue（协办填写页）
- frontend CarouselManagePage.vue（轮播图管理页 per D-09-06）
- frontend router.ts 扩展（/todo/:id/invite 路由 + /carousel/manage 路由）
- frontend src/api/carousel.ts 扩展（createCarousel/updateCarousel/deleteCarousel）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@internal/socialinsurance/scheduler.go
@internal/employee/service.go
@internal/employee/contract_service.go
@internal/employee/invitation_model.go
@frontend/src/views/employee/RegisterPage.vue

From socialinsurance/scheduler.go — gocron + redis locker pattern:
```go
func StartScheduler(rdb *redis.Client, svc *Service) (gocron.Scheduler, error) {
    opts := []gocron.SchedulerOption{gocron.WithLocation(time.FixedZone("CST", 8*3600))}
    if rdb != nil {
        locker := newRedisLocker(rdb, "easyhr:todo:")
        opts = append(opts, gocron.WithDistributedLocker(locker))
    }
    s, err := gocron.NewScheduler(opts...)
    // 每日 02:00 + 08:00
}
```

From RegisterPage.vue — Token verification pattern:
```typescript
const token = route.params.token as string
// GET /registration/:token -> detail.value = ...
// errorState: token invalid (404) / expired (410)
```

D-09-04 Urgency Status Rules (CRITICAL — must be implemented exactly):
- expired: deadline < now - 15 days (deadline - now < -15)
- overdue: deadline in [now - 15 days, now + 7 days] range (deadline - now in [-15, 7])
- normal: deadline > now + 7 days (deadline - now > 7)

7种限时任务生成时机 per D-09-05:
1. contract_new: 员工入职时（service.CreateEmployee 末尾）
2. contract_renew: 合同到期前1个月（每分钟扫描 or 每日cron）
3. tax_declaration: 每月1日生成（gocron每月1日）
4. si_payment: 每月1日生成（gocron每月1日）
5. si_change: 每月5日生成（gocron每月5日）
6. si_annual_base: 每年6月15日-7月15日（gocron每年）
7. fund_annual_base: 同上

D-09-06: CarouselItem management — admin configures 1-3 carousel images via CarouselManagePage.vue. Requires Create/Update/Delete endpoints + OSS upload.
</context>

<interfaces>
From socialinsurance/scheduler.go:
```go
type redisLocker struct {
    rdb *redis.Client
    prefix string
}
func newRedisLocker(rdb *redis.Client, prefix string) *redisLocker
func StartScheduler(rdb *redis.Client, svc *Service) (gocron.Scheduler, error)
```

From internal/todo/service.go (Plan 02 output):
```go
func (s *Service) CreateTodo(ctx context.Context, item *TodoItem) error
func GenerateInviteToken() (string, error)
func (s *Service) ListCarousels(ctx context.Context, orgID int64) ([]CarouselItem, error)
```

From internal/todo/model.go (Plan 02 output):
```go
const TodoTypeContractNew = "contract_new"
const TodoTypeContractRenew = "contract_renew"
const TodoTypeTaxDeclaration = "tax_declaration"
const TodoTypeSIPayment = "si_payment"
const TodoTypeSIChange = "si_change"
const TodoTypeSIAnnualBase = "si_annual_base"
const TodoTypeFundAnnualBase = "fund_annual_base"
const TodoStatusPending = "pending"
const TodoStatusCompleted = "completed"
const TodoStatusTerminated = "terminated"
const UrgencyNormal = "normal"
const UrgencyOverdue = "overdue"
const UrgencyExpired = "expired"
```

From frontend/src/views/employee/RegisterPage.vue:
```typescript
const route = useRoute()
const token = route.params.token as string
// errorState: 404=invalid, 410=expired
// submitted: boolean for success state
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 后端协办邀请 + 终止任务 API + gocron 调度器 + 轮播图CRUD</name>
  <files>internal/todo/repository.go, internal/todo/service.go, internal/todo/handler.go, internal/todo/router.go, internal/todo/scheduler.go, internal/upload/handler.go, internal/upload/router.go</files>
  <read_first>internal/socialinsurance/scheduler.go, internal/employee/invitation_model.go, internal/todo/service.go, internal/todo/repository.go, internal/upload/handler.go</read_first>
  <action>
Wave 1: 扩展 internal/todo/repository.go，追加以下 Repository 方法（追加到现有文件末尾）：

```go
// CreateInvite 创建协办邀请
func (r *Repository) CreateInvite(ctx context.Context, invite *TodoInvite) error {
    return r.db.Create(invite).Error
}

// FindInviteByToken 根据Token查找邀请
func (r *Repository) FindInviteByToken(ctx context.Context, token string) (*TodoInvite, error) {
    var invite TodoInvite
    err := r.db.Where("token = ?", token).First(&invite).Error
    if err != nil {
        return nil, err
    }
    return &invite, nil
}

// MarkInviteUsed 标记邀请已使用
// FIX (checker): was passing pointer-to-time to non-pointer field in Updates map
func (r *Repository) MarkInviteUsed(ctx context.Context, id int64) error {
    now := time.Now()
    return r.db.Model(&TodoInvite{}).Where("id = ?", id).Updates(map[string]interface{}{
        "status":  InviteStatusUsed,
        "used_at": now, // time.Time 是值类型，直接传值
    }).Error
}

// ScanUrgencyStatus 扫描限时任务更新 urgency_status
// D-09-04 规则（精确实现）：
//   expired  : deadline < now - 15 days  (deadline - now < -15)
//   overdue  : deadline in [now - 15 days, now + 7 days] (deadline - now in [-15, 7])
//   normal   : deadline > now + 7 days   (deadline - now > 7)
// 仅更新限时、未完成的记录（排除 completed/terminated）
func (r *Repository) ScanUrgencyStatus(ctx context.Context) (int, error) {
    now := time.Now()
    expiredThreshold := now.AddDate(0, 0, -15)
    overdueStart := now.AddDate(0, 0, -15)
    overdueEnd := now.AddDate(0, 0, 7)
    normalThreshold := now.AddDate(0, 0, 7)

    // 基础过滤器：限时 + 未完成
    activeFilter := "is_time_limited = ? AND status NOT IN ?"
    activeArgs := []interface{}{true, []string{TodoStatusCompleted, TodoStatusTerminated}}

    // 1. expired: deadline < now - 15 days
    result1, err := r.db.Model(&TodoItem{}).
        Where(activeFilter+" AND deadline IS NOT NULL AND deadline < ?", append(activeArgs, expiredThreshold)...).
        Update("urgency_status", UrgencyExpired)
    if err != nil {
        return 0, fmt.Errorf("update expired: %w", err)
    }

    // 2. overdue: deadline in [now-15, now+7] range
    result2, err := r.db.Model(&TodoItem{}).
        Where(activeFilter+" AND deadline IS NOT NULL AND deadline >= ? AND deadline <= ? AND urgency_status != ?",
            append(activeArgs, overdueStart, overdueEnd, UrgencyOverdue)...).
        Update("urgency_status", UrgencyOverdue)
    if err != nil {
        return 0, fmt.Errorf("update overdue: %w", err)
    }

    // 3. normal: deadline > now + 7 days
    result3, err := r.db.Model(&TodoItem{}).
        Where(activeFilter+" AND deadline IS NOT NULL AND deadline > ? AND urgency_status != ?",
            append(activeArgs, normalThreshold, UrgencyNormal)...).
        Update("urgency_status", UrgencyNormal)
    if err != nil {
        return 0, fmt.Errorf("update normal: %w", err)
    }

    return int(result1.RowsAffected + result2.RowsAffected + result3.RowsAffected), nil
}

// UpdateCarouselActivation 根据 start_at/end_at 更新轮播图 active 状态
func (r *Repository) UpdateCarouselActivation(ctx context.Context) (int, error) {
    now := time.Now()
    // 激活：时间区间内且 active=true
    activate := r.db.Model(&CarouselItem{}).
        Where("active = ? AND (start_at IS NULL OR start_at <= ?) AND (end_at IS NULL OR end_at >= ?)",
            false, now, now).
        Update("active", true)
    if activate.Error != nil {
        return 0, activate.Error
    }

    // 停用：超出时间区间
    deactivate := r.db.Model(&CarouselItem{}).
        Where("(start_at IS NOT NULL AND start_at > ?) OR (end_at IS NOT NULL AND end_at < ?)",
            now, now).
        Update("active", false)
    if deactivate.Error != nil {
        return 0, deactivate.Error
    }

    return int(activate.RowsAffected + deactivate.RowsAffected), nil
}

// GenerateMonthlyTodos 生成每月例行待办（个税申报+社保缴费，每月1日触发）
func (r *Repository) GenerateMonthlyTodos(ctx context.Context) error {
    cst := time.FixedZone("CST", 8*3600)
    today := time.Now().In(cst)
    deadline := time.Date(today.Year(), today.Month(), 15, 23, 59, 59, 0, cst)

    // 获取所有活跃组织
    var orgIDs []int64
    r.db.Model(&struct{ OrgID int64 }{}).Table("organizations").Pluck("id", &orgIDs)

    for _, orgID := range orgIDs {
        // 个税申报
        taxDeadline := deadline
        taxTodo := &TodoItem{
            OrgID:         orgID,
            Title:         fmt.Sprintf("%d月个税申报，请于15日前完成", today.Month()),
            Type:          TodoTypeTaxDeclaration,
            Deadline:      &taxDeadline,
            IsTimeLimited: true,
            Status:        TodoStatusPending,
            UrgencyStatus: UrgencyNormal,
            SourceType:    "tax",
        }
        _ = r.CreateTodo(ctx, taxTodo) // 幂等由 CreateTodo 处理

        // 社保缴费
        siDeadline := deadline
        siTodo := &TodoItem{
            OrgID:         orgID,
            Title:         fmt.Sprintf("%d月社保公积金缴费，请于15日前完成", today.Month()),
            Type:          TodoTypeSIPayment,
            Deadline:      &siDeadline,
            IsTimeLimited: true,
            Status:        TodoStatusPending,
            UrgencyStatus: UrgencyNormal,
            SourceType:    "socialinsurance",
        }
        _ = r.CreateTodo(ctx, siTodo)
    }

    return nil
}

// GenerateAnnualBaseTodos 生成年度基数调整待办（每年6月15日触发）
func (r *Repository) GenerateAnnualBaseTodos(ctx context.Context) error {
    cst := time.FixedZone("CST", 8*3600)
    today := time.Now().In(cst)

    // 社保基数调整截止日：7月15日
    siDeadline := time.Date(today.Year(), 7, 15, 23, 59, 59, 0, cst)
    // 公积金基数调整截止日：7月15日
    fundDeadline := siDeadline

    var orgIDs []int64
    r.db.Model(&struct{ OrgID int64 }{}).Table("organizations").Pluck("id", &orgIDs)

    for _, orgID := range orgIDs {
        // 年度社保基数调整
        siTodo := &TodoItem{
            OrgID:         orgID,
            Title:         "年度社保基数调整，请于7月15日前完成",
            Type:          TodoTypeSIAnnualBase,
            Deadline:      &siDeadline,
            IsTimeLimited: true,
            Status:        TodoStatusPending,
            UrgencyStatus: UrgencyNormal,
            SourceType:    "socialinsurance",
        }
        _ = r.CreateTodo(ctx, siTodo)

        // 年度公积金基数调整
        fundTodo := &TodoItem{
            OrgID:         orgID,
            Title:         "年度公积金基数调整，请于7月15日前完成",
            Type:          TodoTypeFundAnnualBase,
            Deadline:      &fundDeadline,
            IsTimeLimited: true,
            Status:        TodoStatusPending,
            UrgencyStatus: UrgencyNormal,
            SourceType:    "socialinsurance",
        }
        _ = r.CreateTodo(ctx, fundTodo)
    }

    return nil
}

// FIX (checker): Carousel CRUD methods added for D-09-06 admin management

// ListAllCarousels 查询企业所有轮播图（管理用，不过滤active状态）
func (r *Repository) ListAllCarousels(ctx context.Context, orgID int64) ([]CarouselItem, error) {
    var items []CarouselItem
    err := r.db.Model(&CarouselItem{}).
        Scopes(middleware.TenantScope(orgID)).
        Order("sort_order DESC, created_at DESC").
        Find(&items).Error
    if err != nil {
        return nil, fmt.Errorf("list all carousels: %w", err)
    }
    return items, nil
}

// CreateCarousel 创建轮播图
func (r *Repository) CreateCarousel(ctx context.Context, item *CarouselItem) error {
    // 限制最多3张
    var count int64
    if err := r.db.Model(&CarouselItem{}).Scopes(middleware.TenantScope(item.OrgID)).Count(&count).Error; err != nil {
        return err
    }
    if count >= 3 {
        return fmt.Errorf("轮播图最多3张，当前已有%d张", count)
    }
    return r.db.Create(item).Error
}

// UpdateCarousel 更新轮播图
func (r *Repository) UpdateCarousel(ctx context.Context, orgID int64, id int64, updates map[string]interface{}) error {
    result := r.db.Model(&CarouselItem{}).
        Scopes(middleware.TenantScope(orgID)).
        Where("id = ?", id).
        Updates(updates)
    if result.Error != nil {
        return result.Error
    }
    if result.RowsAffected == 0 {
        return fmt.Errorf("carousel not found")
    }
    return nil
}

// DeleteCarousel 删除轮播图
func (r *Repository) DeleteCarousel(ctx context.Context, orgID int64, id int64) error {
    result := r.db.Model(&CarouselItem{}).
        Scopes(middleware.TenantScope(orgID)).
        Where("id = ?", id).
        Delete(&CarouselItem{})
    if result.Error != nil {
        return result.Error
    }
    if result.RowsAffected == 0 {
        return fmt.Errorf("carousel not found")
    }
    return nil
}
```

Wave 1: 扩展 internal/todo/service.go，追加以下方法（追加到文件末尾）：

```go
// InviteTodo 创建协办邀请（生成Token）
type InviteResult struct {
    URL string `json:"url"`
}
func (s *Service) InviteTodo(ctx context.Context, orgID int64, todoID int64, userID int64) (*InviteResult, error) {
    // 验证待办存在
    _, err := s.repo.FindTodoByID(ctx, orgID, todoID)
    if err != nil {
        return nil, fmt.Errorf("todo not found")
    }

    token, err := GenerateInviteToken()
    if err != nil {
        return nil, fmt.Errorf("generate token: %w", err)
    }

    now := time.Now()
    invite := &TodoInvite{
        OrgID:     orgID,
        TodoID:   todoID,
        Token:    token,
        Status:   InviteStatusPending,
        CreatedBy: userID,
        ExpiresAt: now.Add(InviteExpiryDuration),
    }
    if err := s.repo.CreateInvite(ctx, invite); err != nil {
        return nil, fmt.Errorf("create invite: %w", err)
    }

    // 返回邀请URL（前端通过 VITE_API_BASE_URL 拼接完整地址）
    inviteURL := fmt.Sprintf("/todo/%d/invite?token=%s", todoID, token)
    return &InviteResult{URL: inviteURL}, nil
}

// TerminateTodo 终止待办任务（状态变为 terminated）
func (s *Service) TerminateTodo(ctx context.Context, orgID int64, todoID int64) error {
    return s.repo.UpdateTodoStatus(ctx, orgID, todoID, TodoStatusTerminated)
}

// VerifyInviteToken 验证协办邀请Token
type VerifyResult struct {
    Valid   bool  `json:"valid"`
    Expired bool  `json:"expired"`
    Title   string `json:"title"`
    TodoID  int64  `json:"todo_id"`
}
func (s *Service) VerifyInviteToken(ctx context.Context, token string) (*VerifyResult, error) {
    invite, err := s.repo.FindInviteByToken(ctx, token)
    if err != nil {
        return &VerifyResult{Valid: false}, nil // 找不到Token → 无效
    }

    if invite.Status == InviteStatusUsed {
        return &VerifyResult{Valid: false}, nil
    }
    if time.Now().After(invite.ExpiresAt) {
        return &VerifyResult{Valid: false, Expired: true}, nil
    }

    // 查询关联待办标题
    todo, err := s.repo.FindTodoByID(ctx, invite.OrgID, invite.TodoID)
    if err != nil {
        return &VerifyResult{Valid: false}, nil
    }

    return &VerifyResult{
        Valid:  true,
        Title:  todo.Title,
        TodoID: invite.TodoID,
    }, nil
}

// SubmitInvite 协办人提交信息（公开接口，无需登录）
// FIX (checker): endpoint was completely missing — must be added here AND registered in router
type SubmitInviteRequest struct {
    Name   string `json:"name" binding:"required"`
    Phone  string `json:"phone"`
    Remark string `json:"remark"`
}
type SubmitInviteResponse struct {
    Success bool   `json:"success"`
    Message string `json:"message"`
}
func (s *Service) SubmitInvite(ctx context.Context, token string, req *SubmitInviteRequest) (*SubmitInviteResponse, error) {
    // 验证Token
    invite, err := s.repo.FindInviteByToken(ctx, token)
    if err != nil {
        return nil, fmt.Errorf("invalid token")
    }
    if invite.Status == InviteStatusUsed {
        return nil, fmt.Errorf("link already used")
    }
    if time.Now().After(invite.ExpiresAt) {
        return nil, fmt.Errorf("link expired")
    }

    // 标记邀请已使用
    if err := s.repo.MarkInviteUsed(ctx, invite.ID); err != nil {
        return nil, fmt.Errorf("mark used: %w", err)
    }

    // TODO: 存储提交内容（可扩展为附件上传等）
    // 当前仅记录协办人已提交，后续可在此扩展实际业务逻辑
    _ = req.Name
    _ = req.Phone
    _ = req.Remark

    return &SubmitInviteResponse{
        Success: true,
        Message: "信息已提交，感谢您的配合",
    }, nil
}

// ScanUrgencyStatus 扫描限时任务更新紧迫状态
func (s *Service) ScanUrgencyStatus(ctx context.Context) (int, error) {
    return s.repo.ScanUrgencyStatus(ctx)
}

// UpdateCarouselActivation 更新轮播图激活状态
func (s *Service) UpdateCarouselActivation(ctx context.Context) (int, error) {
    return s.repo.UpdateCarouselActivation(ctx)
}

// FIX (checker): Carousel CRUD service methods added for D-09-06 admin management

// CarouselRequest 创建/更新轮播图请求
type CarouselRequest struct {
    ImageURL  string     `json:"image_url" binding:"required"`
    LinkURL   string     `json:"link_url"`
    SortOrder int        `json:"sort_order"`
    Active    bool       `json:"active"`
    StartAt   *time.Time `json:"start_at"`
    EndAt     *time.Time `json:"end_at"`
}

// CreateCarousel 创建轮播图（最多3张）
func (s *Service) CreateCarousel(ctx context.Context, orgID int64, req *CarouselRequest) (*CarouselItem, error) {
    item := &CarouselItem{
        OrgID:     orgID,
        ImageURL:  req.ImageURL,
        LinkURL:   req.LinkURL,
        SortOrder: req.SortOrder,
        Active:    req.Active,
        StartAt:   req.StartAt,
        EndAt:     req.EndAt,
    }
    if err := s.repo.CreateCarousel(ctx, item); err != nil {
        return nil, err
    }
    return item, nil
}

// UpdateCarousel 更新轮播图
func (s *Service) UpdateCarousel(ctx context.Context, orgID int64, id int64, req *CarouselRequest) error {
    updates := map[string]interface{}{
        "image_url": req.ImageURL,
        "active":    req.Active,
    }
    if req.LinkURL != "" {
        updates["link_url"] = req.LinkURL
    }
    if req.SortOrder > 0 {
        updates["sort_order"] = req.SortOrder
    }
    if req.StartAt != nil {
        updates["start_at"] = req.StartAt
    }
    if req.EndAt != nil {
        updates["end_at"] = req.EndAt
    }
    return s.repo.UpdateCarousel(ctx, orgID, id, updates)
}

// DeleteCarousel 删除轮播图
func (s *Service) DeleteCarousel(ctx context.Context, orgID int64, id int64) error {
    return s.repo.DeleteCarousel(ctx, orgID, id)
}

// ListAllCarousels 查询所有轮播图（管理用）
func (s *Service) ListAllCarousels(ctx context.Context, orgID int64) ([]CarouselItem, error) {
    return s.repo.ListAllCarousels(ctx, orgID)
}
```

Wave 1: 扩展 internal/todo/handler.go，追加以下 Handler 方法：

```go
import (
    "encoding/json"
    "net/http"
    "strconv"
    "time"

    "github.com/gin-gonic/gin"
    "github.com/wencai/easyhr/internal/common/response"
    "github.com/wencai/easyhr/internal/todo"
)

// InviteTodo handles POST /api/v1/todos/:id/invite
func (h *Handler) InviteTodo(c *gin.Context) {
    orgIDVal, exists := c.Get("org_id")
    if !exists {
        response.Error(c, http.StatusUnauthorized, 40100, "missing org_id")
        return
    }
    userIDVal, _ := c.Get("user_id")
    orgID := orgIDVal.(int64)
    userID, _ := userIDVal.(int64)

    idStr := c.Param("id")
    todoID, err := strconv.ParseInt(idStr, 10, 64)
    if err != nil {
        response.Error(c, http.StatusBadRequest, 40000, "invalid id")
        return
    }

    result, err := h.svc.InviteTodo(c.Request.Context(), orgID, todoID, userID)
    if err != nil {
        response.Error(c, http.StatusInternalServerError, 50000, "invite failed: "+err.Error())
        return
    }

    response.Success(c, result)
}

// TerminateTodo handles PUT /api/v1/todos/:id/terminate
func (h *Handler) TerminateTodo(c *gin.Context) {
    orgIDVal, exists := c.Get("org_id")
    if !exists {
        response.Error(c, http.StatusUnauthorized, 40100, "missing org_id")
        return
    }
    orgID := orgIDVal.(int64)

    idStr := c.Param("id")
    todoID, err := strconv.ParseInt(idStr, 10, 64)
    if err != nil {
        response.Error(c, http.StatusBadRequest, 40000, "invalid id")
        return
    }

    if err := h.svc.TerminateTodo(c.Request.Context(), orgID, todoID); err != nil {
        response.Error(c, http.StatusInternalServerError, 50000, "terminate failed: "+err.Error())
        return
    }

    response.Success(c, gin.H{"id": todoID})
}

// VerifyInviteToken handles GET /api/v1/todos/invite/:token
// Public endpoint — no auth required
func (h *Handler) VerifyInviteToken(c *gin.Context) {
    token := c.Param("token")
    if token == "" {
        response.Error(c, http.StatusBadRequest, 40000, "missing token")
        return
    }

    result, err := h.svc.VerifyInviteToken(c.Request.Context(), token)
    if err != nil {
        response.Error(c, http.StatusInternalServerError, 50000, "verify failed: "+err.Error())
        return
    }

    if !result.Valid {
        if result.Expired {
            response.Error(c, http.StatusGone, 41001, "邀请链接已过期")
            return
        }
        response.Error(c, http.StatusNotFound, 40400, "邀请链接无效")
        return
    }

    response.Success(c, result)
}

// GetInviteTodo handles GET /api/v1/todos/invite/:token/detail — public
func (h *Handler) GetInviteTodo(c *gin.Context) {
    token := c.Param("token")
    result, err := h.svc.VerifyInviteToken(c.Request.Context(), token)
    if err != nil || !result.Valid {
        response.Error(c, http.StatusNotFound, 40400, "邀请不存在")
        return
    }
    response.Success(c, result)
}

// SubmitInvite handles POST /api/v1/todos/invite/:token/submit
// FIX (checker): SubmitInvite endpoint was completely missing from original plan.
// Public endpoint — no auth required. Used by InviteFillPage.vue.
func (h *Handler) SubmitInvite(c *gin.Context) {
    token := c.Param("token")
    if token == "" {
        response.Error(c, http.StatusBadRequest, 40000, "missing token")
        return
    }

    var req struct {
        Name   string `json:"name" binding:"required"`
        Phone  string `json:"phone"`
        Remark string `json:"remark"`
    }
    if err := c.ShouldBindJSON(&req); err != nil {
        response.Error(c, http.StatusBadRequest, 40000, "invalid request: name is required")
        return
    }

    result, err := h.svc.SubmitInvite(c.Request.Context(), token, &req)
    if err != nil {
        // 判断错误类型返回合适状态码
        errMsg := err.Error()
        if errMsg == "invalid token" {
            response.Error(c, http.StatusNotFound, 40400, "邀请链接无效")
            return
        }
        if errMsg == "link already used" {
            response.Error(c, http.StatusConflict, 40900, "该链接已使用过")
            return
        }
        if errMsg == "link expired" {
            response.Error(c, http.StatusGone, 41001, "邀请链接已过期")
            return
        }
        response.Error(c, http.StatusInternalServerError, 50000, "submit failed: "+errMsg)
        return
    }

    response.Success(c, result)
}

// FIX (checker): Carousel CRUD handlers for D-09-06 admin management

// ListAllCarousels handles GET /api/v1/carousels/admin — all carousels for management
func (h *Handler) ListAllCarousels(c *gin.Context) {
    orgIDVal, exists := c.Get("org_id")
    if !exists {
        response.Error(c, http.StatusUnauthorized, 40100, "missing org_id")
        return
    }
    orgID := orgIDVal.(int64)

    items, err := h.svc.ListAllCarousels(c.Request.Context(), orgID)
    if err != nil {
        response.Error(c, http.StatusInternalServerError, 50000, "list carousels failed: "+err.Error())
        return
    }

    response.Success(c, gin.H{"data": items})
}

// CreateCarousel handles POST /api/v1/carousels
func (h *Handler) CreateCarousel(c *gin.Context) {
    orgIDVal, exists := c.Get("org_id")
    if !exists {
        response.Error(c, http.StatusUnauthorized, 40100, "missing org_id")
        return
    }
    orgID := orgIDVal.(int64)

    var req todo.CarouselRequest
    if err := c.ShouldBindJSON(&req); err != nil {
        response.Error(c, http.StatusBadRequest, 40000, "invalid request: image_url is required")
        return
    }

    item, err := h.svc.CreateCarousel(c.Request.Context(), orgID, &req)
    if err != nil {
        // Return 400 for max carousel limit error
        response.Error(c, http.StatusBadRequest, 40001, err.Error())
        return
    }

    response.Success(c, gin.H{"data": item})
}

// UpdateCarousel handles PUT /api/v1/carousels/:id
func (h *Handler) UpdateCarousel(c *gin.Context) {
    orgIDVal, exists := c.Get("org_id")
    if !exists {
        response.Error(c, http.StatusUnauthorized, 40100, "missing org_id")
        return
    }
    orgID := orgIDVal.(int64)

    idStr := c.Param("id")
    id, err := strconv.ParseInt(idStr, 10, 64)
    if err != nil {
        response.Error(c, http.StatusBadRequest, 40000, "invalid id")
        return
    }

    var req todo.CarouselRequest
    if err := c.ShouldBindJSON(&req); err != nil {
        response.Error(c, http.StatusBadRequest, 40000, "invalid request")
        return
    }

    if err := h.svc.UpdateCarousel(c.Request.Context(), orgID, id, &req); err != nil {
        if err.Error() == "carousel not found" {
            response.Error(c, http.StatusNotFound, 40400, "轮播图不存在")
            return
        }
        response.Error(c, http.StatusInternalServerError, 50000, "update carousel failed: "+err.Error())
        return
    }

    response.Success(c, gin.H{"id": id})
}

// DeleteCarousel handles DELETE /api/v1/carousels/:id
func (h *Handler) DeleteCarousel(c *gin.Context) {
    orgIDVal, exists := c.Get("org_id")
    if !exists {
        response.Error(c, http.StatusUnauthorized, 40100, "missing org_id")
        return
    }
    orgID := orgIDVal.(int64)

    idStr := c.Param("id")
    id, err := strconv.ParseInt(idStr, 10, 64)
    if err != nil {
        response.Error(c, http.StatusBadRequest, 40000, "invalid id")
        return
    }

    if err := h.svc.DeleteCarousel(c.Request.Context(), orgID, id); err != nil {
        if err.Error() == "carousel not found" {
            response.Error(c, http.StatusNotFound, 40400, "轮播图不存在")
            return
        }
        response.Error(c, http.StatusInternalServerError, 50000, "delete carousel failed: "+err.Error())
        return
    }

    response.Success(c, gin.H{"id": id})
}
```

Wave 1: 扩展 internal/todo/router.go，追加新路由：

```go
// RegisterRouter 已存在，追加以下路由：
// 注意：VerifyInviteToken / GetInviteTodo / SubmitInvite 为公开路由，不需要 authMiddleware

// 追加受保护路由（authMiddleware 之后）
rg.POST("/todos/:id/invite", handler.InviteTodo)
rg.PUT("/todos/:id/terminate", handler.TerminateTodo)

// 追加公开路由（在 RegisterRouter 函数末尾追加，不需要 authMiddleware）
// public := rg.Group("")  // 空 Group 无中间件，等同于全局注册
// GET /api/v1/todos/invite/:token
rg.GET("/todos/invite/:token", handler.VerifyInviteToken)
// GET /api/v1/todos/invite/:token/detail
rg.GET("/todos/invite/:token/detail", handler.GetInviteTodo)
// POST /api/v1/todos/invite/:token/submit
rg.POST("/todos/invite/:token/submit", handler.SubmitInvite) // FIX (checker): missing route added

// FIX (checker): Carousel CRUD routes for D-09-06 admin management
// All carousel management routes require auth
rg.GET("/carousels/admin", handler.ListAllCarousels) // 管理用：返回所有轮播图（含停用）
rg.POST("/carousels", handler.CreateCarousel)          // 创建轮播图
rg.PUT("/carousels/:id", handler.UpdateCarousel)      // 更新轮播图
rg.DELETE("/carousels/:id", handler.DeleteCarousel)  // 删除轮播图
```

Wave 1: 创建 internal/upload/handler.go（图片 OSS 上传端点）：

```go
package upload

import (
    "fmt"
    "io"
    "net/http"
    "os"
    "path/filepath"
    "time"

    "github.com/gin-gonic/gin"
    "github.com/google/uuid"
    "github.com/wencai/easyhr/internal/common/response"
)

// Handler 图片上传处理器
type Handler struct {
    uploadDir string
    baseURL   string // e.g. https://your-bucket.oss-cn-beijing.aliyuncs.com
}

// NewHandler 创建 Handler
func NewHandler(uploadDir, baseURL string) *Handler {
    return &Handler{uploadDir: uploadDir, baseURL: baseURL}
}

// UploadImage handles POST /upload/image
// Accepts multipart/form-data with field name "file"
// Returns the uploaded file URL
func (h *Handler) UploadImage(c *gin.Context) {
    file, err := c.FormFile("file")
    if err != nil {
        response.Error(c, http.StatusBadRequest, 40000, "missing file")
        return
    }

    // 限制文件大小 5MB
    if file.Size > 5*1024*1024 {
        response.Error(c, http.StatusBadRequest, 40001, "文件大小不能超过5MB")
        return
    }

    // 验证文件类型
    ext := filepath.Ext(file.Filename)
    allowedExts := map[string]bool{".jpg": true, ".jpeg": true, ".png": true, ".gif": true, ".webp": true}
    if !allowedExts[ext] {
        response.Error(c, http.StatusBadRequest, 40002, "仅支持 jpg/jpeg/png/gif/webp 格式")
        return
    }

    // 生成唯一文件名
    filename := fmt.Sprintf("%s_%d%s", uuid.New().String()[:8], time.Now().Unix(), ext)
    dst := filepath.Join(h.uploadDir, filename)

    // 确保目录存在
    os.MkdirAll(h.uploadDir, 0755)

    // 保存文件
    src, err := file.Open()
    if err != nil {
        response.Error(c, http.StatusInternalServerError, 50000, "open file failed")
        return
    }
    defer src.Close()

    dstFile, err := os.Create(dst)
    if err != nil {
        response.Error(c, http.StatusInternalServerError, 50000, "create file failed")
        return
    }
    defer dstFile.Close()

    if _, err := io.Copy(dstFile, src); err != nil {
        response.Error(c, http.StatusInternalServerError, 50000, "save file failed")
        return
    }

    // 返回 URL（实际生产中应替换为 OSS 签名 URL）
    url := fmt.Sprintf("%s/%s", h.baseURL, filename)
    response.Success(c, gin.H{"url": url})
}
```

Wave 1: 创建 internal/upload/router.go：

```go
package upload

import (
    "github.com/gin-gonic/gin"
)

// RegisterRouter 注册 upload 路由（无认证公开接口）
func RegisterRouter(rg *gin.RouterGroup, uploadDir, baseURL string) {
    handler := NewHandler(uploadDir, baseURL)
    // 公开接口，无需 authMiddleware
    rg.POST("/upload/image", handler.UploadImage)
}
```

Wave 1: 创建 internal/todo/scheduler.go：

```go
package todo

import (
	"context"
	"fmt"
	"log"
	"time"

	"github.com/go-co-op/gocron/v2"
	"github.com/redis/go-redis/v9"
)

// Scheduler 定时任务调度器
type Scheduler struct {
	repo            *Repository
	rdb             *redis.Client
	contractService *ContractServiceWrapper // FIX (checker): scheduler needs contractService for renewal check
}

// NewScheduler 创建调度器（接受 todoRepo 和可选的 redis.Client）
func NewScheduler(repo *Repository, rdb *redis.Client, contractSvc *ContractServiceWrapper) *Scheduler {
	return &Scheduler{repo: repo, rdb: rdb, contractService: contractSvc}
}

// ContractServiceWrapper wraps the employee.ContractService to avoid circular imports.
// FIX (checker): scheduler.go must call contractService.CheckContractRenewalReminders,
// but cannot import employee package directly. Use interface-based injection.
type ContractServiceWrapper interface {
	CheckContractRenewalReminders(ctx context.Context) error
}

// Start 启动调度器
func (s *Scheduler) Start() (gocron.Scheduler, error) {
	opts := []gocron.SchedulerOption{
		gocron.WithLocation(time.FixedZone("CST", 8*3600)),
	}

	// Redis 可用时启用分布式锁（复用 socialinsurance 的 redisLocker）
	if s.rdb != nil {
		locker := newTodoRedisLocker(s.rdb)
		opts = append(opts, gocron.WithDistributedLocker(locker))
	}

	sched, err := gocron.NewScheduler(opts...)
	if err != nil {
		return nil, fmt.Errorf("create scheduler: %w", err)
	}

	// 每日 02:00 CST — 扫描 urgency_status
	_, err = sched.NewJob(
		gocron.DailyJob(1, gocron.NewAtTimes(gocron.NewAtTime(2, 0, 0))),
		gocron.NewTask(func() {
			ctx := context.Background()
			count, err := s.repo.ScanUrgencyStatus(ctx)
			if err != nil {
				log.Printf("[todo-scheduler] ScanUrgencyStatus failed: %v", err)
			} else {
				log.Printf("[todo-scheduler] ScanUrgencyStatus updated %d items", count)
			}
		}),
		gocron.WithName("todo-urgency-scan"),
	)
	if err != nil {
		return nil, fmt.Errorf("create urgency scan job: %w", err)
	}

	// 每日 08:00 CST — 更新轮播图激活状态
	_, err = sched.NewJob(
		gocron.DailyJob(1, gocron.NewAtTimes(gocron.NewAtTime(8, 0, 0))),
		gocron.NewTask(func() {
			ctx := context.Background()
			count, err := s.repo.UpdateCarouselActivation(ctx)
			if err != nil {
				log.Printf("[todo-scheduler] UpdateCarouselActivation failed: %v", err)
			} else {
				log.Printf("[todo-scheduler] UpdateCarouselActivation updated %d items", count)
			}
		}),
		gocron.WithName("todo-carousel-activation"),
	)
	if err != nil {
		return nil, fmt.Errorf("create carousel activation job: %w", err)
	}

	// 每月1日 00:01 CST — 生成当月个税申报和社保缴费待办
	_, err = sched.NewJob(
		gocron.CronJob("1 0 1 * *", true), // 每月1日00:01
		gocron.NewTask(func() {
			ctx := context.Background()
			if err := s.repo.GenerateMonthlyTodos(ctx); err != nil {
				log.Printf("[todo-scheduler] GenerateMonthlyTodos failed: %v", err)
			} else {
				log.Printf("[todo-scheduler] GenerateMonthlyTodos completed")
			}
		}),
		gocron.WithName("todo-monthly-generate"),
	)
	if err != nil {
		return nil, fmt.Errorf("create monthly generate job: %w", err)
	}

	// 每月5日 00:01 CST — 生成当月社保增减员待办
	_, err = sched.NewJob(
		gocron.CronJob("1 0 5 * *", true), // 每月5日00:01
		gocron.NewTask(func() {
			ctx := context.Background()
			cst := time.FixedZone("CST", 8*3600)
			today := time.Now().In(cst)
			deadline := time.Date(today.Year(), today.Month(), 20, 23, 59, 59, 0, cst)

			var orgIDs []int64
			s.repo.db.Model(&struct{ OrgID int64 }{}).Table("organizations").Pluck("id", &orgIDs)

			for _, orgID := range orgIDs {
				todo := &TodoItem{
					OrgID:         orgID,
					Title:         fmt.Sprintf("%d月社保公积金增减员，请于20日前完成", today.Month()),
					Type:          TodoTypeSIChange,
					Deadline:      &deadline,
					IsTimeLimited: true,
					Status:        TodoStatusPending,
					UrgencyStatus: UrgencyNormal,
					SourceType:    "socialinsurance",
				}
				_ = s.repo.CreateTodo(ctx, todo)
			}
			log.Printf("[todo-scheduler] GenerateSIChangeTodos completed")
		}),
		gocron.WithName("todo-si-change-generate"),
	)
	if err != nil {
		return nil, fmt.Errorf("create si-change generate job: %w", err)
	}

	// 每年6月15日 00:01 CST — 生成年度基数调整待办
	_, err = sched.NewJob(
		gocron.CronJob("1 0 15 6 *", true), // 每年6月15日00:01
		gocron.NewTask(func() {
			ctx := context.Background()
			if err := s.repo.GenerateAnnualBaseTodos(ctx); err != nil {
				log.Printf("[todo-scheduler] GenerateAnnualBaseTodos failed: %v", err)
			} else {
				log.Printf("[todo-scheduler] GenerateAnnualBaseTodos completed")
			}
		}),
		gocron.WithName("todo-annual-base-generate"),
	)
	if err != nil {
		return nil, fmt.Errorf("create annual base generate job: %w", err)
	}

	// FIX (checker): 每日 02:05 CST — 合同续签检查（实际调用 contractService）
	// 原计划只有日志打印，必须改为实际调用
	_, err = sched.NewJob(
		gocron.DailyJob(1, gocron.NewAtTimes(gocron.NewAtTime(2, 5, 0))),
		gocron.NewTask(func() {
			ctx := context.Background()
			if s.contractService != nil {
				if err := s.contractService.CheckContractRenewalReminders(ctx); err != nil {
					log.Printf("[todo-scheduler] CheckContractRenewalReminders failed: %v", err)
				} else {
					log.Printf("[todo-scheduler] CheckContractRenewalReminders completed")
				}
			} else {
				log.Printf("[todo-scheduler] contractService not available, skipping renewal check")
			}
		}),
		gocron.WithName("todo-contract-renewal-check"),
	)
	if err != nil {
		return nil, fmt.Errorf("create contract renewal check job: %w", err)
	}

	sched.Start()
	return sched, nil
}

// todoRedisLocker 基于 Redis 的分布式锁
type todoRedisLocker struct {
	rdb *redis.Client
}

func newTodoRedisLocker(rdb *redis.Client) *todoRedisLocker {
	return &todoRedisLocker{rdb: rdb}
}

func (l *todoRedisLocker) Lock(ctx context.Context, key string) (gocron.Lock, error) {
	lockKey := "easyhr:todo:lock:" + key
	ok, err := l.rdb.SetNX(ctx, lockKey, "locked", 60*time.Second).Result()
	if err != nil {
		return nil, err
	}
	if !ok {
		return nil, fmt.Errorf("job already locked: %s", key)
	}
	return &todoRedisLock{rdb: l.rdb, key: lockKey}, nil
}

type todoRedisLock struct {
	rdb *redis.Client
	key string
}

func (l *todoRedisLock) Unlock(ctx context.Context) error {
	return l.rdb.Del(ctx, l.key).Err()
}
```

Wave 1: 检查 main.go，确认在启动时调用 todo scheduler：

在 main.go 中查找 socialinsurance.StartScheduler 调用，在其后追加：
```go
// 创建 todoRepo（todo.NewRepository 需要 db）
todoRepo := todo.NewRepository(db)
todoSvc := todo.NewService(todoRepo)

// 获取 contractService（已在 main.go 创建）
var contractSvcWrapper todo.ContractServiceWrapper
if contractService != nil {
    contractSvcWrapper = contractService // employee.ContractService implements ContractServiceWrapper
}

// 启动 todo scheduler
todoScheduler := todo.NewScheduler(todoRepo, rdb, contractSvcWrapper)
_, err = todoScheduler.Start()
if err != nil {
    log.Fatalf("start todo scheduler: %v", err)
}
```

确保 todo router 注册在 main.go 中（在 v1 group 注册之后）：
```go
todo.RegisterRouter(v1, authMiddleware, db)
```

同时确保 upload router 注册：
```go
upload.RegisterRouter(v1, uploadDir, uploadBaseURL)
```

FIX (checker): Also check that socialinsurance/scheduler.go no longer references socialinsurance scheduler triggering todo generation — the todo scheduler itself handles all monthly/annual todo generation directly. Remove any redundant socialinsurance -> todo trigger code from socialinsurance/scheduler.go.
  </action>
  <verify>
    <automated>grep -n "InviteTodo\|TerminateTodo\|SubmitInvite\|ScanUrgencyStatus\|GenerateMonthly\|GenerateAnnual\|CheckContractRenewal\|CreateCarousel\|UpdateCarousel\|DeleteCarousel\|ListAllCarousels\|UploadImage" internal/todo/*.go internal/upload/*.go | head -40</automated>
  </verify>
  <done>
    POST /api/v1/todos/:id/invite 生成协办邀请，返回 /todo/:id/invite?token=xxx 链接
    PUT /api/v1/todos/:id/terminate 将待办状态更新为 terminated
    GET /api/v1/todos/invite/:token 公开验证 Token（无需登录）
    POST /api/v1/todos/invite/:token/submit 公开提交协办信息（无需登录）
    GET /api/v1/carousels/admin 返回所有轮播图（管理用）
    POST /api/v1/carousels 创建轮播图（最多3张）
    PUT /api/v1/carousels/:id 更新轮播图
    DELETE /api/v1/carousels/:id 删除轮播图
    POST /upload/image 图片上传端点（multipart/form-data）
    gocron scheduler 每日 02:00 扫描更新 urgency_status（expired/overdue/normal 三段式精确条件）
    gocron scheduler 每日 08:00 更新轮播图激活状态
    gocron scheduler 每月1日生成个税申报+社保缴费待办
    gocron scheduler 每月5日生成社保增减员待办
    gocron scheduler 每年6月15日生成年度基数调整待办
    gocron scheduler 每日 02:05 实际调用 contractService.CheckContractRenewalReminders
  </done>
  <acceptance_criteria>
    - [ ] internal/todo/scheduler.go exists with 6 scheduled jobs (urgency scan, carousel activation, monthly todos, monthly SI change, annual base, contract renewal)
    - [ ] internal/todo/handler.go has InviteTodo, TerminateTodo, VerifyInviteToken, GetInviteTodo, SubmitInvite handlers
    - [ ] internal/todo/handler.go has ListAllCarousels, CreateCarousel, UpdateCarousel, DeleteCarousel handlers
    - [ ] internal/todo/router.go registers POST /todos/:id/invite, PUT /todos/:id/terminate, GET /todos/invite/:token, POST /todos/invite/:token/submit (public)
    - [ ] internal/todo/router.go registers GET /carousels/admin, POST /carousels, PUT /carousels/:id, DELETE /carousels/:id
    - [ ] internal/todo/repository.go has ScanUrgencyStatus with CORRECT D-09-04 rules: expired (deadline < now-15d), overdue (deadline in [now-15d, now+7d]), normal (deadline > now+7d)
    - [ ] internal/todo/repository.go has CreateCarousel, UpdateCarousel, DeleteCarousel, ListAllCarousels methods
    - [ ] internal/upload/handler.go has UploadImage handler
    - [ ] internal/upload/router.go registers POST /upload/image
    - [ ] Scheduler uses time.FixedZone("CST", 8*3600) for timezone correctness
    - [ ] Scheduler uses redis distributed lock when rdb != nil
    - [ ] TerminateTodo updates status to TodoStatusTerminated (not soft delete)
    - [ ] VerifyInviteToken returns 404 for invalid token, 410 for expired token, 200 for valid
    - [ ] SubmitInvite is a public endpoint (no auth), marks invite as used, returns 200 on success
    - [ ] ScanUrgencyStatus does NOT have "deadline <= now+7d OR deadline >= now-15d" pattern
    - [ ] Scheduler 02:05 job actually calls contractService.CheckContractRenewalReminders (not just log)
    - [ ] MarkInviteUsed uses time.Time value directly (not pointer)
    - [ ] CreateCarousel enforces max 3 images limit
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 各模块限时任务触发 + 前端协办填写页 + 轮播图管理页</name>
  <files>internal/employee/service.go, internal/employee/contract_service.go, internal/socialinsurance/scheduler.go, frontend/src/api/todo.ts, frontend/src/api/carousel.ts, frontend/src/views/todo/TodoListView.vue, frontend/src/views/todo/InviteFillPage.vue, frontend/src/views/todo/CarouselManagePage.vue, frontend/src/router/index.ts</files>
  <read_first>internal/employee/service.go, internal/employee/contract_service.go, internal/socialinsurance/scheduler.go, frontend/src/views/employee/RegisterPage.vue, frontend/src/api/todo.ts</read_first>
  <action>
Wave 1: 扩展 internal/employee/service.go — 在 CreateEmployee 末尾添加合同新签限时任务创建：

首先，检查 employee.Service 是否持有 todo.Repository 的引用。Plan 02 中 todo.NewRepository 在 todo/router.go 中创建，不在 main.go。如果 EmployeeService.CreateEmployee 需要调用 todo.CreateTodo，必须从 main.go 传入 todoRepo。

修复方案（FIX checker issue #3）：在 main.go 中，employee.NewService 调用处需要传入 todoRepo。这要求：
1. todo.Repository 在 main.go 中创建（而非在 todo/router.go 中）
2. todo.Service 在 main.go 中创建（而非在 todo/router.go 中）
3. employee.NewService 接受 todoRepo 参数

**但在 Plan 03 中，我们只需关注 EmployeeService.CreateEmployee 如何调用 todo.CreateTodo。**

以下是需要添加到 internal/employee/service.go 的代码（假设 main.go 已在前面正确传入 todoRepo）：

```go
// Service struct 扩展，添加 todoRepo 字段
// 在现有的 Service struct 中追加：
type Service struct {
    repo      *Repository
    cryptoCfg config.CryptoConfig
    todoRepo  *todo.Repository  // FIX (checker): plan 03 references todoRepo but never creates it; this field must be added here
}

// NewService 签名需要修改为接受 todoRepo 参数
// 在 NewService 函数签名中添加：
func NewService(repo *Repository, cryptoCfg config.CryptoConfig, todoRepo *todo.Repository) *Service {
    return &Service{repo: repo, cryptoCfg: cryptoCfg, todoRepo: todoRepo}
}
```

然后在 CreateEmployee 方法末尾（return s.toResponse(emp) 之前）添加：

```go
// 创建合同新签限时待办（入职30日内截止）
hireDate := emp.HireDate
contractDeadline := hireDate.AddDate(0, 0, 30)
contractTodo := &todo.TodoItem{
    OrgID:         orgID,
    Title:         fmt.Sprintf("员工 %s 入职30日内请完成劳动合同签署", emp.Name),
    Type:          todo.TodoTypeContractNew,
    EmployeeID:    &emp.ID,
    EmployeeName:  emp.Name,
    Deadline:      &contractDeadline,
    IsTimeLimited: true,
    Status:        todo.TodoStatusPending,
    UrgencyStatus: todo.UrgencyNormal,
    SourceType:    "employee",
    SourceID:     &emp.ID,
    CreatorName:   "系统",
}
_ = s.todoRepo.CreateTodo(ctx, contractTodo)
```

Wave 1: 扩展 internal/employee/contract_service.go — 新增 CheckContractRenewal 限时任务生成：

首先确保 ContractService 持有 todoRepo：

```go
// ContractService struct 扩展，添加 todoRepo 字段
// FIX (checker): CheckContractRenewalReminders uses s.todoRepo but todoRepo field was not in the struct
type ContractService struct {
    repo     *ContractRepository
    db       *gorm.DB
    todoRepo *todo.Repository  // FIX (checker): missing field added
}
```

在 contract_service.go 末尾添加以下完整实现：

```go
import (
    "github.com/wencai/easyhr/internal/todo"
)

// CheckContractRenewalReminders 扫描即将到期的合同并生成续签待办
// 由 todo scheduler 每日 02:05 调用（在 todo/scheduler.go 的 gocron job 中）
// FIX (checker): was empty log-only stub; must actually call CheckContractRenewalReminders
func (s *ContractService) CheckContractRenewalReminders(ctx context.Context) error {
    // 查找合同到期日在未来30天内的 active 合同
    now := time.Now()
    deadline := now.AddDate(0, 1, 0) // 30天后

    contracts, err := s.FindContractsExpiringSoon(ctx, now, deadline)
    if err != nil {
        return fmt.Errorf("find expiring contracts: %w", err)
    }

    for _, contract := range contracts {
        // 幂等检查：是否已存在该合同续签待办
        exists, _ := s.todoRepo.ExistsBySource(ctx, contract.OrgID, todo.TodoTypeContractRenew, contract.ID)
        if exists {
            continue
        }

        // 获取员工姓名
        emp, _ := s.FindEmployeeByID(ctx, contract.OrgID, contract.EmployeeID)
        empName := ""
        if emp != nil {
            empName = emp.Name
        }

        renewalTodo := &todo.TodoItem{
            OrgID:         contract.OrgID,
            Title:         fmt.Sprintf("员工 %s 的劳动合同即将到期（%s），请及时处理续签", empName, contract.EndDate.Format("2006-01-02")),
            Type:          todo.TodoTypeContractRenew,
            EmployeeID:    &contract.EmployeeID,
            EmployeeName:  empName,
            Deadline:      contract.EndDate,
            IsTimeLimited: true,
            Status:        todo.TodoStatusPending,
            UrgencyStatus: todo.UrgencyNormal,
            SourceType:    "contract",
            SourceID:      &contract.ID,
            CreatorName:   "系统",
        }
        _ = s.todoRepo.CreateTodo(ctx, renewalTodo)
    }

    return nil
}

// FindContractsExpiringSoon 查找即将到期的合同
// FIX (checker): method was referenced but not implemented
func (s *ContractService) FindContractsExpiringSoon(ctx context.Context, now, deadline time.Time) ([]Contract, error) {
    var contracts []Contract
    err := s.db.Model(&Contract{}).
        Where("status = ? AND end_date IS NOT NULL AND end_date >= ? AND end_date <= ?",
            ContractStatusActive, now, deadline).
        Find(&contracts).Error
    return contracts, err
}

// FindEmployeeByID 查找员工
// FIX (checker): method was referenced but not implemented
func (s *ContractService) FindEmployeeByID(ctx context.Context, orgID, empID int64) (*Employee, error) {
    var emp Employee
    err := s.db.Where("org_id = ? AND id = ?", orgID, empID).First(&emp).Error
    if err != nil {
        return nil, err
    }
    return &emp, nil
}
```

确保 contract_service.go 中导入了 todo 包：`import "github.com/wencai/easyhr/internal/todo"`

FIX (checker): Remove any redundant socialinsurance scheduler -> todo generation code since todo/scheduler.go handles it all. Update socialinsurance/scheduler.go to remove any duplicate "GenerateTodoTasks" or similar calls that would conflict.

Wave 1: 扩展 frontend/src/api/todo.ts，追加协办API：

```typescript
export interface InviteResult {
  url: string
}

export interface VerifyResult {
  valid: boolean
  expired: boolean
  title: string
  todo_id: number
}

export function inviteTodo(todoId: number): Promise<InviteResult> {
  return request.post(`/todos/${todoId}/invite`).then((res) => res.data)
}

export function terminateTodo(todoId: number): Promise<void> {
  return request.put(`/todos/${todoId}/terminate`).then((res) => res.data)
}

export function verifyInviteToken(token: string): Promise<VerifyResult> {
  return request.get(`/todos/invite/${token}`).then((res) => res.data)
}
```

Wave 1: 扩展 frontend/src/api/carousel.ts，追加 CRUD API：

```typescript
// FIX (checker): D-09-06 — Carousel CRUD API for admin management page
export interface CarouselItem {
  id: number
  org_id: number
  image_url: string
  link_url?: string
  sort_order: number
  active: boolean
  start_at?: string
  end_at?: string
  created_at: string
  updated_at: string
}

export interface CarouselFormData {
  image_url: string
  link_url?: string
  sort_order?: number
  active?: boolean
  start_at?: string
  end_at?: string
}

export function listAllCarousels(): Promise<{ data: CarouselItem[] }> {
  return request.get('/carousels/admin').then((res) => res.data)
}

export function createCarousel(data: CarouselFormData): Promise<{ data: CarouselItem }> {
  return request.post('/carousels', data).then((res) => res.data)
}

export function updateCarousel(id: number, data: Partial<CarouselFormData>): Promise<void> {
  return request.put(`/carousels/${id}`, data).then((res) => res.data)
}

export function deleteCarousel(id: number): Promise<void> {
  return request.delete(`/carousels/${id}`).then((res) => res.data)
}

export function uploadImage(file: File): Promise<string> {
  const formData = new FormData()
  formData.append('file', file)
  return request.post('/upload/image', formData, {
    headers: { 'Content-Type': 'multipart/form-data' },
  }).then((res) => res.data.url as string)
}
```

Wave 1: 扩展 frontend/src/views/todo/TodoListView.vue — 添加协办和终止操作列：

在 el-table-column 操作列（置顶列之后）添加：

```html
<el-table-column label="操作" width="160" fixed="right">
  <template #default="{ row }">
    <el-dropdown trigger="click" @command="(cmd: string) => handleAction(cmd, row)">
      <el-button type="primary" link size="small">
        更多<el-icon class="el-icon--right"><ArrowDown /></el-icon>
      </el-button>
      <template #dropdown>
        <el-dropdown-menu>
          <el-dropdown-item command="invite" :disabled="row.status === 'terminated'">
            <el-icon><Message /></el-icon> 邀请协办
          </el-dropdown-item>
          <el-dropdown-item
            v-if="row.status !== 'terminated' && row.status !== 'completed'"
            command="terminate"
          >
            <el-icon style="color: #FF5630"><Close /></el-icon>
            <span style="color: #FF5630">终止任务</span>
          </el-dropdown-item>
        </el-dropdown-menu>
      </template>
    </el-dropdown>
  </template>
</el-table-column>
```

在 script 中添加：

```typescript
import { ArrowDown, Message, Close } from '@element-plus/icons-vue'
import { inviteTodo, terminateTodo } from '@/api/todo'

async function handleAction(cmd: string, row: TodoItem) {
  if (cmd === 'invite') {
    try {
      const result = await inviteTodo(row.id)
      await navigator.clipboard.writeText(result.url)
      ElMessage.success('邀请链接已复制到剪贴板')
    } catch {
      ElMessage.error('邀请失败')
    }
  } else if (cmd === 'terminate') {
    try {
      await ElMessageBox.confirm(
        '终止后可在筛选中查看，数据保留',
        '确认终止此待办？',
        { confirmButtonText: '确认终止', cancelButtonText: '取消', type: 'warning' }
      )
      await terminateTodo(row.id)
      ElMessage.success('任务已终止')
      loadData()
    } catch {
      // user cancelled
    }
  }
}
```

确保在 imports 中添加 ElMessageBox from element-plus。

Wave 1: 创建 frontend/src/views/todo/InviteFillPage.vue（协办填写页，复用 RegisterPage.vue 模式）：

```vue
<template>
  <div class="invite-fill-page">
    <!-- 错误状态 -->
    <div v-if="errorState" class="error-state">
      <el-icon :size="48" color="#909399"><CircleCloseFilled /></el-icon>
      <p class="error-message">{{ errorMessage }}</p>
    </div>

    <!-- 成功状态 -->
    <div v-else-if="submitted" class="success-state">
      <el-icon :size="48" color="#67c23a"><CircleCheckFilled /></el-icon>
      <p class="success-message">信息已提交，感谢您的配合</p>
    </div>

    <!-- 表单状态 -->
    <div v-else-if="inviteDetail" class="form-container">
      <h1 class="page-title">协办任务</h1>
      <div class="todo-title-box">
        <el-tag type="info">{{ inviteDetail.title }}</el-tag>
      </div>

      <el-form ref="formRef" :model="form" label-position="top" class="fill-form">
        <!-- 协办填写表单（通用字段） -->
        <el-form-item label="姓名">
          <el-input v-model="form.name" placeholder="请输入您的姓名" />
        </el-form-item>
        <el-form-item label="手机号码">
          <el-input v-model="form.phone" placeholder="请输入手机号码" maxlength="11" />
        </el-form-item>
        <el-form-item label="备注说明">
          <el-input v-model="form.remark" type="textarea" placeholder="如有补充信息请在此填写" :rows="3" />
        </el-form-item>

        <el-button
          type="primary"
          class="submit-btn"
          :loading="submitting"
          @click="handleSubmit"
        >
          提交信息
        </el-button>
      </el-form>
    </div>

    <!-- 加载状态 -->
    <div v-else class="loading-state">
      <el-icon :size="32" class="is-loading"><Loading /></el-icon>
      <p>正在加载...</p>
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref, onMounted } from 'vue'
import { useRoute } from 'vue-router'
import { ElMessage } from 'element-plus'
import { CircleCloseFilled, CircleCheckFilled, Loading } from '@element-plus/icons-vue'
import { verifyInviteToken, type VerifyResult } from '@/api/todo'
import request from '@/api/request'

const route = useRoute()
const token = route.query.token as string

const inviteDetail = ref<VerifyResult | null>(null)
const errorState = ref(false)
const errorMessage = ref('')
const submitted = ref(false)
const submitting = ref(false)

const form = ref({
  name: '',
  phone: '',
  remark: '',
})

async function loadDetail() {
  try {
    inviteDetail.value = await verifyInviteToken(token)
  } catch (err: unknown) {
    errorState.value = true
    const errorObj = err as { response?: { status?: number; data?: { message?: string } } }
    if (errorObj.response?.status === 410) {
      errorMessage.value = '该链接已过期，请联系管理员重新发送'
    } else if (errorObj.response?.status === 404) {
      errorMessage.value = '链接无效，请确认链接是否正确'
    } else {
      errorMessage.value = errorObj.response?.data?.message || '加载失败，请稍后重试'
    }
  }
}

async function handleSubmit() {
  if (!inviteDetail.value?.todo_id) return
  submitting.value = true
  try {
    await request.post(`/todos/invite/${token}/submit`, {
      name: form.value.name,
      phone: form.value.phone,
      remark: form.value.remark,
    })
    submitted.value = true
  } catch {
    ElMessage.error('提交失败，请稍后重试')
  } finally {
    submitting.value = false
  }
}

onMounted(() => {
  if (token) {
    loadDetail()
  } else {
    errorState.value = true
    errorMessage.value = '链接无效，请确认链接是否正确'
  }
})
</script>

<style scoped lang="scss">
.invite-fill-page {
  min-height: 100vh;
  background: #f5f7fa;
  padding: 16px;
}

.form-container {
  max-width: 480px;
  margin: 0 auto;
}

.page-title {
  font-size: 22px;
  font-weight: 700;
  text-align: center;
  margin: 16px 0 24px;
  color: #303133;
}

.todo-title-box {
  text-align: center;
  margin-bottom: 24px;
}

.fill-form {
  background: #fff;
  border-radius: 12px;
  padding: 20px;
}

.submit-btn {
  width: 100%;
  height: 44px;
  font-size: 16px;
  margin-top: 8px;
}

.error-state,
.success-state,
.loading-state {
  max-width: 480px;
  margin: 80px auto 0;
  text-align: center;
}

.error-message {
  margin-top: 16px;
  font-size: 16px;
  color: #909399;
}

.success-message {
  margin-top: 16px;
  font-size: 16px;
  color: #67c23a;
}

.loading-state {
  margin-top: 120px;
  color: #909399;
}
</style>
```

Wave 1: 创建 frontend/src/views/todo/CarouselManagePage.vue（轮播图管理页面 per D-09-06, UI-SPEC §CarouselManagePage）：

```vue
<template>
  <div class="carousel-manage-page">
    <!-- 页面标题 -->
    <div class="page-header">
      <div class="header-left">
        <h1 class="page-title">轮播图管理</h1>
        <span class="carousel-hint">最多配置3张轮播图</span>
      </div>
      <div class="header-right">
        <el-button type="primary" :disabled="carouselList.length >= 3" @click="showAddDialog">
          <el-icon><Plus /></el-icon>
          添加轮播图
        </el-button>
      </div>
    </div>

    <!-- 轮播图列表 -->
    <div class="section">
      <div v-if="loading" class="loading">
        <el-icon class="is-loading" size="24"><Loading /></el-icon>
      </div>
      <div v-else-if="carouselList.length === 0" class="empty-state">
        <el-empty description="暂无轮播图，点击上方按钮添加" :image-size="80" />
      </div>
      <el-table v-else :data="carouselList" stripe>
        <el-table-column label="排序" width="80" align="center">
          <template #default="{ $index }">
            <span class="sort-num">{{ $index + 1 }}</span>
          </template>
        </el-table-column>
        <el-table-column label="预览图" width="180">
          <template #default="{ row }">
            <el-image
              :src="row.image_url"
              fit="cover"
              style="width: 120px; height: 60px; border-radius: 4px"
              :preview-src-list="[row.image_url]"
            />
          </template>
        </el-table-column>
        <el-table-column prop="link_url" label="跳转链接" min-width="160">
          <template #default="{ row }">
            <span v-if="row.link_url" class="link-text">{{ row.link_url }}</span>
            <span v-else class="text-tertiary">--</span>
          </template>
        </el-table-column>
        <el-table-column label="生效时间" width="160">
          <template #default="{ row }">
            <span v-if="row.start_at">{{ row.start_at }}</span>
            <span v-else class="text-tertiary">--</span>
          </template>
        </el-table-column>
        <el-table-column label="失效时间" width="160">
          <template #default="{ row }">
            <span v-if="row.end_at">{{ row.end_at }}</span>
            <span v-else class="text-tertiary">--</span>
          </template>
        </el-table-column>
        <el-table-column label="状态" width="100" align="center">
          <template #default="{ row }">
            <el-switch
              :model-value="row.active"
              size="small"
              @change="handleToggleActive(row)"
            />
          </template>
        </el-table-column>
        <el-table-column label="操作" width="140" fixed="right">
          <template #default="{ row }">
            <el-button type="primary" link size="small" @click="handleEdit(row)">
              <el-icon><Edit /></el-icon>编辑
            </el-button>
            <el-button type="danger" link size="small" @click="handleDelete(row)">
              <el-icon><Delete /></el-icon>删除
            </el-button>
          </template>
        </el-table-column>
      </el-table>
    </div>

    <!-- 添加/编辑对话框 -->
    <el-dialog
      v-model="dialogVisible"
      :title="dialogMode === 'add' ? '添加轮播图' : '编辑轮播图'"
      width="560px"
      @close="handleDialogClose"
    >
      <el-form ref="formRef" :model="form" label-position="top">
        <el-form-item label="轮播图片" required>
          <div class="upload-area">
            <el-upload
              v-if="!form.image_url"
              class="image-uploader"
              :show-file-list="false"
              :before-upload="handleBeforeUpload"
              :http-request="handleUpload"
            >
              <el-icon class="upload-icon"><Plus /></el-icon>
              <div class="upload-text">点击上传</div>
            </el-upload>
            <div v-else class="upload-preview">
              <el-image :src="form.image_url" fit="cover" style="width: 200px; height: 100px; border-radius: 8px" />
              <el-button type="danger" size="small" class="replace-btn" @click="form.image_url = ''">替换</el-button>
            </div>
          </div>
          <div class="upload-tip">支持 jpg/jpeg/png/gif/webp，不超过5MB，建议尺寸 750x375</div>
        </el-form-item>
        <el-form-item label="跳转链接">
          <el-input v-model="form.link_url" placeholder="http:// 或 /path，可留空" />
        </el-form-item>
        <el-form-item label="生效时间">
          <el-date-picker
            v-model="form.start_at"
            type="datetime"
            placeholder="留空表示立即生效"
            value-format="YYYY-MM-DD HH:mm:ss"
            style="width: 100%"
          />
        </el-form-item>
        <el-form-item label="失效时间">
          <el-date-picker
            v-model="form.end_at"
            type="datetime"
            placeholder="留空表示永久有效"
            value-format="YYYY-MM-DD HH:mm:ss"
            style="width: 100%"
          />
        </el-form-item>
        <el-form-item label="是否启用">
          <el-switch v-model="form.active" />
        </el-form-item>
      </el-form>
      <template #footer>
        <el-button @click="dialogVisible = false">取消</el-button>
        <el-button type="primary" :loading="submitting" @click="handleSubmit">确定</el-button>
      </template>
    </el-dialog>
  </div>
</template>

<script setup lang="ts">
import { ref, reactive, onMounted } from 'vue'
import { ElMessage, ElMessageBox } from 'element-plus'
import { Plus, Edit, Delete, Loading } from '@element-plus/icons-vue'
import {
  listAllCarousels,
  createCarousel,
  updateCarousel,
  deleteCarousel,
  uploadImage,
  type CarouselItem,
} from '@/api/carousel'

const loading = ref(false)
const submitting = ref(false)
const carouselList = ref<CarouselItem[]>([])
const dialogVisible = ref(false)
const dialogMode = ref<'add' | 'edit'>('add')
const editingId = ref<number | null>(null)
const formRef = ref()

const form = reactive({
  image_url: '',
  link_url: '',
  start_at: '',
  end_at: '',
  active: true,
})

async function loadData() {
  loading.value = true
  try {
    const res = await listAllCarousels()
    carouselList.value = res.data || []
  } catch {
    ElMessage.error('加载轮播图列表失败')
  } finally {
    loading.value = false
  }
}

function showAddDialog() {
  dialogMode.value = 'add'
  editingId.value = null
  resetForm()
  dialogVisible.value = true
}

function handleEdit(row: CarouselItem) {
  dialogMode.value = 'edit'
  editingId.value = row.id
  form.image_url = row.image_url
  form.link_url = row.link_url || ''
  form.start_at = row.start_at || ''
  form.end_at = row.end_at || ''
  form.active = row.active
  dialogVisible.value = true
}

function handleDialogClose() {
  resetForm()
}

function resetForm() {
  form.image_url = ''
  form.link_url = ''
  form.start_at = ''
  form.end_at = ''
  form.active = true
}

function handleBeforeUpload(file: File) {
  const isImage = file.type.startsWith('image/')
  const isLt5M = file.size <= 5 * 1024 * 1024
  if (!isImage) {
    ElMessage.error('只能上传图片文件')
    return false
  }
  if (!isLt5M) {
    ElMessage.error('图片大小不能超过5MB')
    return false
  }
  return true
}

async function handleUpload(option: { file: File }) {
  try {
    const url = await uploadImage(option.file)
    form.image_url = url
    ElMessage.success('上传成功')
  } catch {
    ElMessage.error('上传失败')
  }
}

async function handleSubmit() {
  if (!form.image_url) {
    ElMessage.error('请上传轮播图片')
    return
  }
  submitting.value = true
  try {
    const data = {
      image_url: form.image_url,
      link_url: form.link_url || undefined,
      active: form.active,
      start_at: form.start_at ? new Date(form.start_at) : undefined,
      end_at: form.end_at ? new Date(form.end_at) : undefined,
    }
    if (dialogMode.value === 'add') {
      await createCarousel(data)
      ElMessage.success('添加成功')
    } else if (editingId.value !== null) {
      await updateCarousel(editingId.value, data)
      ElMessage.success('更新成功')
    }
    dialogVisible.value = false
    loadData()
  } catch (err: unknown) {
    const errorObj = err as { message?: string }
    ElMessage.error(errorObj.message || '操作失败')
  } finally {
    submitting.value = false
  }
}

async function handleToggleActive(row: CarouselItem) {
  try {
    await updateCarousel(row.id, { active: !row.active })
    row.active = !row.active
    ElMessage.success(row.active ? '已启用' : '已停用')
  } catch {
    ElMessage.error('操作失败')
  }
}

async function handleDelete(row: CarouselItem) {
  try {
    await ElMessageBox.confirm('删除后不可恢复，确认删除？', '删除确认', {
      confirmButtonText: '确认删除',
      cancelButtonText: '取消',
      type: 'warning',
    })
    await deleteCarousel(row.id)
    ElMessage.success('删除成功')
    loadData()
  } catch {
    // user cancelled
  }
}

onMounted(loadData)
</script>

<style scoped lang="scss">
.carousel-manage-page {
  padding: 20px 24px;
}

.page-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 20px;
}

.header-left {
  display: flex;
  align-items: baseline;
  gap: 12px;
}

.page-title {
  font-size: 22px;
  font-weight: 700;
  color: #1a1a1a;
  margin: 0;
}

.carousel-hint {
  font-size: 13px;
  color: #97a0af;
}

.section {
  background: #fff;
  border-radius: 8px;
  padding: 20px;
}

.loading {
  display: flex;
  justify-content: center;
  padding: 32px;
}

.sort-num {
  font-weight: 600;
  color: #4f6ef7;
}

.link-text {
  color: #4f6ef7;
  font-size: 13px;
  word-break: break-all;
}

.text-tertiary {
  color: #97a0af;
  font-size: 13px;
}

// 上传区域
.upload-area {
  display: flex;
  align-items: center;
  gap: 16px;
}

.image-uploader {
  width: 200px;
  height: 100px;
  border: 1px dashed #d9d9d9;
  border-radius: 8px;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  transition: border-color 0.2s;
  &:hover {
    border-color: #4f6ef7;
  }
}

.upload-icon {
  font-size: 28px;
  color: #8c8c8c;
}

.upload-text {
  font-size: 13px;
  color: #8c8c8c;
  margin-top: 4px;
}

.upload-preview {
  position: relative;
}

.replace-btn {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  opacity: 0;
  transition: opacity 0.2s;
}

.upload-preview:hover .replace-btn {
  opacity: 1;
}

.upload-tip {
  font-size: 12px;
  color: #97a0af;
  margin-top: 8px;
}
</style>
```

Wave 1: 更新 frontend/src/router/index.ts，添加协办填写页和轮播图管理页路由：

在路由数组末尾（salary-slip-h5 路由之后）添加：

```typescript
// 协办填写页（无需登录）
{
  path: '/todo/:id/invite',
  name: 'todo-invite',
  component: () => import('@/views/todo/InviteFillPage.vue'),
},

// 轮播图管理页（需要登录）
{
  path: '/carousel/manage',
  name: 'carousel-manage',
  component: () => import('@/views/todo/CarouselManagePage.vue'),
},
```

同时在 beforeEach 的公开路径检查中添加：
```typescript
// 公开路由：/register, /salary/slip/, /todo/*/invite
const isPublicRoute =
    to.path.startsWith('/register') ||
    to.path.startsWith('/salary/slip/') ||
    to.path.includes('/invite')
if (isPublicRoute) {
    return
}
```

在 beforeEach 的 isProtectedRoute 中添加：
```typescript
to.path.startsWith('/carousel/manage') ||
```

CRITICAL: Employee.Service.CreateEmployee 方法签名需要改为接受 context.Context 和 todoRepo 参数。检查 main.go 中 NewService 调用处，更新为：
```go
svc := employee.NewService(repo, cfg.Crypto, todoRepo)
```
确保 main.go 中 todo.NewRepository 和 todo.NewService 在 employee.NewService 之前创建。

CRITICAL: ContractService 需要添加 todoRepo。检查 main.go 中 ContractService 创建处，更新 NewContractService 为接受 todoRepo 参数。

CRITICAL: GenerateMonthlyTodos 和 GenerateAnnualBaseTodos 中的 orgIDs 查询可能为空（如果 organizations 表不存在或查询失败），需要确保幂等。
  </action>
  <verify>
    <automated>grep -n "InviteTodo\|terminateTodo\|InviteFillPage\|inviteTodo\|VerifyInviteToken\|CheckContractRenewalReminders\|FindContractsExpiringSoon\|FindEmployeeByID\|CreateCarousel\|UpdateCarousel\|DeleteCarousel\|CarouselManagePage\|uploadImage\|listAllCarousels" internal/employee/*.go frontend/src/views/todo/*.vue frontend/src/api/todo.ts frontend/src/api/carousel.ts frontend/src/router/index.ts | head -40</automated>
  </verify>
  <done>
    待办列表操作列有"邀请协办"和"终止任务"下拉菜单
    协办邀请生成Token并提供复制链接功能
    终止任务弹窗确认后调用 API
    /todo/:id/invite?token=xxx 页面可访问
    协办填写页正确验证 Token（无效=404，过期=410，有效=显示表单）
    合同新签限时任务在员工入职时自动创建
    合同续签限时任务每日扫描即将到期合同创建
    POST /todos/invite/:token/submit 提交协办信息
    /carousel/manage 页面可访问，支持上传/编辑/删除/启用停用轮播图
    最多3张轮播图限制在前端和后端均强制执行
    图片上传调用 POST /upload/image 获取 OSS URL
  </done>
  <acceptance_criteria>
    - [ ] TodoListView.vue actions column has "邀请协办" and "终止任务" dropdown items
    - [ ] inviteTodo API function added to frontend/src/api/todo.ts
    - [ ] terminateTodo API function added to frontend/src/api/todo.ts
    - [ ] verifyInviteToken function added to frontend/src/api/todo.ts
    - [ ] frontend/src/views/todo/InviteFillPage.vue exists following RegisterPage.vue pattern
    - [ ] /todo/:id/invite route registered in router/index.ts (public, no auth guard)
    - [ ] ContractService has todoRepo field, CheckContractRenewalReminders, FindContractsExpiringSoon, FindEmployeeByID methods fully implemented
    - [ ] EmployeeService.CreateEmployee calls todo.CreateTodo for contract_new (requires todoRepo field and updated NewService signature)
    - [ ] Error states: 404=invalid token, 410=expired token, 200=valid token with title display
    - [ ] SubmitInvite public endpoint marks invite as used
    - [ ] CarouselManagePage.vue exists with el-upload, el-switch, sort buttons, max 3 images limit
    - [ ] /carousel/manage route registered in router/index.ts (protected, requires auth)
    - [ ] createCarousel, updateCarousel, deleteCarousel, listAllCarousels, uploadImage functions in frontend/src/api/carousel.ts
    - [ ] CarouselManagePage.vue shows table with columns: 排序, 预览图, 跳转链接, 生效时间, 失效时间, 状态, 操作
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public /todo/:id/invite -> API | Token验证替代登录，验证方为 org_id + todo_id |
| browser -> API | Terminate/Invite calls require JWT auth |
| browser -> /upload/image | Public endpoint for image upload, file type/size validated server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-01 | Information Disclosure | TerminateTodo API | mitigate | org_id JWT, TenantScope |
| T-9-05 | Token Enumeration | VerifyInviteToken | mitigate | crypto/rand 32-byte (64 hex chars), 7-day expiry |
| T-9-06 | Race Condition | SubmitInvite | mitigate | MarkInviteUsed is idempotent, link already-used returns 409 |
| T-9-07 | File Upload | /upload/image | mitigate | File type whitelist (.jpg/.jpeg/.png/.gif/.webp), 5MB size limit, filename UUID |
| T-9-08 | Carousel Limit | CreateCarousel | mitigate | Max 3 images enforced server-side (returns 400 with count), UI disabled |
</threat_model>

<verification>
After Plan 03:
1. `grep -n "InviteTodo\|TerminateTodo\|SubmitInvite\|InviteFillPage" internal/todo/*.go frontend/src/views/todo/*.vue | head -20`
2. Backend builds: `cd /Users/wencai/github/EasyHR && go build ./...`
3. `grep -n "todo-invite\|/todo\|carousel-manage\|/carousel" frontend/src/router/index.ts`
4. `grep -n "ScanUrgencyStatus" internal/todo/repository.go` — verify no "deadline <= now+7d OR deadline >= now-15d" pattern
5. `grep -n "CheckContractRenewalReminders" internal/employee/contract_service.go` — verify actual implementation (not empty)
6. `grep -n "CreateCarousel\|UpdateCarousel\|DeleteCarousel\|UploadImage" internal/todo/*.go internal/upload/*.go`
7. `grep -n "CarouselManagePage\|uploadImage\|listAllCarousels" frontend/src/views/todo/CarouselManagePage.vue frontend/src/api/carousel.ts`
</verification>

<success_criteria>
- POST /api/v1/todos/:id/invite 返回邀请链接 /todo/:id/invite?token=xxx
- PUT /api/v1/todos/:id/terminate 将待办状态变为 terminated（不软删除）
- GET /api/v1/todos/invite/:token 公开验证（404无效，410过期，200有效+title）
- POST /api/v1/todos/invite/:token/submit 公开提交协办信息（200成功，409已使用，410过期）
- /todo/:id/invite?token=xxx 页面正确渲染协办表单
- gocron scheduler 每日 02:00 扫描更新 urgency_status（expired/overdue/normal 三段精确条件）
- gocron scheduler 每月1日生成个税申报+社保缴费待办
- gocron scheduler 每月5日生成社保增减员待办
- gocron scheduler 每年6月15日生成年度基数调整待办
- gocron scheduler 每日 02:05 实际调用 contractService.CheckContractRenewalReminders
- 员工入职时自动创建合同新签限时待办（30日截止）
- 合同续签限时待办每日扫描创建
- GET /api/v1/carousels/admin 返回所有轮播图（管理用）
- POST /api/v1/carousels 创建轮播图（最多3张，400超限错误）
- PUT /api/v1/carousels/:id 更新轮播图
- DELETE /api/v1/carousels/:id 删除轮播图
- POST /upload/image 图片上传（5MB限制，图片格式白名单）
- /carousel/manage 页面可访问，支持增删改查+启用停用轮播图
</success_criteria>

<output>
After completion, create `.planning/phases/09-待办中心/09-03-PLAN-SUMMARY.md`
</output>
