---
phase: 08-social-insurance-enhancement
plan: 02
type: execute
wave: 2
depends_on: [08-PLAN-01]
files_modified:
  - frontend/src/views/socialinsurance/SIDashboard.vue
  - frontend/src/components/socialinsurance/EnrollDialog.vue
  - frontend/src/views/tool/SITool.vue
  - frontend/src/components.d.ts
autonomous: true
requirements: [SI-01, SI-02, SI-03, SI-04, SI-05, SI-06, SI-07, SI-08, SI-19]

must_haves:
  truths:
    - 管理员可在社保数据看板看到4张纯数字卡片（应缴总额/单位部分/个人部分/欠缴金额）
    - 增员弹窗支持姓名检索、起始月份、城市、社保基数、公积金比例和基数
    - 参保操作 Tab 顶部显示红色欠缴横幅
  artifacts:
    - path: frontend/src/views/socialinsurance/SIDashboard.vue
      provides: 4卡片数据看板组件
      contains: 应缴总额 / 单位部分 / 个人部分 / 欠缴金额
    - path: frontend/src/components/socialinsurance/EnrollDialog.vue
      provides: 增员弹窗（姓名检索 + 表单 + 提交）
      contains: el-select + el-date-picker + el-input-number
    - path: frontend/src/views/tool/SITool.vue
      provides: 参保操作 Tab（含红色横幅）
      contains: 参保操作 tab + 红色横幅
  key_links:
    - from: SIDashboard.vue
      to: /api/v1/socialinsurance/dashboard
      via: axios GET 获取数据
    - from: EnrollDialog.vue
      to: /api/v1/socialinsurance/enroll
      via: axios POST 提交增员
    - from: SITool.vue
      to: SIDashboard.vue
      via: 组件引入
    - from: SITool.vue (参保操作)
      to: EnrollDialog.vue
      via: 弹窗打开
---

<objective>
实现前端社保数据看板 + 优化增员弹窗 + SIToolContent 重构（参保操作 Tab 含红色横幅）。

Purpose: 管理员可在数据看板看到社保汇总，点击增员弹窗完成增员操作
Output: SIDashboard.vue + EnrollDialog.vue + SITool.vue 重构
</objective>

<context>
@.planning/phases/08-社保公积金增强/08-CONTEXT.md
@.planning/phases/08-社保公积金增强/08-UI-SPEC.md
@.planning/phases/08-社保公积金增强/08-RESEARCH.md
@frontend/src/views/tool/SalaryDashboard.vue
@frontend/src/views/tool/SalaryList.vue
@frontend/src/components/attendance/AttendanceStatsCard.vue
@frontend/src/views/tool/SITool.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 SIDashboard.vue 数据看板</name>
  <files>frontend/src/views/socialinsurance/SIDashboard.vue</files>
  <action>
创建 `frontend/src/views/socialinsurance/SIDashboard.vue`（4卡片风格，完全复用 SalaryDashboard.vue 模式）：

**API 调用：**
```typescript
const { data } = await axios.get('/api/v1/socialinsurance/dashboard')
```

**DashboardStat 显示格式：**
```typescript
// value 格式：data.value → "¥12,345.67"（后端已格式化为人民币格式）
// trend 计算：
//   revenue metrics (total/company/personal): 正值=green up-arrow, 负值=red down-arrow
//   overdue metric: 正值=red up-arrow (bad), 负值=green down-arrow (good)
//   no prior data: 显示 "--"
```

**4张卡片配置（per D-SI-05 + UI-SPEC）：**

| Card | Label | Accent | Trend Logic |
|------|-------|--------|-------------|
| 应缴总额 | 应缴总额（元） | #4F6EF7 | 正值 green up |
| 单位部分 | 单位部分合计（元） | #4F6EF7 | 正值 green up |
| 个人部分 | 个人部分合计（元） | #4F6EF7 | 正值 green up |
| 欠缴金额 | 欠缴金额（元） | #FF5630 | 正值 red up |

**样式（参考 SalaryDashboard.vue）：**
```scss
// grid: repeat(4, 1fr), gap: 8px
// breakpoint < 900px: repeat(2, 1fr)
// card: background #FAFBFF, border-radius: 12px, padding: 16px
// stat-value: font-size: 24px; font-weight: 700; color: accent
```

**无月度筛选器**（per D-SI-05：默认显示当月数据）

**加载状态：** `v-loading` overlay
**空状态：** 4张卡片 value="0.00"，trend="--"

**注意：** 需要在 vite.config.ts 或 router 中注册 `/socialinsurance` 路由指向 SITool.vue
</action>
  <verify>
    <automated>grep -n "应缴总额\|单位部分\|欠缴金额\|#FF5630" frontend/src/views/socialinsurance/SIDashboard.vue</automated>
  </verify>
  <done>SIDashboard.vue 已创建，4张卡片样式与 SalaryDashboard.vue 一致</done>
</task>

<task type="auto">
  <name>Task 2: 创建 EnrollDialog.vue 增员弹窗</name>
  <files>frontend/src/components/socialinsurance/EnrollDialog.vue</files>
  <action>
创建 `frontend/src/components/socialinsurance/EnrollDialog.vue`（per D-SI-07）：

**触发方式：** "增员" 按钮在 SITool.vue 参保操作 Tab

**表单字段（per UI-SPEC Component 3）：**

| Field | Component | Default | Required |
|-------|-----------|---------|----------|
| 员工姓名 | el-select (filterable, remote, remote-method) | — | Yes |
| 身份证号 | el-input (readonly) | auto-filled | Auto |
| 起始月份 | el-date-picker type="month" | 当月(YYYY-MM) | Yes |
| 缴费城市 | el-input / el-select | — | Yes |
| 社保基数 | el-input-number (precision=2) | — | Yes |
| 公积金基数 | el-input-number (precision=2) | — | No |
| 公积金比例 | el-input-number | 12% | No |

**姓名检索（per D-SI-07）：**
```typescript
const searchEmployee = async (query: string) => {
  if (!query) return
  const { data } = await axios.get('/api/v1/employees/search', { params: { name: query } })
  employeeOptions.value = data.data || []
}
// debounce 300ms
// el-select: filterable, remote, :remote-method="searchEmployee"
```

**提交逻辑：**
```typescript
const submit = async () => {
  await axios.post('/api/v1/socialinsurance/enroll', {
    employeeID: form.employeeID,
    startYearMonth: form.startYearMonth, // "YYYYMM"
    cityCode: form.cityCode,
    siBase: form.siBase,
    hfBase: form.hfBase,
    hfRatio: form.hfRatio
  })
  ElMessage.success('增员成功')
  emit('success') // 通知父组件刷新
}
```

**布局：** 单表单，垂直排列，宽度 480px
**CTAs：** "取消" (text) + "确认增员" (primary #4F6EF7)
**destroy-on-close** 重置状态
</action>
  <verify>
    <automated>grep -n "确认增员\|员工姓名\|el-select\|remote-method" frontend/src/components/socialinsurance/EnrollDialog.vue</automated>
  </verify>
  <done>EnrollDialog.vue 已创建，支持姓名检索 + 全字段 + 提交</done>
</task>

<task type="auto">
  <name>Task 3: 重构 SITool.vue — 参保操作 Tab + 红色横幅</name>
  <files>frontend/src/views/tool/SITool.vue</files>
  <action>
重构 `frontend/src/views/tool/SITool.vue`（per D-SI-10 + UI-SPEC）：

**Tab 结构（3个 Tab）：**
1. 政策库（现有不变）
2. 参保操作（新增红色横幅 + 增员按钮）
3. 参保记录（SIRecordsTable 增强）

**参保操作 Tab 布局：**
```
[红色横幅 — 仅当 overdueItems.length > 0 时显示]
  ↓
[增员按钮 + 批量操作按钮]
  ↓
[参保记录列表（当前页）]
```

**红色横幅（per D-SI-10 + UI-SPEC Component 2）：**
```vue
<div v-if="overdueItems.length > 0" class="overdue-banner">
  <el-icon><WarningFilled /></el-icon>
  <div class="banner-content">
    <div class="banner-headline">
      存在欠缴记录，请及时处理
    </div>
    <div class="banner-detail">
      最大欠缴：{{ overdueItems[0].employeeName }} {{ overdueItems[0].city }} {{ overdueItems[0].yearMonth }} 欠缴 ¥{{ overdueItems[0].amount }}
    </div>
    <div class="banner-scroll-list" v-if="overdueItems.length > 1">
      <div v-for="item in overdueItems.slice(1)" :key="item.id">
        {{ item.employeeName }} {{ item.city }} {{ item.yearMonth }} ¥{{ item.amount }}
      </div>
    </div>
  </div>
  <el-icon class="close-btn" @click="dismissBanner"><Close /></el-icon>
</div>
```

**样式（per UI-SPEC Component 2）：**
```scss
.overdue-banner {
  background: #FF563015;
  border-left: 3px solid #FF5630;
  border-radius: 8px;
  padding: 12px 16px;
  color: #FF5630;
  font-size: 14px;
  display: flex;
  align-items: flex-start;
  gap: 8px;
}
```

**overdueItems 数据加载：**
```typescript
// 在 created() 中调用 GET /api/v1/socialinsurance/dashboard
// 取 dashboard 中的 overdueItems 字段（后端返回欠缴列表）
```

**新增路由注册**（在 frontend/src/router 中）：
```typescript
{
  path: '/socialinsurance',
  component: () => import('@/views/tool/SITool.vue'),
  meta: { title: '社保公积金' }
}
```
</action>
  <verify>
    <automated>grep -n "overdue-banner\|WarningFilled\|参保操作\|overdueItems" frontend/src/views/tool/SITool.vue</automated>
  </verify>
  <done>SITool.vue 参保操作 Tab 重构完成，红色横幅正确显示</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | 表单数据校验，org_id 来自 JWT token |
| input→display | 后端返回数据直接展示，横幅文本防 XSS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | I | SIDashboard | mitigate | XSS 防护：后端返回数据不包含 HTML，Vue 默认转义 |
| T-08-02 | S | EnrollDialog | mitigate | 前端表单校验（required/min/max），后端二次校验 |
</threat_model>

<verification>
- [ ] `grep -r "SIDashboard" frontend/src/` 看板组件被引用
- [ ] `grep -r "EnrollDialog" frontend/src/` 增员弹窗被引用
- [ ] `grep -r "overdue-banner\|WarningFilled" frontend/src/views/tool/SITool.vue` 横幅存在
- [ ] `grep "socialinsurance/dashboard" frontend/src/` API 调用存在
</verification>

<success_criteria>
- [ ] SIDashboard.vue 4张卡片样式与 SalaryDashboard.vue 完全一致
- [ ] EnrollDialog.vue 支持姓名检索（debounce 300ms）+ 所有表单字段
- [ ] SITool.vue 参保操作 Tab 顶部红色横幅（overdueItems 非空时显示）
- [ ] 横幅可关闭（dismissBanner 写入 localStorage，下次刷新重新显示）
- [ ] 横幅超5条显示前5条 + "还有N项"折叠
</success_criteria>

<output>
After completion, create `.planning/phases/08-社保公积金增强/08-PLAN-02-SUMMARY.md`
</output>
