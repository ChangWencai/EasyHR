---
phase: 08-social-insurance-enhancement
plan: 03
type: execute
wave: 2
depends_on: [08-PLAN-01]
files_modified:
  - frontend/src/components/socialinsurance/StopDialog.vue
  - frontend/src/components/socialinsurance/SIDetailDialog.vue
  - frontend/src/views/tool/SITool.vue
  - frontend/src/views/socialinsurance/SIRecordsTable.vue
autonomous: true
requirements: [SI-09, SI-10, SI-11, SI-12, SI-13, SI-17, SI-18, SI-19, SI-20]

must_haves:
  truths:
    - 管理员可在减员弹窗按姓名检索员工，设置终止月份、原因、转出日期
    - 减员弹窗显示转出生效规则提示（每月5日/5-25日/25日后）
    - 参保记录列表显示5种状态标签 + 缴费渠道列 + 详情弹窗
    - 点击详情打开五险分项弹窗（6险×2列 + 其他缴费 + 合计）
  artifacts:
    - path: frontend/src/components/socialinsurance/StopDialog.vue
      provides: 减员弹窗（姓名检索 + 终止月 + 原因 + 转出日期 + 生效规则提示）
      contains: el-select + el-date-picker + el-tooltip
    - path: frontend/src/components/socialinsurance/SIDetailDialog.vue
      provides: 五险分项弹窗（养老/医疗/失业/工伤/生育/公积金各自单位+个人）
      contains: el-dialog + el-table + toLocaleString
    - path: frontend/src/views/socialinsurance/SIRecordsTable.vue
      provides: 参保记录列表（5状态标签 + 渠道列 + 详情按钮）
      contains: el-table + 5色状态标签 + 详情按钮
  key_links:
    - from: SIRecordsTable.vue
      to: StopDialog.vue
      via: 行内"减员"按钮打开
    - from: SIRecordsTable.vue
      to: SIDetailDialog.vue
      via: 行内"详情"按钮打开
    - from: SIRecordsTable.vue
      to: /api/v1/socialinsurance/records
      via: axios GET 获取记录列表
---

<objective>
实现减员弹窗 + 参保记录 Tab 增强（SIRecordsTable）+ 五险分项详情弹窗。

Purpose: 管理员可完成减员操作，查看参保记录详情
Output: StopDialog.vue + SIDetailDialog.vue + SIRecordsTable.vue
</objective>

<context>
@.planning/phases/08-社保公积金增强/08-CONTEXT.md
@.planning/phases/08-社保公积金增强/08-UI-SPEC.md
@frontend/src/views/tool/SalaryList.vue
@frontend/src/views/tool/SITool.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 StopDialog.vue 减员弹窗</name>
  <files>frontend/src/components/socialinsurance/StopDialog.vue</files>
  <action>
创建 `frontend/src/components/socialinsurance/StopDialog.vue`（per D-SI-08 + UI-SPEC Component 4）：

**触发方式：** 行内"减员"按钮（per row），或批量减员按钮
**预填逻辑：** 从 SIRecordsTable.vue 行数据获取员工ID和姓名，自动填充

**表单字段（per UI-SPEC Component 4）：**

| Field | Component | Default | Required | Notes |
|-------|-----------|---------|----------|-------|
| 员工姓名 | el-select filterable | auto-filled (from row) | Yes | 姓名搜索 |
| 身份证号 | el-input readonly | auto-filled | Auto | — |
| 终止月份 | el-date-picker type="month" | 当月 | Yes | min = current month（per SI-10）|
| 减员原因 | el-select (三选一) | — | Yes | 跳槽 / 退休 / 其他 |
| 转出日期 | el-date-picker | — | No | 显示 SI-13 生效规则 tooltip |
| 公积金封存日期 | el-date-picker | — | No | 默认同转出日期 |

**转出生效规则 Tooltip（per D-SI-08 + UI-SPEC Component 4）：**
```vue
<el-tooltip content="转出生效规则：
• 每月5日前转出 → 当月生效
• 5-25日转出 → 次月生效
• 25日后转出 → 下下月生效" placement="top">
  <el-icon><WarningFilled /></el-icon>
</el-tooltip>
```

**提交逻辑：**
```typescript
const submit = async () => {
  // 终止月份不可早于当月（前端校验）
  const currentMonth = dayjs().format('YYYYMM')
  if (form.stopYearMonth < currentMonth) {
    ElMessage.error('终止月份不可早于当月')
    return
  }
  // 危险操作确认在 API 调用之前（per UX 安全规范）
  await ElMessageBox.confirm(
    '减员后不可逆，是否确认停缴？',
    '确认减员',
    { confirmButtonText: '确认减员', cancelButtonText: '取消', type: 'warning' }
  )
  await axios.post('/api/v1/socialinsurance/stop', {
    employeeID: form.employeeID,
    stopYearMonth: form.stopYearMonth,
    reason: form.reason,
    transferDate: form.transferDate,
    hfFreezeDate: form.hfFreezeDate
  })
  ElMessage.success('减员成功')
  emit('success')
}
```

**CTAs：** "取消" (text) + "确认减员" (danger #FF5630)
**destroy-on-close** 重置状态
</action>
  <verify>
    <automated>grep -n "确认减员\|终止月份\|减员原因\|转出生效规则" frontend/src/components/socialinsurance/StopDialog.vue</automated>
  </verify>
  <done>StopDialog.vue 已创建，终止月份校验 + 生效规则提示 + 危险操作确认</done>
</task>

<task type="auto">
  <name>Task 2: 创建 SIRecordsTable.vue 参保记录列表</name>
  <files>frontend/src/views/socialinsurance/SIRecordsTable.vue</files>
  <action>
创建 `frontend/src/views/socialinsurance/SIRecordsTable.vue`（per D-SI-11 + UI-SPEC Component 6）：

**API 调用：**
```typescript
const fetchRecords = async () => {
  const { data } = await axios.get('/api/v1/socialinsurance/records', {
    params: { page, pageSize }
  })
  records.value = data.data.list
  total.value = data.data.total
}
```

**新增列（相比现有 SITool.vue 参保记录）：**

| Column | Width | Notes |
|--------|-------|-------|
| 缴费渠道 | 100px | Tag: 自主缴费 / 代理新客 / 代理已合作 |
| 状态 | 90px | 5色标签（per UI-SPEC Component 6）|
| 操作 | 80px | "详情" 按钮 → 打开 SIDetailDialog |

**状态 Tag 渲染（per UI-SPEC Component 6）：**
```typescript
const statusTagType: Record<string, string> = {
  normal: 'success',
  pending: 'warning',
  overdue: 'danger',
  transferred: 'info',
  not_transferred: '' // custom blue
}
const statusLabelMap: Record<string, string> = {
  normal: '正常',
  pending: '待缴',
  overdue: '欠缴',
  transferred: '已转出',
  not_transferred: '未转出'
}
```

```vue
<el-table>
  <!-- 现有列... -->
  <el-table-column label="缴费渠道" width="100">
    <template #default="{row}">
      <el-tag size="small">{{ paymentChannelLabelMap[row.paymentChannel] }}</el-tag>
    </template>
  </el-table-column>
  <el-table-column label="状态" width="90">
    <template #default="{row}">
      <el-tag :type="statusTagType[row.status]" size="small">
        {{ statusLabelMap[row.status] }}
      </el-tag>
    </template>
  </el-table-column>
  <el-table-column label="操作" width="80">
    <template #default="{row}">
      <el-button size="small" link @click="openDetail(row)">详情</el-button>
    </template>
  </el-table-column>
</el-table>
```

**欠缴行高亮（per UI-SPEC Component 6）：**
```typescript
rowClassName(row) {
  return row.status === 'overdue' ? 'overdue-row' : ''
}
```
```scss
::v-deep(.overdue-row) {
  background: #FF563010 !important;
}
```

**行内"减员"按钮：** 在操作列添加
```vue
<el-button size="small" link type="danger" @click="openStopDialog(row)">减员</el-button>
```

**导出按钮：** 在卡片标题栏添加（复用 SalaryList.vue 导出模式）
</action>
  <verify>
    <automated>grep -n "statusTagType\|statusLabelMap\|overdue-row\|缴费渠道\|openDetail" frontend/src/views/socialinsurance/SIRecordsTable.vue</automated>
  </verify>
  <done>SIRecordsTable.vue 已创建，5色状态标签 + 渠道列 + 详情按钮</done>
</task>

<task type="auto">
  <name>Task 3: 创建 SIDetailDialog.vue 五险分项弹窗</name>
  <files>frontend/src/components/socialinsurance/SIDetailDialog.vue</files>
  <action>
创建 `frontend/src/components/socialinsurance/SIDetailDialog.vue`（per D-SI-12 + UI-SPEC Component 5）：

**触发方式：** SIRecordsTable.vue 行内"详情"按钮

**弹窗属性：**
```vue
<el-dialog title="社保明细" width="640px" :destroy-on-close="true">
```

**表格结构（per UI-SPEC Component 5）：**

| 险种 | 单位缴纳（元） | 个人缴纳（元） |
|------|-------------|-------------|
| 养老保险 | ¥X,XXX.XX | ¥X,XXX.XX |
| 医疗保险 | ¥X,XXX.XX | ¥X,XXX.XX |
| 失业保险 | ¥X,XXX.XX | ¥X,XXX.XX |
| 工伤保险 | ¥X,XXX.XX | ¥0.00 |
| 生育保险 | ¥X,XXX.XX | ¥0.00 |
| 住房公积金 | ¥X,XXX.XX | ¥X,XXX.XX |
| **合计** | **¥X,XXX.XX** | **¥X,XXX.XX** |

**数字格式（per D-SI-12 + UI-SPEC）：**
```typescript
const formatCurrency = (value: number | string) => {
  return '¥' + Number(value).toLocaleString('zh-CN', { minimumFractionDigits: 2, maximumFractionDigits: 2 })
}
```

**其他缴费（per D-SI-12）：**
```vue
<el-descriptions title="其他缴费" :column="2" border>
  <el-descriptions-item label="滞纳金">¥{{ detail.otherFees.lateFee || '0.00' }}</el-descriptions-item>
  <el-descriptions-item label="残保金">¥{{ detail.otherFees.disabilityFee || '0.00' }}</el-descriptions-item>
  <el-descriptions-item label="漏缴">¥{{ detail.otherFees.missedFee || '0.00' }}</el-descriptions-item>
  <el-descriptions-item label="补缴">¥{{ detail.otherFees.backPayFee || '0.00' }}</el-descriptions-item>
</el-descriptions>
```

**底部合计行：** 单独计算 sum（使用 computed）
**CTAs：** "关闭" (text button) — 仅读弹窗

**数据来源：** 后端 API `/api/v1/socialinsurance/records/:id/detail` 或包含在 records 列表每行中
</action>
  <verify>
    <automated>grep -n "养老保险\|住房公积金\|toLocaleString\|其他缴费\|el-descriptions" frontend/src/components/socialinsurance/SIDetailDialog.vue</automated>
  </verify>
  <done>SIDetailDialog.vue 已创建，五险分项 + 其他缴费 + 合计</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | 减员操作需确认，org_id 隔离 |
| input→display | 金额 toLocaleString 无 XSS 风险（数字格式） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | S | StopDialog | mitigate | 减员操作必须 ElMessageBox.confirm 确认，org_id 隔离 |
| T-08-06 | I | SIDetailDialog | mitigate | 数字格式化后端处理，前端无拼接 HTML |
</threat_model>

<verification>
- [ ] `grep -r "StopDialog\|openStopDialog" frontend/src/` 减员弹窗被引用
- [ ] `grep -r "SIDetailDialog\|openDetail" frontend/src/` 详情弹窗被引用
- [ ] `grep -r "SIRecordsTable" frontend/src/` 记录表被引用
- [ ] `grep "socialinsurance/records" frontend/src/` API 调用存在
</verification>

<success_criteria>
- [ ] StopDialog.vue 终止月份默认当月且不可早于当月校验
- [ ] 转出生效规则 tooltip 显示正确（三档规则）
- [ ] 减员提交后 ElMessageBox.confirm 危险确认
- [ ] SIRecordsTable.vue 欠缴行背景标红（#FF563010）
- [ ] SIDetailDialog.vue 五险分项金额使用千分位格式（toLocaleString）
</success_criteria>

<output>
After completion, create `.planning/phases/08-社保公积金增强/08-PLAN-03-SUMMARY.md`
</output>
