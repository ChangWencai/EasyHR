---
phase: 01-login-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/styles/variables.scss
  - frontend/src/styles/global.scss
  - frontend/src/main.ts
  - frontend/src/views/layout/LoginView.vue
autonomous: true
requirements: [UI-01]
must_haves:
  truths:
    - "登录页以左右分栏布局正常显示，左侧720px渐变品牌区，右侧440px白色表单卡"
    - "渐变背景色 linear-gradient(135deg, #1A2D6B 0%, #4F6EF7 60%, #7B9FFF 100%) 正确渲染"
    - "登录表单手机号、验证码输入框和发送按钮正常显示，倒计时显示60s"
    - "版权信息显示 '© 2025 易人事 · 专为小微企业打造'"
  artifacts:
    - path: "frontend/src/styles/variables.scss"
      provides: "全局设计 Token（CSS 自定义属性 + Element Plus 颜色覆盖）"
      min_lines: 50
    - path: "frontend/src/styles/global.scss"
      provides: "导入 variables.scss，全局重置样式"
      contains: "@import './variables.scss'"
    - path: "frontend/src/views/layout/LoginView.vue"
      provides: "左右分栏登录页（左侧品牌区720px + 右侧表单卡440px）"
      exports: "登录页组件"
  key_links:
    - from: "frontend/src/main.ts"
      to: "frontend/src/styles/global.scss"
      via: "import 语句"
      pattern: "import.*global\\.scss"
    - from: "frontend/src/styles/global.scss"
      to: "frontend/src/styles/variables.scss"
      via: "@import 语句"
      pattern: "@import.*variables"
---

<objective>
将 LoginView.vue 从单卡片样式重构为左右分栏布局（左侧品牌区720px渐变 + 右侧表单卡440px），同时建立统一的 CSS 设计 Token 体系。

**核心约束：** 保持所有现有登录逻辑（request.post API 调用、倒计时逻辑、handleLoginSuccess）完全不变。
</objective>

<context>
@frontend/src/styles/variables.scss
@frontend/src/styles/global.scss
@frontend/src/main.ts
@frontend/src/views/layout/LoginView.vue
</context>

<interfaces>
<!-- 从 main.ts 确认的导入链 -->
// main.ts 当前导入链
import '@/styles/global.scss'  // global.scss 将 import variables.scss

<!-- 从 LoginView.vue 确认需要保留的逻辑（script 部分不改动） -->
// 以下逻辑必须保留：
// - useAuthStore, request.post('/auth/login', ...)
// - request.post('/auth/send-code', ...)
// - request.post('/auth/register', ...)
// - request.post('/auth/login/password', ...)
// - handleLoginSuccess, startCountdown, countdown ref
// - el-tabs v-model="activeTab", 三个 tab: sms/password/register
// - 全部 import 语句（User, Lock, ElementPlus icons 等）
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 建立设计 Token 体系（variables.scss + global.scss）</name>
  <files>frontend/src/styles/variables.scss, frontend/src/styles/global.scss</files>
  <action>
**variables.scss 重写：** 完全重写该文件，将 reset 样式保留，新增完整 CSS 自定义属性：

```scss
// === 重置样式（从当前 variables.scss 保留） ===
*,
*::before,
*::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html,
body {
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

.app-container {
  min-height: 100vh;
  padding-bottom: calc(60px + var(--safe-area-bottom, 0px));
}

// === 设计 Token ===
:root {
  // Primary 调色板
  --primary: #4F6EF7;
  --primary-hover: #6B84F9;
  --primary-dark: #3651D9;
  --primary-light: #EEF1FF;

  // 背景色
  --bg-page: #F0F2F5;
  --bg-sidebar: #0D1B2A;
  --bg-sidebar-hover: #1A2D42;
  --bg-sidebar-active: #4F6EF7;
  --bg-surface: #FFFFFF;

  // 边框
  --border: #E8ECF0;

  // 文字色
  --text-primary: #172B4D;
  --text-secondary: #5E6C84;
  --text-tertiary: #97A0AF;
  --text-sidebar: #CDD3E0;
  --text-sidebar-active: #FFFFFF;

  // 状态色
  --danger: #FF5630;
  --success: #36B37E;
  --warning: #FFAB00;

  // 圆角
  --radius-lg: 12px;
  --radius-md: 8px;

  // 间距
  --spacing-lg: 24px;
  --spacing-md: 16px;

  // 安全区
  --safe-area-bottom: env(safe-area-inset-bottom, 0px);
}
```

**global.scss 重写：** 清空现有内容，只保留 `@import './variables.scss'` 一行导入语句（重置样式已移入 variables.scss）。
  </action>
  <verify>
grep -c "\-\-primary:" frontend/src/styles/variables.scss
grep "@import.*variables" frontend/src/styles/global.scss</verify>
  <done>variables.scss 包含所有设计 Token（--primary, --bg-sidebar, --text-sidebar 等），global.scss 只导入 variables.scss</done>
</task>

<task type="auto">
  <name>Task 2: 重写 main.ts 导入（如果需要）</name>
  <files>frontend/src/main.ts</files>
  <action>
检查 main.ts 当前导入。由于 global.scss 已经 import 了 variables.scss，无需在 main.ts 中额外添加导入。确认 main.ts 保持以下导入不变：

```typescript
import '@/styles/global.scss'  // 保持现有行不变
```

main.ts 无需修改。
  </action>
  <verify>
grep "global.scss" frontend/src/main.ts</verify>
  <done>main.ts 保持 import '@/styles/global.scss' 不变</done>
</task>

<task type="auto">
  <name>Task 3: 完全重写 LoginView.vue（保留 script 逻辑，改写 template + style）</name>
  <files>frontend/src/views/layout/LoginView.vue</files>
  <action>
**完全重写 LoginView.vue 的 `<template>` 和 `<style>` 部分，`<script>` 部分保持原样不变。**

template 改为左右分栏结构：
- 根元素: `<div class="login-layout">`
- 左品牌区 `<aside class="login-brand">`: 720px 宽，渐变背景，内含 Logo + 标语 + 分隔线 + 5个功能特性列表（使用白色文字）
- 右表单区 `<main class="login-form-panel">`: 白色背景，flex 居中，内含 `.login-card` 表单卡（440px 宽，48px padding）
- 表单卡片内保留现有的 el-tabs + 三个 tab-pane 内容（script 中的 v-model 等逻辑不变）

**品牌区内容（来自 UI-SPEC.md）：**
- Logo: `<Management />` 图标 + "易人事"（白色，20px 700）
- 标语: "轻量一站式人事管理平台"（白色 80% 透明度，16px）
- 分隔线: 40px 宽 3px 高，白色 40% 透明度
- 功能列表（5项）:
  1. 入职管理
  2. 薪资核算
  3. 社保公积金
  4. 财务记账
  5. 员工工资条
- 版权: "© 2025 易人事 · 专为小微企业打造"（底部白色 60% 透明度）

**表单卡片内容（保留现有逻辑）：**
- brand-header: "易人事" 品牌名（primary 色）+ "老板管理后台" 副标题
- el-tabs: 手机验证码 / 密码登录 / 注册（保持 activeTab v-model）
- 三个 tab 内容保持不变（request.post 调用不变）
- copyright: "© 2025 易人事 · 专为小微企业打造"

**样式关键点：**
```scss
.login-layout {
  display: grid;
  grid-template-columns: 720px 1fr;
  min-height: 100vh;

  @media (max-width: 768px) {
    grid-template-columns: 1fr;
  }
}

.login-brand {
  background: linear-gradient(135deg, #1A2D6B 0%, #4F6EF7 60%, #7B9FFF 100%);
  // 装饰圆使用 ::before / ::after 伪元素
  // 移动端 display: none
}

.login-form-panel {
  background: var(--bg-surface);
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 24px;
  min-width: 0;

  @media (max-width: 768px) {
    align-items: flex-start;
    padding-top: 48px;
  }
}

.login-card {
  width: 100%;
  max-width: 440px;
  padding: 48px;
  border-radius: var(--radius-lg);
  box-shadow: 0 4px 24px rgba(0, 0, 0, 0.08);
  animation: slideUp 0.4s ease;
}
```

**移动端响应式：**
- 品牌区隐藏（display: none）
- 表单面板占满屏幕，顶部 48px padding
- 版权文字保持可见

**禁止事项：**
- 不得修改 `<script>` 部分任何代码
- 不得修改 el-tabs 的 v-model、tab-pane 数量、form 表单结构
- 不得修改 request.post 调用参数
- 不得修改 countdown 逻辑
  </action>
  <verify>
grep -c "login-layout\|login-brand\|login-form-panel\|login-card" frontend/src/views/layout/LoginView.vue</verify>
  <done>登录页显示左右分栏（左侧渐变品牌区720px + 右侧白色表单卡440px），所有现有登录逻辑（request.post, countdown）保持不变</done>
</task>

</tasks>

<verification>
## 人工验收清单（checkpoint:human-verify）

1. 访问 `/login` 路由，确认左右分栏布局正常显示
2. 左侧品牌区渐变背景色正确（深蓝→蓝→浅蓝渐变）
3. 品牌区 Logo + 标语 + 5个功能列表正常显示
4. 右侧白色表单卡居中，440px 宽，48px padding
5. 手机号输入框、验证码输入框、发送按钮正常显示
6. 点击「获取验证码」按钮，显示「已发送(59s)」倒计时
7. 移动端（浏览器 DevTools 模拟 375px）品牌区隐藏，表单占满屏幕
8. 打开浏览器 DevTools → Elements，搜索 `1677ff` 确认无硬编码旧主色残留
</verification>

<success_criteria>
- 登录页左右分栏布局正常渲染（左侧720px渐变品牌区 + 右侧440px白色表单卡）
- 渐变背景使用 linear-gradient(135deg, #1A2D6B 0%, #4F6EF7 60%, #7B9FFF 100%)
- 所有登录 API 调用（login, send-code, register, login/password）保持不变
- 60s 倒计时功能正常
- 移动端响应式正确（品牌区隐藏，表单占满屏幕）
- 全局无 #1677ff 硬编码残留（已迁移到 var(--primary)）
</success_criteria>

<output>
完成后创建 `.planning/phases/01-login-layout/01-01-SUMMARY.md`
</output>
