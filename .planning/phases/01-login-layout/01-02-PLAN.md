---
phase: 01-login-layout
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/views/layout/AppLayout.vue
autonomous: true
requirements: [UI-13]
must_haves:
  truths:
    - "侧边栏固定220px宽度显示，深色背景 #0D1B2A"
    - "Logo 区域图标蓝色方块 + '易人事' 白色文字显示正确"
    - "菜单项文字灰色 #CDD3E0，hover 背景 #1A2D42"
    - "激活菜单项背景 #4F6EF7，文字白色"
    - "侧边栏折叠/展开有过渡动画（0.2s ease）"
    - "移动端抽屉式导航正常呈现（240px）"
  artifacts:
    - path: "frontend/src/views/layout/AppLayout.vue"
      provides: "深色侧边栏布局（220px固定宽，64px折叠）"
      exports: "AppLayout 组件"
  key_links:
    - from: "frontend/src/views/layout/AppLayout.vue"
      to: "frontend/src/styles/variables.scss"
      via: "CSS var(--bg-sidebar) 等变量引用"
      pattern: "var\\(--bg-sidebar\\)"
---

<objective>
将 AppLayout.vue 的侧边栏从白色主题改为深色主题（#0D1B2A 背景），同时优化菜单项样式和过渡动画。移动端抽屉样式也同步暗色化。

**核心约束：** 保持所有现有布局结构（el-menu、el-drawer、移动端顶部栏）、isCollapsed ref、pageTitleMap、activeMenu computed 等逻辑不变。
</objective>

<context>
@frontend/src/views/layout/AppLayout.vue
@frontend/src/styles/variables.scss
</context>

<interfaces>
<!-- 从 AppLayout.vue 确认需要保留的逻辑 -->
// script 部分（必须保持不变）：
// - import { ref, computed } from 'vue'
// - import { useRoute } from 'vue-router'
// - 所有 el-icon imports（HomeFilled, UserFilled, Tools, Money, Avatar, Menu, DArrowLeft, DArrowRight, Management）
// - const isCollapsed = ref(false)
// - const drawerVisible = ref(false)
// - const activeMenu = computed(() => { ... })  // 路由逻辑保持不变
// - const pageTitleMap: Record<string, string> = { ... }
// - const currentPageTitle = computed(() => pageTitleMap[route.path] || '易人事')

// template 结构（必须保持不变）：
// - .app-layout > .mobile-header + .sidebar + .main-wrapper + .el-drawer
// - .sidebar 包含 .sidebar-logo + el-scrollbar > el-menu + .sidebar-footer
// - el-menu 有 router mode, :default-active="activeMenu", :collapse="isCollapsed"
// - 所有 menu-item 和 sub-menu 结构和内容保持不变
// - .sidebar-footer collapse 按钮绑定 @click="isCollapsed = !isCollapsed"
// - el-drawer v-model="drawerVisible" direction="ltr" size="240"
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 重写 AppLayout.vue 侧边栏为暗色主题</name>
  <files>frontend/src/views/layout/AppLayout.vue</files>
  <action>
**完全重写 AppLayout.vue 的 `<style scoped lang="scss">` 部分（桌面端侧边栏样式），`<template>` 和 `<script>` 部分保持原样不变。**

将现有 `<style scoped lang="scss">` 块中所有样式替换为以下暗色主题：

```scss
<style scoped lang="scss">
.app-layout {
  display: flex;
  min-height: 100vh;
  background: var(--bg-page);  // #F0F2F5
}

// === 移动端顶部栏 ===
.mobile-header {
  display: none;
  position: sticky;
  top: 0;
  z-index: 100;
  height: 56px;
  background: #fff;
  border-bottom: 1px solid var(--border);
  padding: 0 16px;
  align-items: center;
  justify-content: space-between;
  box-shadow: 0 1px 4px rgba(0, 0, 0, 0.06);
}

.page-title {
  font-size: 16px;
  font-weight: 600;
  color: var(--text-primary);
}

// === 侧边栏（暗色主题） ===
.sidebar {
  width: 220px;
  min-width: 220px;
  height: 100vh;
  position: fixed;
  left: 0;
  top: 0;
  background: var(--bg-sidebar);  // #0D1B2A
  display: flex;
  flex-direction: column;
  transition: width 0.2s ease, min-width 0.2s ease;
  z-index: 200;
  overflow: hidden;

  &.collapsed {
    width: 64px;
    min-width: 64px;
  }
}

// === Logo 区域 ===
.sidebar-logo {
  height: 56px;
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 0 20px;
  border-bottom: 1px solid rgba(255, 255, 255, 0.08);
  flex-shrink: 0;

  .logo-icon {
    width: 32px;
    height: 32px;
    background: var(--primary);  // #4F6EF7 蓝色方块
    border-radius: var(--radius-md);  // 8px
    display: flex;
    align-items: center;
    justify-content: center;
    color: #fff;
    flex-shrink: 0;
  }

  .logo-text {
    font-size: 16px;
    font-weight: 700;
    color: #fff;  // 白色
    white-space: nowrap;
    overflow: hidden;
  }
}

// === Element Plus 菜单暗色覆盖 ===
.sidebar-el-menu {
  border-right: none !important;
  background: transparent !important;
  transition: background 0.15s ease;

  &:not(.el-menu--collapse) {
    width: 100%;
  }

  // 子菜单激活态
  .is-active > .el-sub-menu__title {
    color: var(--primary) !important;
    .el-icon {
      color: var(--primary) !important;
    }
  }

  .el-menu-item,
  .el-sub-menu__title {
    height: 44px;
    line-height: 44px;
    font-size: 14px;
    color: var(--text-sidebar) !important;  // #CDD3E0
    background: transparent !important;
    transition: background 0.15s ease, color 0.15s ease;

    .el-icon {
      color: var(--text-sidebar) !important;
    }

    &:hover {
      background: var(--bg-sidebar-hover) !important;  // #1A2D42
      color: #fff !important;
    }
  }

  .el-menu-item.is-active {
    background: var(--bg-sidebar-active) !important;  // #4F6EF7
    color: var(--text-sidebar-active) !important;  // #FFFFFF
    border-right: none !important;

    .el-icon {
      color: #fff !important;
    }
  }

  // 子菜单箭头
  .el-sub-menu .el-icon {
    color: var(--text-sidebar) !important;
  }

  // 子菜单弹出面板
  .el-menu--inline {
    background: rgba(0, 0, 0, 0.15) !important;
  }
}

// === 滚动区域 ===
.sidebar-scroll {
  overflow-x: hidden !important;
  flex: 1;
}

// === 折叠按钮 ===
.sidebar-footer {
  flex-shrink: 0;
  border-top: 1px solid rgba(255, 255, 255, 0.08);
  padding: 8px 8px 8px 4px;
  display: flex;
  justify-content: flex-end;

  .collapse-btn {
    color: rgba(255, 255, 255, 0.5);
    padding: 8px;
    transition: color 0.15s ease, background 0.15s ease;

    &:hover {
      color: #fff;
      background: var(--bg-sidebar-hover);
    }
  }
}

// === 主内容区 ===
.main-wrapper {
  flex: 1;
  margin-left: 220px;
  min-height: 100vh;
  transition: margin-left 0.2s ease;

  &.sidebar-collapsed {
    margin-left: 64px;
  }
}

// === 移动端抽屉（暗色主题） ===
:deep(.el-drawer) {
  background: var(--bg-sidebar) !important;

  .el-drawer__header {
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
    margin-bottom: 0;
    padding: 16px 20px;
    color: #fff;
  }

  .drawer-header {
    display: flex;
    align-items: center;
    gap: 10px;
    color: #fff;
    font-size: 16px;
    font-weight: 700;
  }

  .el-menu {
    background: transparent !important;
    border-right: none !important;
  }

  .el-menu-item,
  .el-sub-menu__title {
    color: var(--text-sidebar) !important;

    .el-icon {
      color: var(--text-sidebar) !important;
    }

    &:hover {
      background: var(--bg-sidebar-hover) !important;
    }
  }

  .el-menu-item.is-active {
    background: var(--bg-sidebar-active) !important;
    color: #fff !important;

    .el-icon {
      color: #fff !important;
    }
  }
}

// === 页面切换动画 ===
.fade-enter-active,
.fade-leave-active {
  transition: opacity 0.15s ease;
}
.fade-enter-from,
.fade-leave-to {
  opacity: 0;
}

// === 响应式 ===
@media (max-width: 768px) {
  .mobile-header {
    display: flex;
  }

  .sidebar {
    display: none;
  }

  .main-wrapper {
    margin-left: 0;

    &.sidebar-collapsed {
      margin-left: 0;
    }
  }
}
</style>
```

**禁止事项：**
- 不得修改 `<template>` 部分任何 HTML 结构
- 不得修改 `<script setup>` 部分任何代码
- 不得修改 menu-item 的内容文案（"首页"、"员工管理" 等）
- 不得修改 isCollapsed ref 的默认值（false）
- 不得修改 el-drawer 的 direction="ltr" 或 size="240" 属性
- 不得修改 pageTitleMap 的内容
  </action>
  <verify>
grep -c "bg-sidebar\|text-sidebar\|sidebar-active\|sidebar-hover" frontend/src/views/layout/AppLayout.vue</verify>
  <done>AppLayout.vue 侧边栏为深色主题（#0D1B2A），菜单项暗色样式正确，折叠动画 0.2s，移动端抽屉暗色化</done>
</task>

</tasks>

<verification>
## 人工验收清单（checkpoint:human-verify）

1. 访问 `/home` 路由，确认侧边栏深色背景 #0D1B2A
2. Logo 区域蓝色方块图标（#4F6EF7）+ "易人事" 白色文字显示正确
3. 菜单项文字 #CDD3E0，hover 背景 #1A2D42
4. 点击激活菜单项（如"首页"），背景 #4F6EF7，文字白色
5. 点击底部折叠按钮，侧边栏平滑折叠到 64px，main-wrapper 同步右移，过渡动画 0.2s
6. 再点击折叠按钮，侧边栏恢复 220px
7. 移动端（浏览器 DevTools 模拟 375px）顶部栏正常显示
8. 点击顶部菜单按钮，抽屉从左侧滑出（240px），背景深色 #0D1B2A
9. 抽屉内 Logo + 菜单项样式与桌面端一致
10. 打开 DevTools → Elements，搜索 `background: #fff` 在 `.sidebar` 区域，确认无白色背景残留
</verification>

<success_criteria>
- 侧边栏背景为 #0D1B2A（var(--bg-sidebar)）
- Logo 区域蓝色图标 + 白色文字正确显示
- 菜单文字 #CDD3E0，hover #1A2D42，active #4F6EF7 + 白字
- 折叠/展开过渡动画 0.2s 正常
- 移动端抽屉 240px 暗色背景，菜单项样式正确
- 无 #1677ff 旧主色在侧边栏残留
</success_criteria>

<output>
完成后创建 `.planning/phases/01-login-layout/01-02-SUMMARY.md`
</output>
