---
phase: 01-foundation-auth
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - internal/user/model.go
  - internal/user/dto.go
  - internal/user/repository.go
  - internal/user/service.go
  - internal/user/handler.go
  - internal/user/repository_test.go
  - internal/user/service_test.go
  - internal/user/handler_test.go
  - internal/common/middleware/auth.go
  - internal/common/middleware/rbac.go
  - internal/common/middleware/audit.go
  - internal/common/middleware/rbac_test.go
  - internal/common/middleware/audit_test.go
  - cmd/server/main.go
  - cmd/server/routes.go
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - PLAT-01
  - PLAT-02

must_haves:
  truths:
    - "新手机号发送验证码后可登录，自动创建用户和企业记录"
    - "已有手机号登录后返回已有用户数据，不重复创建"
    - "首次登录返回 onboarding=true，引导前端进入企业信息录入页"
    - "企业信息录入完成后 onboarding 状态更新为 false"
    - "OWNER/ADMIN 可创建子账号，MEMBER 不能"
    - "OWNER 角色不能被删除或修改为其他角色"
    - "所有写操作（Create/Update/Delete）自动记录审计日志"
    - "审计日志包含 org_id/user_id/module/action/target_type/target_id/detail/ip_address/created_at"
  artifacts:
    - path: "internal/user/model.go"
      provides: "User和Organization GORM模型"
      contains: "type User struct"
    - path: "internal/user/handler.go"
      provides: "认证和用户管理HTTP Handler"
      exports: ["SendCode", "Login", "CompleteOnboarding", "CreateSubAccount", "ListUsers", "UpdateUserRole"]
    - path: "internal/common/middleware/auth.go"
      provides: "JWT认证中间件"
      exports: ["Auth"]
    - path: "internal/common/middleware/rbac.go"
      provides: "RBAC角色检查中间件"
      exports: ["RequireRole"]
    - path: "internal/common/middleware/audit.go"
      provides: "审计日志GORM Hook"
      contains: "audit_log_callback"
  key_links:
    - from: "internal/common/middleware/auth.go"
      to: "pkg/jwt/jwt.go"
      via: "调用ParseToken验证JWT"
      pattern: "jwt\\.ParseToken"
    - from: "internal/common/middleware/auth.go"
      to: "Redis"
      via: "检查Token黑名单"
      pattern: "token:blacklist"
    - from: "internal/user/handler.go"
      to: "pkg/sms/sms.go"
      via: "发送验证码"
      pattern: "sms\\.SendVerifyCode"
    - from: "internal/common/middleware/audit.go"
      to: "GORM Callback"
      via: "注册Create/Update/Delete后Hook"
      pattern: "Callback\\(\\)\\.Create\\(\\)"
    - from: "internal/user/repository.go"
      to: "internal/common/middleware/tenant.go"
      via: "使用TenantScope自动过滤org_id"
      pattern: "TenantScope"
---

<objective>
实现完整的用户认证流程（手机号+验证码登录/注册/企业引导）、RBAC权限中间件和GORM审计日志Hook。

Purpose: 这是Phase 1的核心业务逻辑层，实现了AUTH-01~03的认证流程、PLAT-01的RBAC权限管理和PLAT-02的审计日志。依赖Plan 01的基础设施和Plan 02的JWT/SMS工具。

Output: 完整的认证API、RBAC中间件、审计日志系统，可进行端到端认证测试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-auth/1-CONTEXT.md
@.planning/phases/01-foundation-auth/1-RESEARCH.md
@.planning/phases/01-foundation-auth/01-01-SUMMARY.md
@.planning/phases/01-foundation-auth/01-02-SUMMARY.md
</context>

<interfaces>
<!-- Key types and contracts from Plan 01 and Plan 02 that this executor needs -->

From pkg/jwt/jwt.go (Plan 02):
```go
type Claims struct {
    UserID int64  `json:"user_id"`
    OrgID  int64  `json:"org_id"`
    Role   string `json:"role"`
    jwt.RegisteredClaims
}
func GenerateToken(userID, orgID int64, role, secret string, ttl time.Duration) (string, error)
func ParseToken(tokenStr, secret string) (*Claims, error)
func GenerateRefreshToken(userID, orgID int64, role, secret string, ttl time.Duration) (string, error)
```

From pkg/sms/sms.go (Plan 02):
```go
type SMSService interface {
    SendVerifyCode(phone string, code string) error
}
func GenerateCode() string
```

From internal/common/response/response.go (Plan 01):
```go
func Success(c *gin.Context, data interface{})
func Error(c *gin.Context, httpStatus int, code int, msg string)
func Unauthorized(c *gin.Context, msg string)
func Forbidden(c *gin.Context, msg string)
func BadRequest(c *gin.Context, msg string)
func PageSuccess(c *gin.Context, list interface{}, total int64, page int, pageSize int)
```

From internal/common/crypto/ (Plan 01):
```go
func Encrypt(plaintext string, key []byte) (string, error)
func Decrypt(encoded string, key []byte) (string, error)
func HashSHA256(input string) string
func MaskPhone(phone string) string
```

From internal/common/database/database.go (Plan 01):
```go
func Init(cfg *config.DatabaseConfig, logger *zap.Logger) (*gorm.DB, error)
func TenantScope(orgID int64) func(db *gorm.DB) *gorm.DB
```

From pkg/config/config.go (Plan 01):
```go
type Config struct {
    Server   ServerConfig
    Database DatabaseConfig
    Redis    RedisConfig
    JWT      JWTConfig
    OSS      OSSConfig
    SMS      SMSConfig
    Crypto   CryptoConfig
}
func Load() (*Config, error)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: User/Organization模型 + Repository + 审计日志Hook + Auth/RBAC中间件</name>
  <files>internal/user/model.go, internal/user/dto.go, internal/user/repository.go, internal/user/repository_test.go, internal/common/middleware/auth.go, internal/common/middleware/rbac.go, internal/common/middleware/audit.go, internal/common/middleware/rbac_test.go, internal/common/middleware/audit_test.go</files>
  <read_first>.planning/phases/01-foundation-auth/1-CONTEXT.md (D-05~D-14, D-22~D-26 所有认证/RBAC/审计决策)
.planning/phases/01-foundation-auth/1-RESEARCH.md (Pattern 1~5 代码示例, Pitfalls 1~5)
tech-architecture.md (lines 148-200, users/organizations表结构)</read_first>
  <acceptance_criteria>
    - `internal/user/model.go` contains `type User struct` with fields Phone, PhoneHash, OrgID, Role
    - `internal/user/model.go` contains `type Organization struct` with fields Name, CreditCode, City, OnboardingDone
    - `internal/user/model.go` contains `type AuditLog struct` with fields OrgID, UserID, Module, Action, TargetType, TargetID, Detail, IPAddress
    - `internal/user/repository.go` contains `type UserRepository interface` with methods FindByPhoneHash, FindByID, Create, Update, ListByOrgID
    - `internal/common/middleware/auth.go` contains `func Auth(jwtSecret string, rdb *redis.Client) gin.HandlerFunc`
    - `internal/common/middleware/rbac.go` contains `func RequireRole(roles ...string) gin.HandlerFunc`
    - `internal/common/middleware/audit.go` contains `func RegisterAuditCallbacks(db *gorm.DB)` and references `Callback().Create()`
    - `go test ./internal/user/... ./internal/common/middleware/... -v -short` exits 0
  </acceptance_criteria>
  <behavior>
    - Test 1: UserRepository.FindByPhoneHash 正确通过SHA-256哈希查找用户
    - Test 2: UserRepository.Create 自动加密手机号并存储phone_hash
    - Test 3: Auth中间件: 无Bearer Token返回401
    - Test 4: Auth中间件: 过期Token返回401
    - Test 5: Auth中间件: 有效Token设置 user_id/org_id/role 到gin.Context
    - Test 6: RequireRole("OWNER") 对ADMIN角色返回403
    - Test 7: RequireRole("OWNER","ADMIN") 对ADMIN角色放行
    - Test 8: 审计Hook: GORM Create操作后自动写入audit_logs表
    - Test 9: 审计Hook: GORM Update/Delete操作后自动写入audit_logs表
  </behavior>
  <action>
    1. 创建 internal/user/model.go:
       - User struct (per tech-architecture.md users表):
         ```go
         type User struct {
             ID           int64      `gorm:"primaryKey"`
             Phone        string     `gorm:"column:phone;type:varchar(200)"`
             PhoneHash    string     `gorm:"column:phone_hash;type:varchar(64);uniqueIndex:idx_phone_hash_where_deleted"`
             Nickname     string     `gorm:"column:nickname;type:varchar(50)"`
             AvatarURL    string     `gorm:"column:avatar_url;type:varchar(500)"`
             OrgID        int64      `gorm:"column:org_id;not null;index"`
             Role         string     `gorm:"column:role;type:varchar(20);not null"`
             Status       string     `gorm:"column:status;type:varchar(20);default:'ACTIVE'"`
             LastLoginAt  *time.Time `gorm:"column:last_login_at"`
             LastLoginIP  string     `gorm:"column:last_login_ip;type:varchar(50)"`
             CreatedBy    int64      `gorm:"column:created_by"`
             CreatedAt    time.Time  `gorm:"column:created_at;autoCreateTime"`
             UpdatedBy    int64      `gorm:"column:updated_by"`
             UpdatedAt    time.Time  `gorm:"column:updated_at;autoUpdateTime"`
             DeletedAt    gorm.DeletedAt `gorm:"column:deleted_at;index"`
         }
         ```
       - Organization struct (per tech-architecture.md organizations表):
         ```go
         type Organization struct {
             ID               int64          `gorm:"primaryKey"`
             Name             string         `gorm:"column:name;type:varchar(200);not null"`
             CreditCode       string         `gorm:"column:credit_code;type:varchar(18);uniqueIndex:idx_credit_code_where_deleted"`
             Province         string         `gorm:"column:province;type:varchar(50)"`
             City             string         `gorm:"column:city;type:varchar(50);not null"`
             District         string         `gorm:"column:district;type:varchar(50)"`
             Industry         string         `gorm:"column:industry;type:varchar(50)"`
             ContactName      string         `gorm:"column:contact_name;type:varchar(50)"`
             ContactPhone     string         `gorm:"column:contact_phone;type:varchar(200)"`
             ContactPhoneHash string         `gorm:"column:contact_phone_hash;type:varchar(64)"`
             LogoURL          string         `gorm:"column:logo_url;type:varchar(500)"`
             OnboardingDone   bool           `gorm:"column:onboarding_done;default:false"`
             Status           string         `gorm:"column:status;type:varchar(20);default:'ACTIVE'"`
             CreatedBy        int64          `gorm:"column:created_by"`
             CreatedAt        time.Time      `gorm:"column:created_at;autoCreateTime"`
             UpdatedBy        int64          `gorm:"column:updated_by"`
             UpdatedAt        time.Time      `gorm:"column:updated_at;autoUpdateTime"`
             DeletedAt        gorm.DeletedAt `gorm:"column:deleted_at;index"`
         }
         ```
       - AuditLog struct (per D-22):
         ```go
         type AuditLog struct {
             ID         int64          `gorm:"primaryKey"`
             OrgID      int64          `gorm:"column:org_id;not null;index"`
             UserID     int64          `gorm:"column:user_id;not null;index"`
             Module     string         `gorm:"column:module;type:varchar(50);not null"`
             Action     string         `gorm:"column:action;type:varchar(20);not null"`
             TargetType string         `gorm:"column:target_type;type:varchar(50);not null"`
             TargetID   int64          `gorm:"column:target_id;not null"`
             Detail     datatypes.JSON `gorm:"column:detail;type:jsonb"`
             IPAddress  string         `gorm:"column:ip_address;type:varchar(50)"`
             CreatedAt  time.Time      `gorm:"column:created_at;autoCreateTime"`
         }
         ```
       - 安装: `go get gorm.io/datatypes` (JSONB支持)
       - 在database.go的Init中添加AutoMigrate: AutoMigrate(&User{}, &Organization{}, &AuditLog{})
       - 部分唯一索引使用 `uniqueIndex:idx_name_where_deleted` 配合 GORM 的软删除 (per D-25)

    2. 创建 internal/user/dto.go:
       - `SendCodeRequest`: Phone string `validate:"required,len=11"`
       - `LoginRequest`: Phone string `validate:"required,len=11"`, Code string `validate:"required,len=6"`
       - `LoginResponse`: AccessToken string, RefreshToken string, Onboarding bool, User *UserDTO
       - `CompleteOnboardingRequest`: Name string, CreditCode string, City string, Province string, District string, ContactName string, ContactPhone string
       - `CreateSubAccountRequest`: Phone string `validate:"required,len=11"`, Nickname string, Role string `validate:"required,oneof=ADMIN MEMBER"`
       - `UpdateUserRoleRequest`: Role string `validate:"required,oneof=ADMIN MEMBER"`
       - `UserDTO`: 从User struct 转换，手机号脱敏 (per D-17)

    3. 创建 internal/user/repository.go (per D-02 handler->service->repository 三层):
       - UserRepository interface:
         ```go
         type UserRepository interface {
             FindByPhoneHash(phoneHash string) (*User, error)
             FindByID(id int64) (*User, error)
             Create(user *User) error
             Update(user *User) error
             ListByOrgID(orgID int64, page, pageSize int) ([]User, int64, error)
             FindByOrgIDAndID(orgID, id int64) (*User, error)
         }
         ```
       - userRepository struct: 包含 *gorm.DB
       - 所有查询使用 `db.Scopes(TenantScope(orgID))` 自动注入org_id (per D-09)
       - FindByPhoneHash: 使用 phone_hash 字段精确查找 (per D-15 双列模式)
       - Create: 加密手机号 -> 存储加密值+哈希值
       - OrganizationRepository interface 和实现: FindByID, Create, Update

    4. 创建 internal/common/middleware/auth.go:
       - `func Auth(jwtSecret string, rdb *redis.Client) gin.HandlerFunc`:
         - 从 Authorization header 提取 Bearer Token
         - 调用 jwt.ParseToken 验证签名和有效期
         - 检查Redis黑名单: `token:blacklist:{jti}` (per RESEARCH.md Pitfall 3)
         - 设置 c.Set("user_id", claims.UserID), c.Set("org_id", claims.OrgID), c.Set("role", claims.Role)
         - 无效Token返回 response.Unauthorized(c, "无效Token")
         - 黑名单Token返回 response.Unauthorized(c, "Token已失效")

    5. 创建 internal/common/middleware/rbac.go (per D-12, D-13):
       - `func RequireRole(roles ...string) gin.HandlerFunc`:
         - 从 gin.Context 获取 role
         - 检查是否在允许列表中
         - 不在列表返回 response.Forbidden(c, "权限不足")
       - 路由使用示例: `v1.POST("/users", middleware.RequireRole("OWNER", "ADMIN"), handler.CreateSubAccount)`

    6. 创建 internal/common/middleware/audit.go (per D-22, D-23):
       - `func RegisterAuditCallbacks(db *gorm.DB)`: 注册 GORM After Create/Update/Delete Callbacks
       - Callback 函数从 gorm.DB.Statement 的 Context 中提取 org_id/user_id/ip_address
       - 写入 audit_logs 表: module=表名, action=CREATE/UPDATE/DELETE, target_type=表名, target_id=记录ID, detail=变更前后JSON
       - 审计日志不触发审计Hook（避免递归）

    7. 编写测试:
       - internal/user/repository_test.go: 测试FindByPhoneHash/Create（含加密验证）
       - internal/common/middleware/rbac_test.go: 测试RequireRole放行/拒绝
       - internal/common/middleware/audit_test.go: 测试GORM写操作后审计日志生成
  </action>
  <verify>
    <automated>cd /Users/wencai/github/EasyHR && go test ./internal/user/... ./internal/common/middleware/... -v -short</automated>
  </verify>
  <done>
    - User/Organization/AuditLog 三个GORM模型定义完成，字段与tech-architecture.md一致
    - UserRepository 通过 phone_hash 查找用户，Create 自动加密手机号
    - Auth中间件验证JWT+黑名单检查，设置 user_id/org_id/role 到Context
    - RequireRole 中间件根据角色列表放行/拒绝 (per D-12)
    - 审计Hook注册GORM Create/Update/Delete Callback，自动写入audit_logs
    - 审计日志包含 D-22 要求的所有字段
    - `go test ./internal/user/... ./internal/common/middleware/... -v -short` 全部通过
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 认证Service/Handler + 子账号管理 + 路由注册</name>
  <files>internal/user/service.go, internal/user/service_test.go, internal/user/handler.go, internal/user/handler_test.go, cmd/server/routes.go, cmd/server/main.go</files>
  <read_first>.planning/phases/01-foundation-auth/1-CONTEXT.md (D-05~D-08 认证流程, D-12~D-14 RBAC)
internal/user/model.go (刚创建的模型)
internal/user/repository.go (刚创建的Repository接口)</read_first>
  <acceptance_criteria>
    - `internal/user/service.go` contains `func (s *UserService) SendVerifyCode(phone string) error`
    - `internal/user/service.go` contains `func (s *UserService) Login(phone, code string) (*LoginResponse, error)`
    - `internal/user/service.go` contains `func (s *UserService) CompleteOnboarding(orgID int64, req CompleteOnboardingRequest) error`
    - `internal/user/service.go` contains `func (s *UserService) CreateSubAccount(orgID int64, operatorRole string, req CreateSubAccountRequest) (*UserDTO, error)`
    - `internal/user/handler.go` contains `func (h *UserHandler) SendCode(c *gin.Context)`
    - `internal/user/handler.go` contains `func (h *UserHandler) Login(c *gin.Context)`
    - `internal/user/handler.go` contains `func (h *UserHandler) CompleteOnboarding(c *gin.Context)`
    - `internal/user/handler.go` contains `func (h *UserHandler) CreateSubAccount(c *gin.Context)`
    - `cmd/server/routes.go` contains `/api/v1/auth/send-code` and `/api/v1/auth/login`
    - `cmd/server/routes.go` contains `/api/v1/org/onboarding` and `/api/v1/users`
    - `cmd/server/main.go` contains RegisterAuditCallbacks
    - `go test ./internal/user/... -v -short` exits 0
    - `go build ./cmd/server/` exits 0
  </acceptance_criteria>
  <behavior>
    - Test 1: UserService.SendVerifyCode 对新手机号: 生成验证码存Redis，调用SMS发送
    - Test 2: UserService.SendVerifyCode 60秒内重复发送返回错误
    - Test 3: UserService.Login 新手机号: 自动创建User+Organization(状态onboarding_done=false)，返回Token+onboarding=true
    - Test 4: UserService.Login 已有手机号: 验证码正确返回Token+onboarding=false
    - Test 5: UserService.Login 验证码错误5次后锁定需重新发送
    - Test 6: UserService.CompleteOnboarding 更新企业信息，设onboarding_done=true
    - Test 7: UserService.CreateSubAccount OWNER/ADMIN可为同企业创建ADMIN/MEMBER子账号
    - Test 8: UserService.CreateSubAccount MEMBER不能创建子账号
    - Test 9: Handler.SendCode POST /api/v1/auth/send-code 返回200
    - Test 10: Handler.Login POST /api/v1/auth/login 返回200+Token
    - Test 11: Handler.CreateSubAccount POST /api/v1/users 需要 OWNER/ADMIN 角色
  </behavior>
  <action>
    1. 创建 internal/user/service.go:
       - UserService struct: 包含 userRepo, orgRepo, rdb(redis.Client), smsSvc(sms.SMSService), aesKey []byte
       - `func (s *UserService) SendVerifyCode(phone string) error` (per D-05, D-07):
         - 检查限流: Redis GET `sms:rate:{phone}` 是否存在 (60秒限制)
         - 生成6位验证码: sms.GenerateCode()
         - 存储到Redis: SET `sms:code:{phone}` TTL=5分钟
         - 设置限流: SET `sms:rate:{phone}` TTL=60秒
         - 调用 smsSvc.SendVerifyCode(phone, code)
         - 错误计数: `sms:err:{phone}` 验证失败5次删除验证码需重新发送 (per Pitfall 2)
       - `func (s *UserService) Login(phone, code string) (*LoginResponse, error)` (per D-05, D-08):
         - 从Redis获取 `sms:code:{phone}` 验证码
         - 验证码匹配检查，失败则递增错误计数
         - 通过 phoneHash 查找用户:
           - 存在: 更新 LastLoginAt/LastLoginIP，检查企业 onboarding_done
           - 不存在(首次登录自动注册 per D-08): 创建 Organization(name=临时名称,onboarding_done=false) + User(role=OWNER)，加密手机号
         - 生成 JWT Access Token (7天) + Refresh Token (30天)
         - 返回 LoginResponse{AccessToken, RefreshToken, Onboarding, UserDTO}
       - `func (s *UserService) CompleteOnboarding(orgID int64, req CompleteOnboardingRequest) error`:
         - 更新 Organization: Name, CreditCode, City, Province, District, ContactName, ContactPhone(加密), OnboardingDone=true
       - `func (s *UserService) CreateSubAccount(orgID int64, operatorRole string, req CreateSubAccountRequest) (*UserDTO, error)` (per D-12, D-14):
         - 校验 operatorRole 为 OWNER 或 ADMIN
         - 检查手机号是否已存在（同企业内不重复）
         - 创建 User: OrgID=orgID, Role=req.Role(ADMIN或MEMBER, 不允许创建OWNER), 手机号加密
       - `func (s *UserService) ListUsers(orgID int64, page, pageSize int) ([]UserDTO, int64, error)`:
         - 分页查询同企业用户，手机号脱敏
       - `func (s *UserService) UpdateUserRole(orgID, operatorID, targetUserID int64, operatorRole, newRole string) error`:
         - 校验操作者权限 (OWNER/ADMIN)
         - OWNER角色不可被修改 (per D-14)
         - 不能修改自己的角色

    2. 创建 internal/user/handler.go:
       - UserHandler struct: 包含 userService *UserService
       - `func (h *UserHandler) SendCode(c *gin.Context)`: POST /api/v1/auth/send-code
         - 绑定 SendCodeRequest, validator 校验
         - 调用 userService.SendVerifyCode(req.Phone)
       - `func (h *UserHandler) Login(c *gin.Context)`: POST /api/v1/auth/login
         - 绑定 LoginRequest, 校验
         - 调用 userService.Login(req.Phone, req.Code)
         - 返回 response.Success(c, loginResp)
       - `func (h *UserHandler) CompleteOnboarding(c *gin.Context)`: POST /api/v1/org/onboarding
         - 需要Auth中间件
         - 从Context获取 orgID
         - 绑定 CompleteOnboardingRequest, 校验
       - `func (h *UserHandler) CreateSubAccount(c *gin.Context)`: POST /api/v1/users
         - 需要Auth + RequireRole("OWNER","ADMIN") 中间件
         - 从Context获取 orgID, role
       - `func (h *UserHandler) ListUsers(c *gin.Context)`: GET /api/v1/users?page=1&page_size=20
         - 需要Auth中间件
       - `func (h *UserHandler) UpdateUserRole(c *gin.Context)`: PUT /api/v1/users/:id/role
         - 需要Auth + RequireRole("OWNER","ADMIN") 中间件

    3. 创建 cmd/server/routes.go:
       - `func RegisterRoutes(r *gin.Engine, authMW gin.HandlerFunc, rbacMW func(...string) gin.HandlerFunc, userHandler *UserHandler)`:
         - 公开路由 (无需Auth):
           - POST /api/v1/auth/send-code -> userHandler.SendCode
           - POST /api/v1/auth/login -> userHandler.Login
           - GET /health -> 健康检查
         - 认证路由 (需要Auth):
           - POST /api/v1/org/onboarding -> userHandler.CompleteOnboarding
           - GET /api/v1/users -> userHandler.ListUsers
         - 权限路由 (需要Auth + RBAC):
           - POST /api/v1/users -> RequireRole("OWNER","ADMIN") -> userHandler.CreateSubAccount
           - PUT /api/v1/users/:id/role -> RequireRole("OWNER","ADMIN") -> userHandler.UpdateUserRole

    4. 更新 cmd/server/main.go:
       - 添加: 初始化Redis客户端 -> 初始化SMS/OSS客户端 -> 创建Repository/Service/Handler -> 调用RegisterRoutes
       - 添加: 调用 RegisterAuditCallbacks(db) 注册审计Hook

    5. 编写测试:
       - internal/user/service_test.go: mock Repository + Redis + SMS, 测试上述11个behavior用例
       - internal/user/handler_test.go: 使用 httptest.NewRecorder 测试HTTP接口
  </action>
  <verify>
    <automated>cd /Users/wencai/github/EasyHR && go test ./internal/user/... -v -short</automated>
  </verify>
  <done>
    - 完整认证流程: 发送验证码 -> 登录(自动注册) -> 企业引导 -> 获取Token
    - 新手机号登录自动创建User+Organization, onboarding_done=false (per D-08)
    - 已有手机号登录返回已有数据, onboarding_done=true/false 取决于企业状态
    - 验证码60秒限流 + 5次错误锁定 (per D-05, D-07)
    - OWNER/ADMIN可创建ADMIN/MEMBER子账号 (per D-12)
    - OWNER角色不可被删除或修改 (per D-14)
    - 路由注册: /api/v1/auth/* 公开, /api/v1/users/* 需要Auth, 部分需要RBAC (per D-21)
    - `go test ./internal/user/... -v -short` 全部通过
  </done>
</task>

</tasks>

<verification>
1. `go test ./internal/... -v -short` 全部通过
2. `go test ./internal/common/middleware/... -v` Auth/RBAC/Audit中间件测试通过
3. `go build ./cmd/server/` 编译无错误
4. 启动服务后 POST /api/v1/auth/send-code 可发送验证码
5. POST /api/v1/auth/login 可登录并返回JWT Token
6. 带Token访问 GET /api/v1/users 返回用户列表
7. 无Token访问 GET /api/v1/users 返回401
8. MEMBER角色POST /api/v1/users 返回403
</verification>

<success_criteria>
- 完整认证流程: 手机号+验证码 -> 自动注册 -> 企业引导 -> JWT Token
- JWT包含 user_id/org_id/role，Token黑名单机制有效
- 三级RBAC (OWNER/ADMIN/MEMBER) 中间件正确拦截
- GORM审计Hook自动记录所有写操作
- 子账号管理: OWNER/ADMIN可创建，MEMBER不可
- 所有API路由注册完成，公开/认证/权限三层正确
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-03-SUMMARY.md`
</output>
