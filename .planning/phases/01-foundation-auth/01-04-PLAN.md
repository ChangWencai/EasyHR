---
phase: 01-foundation-auth
plan: 04
type: execute
wave: 3
depends_on:
  - 01-03
files_modified:
  - internal/user/handler.go
  - internal/user/service.go
  - internal/common/middleware/auth.go
  - cmd/server/routes.go
  - test/testutil/db.go
  - test/testutil/redis.go
  - test/integration/auth_test.go
  - test/integration/tenant_isolation_test.go
  - test/integration/audit_test.go
autonomous: true
requirements:
  - AUTH-04
  - PLAT-06

must_haves:
  truths:
    - "完整认证流程端到端测试: 发送验证码->登录->企业引导->访问受保护API，全部通过"
    - "多租户隔离: 企业A的用户看不到企业B的任何数据（用户列表/企业信息）"
    - "审计日志: 登录/创建子账号/更新角色操作均产生审计记录"
    - "JWT Token刷新: Refresh Token可换取新Access Token"
    - "Token黑名单: 退出登录后Access Token立即失效"
  artifacts:
    - path: "test/integration/auth_test.go"
      provides: "端到端认证流程集成测试"
      contains: "TestLogin_FullFlow"
    - path: "test/integration/tenant_isolation_test.go"
      provides: "多租户数据隔离集成测试"
      contains: "TestTenantIsolation"
    - path: "test/integration/audit_test.go"
      provides: "审计日志集成测试"
      contains: "TestAuditLog"
    - path: "test/testutil/db.go"
      provides: "测试数据库初始化fixture"
      exports: ["SetupTestDB", "CleanupTestDB"]
  key_links:
    - from: "test/integration/tenant_isolation_test.go"
      to: "internal/user/repository.go"
      via: "创建两个企业用户验证数据隔离"
      pattern: "TenantIsolation"
    - from: "test/integration/audit_test.go"
      to: "internal/common/middleware/audit.go"
      via: "验证GORM写操作产生审计日志"
      pattern: "audit_logs"
---

<objective>
编写端到端集成测试验证认证流程、多租户隔离和审计日志，同时补充Token刷新和退出登录（Token黑名单）API。

Purpose: D-11明确要求集成测试必须包含多租户隔离验证。本Plan通过集成测试验证Phase 1所有核心功能的正确性，并补充Plan 03未覆盖的Token刷新和退出登录API。

Output: 完整的集成测试套件，Phase 1所有需求有测试覆盖。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-auth/1-CONTEXT.md
@.planning/phases/01-foundation-auth/1-RESEARCH.md
@.planning/phases/01-foundation-auth/01-01-SUMMARY.md
@.planning/phases/01-foundation-auth/01-02-SUMMARY.md
@.planning/phases/01-foundation-auth/01-03-SUMMARY.md
</context>

<interfaces>
<!-- Key types from prior plans needed for integration testing -->

From internal/user/model.go (Plan 03):
```go
type User struct {
    ID int64; Phone string; PhoneHash string; Nickname string
    OrgID int64; Role string; Status string
    LastLoginAt *time.Time; LastLoginIP string
    CreatedBy int64; CreatedAt time.Time; UpdatedBy int64; UpdatedAt time.Time
    DeletedAt gorm.DeletedAt
}
type Organization struct {
    ID int64; Name string; CreditCode string; City string; Province string
    OnboardingDone bool; Status string
}
type AuditLog struct {
    ID int64; OrgID int64; UserID int64; Module string; Action string
    TargetType string; TargetID int64; Detail datatypes.JSON
    IPAddress string; CreatedAt time.Time
}
```

From internal/user/handler.go (Plan 03):
```go
type UserHandler struct { ... }
func (h *UserHandler) SendCode(c *gin.Context)
func (h *UserHandler) Login(c *gin.Context)
func (h *UserHandler) CompleteOnboarding(c *gin.Context)
func (h *UserHandler) CreateSubAccount(c *gin.Context)
func (h *UserHandler) ListUsers(c *gin.Context)
```

From pkg/jwt/jwt.go (Plan 02):
```go
func GenerateRefreshToken(userID, orgID int64, role, secret string, ttl time.Duration) (string, error)
func RefreshAccessToken(refreshTokenStr, secret string, accessTTL time.Duration) (string, error)
```

From internal/common/database/database.go (Plan 01):
```go
func Init(cfg *config.DatabaseConfig, logger *zap.Logger) (*gorm.DB, error)
func TenantScope(orgID int64) func(db *gorm.DB) *gorm.DB
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 测试工具 + Token刷新/退出API + 端到端认证集成测试</name>
  <files>test/testutil/db.go, test/testutil/redis.go, internal/user/handler.go, internal/user/service.go, internal/common/middleware/auth.go, cmd/server/routes.go, test/integration/auth_test.go</files>
  <read_first>internal/user/handler.go (现有Handler，需添加RefreshToken/Logout方法)
internal/user/service.go (现有Service，需添加RefreshToken/Logout方法)
internal/common/middleware/auth.go (现有Auth中间件)
.planning/phases/01-foundation-auth/1-CONTEXT.md (D-06 JWT刷新, D-11 集成测试)</read_first>
  <acceptance_criteria>
    - `test/testutil/db.go` contains `func SetupTestDB(t *testing.T) *gorm.DB`
    - `test/testutil/db.go` contains `func CleanupTestDB(t *testing.T, db *gorm.DB)`
    - `test/testutil/redis.go` contains `func SetupTestRedis(t *testing.T) *redis.Client`
    - `test/testutil/redis.go` contains `func CleanupTestRedis(t *testing.T, rdb *redis.Client)`
    - `internal/user/service.go` contains `func (s *UserService) RefreshToken(refreshTokenStr, jwtSecret string, accessTTL time.Duration) (*LoginResponse, error)`
    - `internal/user/service.go` contains `func (s *UserService) Logout(tokenJTI string, ttl time.Duration) error`
    - `internal/user/handler.go` contains `func (h *UserHandler) RefreshToken(c *gin.Context)`
    - `internal/user/handler.go` contains `func (h *UserHandler) Logout(c *gin.Context)`
    - `cmd/server/routes.go` contains POST `/api/v1/auth/refresh`
    - `cmd/server/routes.go` contains POST `/api/v1/auth/logout`
    - `test/integration/auth_test.go` contains `func TestLogin_FullFlow`
    - `go test ./test/integration/... -run TestLogin -v -count=1` exits 0
  </acceptance_criteria>
  <behavior>
    - Test 1: RefreshToken API: 用有效Refresh Token换取新Access Token
    - Test 2: RefreshToken API: 用过期Refresh Token返回401
    - Test 3: Logout API: 将Token jti加入Redis黑名单，后续请求返回401
    - Test 4: 端到端流程: 发送验证码 -> 登录 -> 获取Token -> 访问受保护API -> 成功
    - Test 5: 端到端流程: 首次登录 -> onboarding=true -> 完成企业引导 -> onboarding=false
    - Test 6: 端到端流程: 退出登录 -> 用原Token访问 -> 返回401
    - Test 7: 端到端流程: 用Refresh Token刷新 -> 获取新Access Token -> 新Token可用
  </behavior>
  <action>
    1. 创建 test/testutil/db.go:
       - `func SetupTestDB(t *testing.T) *gorm.DB`: 连接测试数据库 easyhr_test, AutoMigrate所有模型, 返回*gorm.DB
       - `func CleanupTestDB(t *testing.T, db *gorm.DB)`: 清空所有表数据 (DELETE FROM)
       - 测试数据库配置: 从环境变量 TEST_DB_HOST/PORT/USER/PASSWORD 读取，默认 localhost

    2. 创建 test/testutil/redis.go:
       - `func SetupTestRedis(t *testing.T) *redis.Client`: 连接测试Redis (db=15 避免冲突)
       - `func CleanupTestRedis(t *testing.T, rdb *redis.Client)`: FLUSHDB

    3. 补充 internal/user/service.go 方法:
       - `func (s *UserService) RefreshToken(refreshTokenStr, jwtSecret string, accessTTL time.Duration) (*LoginResponse, error)`:
         - 调用 jwt.RefreshAccessToken 验证Refresh Token
         - 从Claims中获取 userID, orgID, role
         - 查找用户确认仍有效（status=ACTIVE）
         - 生成新Access Token返回
       - `func (s *UserService) Logout(tokenJTI string, ttl time.Duration) error`:
         - 将 token jti 加入Redis黑名单: SET `token:blacklist:{jti}` TTL=剩余有效期

    4. 补充 internal/user/handler.go 方法:
       - `func (h *UserHandler) RefreshToken(c *gin.Context)`: POST /api/v1/auth/refresh
         - 绑定 {refresh_token: string}
         - 调用 userService.RefreshToken
       - `func (h *UserHandler) Logout(c *gin.Context)`: POST /api/v1/auth/logout
         - 需要Auth中间件
         - 从Context获取Token的jti（Auth中间件需同时设置jti到Context）
         - 调用 userService.Logout(jti, ttl)

    5. 更新 internal/common/middleware/auth.go:
       - Auth中间件增加: c.Set("jti", claims.ID) 将JWT ID注入Context

    6. 更新 cmd/server/routes.go:
       - 公开路由添加: POST /api/v1/auth/refresh -> userHandler.RefreshToken
       - 认证路由添加: POST /api/v1/auth/logout -> userHandler.Logout

    7. 创建 test/integration/auth_test.go:
       - TestLogin_FullFlow: 完整端到端认证流程测试
         1. Mock SMS发送验证码 (Redis中直接SET验证码)
         2. POST /api/v1/auth/login -> 验证返回Token + onboarding=true
         3. POST /api/v1/org/onboarding -> 填写企业信息
         4. GET /api/v1/users -> 用Token访问受保护API，验证成功
         5. POST /api/v1/auth/logout -> 退出登录
         6. GET /api/v1/users -> 用原Token访问，验证返回401
         7. POST /api/v1/auth/refresh -> 用Refresh Token获取新Access Token
         8. GET /api/v1/users -> 用新Token访问，验证成功
       - 使用 httptest.NewServer + gin.Engine 创建测试服务器
       - 使用 testutil.SetupTestDB / SetupTestRedis 初始化测试环境
  </action>
  <verify>
    <automated>cd /Users/wencai/github/EasyHR && go test ./test/integration/... -run TestLogin -v -count=1</automated>
  </verify>
  <done>
    - Refresh Token API 可换取新 Access Token
    - Logout API 将 Token 加入黑名单，后续请求返回401
    - 端到端测试覆盖: 登录->企业引导->受保护API->退出->刷新 全流程
    - 测试工具 SetupTestDB/SetupTestRedis 可复用
    - `go test ./test/integration/... -v` 全部通过
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 多租户隔离集成测试 + 审计日志集成测试</name>
  <files>test/integration/tenant_isolation_test.go, test/integration/audit_test.go</files>
  <read_first>internal/user/repository.go (TenantScope使用方式)
internal/common/middleware/audit.go (审计Hook注册和字段)
.planning/phases/01-foundation-auth/1-CONTEXT.md (D-09~D-11 多租户, D-22~D-23 审计日志)</read_first>
  <acceptance_criteria>
    - `test/integration/tenant_isolation_test.go` contains `func TestTenantIsolation_UserDataIsolation`
    - `test/integration/tenant_isolation_test.go` contains `func TestTenantIsolation_NoClientOrgIDTrust`
    - `test/integration/tenant_isolation_test.go` contains `func TestTenantIsolation_CrossOrgRoleUpdate`
    - `test/integration/audit_test.go` contains `func TestAuditLog_CreateUser`
    - `test/integration/audit_test.go` contains `func TestAuditLog_UpdateUserRole`
    - `test/integration/audit_test.go` contains `func TestAuditLog_InsertOnly`
    - `go test ./test/integration/... -run "TestTenantIsolation|TestAuditLog" -v -count=1` exits 0
  </acceptance_criteria>
  <behavior>
    - Test 1: 创建企业A和企业B的用户，企业A查询用户列表不包含企业B用户
    - Test 2: 企业A用户尝试用企业B的org_id查询，返回空结果（Repository层不信任客户端org_id）
    - Test 3: 企业A管理员不能修改企业B用户的角色
    - Test 4: 创建用户操作产生审计日志，包含正确的org_id/user_id/module/action/target
    - Test 5: 更新用户角色操作产生审计日志，detail包含变更前后信息
    - Test 6: 审计日志为INSERT ONLY，尝试删除审计日志被阻止
  </behavior>
  <action>
    1. 创建 test/integration/tenant_isolation_test.go (per D-11):
       - TestTenantIsolation_UserDataIsolation:
         1. 创建企业A + 用户A(role=OWNER)
         2. 创建企业B + 用户B(role=OWNER)
         3. 企业A管理员创建子账号A2(role=MEMBER)
         4. 企业B管理员创建子账号B2(role=MEMBER)
         5. 用企业A的org_id查询用户列表 -> 只能看到A和A2，看不到B和B2
         6. 用企业B的org_id查询用户列表 -> 只能看到B和B2，看不到A和A2
       - TestTenantIsolation_NoClientOrgIDTrust:
         1. 使用企业A的Token但请求参数传企业B的org_id
         2. Repository层从JWT Context获取org_id，忽略请求参数中的org_id
         3. 返回结果只包含企业A的数据
       - TestTenantIsolation_CrossOrgRoleUpdate:
         1. 企业A的OWNER尝试更新企业B用户的角色
         2. Repository使用TenantScope，找不到跨企业用户
         3. 返回"用户不存在"错误

    2. 创建 test/integration/audit_test.go (per D-22, D-23):
       - TestAuditLog_CreateUser:
         1. 创建一个用户
         2. 查询audit_logs表，验证存在 module="users", action="CREATE", target_type="users"
         3. 验证 org_id, user_id, ip_address 字段正确
         4. 验证 detail(JSONB) 包含新用户信息
       - TestAuditLog_UpdateUserRole:
         1. 创建用户并更新角色
         2. 查询audit_logs表，验证 action="UPDATE"
         3. 验证 detail 包含变更前后信息（old_role, new_role）
       - TestAuditLog_InsertOnly:
         1. 尝试 DELETE FROM audit_logs
         2. 验证GORM不提供审计日志删除操作（不注册Delete回调给AuditLog模型）
         3. 或者验证Service层无DeleteAuditLog方法
  </action>
  <verify>
    <automated>cd /Users/wencai/github/EasyHR && go test ./test/integration/... -run "TestTenantIsolation|TestAuditLog" -v -count=1</automated>
  </verify>
  <done>
    - 多租户隔离: 两个企业数据完全隔离，互不可见
    - 客户端传入org_id被忽略，仅从JWT提取 (per D-10)
    - 跨企业操作被正确拒绝
    - 审计日志自动记录Create/Update操作，包含所有D-22要求字段
    - 审计日志为INSERT ONLY，无删除接口 (per D-23)
    - `go test ./test/integration/... -v` 全部通过
  </done>
</task>

</tasks>

<verification>
1. `go test ./test/integration/... -v` 全部集成测试通过
2. `go test ./... -v -cover` 全套测试通过，覆盖率报告生成
3. 端到端认证流程: 发送验证码->登录->企业引导->受保护API->退出->刷新 全部通过
4. 多租户隔离: 企业A和企业B数据互不可见
5. 审计日志: 所有写操作自动记录且不可删除
6. `go build ./cmd/server/` 编译无错误
</verification>

<success_criteria>
- 端到端认证集成测试全部通过（登录/注册/引导/退出/刷新）
- 多租户隔离集成测试通过（双企业数据隔离验证 per D-11）
- 审计日志集成测试通过（自动记录+INSERT ONLY per D-22/D-23）
- Token刷新和黑名单机制有测试覆盖
- Phase 1所有需求(AUTH-01~04, PLAT-01~07)有对应测试
- `go test ./... -cover` 通过
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-04-SUMMARY.md`
</output>
