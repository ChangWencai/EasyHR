---
phase: 01-foundation-auth
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - test/testutil/db.go
  - test/testutil/redis.go
  - test/testutil/fixture.go
  - test/integration/auth_test.go
  - test/integration/tenant_isolation_test.go
  - test/integration/audit_test.go
  - test/integration/rbac_test.go
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-04
  - PLAT-01
  - PLAT-02
  - PLAT-06

must_haves:
  truths:
    - "完整认证流程集成测试通过：发送验证码 -> 登录 -> 企业引导 -> Token刷新 -> 退出"
    - "两个不同企业的用户数据完全隔离：OrgA 的用户看不到 OrgB 的用户列表"
    - "审计日志记录完整的操作链：谁在什么时候创建/修改/删除了什么"
    - "RBAC 角色隔离：MEMBER 无法创建子账号，ADMIN 无法管理子账号（仅可查看列表）per D-30"
    - "OWNER 角色无法被删除或降级"
    - "城市列表 API 集成测试通过：返回包含 id/name/province 的城市数据"
  artifacts:
    - path: "test/testutil/db.go"
      provides: "测试数据库初始化（PostgreSQL 或 SQLite）"
      exports: ["SetupTestDB", "CleanupTestDB"]
    - path: "test/testutil/redis.go"
      provides: "测试 Redis 初始化（miniredis）"
      exports: ["SetupTestRedis"]
    - path: "test/testutil/fixture.go"
      provides: "测试数据工厂方法"
      exports: ["CreateTestOrg", "CreateTestUser"]
    - path: "test/integration/auth_test.go"
      provides: "认证流程端到端集成测试"
      min_lines: 50
    - path: "test/integration/tenant_isolation_test.go"
      provides: "多租户隔离集成测试"
      min_lines: 40
    - path: "test/integration/audit_test.go"
      provides: "审计日志集成测试"
      min_lines: 30
    - path: "test/integration/rbac_test.go"
      provides: "RBAC 角色权限集成测试"
      min_lines: 30
  key_links:
    - from: "test/integration/auth_test.go"
      to: "internal/user/handler.go"
      via: "HTTP 请求模拟完整认证流程"
      pattern: "httptest\\.NewRecorder"
    - from: "test/integration/tenant_isolation_test.go"
      to: "internal/user/repository.go"
      via: "验证 TenantScope 隔离"
      pattern: "TenantScope"
    - from: "test/integration/audit_test.go"
      to: "internal/audit/repository.go"
      via: "验证审计日志记录完整性"
      pattern: "auditRepo\\.List"
    - from: "test/integration/rbac_test.go"
      to: "internal/common/middleware/rbac.go"
      via: "验证角色权限边界"
      pattern: "RequireRole"
---

<objective>
编写端到端集成测试，验证认证流程、多租户数据隔离、RBAC 权限边界和审计日志完整性。使用真实的数据库连接（SQLite 内存模式或 PostgreSQL 测试库）和 Redis（miniredis），确保所有组件协同工作。

Purpose: Phase 1 的最终验证层。单元测试验证单个函数，集成测试验证组件间协作。多租户隔离是系统安全的核心保证，必须通过集成测试验证 per D-11。

Output: 完整的集成测试套件，覆盖认证流程、多租户隔离、RBAC、审计日志四大场景，`go test ./test/integration/... -v` 全部通过。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-auth/1-CONTEXT.md
@.planning/phases/01-foundation-auth/1-RESEARCH.md
@.planning/phases/01-foundation-auth/01-01-SUMMARY.md
@.planning/phases/01-foundation-auth/01-02-SUMMARY.md
@.planning/phases/01-foundation-auth/01-03-SUMMARY.md
</context>

<interfaces>
<!-- Plan 01-03 产出的关键接口 -->

From internal/user/handler.go:
```go
type Handler struct { /* ... */ }
func NewHandler(svc Service) *Handler
func (h *Handler) RegisterRoutes(rg *gin.RouterGroup, authMiddleware, rbacMiddleware gin.HandlerFunc)
```

From internal/user/service.go:
```go
type Service interface {
    SendCode(ctx context.Context, phone string) error
    Login(ctx context.Context, phone, code string) (*LoginResponse, error)
    RefreshToken(ctx context.Context, refreshToken string) (*jwt.TokenPair, error)
    Logout(ctx context.Context, accessToken, refreshToken string) error
    CompleteOnboarding(ctx context.Context, orgID int64, req *CompleteOnboardingRequest) error
    CreateSubAccount(ctx context.Context, orgID, creatorID int64, req *CreateSubAccountRequest) error
    ListSubAccounts(ctx context.Context, orgID int64, page, pageSize int) ([]UserInfoResponse, int64, error)
    UpdateSubAccountRole(ctx context.Context, orgID, targetUserID int64, role string) error
    DeleteSubAccount(ctx context.Context, orgID, targetUserID int64) error
}
```

From internal/user/repository.go:
```go
type Repository interface {
    FindByPhoneHash(phoneHash string) (*model.User, error)
    CreateUser(user *model.User) error
    CreateOrg(org *model.Organization) error
    ListUsers(orgID int64, page, pageSize int) ([]model.User, int64, error)
    // ...
}
```

From internal/city/handler.go:
```go
type Handler struct { /* ... */ }
func NewHandler() *Handler
func (h *Handler) RegisterRoutes(rg *gin.RouterGroup)
```

From internal/audit/repository.go:
```go
type Repository interface {
    List(orgID int64, module, action string, page, pageSize int) ([]AuditLog, int64, error)
}
```

From internal/common/middleware/rbac.go:
```go
func RequireRole(roles ...string) gin.HandlerFunc
```

From internal/common/middleware/auth.go:
```go
func Auth(jwtSecret string, rdb *redis.Client) gin.HandlerFunc
```

From pkg/jwt/jwt.go:
```go
func GenerateAccessToken(userID, orgID int64, role, secret string, ttl time.Duration) (string, error)
func GenerateRefreshToken(userID int64, secret string, ttl time.Duration) (string, error)
func ParseToken(tokenStr, secret string) (*Claims, error)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 测试基础设施 + 认证流程集成测试</name>
  <files>
    test/testutil/db.go,
    test/testutil/redis.go,
    test/testutil/fixture.go,
    test/integration/auth_test.go
  </files>
  <read_first>
    .planning/phases/01-foundation-auth/1-CONTEXT.md (D-05 登录, D-06 JWT, D-07 验证码Redis, D-08 首次引导, D-11 集成测试要求, D-28 refresh轮转, D-29 退出)
    .planning/phases/01-foundation-auth/1-RESEARCH.md (Validation Architecture: 测试框架)
  </read_first>
  <action>
    1. 创建 `test/testutil/db.go`：
       - 使用 GORM + SQLite 内存模式（`gorm.io/driver/sqlite`）创建测试数据库
       - `SetupTestDB(t *testing.T) *gorm.DB`：创建内存 DB，运行 AutoMigrate（User, Organization, AuditLog 模型）
       - `CleanupTestDB(t *testing.T, db *gorm.DB)`
       - 注意：SQLite 不支持 JSONB，审计日志的 Detail 字段在 SQLite 中使用 TEXT 类型。可通过 GORM tag `type:json` + SQLite 忽略类型差异处理。如果 JSONB 兼容性困难，Detail 字段改用 `map[string]interface{}` + GORM 自动序列化。
       - 备选方案：如果使用 Docker 环境中有 PostgreSQL，优先使用 PostgreSQL 测试库（`SetupTestDB` 检查 `TEST_DATABASE_URL` 环境变量，有则用 PostgreSQL，无则用 SQLite）

    2. 创建 `test/testutil/redis.go`：
       - 使用 `github.com/alicebob/miniredis/v2` 创建内存 Redis
       - `SetupTestRedis(t *testing.T) (*miniredis.Miniredis, *redis.Client)`
       - Cleanup 函数关闭 miniredis

    3. 创建 `test/testutil/fixture.go`：
       - `CreateTestOrg(db *gorm.DB, name string) *model.Organization`：创建测试企业
       - `CreateTestUser(db *gorm.DB, orgID int64, phone, name, role string) *model.User`：创建测试用户（手机号加密+哈希）
       - `CreateAuthenticatedRouter(db *gorm.DB, rdb *redis.Client, jwtSecret string) *gin.Engine`：创建包含所有中间件和路由的测试 Gin Engine

    4. 创建 `test/integration/auth_test.go`：
       测试场景：
       - `TestAuth_FullFlow`：完整认证流程
         1. SendCode("13800138000") -> 成功
         2. SendCode("13800138000") 立即再次发送 -> 限流错误
         3. 从 Redis 获取验证码，Login("13800138000", code) -> 返回 token + onboarding_required=true
         4. CompleteOnboarding(orgID, {name, credit_code, city, ...}) -> 成功
         5. 用 access token 调用 GET /api/v1/users -> 成功（看到自己）
         6. RefreshToken(refresh_token) -> 返回新 token pair
         7. 用旧 refresh token 再次刷新 -> 失败（已黑名单）
         8. Logout(access_token, refresh_token) -> 成功
         9. 用已退出的 access token 调用 GET /api/v1/users -> 401

       - `TestAuth_LoginWithWrongCode`：验证码错误测试
         1. SendCode -> 成功
         2. Login("13800138000", "000000") x 5 -> 每次返回错误
         3. 第6次即使验证码正确也失败（key 已被删除）

       - `TestAuth_ExistingUserLogin`：已有用户登录
         1. 创建测试用户（直接 DB 插入）
         2. SendCode -> Login -> onboarding_required=false

       - `TestAuth_CityListAPI`：城市列表 API 集成测试 per AUTH-03
         1. GET /api/v1/cities（无需认证）-> 200，返回非空城市列表
         2. 验证每个城市包含 id, name, province 字段
         3. GET /api/v1/cities?province=广东省 -> 仅返回广东省的城市

    5. 添加 go.mod 依赖：`gorm.io/driver/sqlite`、`github.com/alicebob/miniredis/v2`
  </action>
  <verify>
    <automated>cd /Users/wencai/github/EasyHR && go test ./test/integration/... -run TestAuth -v</automated>
  </verify>
  <done>
    - 完整认证流程集成测试通过：SendCode -> Login -> Onboarding -> Refresh -> Logout
    - 验证码限流（60秒间隔）测试通过
    - 验证码错误5次锁定测试通过
    - Token 黑名单测试通过
    - 城市列表 API 集成测试通过 per AUTH-03
    - `go test ./test/integration/... -run TestAuth -v` 全部通过
  </done>
</task>

<task type="auto">
  <name>Task 2: 多租户隔离 + RBAC + 审计日志集成测试</name>
  <files>
    test/integration/tenant_isolation_test.go,
    test/integration/audit_test.go,
    test/integration/rbac_test.go
  </files>
  <read_first>
    .planning/phases/01-foundation-auth/1-CONTEXT.md (D-09 多租户, D-10 中间件注入, D-11 集成测试必须验证, D-12 三级角色, D-14 OWNER不可删, D-22 审计日志, D-23 审计只增不改, D-30 权限规则)
    .planning/phases/01-foundation-auth/01-VALIDATION.md (验证策略)
  </read_first>
  <action>
    1. 创建 `test/integration/tenant_isolation_test.go` per D-11：
       测试场景：
       - `TestTenantIsolation_UserList`：
         1. 创建 OrgA（含 3 个用户）和 OrgB（含 2 个用户）
         2. 以 OrgA 的 OWNER 登录，调用 GET /api/v1/users -> 只看到 3 个 OrgA 用户
         3. 以 OrgB 的 OWNER 登录，调用 GET /api/v1/users -> 只看到 2 个 OrgB 用户
         4. OrgA 用户尝试用 OrgB 的 org_id 查询 -> 仍然只看到 OrgA 的数据（org_id 从 JWT 提取，不信任客户端）

       - `TestTenantIsolation_AuditLog`：
         1. OrgA 用户执行操作
         2. OrgB 用户查询审计日志 -> 只能看到 OrgB 的日志
         3. 验证审计日志按 org_id 完全隔离

       - `TestTenantIsolation_SubAccountCreation`：
         1. OrgA OWNER 创建子账号
         2. OrgB 的用户列表不受影响
         3. 新创建的用户 phone_hash 在 OrgB 中查询不到

    2. 创建 `test/integration/rbac_test.go` per D-12/D-13/D-14/D-30：
       测试场景：
       - `TestRBAC_OwnerFullAccess`：
         1. OWNER 可创建子账号 -> 201
         2. OWNER 可修改子账号角色 -> 200
         3. OWNER 可删除子账号（非 OWNER） -> 200
         4. OWNER 可查看用户列表 -> 200

       - `TestRBAC_AdminLimitedAccess` per D-30（ADMIN 不可管理子账号，仅可查看业务数据）：
         1. ADMIN 可查看用户列表 -> 200（per D-30: ADMIN 可增删改查业务数据，查看用户列表属于此范畴）
         2. ADMIN 创建子账号 -> 403（per D-30: ADMIN 不可管理子账号，POST /api/v1/users 仅 OWNER）
         3. ADMIN 修改角色（PUT /api/v1/users/:id/role） -> 403
         4. ADMIN 删除用户（DELETE /api/v1/users/:id） -> 403

       - `TestRBAC_MemberReadOnly`：
         1. MEMBER 可查看用户列表 -> 200（per D-30: MEMBER 只读）
         2. MEMBER 无法创建子账号 -> 403
         3. MEMBER 无法修改角色 -> 403
         4. MEMBER 无法删除用户 -> 403

       - `TestRBAC_OwnerCannotBeDeleted` per D-14：
         1. 尝试删除 OWNER 用户 -> 返回错误
         2. 尝试将 OWNER 角色改为 ADMIN -> 返回错误

    3. 创建 `test/integration/audit_test.go` per D-22/D-23：
       测试场景：
       - `TestAudit_CreateUserLogged`：
         1. OWNER 创建子账号
         2. 查询审计日志，验证有一条 action=create, target_type=users 的记录
         3. 验证记录包含正确的 org_id, user_id, target_id

       - `TestAudit_UpdateRoleLogged`：
         1. OWNER 修改用户角色
         2. 查询审计日志，验证 action=update 记录
         3. 验证 detail JSONB 包含 old_role 和 new_role

       - `TestAudit_DeleteUserLogged`：
         1. OWNER 删除用户
         2. 查询审计日志，验证 action=delete 记录

       - `TestAudit_ReadOnly` per D-23：
         1. 验证审计日志接口只有 GET，没有 PUT/DELETE
         2. 直接尝试 DELETE /api/v1/audit-logs/:id -> 404 或 405

       - `TestAudit_FilterByModule`：
         1. 执行多个模块的操作
         2. 按 module 筛选审计日志 -> 只返回对应模块的记录

    4. 更新 go.mod 添加测试依赖（如果尚未添加）。
  </action>
  <verify>
    <automated>cd /Users/wencai/github/EasyHR && go test ./test/integration/... -v</automated>
  </verify>
  <done>
    - 多租户隔离集成测试通过：OrgA 数据对 OrgB 不可见 per D-11
    - RBAC 集成测试通过：OWNER 全权限，ADMIN 仅查看用户列表不可管理子账号，MEMBER 只读 per D-12/D-30
    - OWNER 不可被删除或降级 per D-14
    - 审计日志记录所有写操作 per D-22
    - 审计日志不可删除/修改 per D-23
    - `go test ./test/integration/... -v` 全部通过
    - `go test ./... -v` 全部通过（包括所有单元测试和集成测试）
  </done>
</task>

</tasks>

<verification>
go build ./cmd/server
go test ./... -v -cover
</verification>

<success_criteria>
1. 完整认证流程集成测试通过 per AUTH-01/AUTH-02/AUTH-04
2. 城市列表 API 集成测试通过 per AUTH-03
3. 多租户数据完全隔离：两个企业的用户互不可见 per D-11/PLAT-06
4. RBAC 三级角色权限边界正确 per PLAT-01/D-12/D-14/D-30（ADMIN 不可管理子账号）
5. 审计日志自动记录所有写操作 per PLAT-02/D-22
6. 审计日志只增不改 per D-23
7. `go test ./... -v` 全部通过，覆盖 test/integration 包
8. 所有 Phase 1 需求（AUTH-01~04, PLAT-01~07）在前4个计划中得到完整覆盖
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-04-SUMMARY.md`
</output>
