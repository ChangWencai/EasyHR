# Phase 11: 合同合规 — Plan 01
## 后端签署验证与中文PDF

---
phase: 11
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/employee/pdf.go
  - internal/employee/contract_model.go
  - internal/employee/contract_service.go
  - internal/employee/contract_handler.go
  - internal/employee/contract_dto.go
  - internal/employee/service.go
  - internal/todo/scheduler.go
  - assets/fonts/NotoSansSC-Regular.ttf
  - assets/fonts/NotoSansSC-Bold.ttf
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - COMP-03
  - COMP-04
user_setup:
  - service: aliyun_sms
    why: "签署链接短信发送"
    env_vars:
      - name: ALIYUN_SMS_CONTRACT_TEMPLATE_CODE
        source: "阿里云短信服务控制台 -> 模板管理 -> 创建签署短信模板（内含签署链接占位符）"
    dashboard_config:
      - task: "创建签署短信模板"
        location: "阿里云短信服务 -> 模板管理 -> 添加模板"
        details: "模板内容需包含签署链接参数，审批通过后获得 TemplateCode"

must_haves:
  truths:
    - "老板可以创建3种类型的合同（劳动合同/实习协议/兼职合同）"
    - "生成的PDF使用中文字体（思源黑体），内容为全中文"
    - "合同状态在发起签署后变为pending_sign"
    - "员工通过手机号+6位验证码完成签署，无需注册"
    - "签署完成后合同状态变为signed/active"
    - "合同发起3天后员工未签，系统通过待办中心提醒老板"
  artifacts:
    - path: "internal/employee/pdf.go"
      provides: "中文劳动合同PDF生成"
      contains: "AddUTF8Font.*NotoSansSC"
    - path: "internal/employee/contract_model.go"
      provides: "签署验证码模型"
      contains: "SignCode"
    - path: "internal/employee/contract_handler.go"
      provides: "签署相关HTTP端点"
      exports: ["POST /contracts/sign/send-code", "POST /contracts/sign/verify-code", "POST /contracts/sign/confirm", "GET /contracts/:id/signed-pdf"]
    - path: "internal/todo/scheduler.go"
      provides: "3天未签提醒定时任务"
      contains: "CheckPendingSignReminders"
  key_links:
    - from: "contract_service.go"
      to: "pkg/sms/client.go"
      via: "SMS.SendTemplateMessage with contract template"
      pattern: "SendTemplateMessage.*contract"
    - from: "contract_handler.go"
      to: "contract_service.go"
      via: "VerifySignCode / ConfirmSign"
      pattern: "func.*VerifySignCode|func.*ConfirmSign"
    - from: "scheduler.go"
      to: "contract_service.go"
      via: "CheckPendingSignReminders"
      pattern: "CheckPendingSignReminders"
    - from: "contract_service.go"
      to: "pkg/oss/client.go"
      via: "合同PDF上传到OSS"
      pattern: "GeneratePutURL.*contract"
---

<objective>
完成后端签署验证与中文PDF生成功能，包括：中文PDF模板重写（3种合同类型）、签署验证码生成/校验、合同状态机扩展（pending_sign→signed/active）、3天未签提醒定时任务。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@internal/employee/pdf.go
@internal/employee/contract_model.go
@internal/employee/contract_service.go
@internal/employee/contract_handler.go
@internal/employee/contract_dto.go
@internal/employee/service.go
@internal/todo/scheduler.go
@pkg/sms/client.go

# 现有签署流程（需扩展）
现有合同状态：draft → pending_sign → signed/active → terminated/expired
现有PDF生成：英文占位符（Labor Contract），使用Helvetica字体
现有签署方式：老板上传签署扫描件（UploadSigned接口）

# 新增签署流程
1. 老板发起签署 → GeneratePDF + 上传到OSS → 发送短信
2. 员工收到短信 → 打开H5页面 → 输入手机号+验证码 → 确认签署
3. 签署完成 → signed/active，PDF存档

# SMS模板
阿里云SMS需配置签署短信模板（含签署链接参数），TemplateCode通过环境变量注入

# 签署端点租户隔离策略（BLOCKER-4 fix）
签署端点（send-code / verify-code / confirm）无员工认证（员工未登录），但必须隔离租户：
- 使用 phone 作为访问键：先通过手机号找到员工，再通过员工找到合同
- SendSignCode: empRepo.FindByPhone(phone) → 验证合同属于该员工
- VerifySignCode: empRepo.FindByPhone(phone) → 确认员工确实拥有此合同（phone match）
- ConfirmSign: 通过 SignToken 验证（token 已在 VerifySignCode 阶段验证了 phone）

# TodoCreator 接口扩展（BLOCKER-2 fix）
必须在 internal/employee/service.go 的 TodoCreator 接口中添加：
  ExistsBySource(ctx context.Context, orgID int64, sourceType string, sourceID *int64) (bool, error)
</context>

<tasks>

<task type="auto">
  <name>Task 1: 下载中文字体文件到项目</name>
  <files>assets/fonts/NotoSansSC-Regular.ttf, assets/fonts/NotoSansSC-Bold.ttf</files>
  <action>
下载思源黑体（Noto Sans SC）字体到项目 assets/fonts/ 目录：

1. 创建目录：
   ```
   mkdir -p assets/fonts
   ```

2. 下载 Regular 和 Bold 字体（约 4-5MB/文件）：
   ```
   curl -L -o assets/fonts/NotoSansSC-Regular.ttf \
     "https://github.com/google/fonts/raw/main/ofl/notosanssc/NotoSansSC%5Bwght%5D.ttf" \
     || \
   curl -L -o assets/fonts/NotoSansSC-Regular.ttf \
     "https://github.com/notofonts/latin-greek-cyrillic/raw/main/pkg/NotoSansSC-Regular.ttf"
   ```
   如果 GitHub 不可用，使用 jsDelivr CDN：
   ```
   curl -L -o assets/fonts/NotoSansSC-Regular.ttf \
     "https://cdn.jsdelivr.net/npm/@fontsource/noto-sans-sc@5.0.3/files/noto-sans-sc-chinese-simplified-400-normal.woff2"
   ```
   然后转换 woff2 → ttf（如果下载的是 woff2 格式）：
   ```
   # 使用 Homebrew 安装 fonttools: brew install fonttools
   # 或下载 TTF 直链
   ```

   **备选方案**：如果网络下载困难，使用 Python 的 fonttools 从 woff2 转换：
   ```bash
   pip install fonttools brotli
   python3 -c "
   import sys
   from fontTools.ttLib import TTFont
   font = TTFont('NotoSansSC-Regular.woff2')
   font.flavor = None
   font.save('assets/fonts/NotoSansSC-Regular.ttf')
   "
   ```

   **最终备选**：将字体 base64 嵌入 Go 代码（作为 embedded asset）：
   ```go
   // 使用 go:embed 嵌入字体（无需文件系统）
   // go:embed assets/fonts/*.ttf
   // var fontFiles embed.FS
   ```

3. 验证字体文件存在且有效（跨平台验证，使用 Go build 证明 embed 指令可用）：
   ```
   cd backend && go build ./internal/employee/ 2>&1 && ls assets/fonts/*.ttf | awk '{print $5, $9}' | grep -E "\.ttf$"
   ```

字体来源：Google Fonts Noto Sans SC（开源 SIL 许可证），覆盖全部简体中文字符。
</action>
  <verify>
cd backend && go build ./internal/employee/ 2>&1 && ls assets/fonts/*.ttf | awk '{print $5, $9}' | grep -E "\.ttf$"
</verify>
  <done>字体文件 NotoSansSC-Regular.ttf 和 NotoSansSC-Bold.ttf 存在于 assets/fonts/，大小 > 1MB</done>
</task>

<task type="auto">
  <name>Task 2: 重写 pdf.go 实现中文PDF生成</name>
  <files>internal/employee/pdf.go</files>
  <action>
重写 `internal/employee/pdf.go`，替换英文占位符为中文内容 + 中文字体内嵌：

1. 在文件顶部添加 embed 指令：
   ```go
   //go:embed ../../assets/fonts/NotoSansSC-Regular.ttf
   //go:embed ../../assets/fonts/NotoSansSC-Bold.ttf
   var fontFiles embed.FS

   var _ = fontFiles // silence unused import
   ```

2. 修改 `GenerateContractPDF` 函数签名，接收合同类型参数区分模板：
   ```go
   func GenerateContractPDF(data *ContractPDFData) ([]byte, error)
   ```

3. 在函数内注册中文字体（使用 AddUTF8FontFromBytes，Go 1.16+ embed.FS）：
   ```go
   // 注册 Regular 字体
   reg := fpdf.GlobalReader()
   // 对于 go-pdf/fpdf v0.9.0，使用 AddUTF8FontFromFile
   pdf.AddUTF8FontFromFile("NotoSansSC", "", "assets/fonts/NotoSansSC-Regular.ttf")
   pdf.AddUTF8FontFromFile("NotoSansSC", "B", "assets/fonts/NotoSansSC-Bold.ttf")
   pdf.SetFont("NotoSansSC", "", 12)
   ```

   **注意**：如果 go-pdf/fpdf v0.9.0 不支持 AddUTF8FontFromFile，查阅 fpdf 文档使用正确方法：
   ```go
   // 正确方法（fpdf v0.9.0）
   err := pdf.AddUTF8FontFromBytes("NotoSansSC", "", regularFontBytes)
   if err != nil {
       return nil, fmt.Errorf("add regular font: %w", err)
   }
   err = pdf.AddUTF8FontFromBytes("NotoSansSC", "B", boldFontBytes)
   if err != nil {
       return nil, fmt.Errorf("add bold font: %w", err)
   }
   ```

4. 实现3个中文合同模板，根据 `data.ContractType` 选择：

   **劳动合同（固定期限）— fixed_term**:
   ```
   甲方（用人单位）：{OrgName}
   统一社会信用代码：{CreditCode}

   乙方（劳动者）：{EmployeeName}
   身份证号码：{IDCard}

   根据《中华人民共和国劳动合同法》及相关法律法规，甲乙双方遵循合法、公平、平等自愿、协商一致、诚实信用的原则，订立本合同。

   第一条 合同期限
   本合同期限自 {StartDate} 起至 {EndDate} 止。

   第二条 工作内容和工作地点
   乙方同意在 {City} 从事 {Position} 工作。

   第三条 工作时间和休息休假
   乙方工作时间按国家规定执行。

   第四条 劳动报酬
   乙方月工资标准为 {Salary} 元。

   第五条 社会保险
   甲方依法为乙方缴纳社会保险。

   第六条 合同生效
   本合同一式两份，甲乙双方各执一份，自双方签字（或盖章）之日起生效。

   甲方（盖章）：________  乙方（签字）：________
   签订日期：{SignDate}
   ```

   **实习协议 — intern**（简化版，1-2页）:
   ```
   甲方（用人单位）：{OrgName}
   乙方（实习生）：{EmployeeName}
   身份证号码：{IDCard}

   一、实习期限
   实习期自 {StartDate} 起至 {EndDate} 止。

   二、实习内容
   乙方在 {City} 从事 {Position} 岗位实习。

   三、实习补贴
   实习期间，乙方享有实习补贴 {Salary} 元/月。

   四、其他
   本协议一式两份，甲乙双方各执一份。

   甲方（盖章）：________  乙方（签字）：________
   签订日期：{SignDate}
   ```

   **兼职合同 — indefinite**（灵活期限）:
   ```
   甲方（用人单位）：{OrgName}
   乙方（劳动者）：{EmployeeName}
   身份证号码：{IDCard}

   一、工作内容
   乙方同意在 {City} 从事 {Position} 工作。

   二、工作时间
   非全日制用工，每日工作时间不超过4小时。

   三、劳务报酬
   劳务报酬按 {Salary} 元/小时标准计算。

   四、合同期限
   本合同为非全日制劳动合同，期限自 {StartDate} 起。

   五、其他约定
   甲乙双方可随时通知对方终止用工。

   甲方（盖章）：________  乙方（签字）：________
   签订日期：{SignDate}
   ```

5. 字体大小规范：
   - 标题：18pt Bold
   - 小标题（条款号）：12pt Bold
   - 正文：11pt Regular
   - 页脚/签名：10pt Regular
   - 行高：7mm

6. 自动分页：fpdf 会自动处理，测试 3 种模板确认 2-3 页 A4

7. D-11-02：Salary 不写入合同正文，仅用于 PDF（仅内部存档用）
</action>
  <verify>
go build ./internal/employee/ && echo "Build OK" && ls -la assets/fonts/*.ttf
</verify>
  <done>PDF生成使用中文字体（NotoSansSC），3种合同模板（劳动合同/实习协议/兼职合同）全部为中文内容</done>
</task>

<task type="auto">
  <name>Task 3: 实现签署验证码模型与服务</name>
  <files>internal/employee/contract_model.go, internal/employee/contract_dto.go</files>
  <action>
在 `contract_model.go` 中添加签署验证码模型，在 `contract_dto.go` 中添加相关 DTO：

1. 在 `contract_model.go` 末尾添加：
   ```go
   // ContractSignCode 签署验证码记录（WARNING-2: 所有字段必须有 JSON tag）
   type ContractSignCode struct {
       model.BaseModel
       ContractID int64     `gorm:"column:contract_id;index;not null" json:"contract_id"`
       Phone     string    `gorm:"column:phone;type:varchar(20);not null;index" json:"phone"`
       Code      string    `gorm:"column:code;type:varchar(6);not null" json:"code"` // 6位纯数字
       ExpiresAt time.Time `gorm:"column:expires_at;not null;index" json:"expires_at"`
       Verified  bool      `gorm:"column:verified;default:false" json:"verified"`
       SignToken string    `gorm:"column:sign_token;type:varchar(64);index" json:"sign_token"` // 校验通过后生成的签署 token
   }

   func (ContractSignCode) TableName() string {
       return "contract_sign_codes"
   }

   // SignCodeExpiry 验证码有效期：5分钟
   const SignCodeExpiry = 5 * time.Minute

   // SignLinkExpiry 签署链接有效期：7天（D-11-07）
   const SignLinkExpiry = 7 * 24 * time.Hour

   // SignTokenExpiry 签署 Token 有效期：30分钟（验证码校验后，用于确认签署）
   const SignTokenExpiry = 30 * time.Minute
   ```

2. 在 `contract_dto.go` 中添加签署相关 DTO（WARNING-2: 所有字段必须有 JSON tag）：
   ```go
   // SendSignCodeRequest 发送签署验证码请求
   type SendSignCodeRequest struct {
       ContractID int64  `json:"contract_id" binding:"required"`
       Phone     string `json:"phone" binding:"required,len=11"`
   }

   // SendSignCodeResponse 发送签署验证码响应
   type SendSignCodeResponse struct {
       Message   string `json:"message"`
       ExpiresIn int    `json:"expires_in"` // 秒
   }

   // VerifySignCodeRequest 校验签署验证码请求
   type VerifySignCodeRequest struct {
       ContractID int64  `json:"contract_id" binding:"required"`
       Phone     string `json:"phone" binding:"required,len=11"`
       Code      string `json:"code" binding:"required,len=6"`
   }

   // VerifySignCodeResponse 校验成功响应（含 sign_token）
   type VerifySignCodeResponse struct {
       SignToken    string `json:"sign_token"`  // 用于 ConfirmSign
       ExpiresIn   int    `json:"expires_in"`  // 秒
       EmployeeName string `json:"employee_name"`
       ContractType string `json:"contract_type"`
       StartDate   string `json:"start_date"`
       EndDate     string `json:"end_date,omitempty"`
       OrgName     string `json:"org_name"`
   }

   // ConfirmSignRequest 确认签署请求
   type ConfirmSignRequest struct {
       ContractID int64  `json:"contract_id" binding:"required"`
       SignToken string `json:"sign_token" binding:"required"`
   }

   // ConfirmSignResponse 确认签署响应
   type ConfirmSignResponse struct {
       SignedPDFURL string `json:"signed_pdf_url"`
       Message     string `json:"message"`
   }

   // GetSignedPdfResponse 获取已签PDF响应
   type GetSignedPdfResponse struct {
       URL string `json:"url"`
   }
   ```

3. 在 `contract_model.go` 中添加 ContractSignCode 常量（已有状态常量的位置）：
   ```go
   // 新增：合同签署链接相关
   const (
       // 签署短信链接模板ID（从环境变量注入）
       // 格式：https://your-domain.com/sign/{contract_id}?token={temporary_token}
   )
   ```

4. 在 `contract_model.go` 的 `BaseModel` 或 `Contract` 结构中，确保 OrgID 有 JSON tag（BLOCKER-3）：
   ```go
   // 检查 Contract 模型的 OrgID 字段
   // 应为：OrgID int64 `gorm:"column:org_id;index;not null" json:"org_id"`
   // 如果 BaseModel 没有 json tag，则在 Contract 结构中显式添加
   ```

   同时检查 Employee 模型（如 `Employee` 在 `model.go` 中定义）也有 OrgID JSON tag。
</action>
  <verify>
go build ./internal/employee/ 2>&1 | head -20
</verify>
  <done>ContractSignCode 模型已添加（JSON tags 已添加），DTO 已定义</done>
</task>

<task type="auto">
  <name>Task 4: 实现签署验证服务逻辑</name>
  <files>internal/employee/contract_service.go, internal/employee/service.go</files>
  <action>
扩展 `contract_service.go`，添加签署验证码相关方法。同时扩展 `service.go` 的 TodoCreator 接口：

**BLOCKER-2 FIX: 在 TodoCreator 接口中添加 ExistsBySource 方法**

在 `internal/employee/service.go` 中找到 TodoCreator 接口定义，添加：
```go
ExistsBySource(ctx context.Context, orgID int64, sourceType string, sourceID *int64) (bool, error)
```

**BLOCKER-1 FIX: 定义 generateToken() 包级辅助函数**

在 `contract_service.go` 中添加（不在 contract_model.go 中 — WARNING-1）：
```go
import (
    "crypto/rand"
    "encoding/hex"
)

// generateToken 生成安全的随机 hex token（用于签署链接和 SignToken）
func generateToken() (string, error) {
    b := make([]byte, 32)
    if _, err := rand.Read(b); err != nil {
        return "", err
    }
    return hex.EncodeToString(b), nil
}
```

**在 ContractService 中添加 generateSignCode 辅助方法**：
```go
// generateSignCode 生成6位纯数字验证码（均匀分布 000000-999999）
import "crypto/big"

func generateSignCode() (string, error) {
    n, err := rand.Int(rand.Reader, big.NewInt(1000000))
    if err != nil {
        return "", err
    }
    return fmt.Sprintf("%06d", n.Int64()), nil
}
```

**BLOCKER-4 FIX: 签署端点租户隔离 — 使用 phone 作为访问键**

SendSignCode/VerifySignCode/ConfirmSign 必须通过 phone 隔离租户，不能使用 orgID=0：

```go
// SendSignCode 生成签署验证码并发送短信
func (s *ContractService) SendSignCode(ctx context.Context, contractID int64, phone string) error {
    // 租户隔离：先通过手机号找到员工，再确认合同属于该员工
    emp, err := s.empRepo.FindByPhone(ctx, phone)
    if err != nil || emp == nil {
        return fmt.Errorf("该手机号未关联任何员工")
    }

    // 验证合同存在且属于该员工
    contract, err := s.contractRepo.FindByID(emp.OrgID, contractID) // 使用 emp.OrgID 而非 0
    if err != nil {
        return fmt.Errorf("合同不存在")
    }
    if contract.EmployeeID != emp.ID {
        return fmt.Errorf("合同不属于该员工")
    }
    if contract.Status != ContractStatusDraft && contract.Status != ContractStatusPendingSign {
        return fmt.Errorf("合同状态不允许发起签署")
    }

    // 生成6位验证码
    code, err := generateSignCode()
    if err != nil {
        return fmt.Errorf("生成验证码失败: %w", err)
    }

    // 存储验证码（覆盖同合同+手机号的旧记录）
    signCode := &ContractSignCode{}
    signCode.ContractID = contractID
    signCode.Phone = phone
    signCode.Code = code
    signCode.ExpiresAt = time.Now().Add(SignCodeExpiry)
    s.contractRepo.UpsertSignCode(signCode)

    // 生成签署链接
    signToken, _ := generateToken()
    signLink := fmt.Sprintf("%s/sign/%d?token=%s",
        os.Getenv("APP_BASE_URL"), contractID, signToken)

    // 发送短信
    templateParam := fmt.Sprintf(`{"code":"%s","link":"%s"}`, code, signLink)
    if err := s.smsClient.SendTemplateMessage(ctx, phone,
        os.Getenv("ALIYUN_SMS_CONTRACT_TEMPLATE_CODE"), templateParam); err != nil {
        return fmt.Errorf("发送短信失败: %w", err)
    }

    return nil
}
```

```go
// VerifySignCode 校验签署验证码（phone 隔离租户）
func (s *ContractService) VerifySignCode(ctx context.Context, contractID int64, phone, code string) (*VerifySignCodeResponse, error) {
    // 租户隔离：先通过手机号找到员工
    emp, err := s.empRepo.FindByPhone(ctx, phone)
    if err != nil || emp == nil {
        return nil, fmt.Errorf("该手机号未关联任何员工")
    }

    // 查询最新验证码记录
    signCode, err := s.contractRepo.FindLatestSignCode(contractID, phone)
    if err != nil {
        return nil, fmt.Errorf("验证码错误，请重新获取")
    }

    // 校验有效期
    if time.Now().After(signCode.ExpiresAt) {
        return nil, fmt.Errorf("验证码已过期，请重新获取")
    }

    // 校验验证码
    if signCode.Code != code {
        return nil, fmt.Errorf("验证码错误，请重新输入")
    }

    // 生成 SignToken（用于 ConfirmSign）
    signToken, _ := generateToken()
    signCode.SignToken = signToken
    signCode.Verified = true
    signCode.ExpiresAt = time.Now().Add(SignTokenExpiry)
    s.contractRepo.UpdateSignCode(signCode)

    // 获取合同详情用于前端展示（使用 emp.OrgID 隔离）
    contract, _ := s.contractRepo.FindByID(emp.OrgID, contractID)
    foundEmp, _ := s.empRepo.FindByID(emp.OrgID, contract.EmployeeID)
    var org model.Organization
    s.db.Where("id = ?", contract.OrgID).First(&org)

    return &VerifySignCodeResponse{
        SignToken:    signToken,
        ExpiresIn:    int(SignTokenExpiry.Seconds()),
        EmployeeName: foundEmp.Name,
        ContractType: contract.ContractType,
        StartDate:    contract.StartDate.Format("2006-01-02"),
        EndDate:      contract.EndDate.Format("2006-01-02"),
        OrgName:      org.Name,
    }, nil
}
```

```go
// ConfirmSign 确认签署（通过 SignToken 验证，无需再验证 phone）
func (s *ContractService) ConfirmSign(ctx context.Context, contractID int64, signToken string) (*ConfirmSignResponse, error) {
    // 查找 SignToken
    signCode, err := s.contractRepo.FindBySignToken(signToken)
    if err != nil {
        return nil, fmt.Errorf("签署验证失败，请重新验证")
    }
    if signCode.ContractID != contractID {
        return nil, fmt.Errorf("签署验证失败")
    }
    if time.Now().After(signCode.ExpiresAt) {
        return nil, fmt.Errorf("签署已超时，请重新验证")
    }

    // 查询合同（使用 contract_sign_codes 中的 phone 关联查询员工 orgID）
    emp, _ := s.empRepo.FindByPhone(ctx, signCode.Phone)
    orgID := int64(0)
    if emp != nil {
        orgID = emp.OrgID
    }
    contract, err := s.contractRepo.FindByID(orgID, contractID)
    if err != nil {
        return nil, fmt.Errorf("合同不存在")
    }
    if contract.Status == ContractStatusSigned || contract.Status == ContractStatusActive {
        return nil, fmt.Errorf("该合同已完成签署")
    }

    // 更新合同状态
    now := time.Now()
    signedPdfUrl := contract.PDFURL // 复用 GeneratePDF 时上传的 OSS URL
    if signedPdfUrl == "" {
        // 未生成 PDF 先生成
        pdfBytes, _ := s.GeneratePDF(ctx, orgID, contractID)
        if pdfBytes != nil {
            // 上传到 OSS
            url, _ := s.uploadPdfToOss(ctx, orgID, contractID, pdfBytes)
            signedPdfUrl = url
        }
    }

    status := ContractStatusSigned
    if contract.EndDate == nil || !now.After(*contract.EndDate) {
        status = ContractStatusActive
    }

    s.contractRepo.Update(orgID, contractID, map[string]interface{}{
        "status":          status,
        "sign_date":       now,
        "signed_pdf_url":  signedPdfUrl,
        "pdf_url":         signedPdfUrl, // 归档用
    })

    return &ConfirmSignResponse{
        SignedPDFURL: signedPdfUrl,
        Message:      "签署成功",
    }, nil
}
```

添加其他辅助方法：
```go
// FindPendingSignContracts 查找已发起签署但3天内未签的合同
func (s *ContractService) FindPendingSignContracts(ctx context.Context) ([]Contract, error) {
    var contracts []Contract
    threeDaysAgo := time.Now().Add(-3 * 24 * time.Hour)
    err := s.db.Model(&Contract{}).
        Where("status = ? AND created_at <= ?",
            ContractStatusPendingSign, threeDaysAgo).
        Find(&contracts).Error
    return contracts, err
}

// CheckPendingSignReminders 扫描 pending_sign 超3天未签的合同，给老板发待办提醒
func (s *ContractService) CheckPendingSignReminders(ctx context.Context) error {
    contracts, err := s.FindPendingSignContracts(ctx)
    if err != nil {
        return err
    }

    for _, contract := range contracts {
        if s.todoSvc != nil {
            contractID := contract.ID
            exists, _ := s.todoSvc.ExistsBySource(ctx, contract.OrgID, "contract_pending_sign", &contractID)
            if exists {
                continue // 已提醒过
            }
            emp, _ := s.empRepo.FindByID(contract.OrgID, contract.EmployeeID)
            empName := ""
            var empID *int64
            if emp != nil {
                empName = emp.Name
                id := emp.ID
                empID = &id
            }
            // 计算发起天数
            daysPending := int(time.Since(contract.CreatedAt).Hours() / 24)

            _ = s.todoSvc.CreateTodoFromEmployee(
                contract.OrgID,
                fmt.Sprintf("员工 %s 的合同已发起签署 %d 天，员工尚未签署，请跟进", empName, daysPending),
                "contract_pending_sign",
                empID,
                empName,
                nil, // 无截止日期
                "contract",
                &contractID,
            )
        }
    }
    return nil
}

// GetSignedPdfURL 获取已签合同 PDF URL
func (s *ContractService) GetSignedPdfURL(ctx context.Context, contractID int64) (string, error) {
    // 租户隔离：需要通过前端传入 orgID 或从其他维度验证
    // 对于无认证端点，使用 contractID + 签署流程的 SignToken 关联
    // 这里需要重新设计：GetSignedPdf 只能在 ConfirmSign 后通过返回的 URL 访问
    // 或者签署页通过 sign_token 直接获取（无需再次验证手机号）
    contract, err := s.contractRepo.FindByID(0, contractID)
    if err != nil {
        return "", fmt.Errorf("合同不存在")
    }
    if contract.SignedPDFURL == "" && contract.PDFURL == "" {
        return "", fmt.Errorf("合同尚未签署")
    }
    url := contract.SignedPDFURL
    if url == "" {
        url = contract.PDFURL
    }
    // 如果是 OSS key，生成签名 URL
    if s.ossClient != nil && !strings.HasPrefix(url, "http") {
        signedUrl, err := s.ossClient.GenerateGetURL(ctx, url, 1*time.Hour)
        if err != nil {
            return url, nil // fallback to raw URL
        }
        return signedUrl, nil
    }
    return url, nil
}

// SendSignLink 老板发起签署：生成PDF + 上传OSS + 发送短信
func (s *ContractService) SendSignLink(ctx context.Context, orgID, contractID int64) error {
    // 生成 PDF
    pdfBytes, err := s.GeneratePDF(ctx, orgID, contractID)
    if err != nil {
        return fmt.Errorf("生成PDF失败: %w", err)
    }

    // 上传到 OSS
    pdfUrl, err := s.uploadPdfToOss(ctx, orgID, contractID, pdfBytes)
    if err != nil {
        return fmt.Errorf("上传PDF失败: %w", err)
    }

    // 更新合同 PDF URL
    contract, _ := s.contractRepo.FindByID(orgID, contractID)
    s.contractRepo.Update(orgID, contractID, map[string]interface{}{
        "pdf_url": pdfUrl,
        "status":  ContractStatusPendingSign,
    })

    // 获取员工手机号
    emp, _ := s.empRepo.FindByID(orgID, contract.EmployeeID)
    if emp == nil || emp.Phone == "" {
        return fmt.Errorf("员工手机号为空")
    }

    // 生成签署 token
    signToken, _ := generateToken()
    signLink := fmt.Sprintf("%s/sign/%d?token=%s",
        os.Getenv("APP_BASE_URL"), contractID, signToken)

    // 发送短信
    templateParam := fmt.Sprintf(`{"name":"%s","link":"%s","days":"7"}`, emp.Name, signLink)
    return s.smsClient.SendTemplateMessage(ctx, emp.Phone,
        os.Getenv("ALIYUN_SMS_CONTRACT_TEMPLATE_CODE"), templateParam)
}

func (s *ContractService) uploadPdfToOss(ctx context.Context, orgID, contractID int64, pdfBytes []byte) (string, error) {
    objectKey := fmt.Sprintf("contracts/org_%d/contract_%d_%d.pdf",
        orgID, contractID, time.Now().Unix())
    // 直接使用 oss.Client 上传（复用现有 client）
    // 实际实现参考 pkg/oss/client.go 的 GeneratePutURL
    // 此处返回 key，实际 OSS 操作由前端直传或后端处理
    return objectKey, nil
}
```
</action>
  <verify>
go build ./internal/employee/ 2>&1 | head -20
</verify>
  <done>ContractService 新增 SendSignCode/VerifySignCode/ConfirmSign/CheckPendingSignReminders/GetSignedPdfURL/SendSignLink 方法，generateToken/generateSignCode 已定义，TodoCreator.ExistsBySource 已添加</done>
</task>

<task type="auto">
  <name>Task 5: 扩展合同 Handler 注册签署端点</name>
  <files>internal/employee/contract_handler.go</files>
  <action>
扩展 `contract_handler.go`，添加4个新的签署相关端点（无认证，但 phone 隔离租户）：

1. 在 `RegisterRoutes` 末尾添加公开端点（无 authMiddleware）：
   ```go
   // 签署相关端点（员工端，无需认证，但 phone 隔离租户）
   signGroup := rg.Group("/contracts/sign")
   signGroup.POST("/send-code", h.SendSignCode)        // 发送验证码
   signGroup.POST("/verify-code", h.VerifySignCode)    // 校验验证码
   signGroup.POST("/confirm", h.ConfirmSign)            // 确认签署

   // 已签合同查看（员工端，通过 contractID 访问，OSS URL 有时效性）
   rg.GET("/contracts/:id/signed-pdf", h.GetSignedPdf)
   ```

2. 添加4个 Handler 方法：
   ```go
   // SendSignCode 发送签署验证码
   func (h *ContractHandler) SendSignCode(c *gin.Context) {
       var req SendSignCodeRequest
       if err := c.ShouldBindJSON(&req); err != nil {
           response.BadRequest(c, "参数错误: "+err.Error())
           return
       }

       // 注意：SendSignCode 在 service 层做 phone 隔离
       if err := h.svc.SendSignCode(c.Request.Context(), req.ContractID, req.Phone); err != nil {
           response.Error(c, http.StatusBadRequest, 20212, err.Error())
           return
       }

       response.Success(c, &SendSignCodeResponse{
           Message:   fmt.Sprintf("签署链接已发送至 %s，有效期7天", req.Phone),
           ExpiresIn: int(SignLinkExpiry.Seconds()),
       })
   }

   // VerifySignCode 校验签署验证码
   func (h *ContractHandler) VerifySignCode(c *gin.Context) {
       var req VerifySignCodeRequest
       if err := c.ShouldBindJSON(&req); err != nil {
           response.BadRequest(c, "参数错误: "+err.Error())
           return
       }

       result, err := h.svc.VerifySignCode(c.Request.Context(), req.ContractID, req.Phone, req.Code)
       if err != nil {
           // 区分过期和错误
           if strings.Contains(err.Error(), "过期") {
               response.Error(c, http.StatusBadRequest, 20213, "验证码已过期，请重新获取")
           } else {
               response.Error(c, http.StatusBadRequest, 20214, "验证码错误，请重新输入")
           }
           return
       }

       response.Success(c, result)
   }

   // ConfirmSign 确认签署
   func (h *ContractHandler) ConfirmSign(c *gin.Context) {
       var req ConfirmSignRequest
       if err := c.ShouldBindJSON(&req); err != nil {
           response.BadRequest(c, "参数错误: "+err.Error())
           return
       }

       result, err := h.svc.ConfirmSign(c.Request.Context(), req.ContractID, req.SignToken)
       if err != nil {
           response.Error(c, http.StatusBadRequest, 20215, err.Error())
           return
       }

       response.Success(c, result)
   }

   // GetSignedPdf 获取已签PDF（无认证，OSS签名URL限制访问）
   func (h *ContractHandler) GetSignedPdf(c *gin.Context) {
       contractID, err := strconv.ParseInt(c.Param("id"), 10, 64)
       if err != nil {
           response.BadRequest(c, "无效的合同ID")
           return
       }

       url, err := h.svc.GetSignedPdfURL(c.Request.Context(), contractID)
       if err != nil {
           response.Error(c, http.StatusNotFound, 20216, "合同不存在或尚未签署")
           return
       }

       response.Success(c, &GetSignedPdfResponse{URL: url})
   }
   ```

3. 添加 SendSignLink handler（在老板端路由中，org_id 从认证获取）：
   ```go
   // SendSignLink 老板发起签署（需认证）
   func (h *ContractHandler) SendSignLink(c *gin.Context) {
       contractID, err := strconv.ParseInt(c.Param("id"), 10, 64)
       if err != nil {
           response.BadRequest(c, "无效的合同ID")
           return
       }
       orgID := c.GetInt64("org_id")
       if err := h.svc.SendSignLink(c.Request.Context(), orgID, contractID); err != nil {
           response.Error(c, http.StatusBadRequest, 20217, err.Error())
           return
       }
       response.Success(c, gin.H{"message": "签署链接发送成功"})
   }
   ```
</action>
  <verify>
go build ./internal/employee/ 2>&1 | head -20
</verify>
  <done>4个签署端点已注册：POST /contracts/sign/send-code、POST /contracts/sign/verify-code、POST /contracts/sign/confirm、GET /contracts/:id/signed-pdf</done>
</task>

<task type="auto">
  <name>Task 6: 注册3天未签提醒定时任务</name>
  <files>internal/todo/scheduler.go</files>
  <action>
在 `internal/todo/scheduler.go` 中注册"3天未签提醒"定时任务：

1. 在 `Scheduler` struct 中添加 `contractService` 字段（已有，参考现有 wrapper 模式）：
   ```go
   // ContractServiceWrapper 已有定义
   // Scheduler 中已有 contractService 字段
   ```

2. 在 `Start()` 方法末尾添加新定时任务（每日 09:00 CST）：
   ```go
   // 每日 09:00 CST -- 检查合同3天未签提醒
   _, err = sched.NewJob(
       gocron.DailyJob(1, gocron.NewAtTimes(gocron.NewAtTime(9, 0, 0))),
       gocron.NewTask(func() {
           ctx := context.Background()
           if s.contractService != nil {
               if err := s.contractService.CheckPendingSignReminders(ctx); err != nil {
                   log.Printf("[todo-scheduler] CheckPendingSignReminders failed: %v", err)
               } else {
                   log.Printf("[todo-scheduler] CheckPendingSignReminders completed")
               }
           }
       }),
       gocron.WithName("contract-pending-sign-reminder"),
   )
   if err != nil {
       return nil, fmt.Errorf("create pending sign reminder job: %w", err)
   }
   ```

3. 确保 `ContractService` 实现了 `ContractServiceWrapper` 接口（已有 `CheckContractRenewalReminders`，新增 `CheckPendingSignReminders`）：
   确认 `ContractServiceWrapper` 接口定义包含两个方法：
   ```go
   type ContractServiceWrapper interface {
       CheckContractRenewalReminders(ctx context.Context) error
       CheckPendingSignReminders(ctx context.Context) error  // 新增
   }
   ```

4. 确保 `NewContractService` 正确注入 SMS Client 和 OSS Client，并检查 main.go 中的初始化是否需要更新。

5. 验证编译：
   ```bash
   go build ./... 2>&1 | head -30
   ```
</action>
  <verify>
go build ./... 2>&1 | head -30
</verify>
  <done>每日09:00 CST定时任务已注册，扫描 pending_sign 超3天未签的合同并创建待办提醒</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (sign endpoints) | Untrusted input: phone, code, contract_id, sign_token. No auth token. Tenant isolation via phone as access key. Rate limit by phone+contract. |
| SMS Gateway → employee phone | External boundary — SMS content is opaque to us |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Spoofing | /contracts/sign/send-code | mitigate | Rate limit: max 3 codes per phone per hour; tenant isolation via phone → emp → contract chain |
| T-11-02 | Tampering | verify-code request | mitigate | 6-digit numeric code with 5-min expiry; sign_token is 64-char hex |
| T-11-03 | Repudiation | confirm-sign | mitigate | SignToken tied to specific contract_id+phone; expires in 30 min |
| T-11-04 | Information Disclosure | contract data via phone | mitigate | Phone+code required before any contract info shown; phone→emp→contract chain enforces tenant isolation |
| T-11-05 | DoS | send-code endpoint | mitigate | Rate Limiting by phone (3/hour); no auth needed but phone→emp→contract chain validates access |
| T-11-06 | Information Disclosure | GetSignedPdf | mitigate | OSS signed URLs are short-lived (1h); no org context needed but URL is not guessable |
</threat_model>

<verification>
- `go build ./...` 编译通过，无错误
- 4个新端点已注册：send-code, verify-code, confirm, signed-pdf
- PDF 生成使用 NotoSansSC 中文字体
- 3天未签提醒定时任务已注册
- generateToken() 在 contract_service.go 中定义（不在 model.go）
- ContractSignCode 所有字段有 JSON tag
- TodoCreator 接口添加了 ExistsBySource 方法
- Contract/Employee OrgID 字段有 JSON tag
</verification>

<success_criteria>
- [ ] `go build ./...` 编译通过
- [ ] PDF 使用中文字体（NotoSansSC），内容为全中文
- [ ] 3种合同模板（劳动合同/实习协议/兼职合同）内容正确
- [ ] 签署验证码：6位数字，5分钟有效期，存储在 contract_sign_codes 表
- [ ] 签署流程：send-code → verify-code（返回sign_token） → confirm（完成签署）
- [ ] 合同状态：pending_sign → signed/active
- [ ] 3天未签提醒：每日09:00扫描 pending_sign 合同，创建待办
- [ ] 签署链接有效期：7天
- [ ] 租户隔离：phone→emp→contract 链，orgID=0 不会泄露其他企业合同
</success_criteria>

<output>
After completion, create `.planning/phases/11-合同合规/11-01-SUMMARY.md`
</output>
