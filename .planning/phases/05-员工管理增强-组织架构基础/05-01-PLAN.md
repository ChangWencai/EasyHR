---
phase: 05
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/dashboard/service.go
  - internal/dashboard/model.go
  - internal/dashboard/handler.go
  - internal/dashboard/handler_test.go
  - frontend/src/views/employee/EmployeeDashboard.vue
  - frontend/src/api/employee.ts
  - frontend/src/router/index.ts
autonomous: true
requirements:
  - EMP-01
  - EMP-02

must_haves:
  truths:
    - "管理员可在员工看板看到在职人数、当月新入职人数、当月离职人数"
    - "管理员可在员工看板看到当月离职率百分比"
    - "离职率分母为0时显示0%而非NaN"
  artifacts:
    - path: "internal/dashboard/service.go"
      provides: "GetEmployeeDashboard 方法，返回4项指标+离职率"
      contains: "GetEmployeeDashboard"
    - path: "frontend/src/views/employee/EmployeeDashboard.vue"
      provides: "员工数据看板组件，4张卡片"
      min_lines: 30
  key_links:
    - from: "frontend/src/views/employee/EmployeeDashboard.vue"
      to: "/api/v1/employee/dashboard"
      via: "employeeApi.getDashboard()"
      pattern: "getDashboard"
---

<objective>
实现员工数据看板：后端提供在职/新入职/离职人数 + 离职率 API，前端4张纯数字卡片展示

Purpose: 让管理员一眼掌握团队人员动态，为 HR 决策提供数据支撑
Output: Dashboard 新 API + 前端员工看板页面
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-员工管理增强-组织架构基础/05-RESEARCH.md
@.planning/phases/05-员工管理增强-组织架构基础/05-UI-SPEC.md
@.planning/phases/05-员工管理增强-组织架构基础/05-PATTERNS.md

<interfaces>
<!-- Dashboard 现有模式 -->

From internal/dashboard/service.go:
```go
type ServiceInterface interface {
    GetDashboard(ctx context.Context, orgID int64) (*DashboardResult, error)
}

type DashboardService struct {
    repo DashboardRepository
}

func NewService(repo DashboardRepository) *DashboardService
```

From internal/dashboard/repository.go:
```go
type DashboardRepository interface {
    GetEmployeeStats(ctx context.Context, orgID int64) (active, joined, left int, err error)
    // ... 其他方法
}
```

From internal/dashboard/model.go:
```go
type Overview struct {
    EmployeeCount        int    `json:"employee_count"`
    JoinedThisMonth      int    `json:"joined_this_month"`
    LeftThisMonth        int    `json:"left_this_month"`
    SocialInsuranceTotal string `json:"social_insurance_total"`
    PayrollTotal         string `json:"payroll_total"`
}

type DashboardResult struct {
    Todos    []TodoItem `json:"todos"`
    Overview Overview   `json:"overview"`
}
```

From internal/dashboard/handler.go (路由注册模式):
```go
func RegisterRouter(rg *gin.RouterGroup, authMiddleware gin.HandlerFunc, db *gorm.DB)
```

From cmd/server/main.go line 208:
```go
dashboard.RegisterRouter(v1.Group("/dashboard"), authMiddleware, db)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 后端 - 新增 GetEmployeeDashboard API</name>
  <files>internal/dashboard/model.go, internal/dashboard/service.go, internal/dashboard/handler.go, internal/dashboard/handler_test.go</files>
  <read_first>
    - internal/dashboard/service.go
    - internal/dashboard/model.go
    - internal/dashboard/handler.go
    - internal/dashboard/handler_test.go
    - internal/dashboard/repository.go
  </read_first>
  <action>
1. 在 `internal/dashboard/model.go` 中新增 EmployeeDashboardResult 结构体：
```go
// EmployeeDashboardResult 员工数据看板结果
type EmployeeDashboardResult struct {
    ActiveCount     int     `json:"active_count"`
    JoinedThisMonth int     `json:"joined_this_month"`
    LeftThisMonth   int     `json:"left_this_month"`
    TurnoverRate    float64 `json:"turnover_rate"` // 离职率百分比，保留2位小数
}
```

2. 在 `internal/dashboard/service.go` 中为 DashboardService 新增方法 GetEmployeeDashboard：
```go
func (s *DashboardService) GetEmployeeDashboard(ctx context.Context, orgID int64) (*EmployeeDashboardResult, error) {
    active, joined, left, err := s.repo.GetEmployeeStats(ctx, orgID)
    if err != nil {
        return nil, err
    }
    // D-02: 离职率 = 离职人数 / (离职人数 + 期末人数) x 100%
    turnoverRate := 0.0
    denominator := float64(left + active)
    if denominator > 0 {
        turnoverRate = float64(left) / denominator * 100
    }
    return &EmployeeDashboardResult{
        ActiveCount:     active,
        JoinedThisMonth: joined,
        LeftThisMonth:   left,
        TurnoverRate:    math.Round(turnoverRate*100) / 100,
    }, nil
}
```
注意 import "math"。

3. 在 `internal/dashboard/handler.go` 中新增 GetEmployeeDashboard handler 方法，复用现有的 handler 模式（从 context 获取 org_id，调用 service，返回 response.Success）。路由注册在 RegisterRouter 函数中添加：`authGroup.GET("/employee-dashboard", h.GetEmployeeDashboard)`

4. 在 `internal/dashboard/handler_test.go` 中新增测试：
   - TestGetEmployeeDashboard_Success: 验证正常返回 active=3, joined=1, left=1, turnover_rate=25.00
   - TestGetEmployeeDashboard_ZeroDenominator: 验证 active=0, left=0 时 turnover_rate=0.0
   使用现有的 MockDashboardService 或 table-driven test 模式。
  </action>
  <verify>
    <automated>cd /Users/wencai/github/EasyHR && go test ./internal/dashboard/... -run TestGetEmployeeDashboard -v</automated>
  </verify>
  <done>
    - GetEmployeeDashboard 方法返回在职/新入职/离职人数 + 离职率
    - 离职率计算公式: left/(left+active)*100，分母为0时返回0%
    - TurnoverRate 保留2位小数
    - handler_test.go 中有 TestGetEmployeeDashboard_Success 和 TestGetEmployeeDashboard_ZeroDenominator 两个测试通过
  </done>
  <acceptance_criteria>
    - `internal/dashboard/model.go` contains `EmployeeDashboardResult`
    - `internal/dashboard/service.go` contains `func (s *DashboardService) GetEmployeeDashboard`
    - `internal/dashboard/service.go` contains `math.Round(turnoverRate*100) / 100`
    - `internal/dashboard/handler.go` contains `GetEmployeeDashboard`
    - `go test ./internal/dashboard/... -run TestGetEmployeeDashboard -v` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 前端 - 员工数据看板组件 EmployeeDashboard.vue</name>
  <files>frontend/src/views/employee/EmployeeDashboard.vue, frontend/src/api/employee.ts, frontend/src/router/index.ts</files>
  <read_first>
    - frontend/src/views/employee/EmployeeDashboard.vue (不存在则为新建)
    - frontend/src/views/home/HomeView.vue
    - frontend/src/api/employee.ts
    - frontend/src/router/index.ts
  </read_first>
  <action>
1. 在 `frontend/src/api/employee.ts` 中新增 API 方法：
```typescript
export interface EmployeeDashboard {
  active_count: number
  joined_this_month: number
  left_this_month: number
  turnover_rate: number
}

// 在 employeeApi 对象中新增:
getDashboard: () =>
  request.get<EmployeeDashboard>('/dashboard/employee-dashboard'),
```

2. 创建 `frontend/src/views/employee/EmployeeDashboard.vue`：
   - 页面标题 "员工看板"（16px/700, per UI-SPEC Display role）
   - 4张数据卡片采用 CSS Grid 4列等宽布局（grid-template-columns: repeat(4, 1fr), gap: 8px）
   - 每张卡片: #FAFAFA 背景, padding 16px 四边统一, 8px 圆角 (per UI-SPEC Spacing)
   - 数值使用 24px/700, #1677FF 蓝色 (per UI-SPEC Stat Value role)
   - 标签使用 Element Plus 默认 12px, #8C8C8C (per UI-SPEC Typography)
   - 4张卡片分别是: 在职人数(active_count), 本月新入职(joined_this_month), 本月离职(left_this_month), 当月离职率(turnover_rate%)
   - 离职率数字使用 #F56C6C 红色 (per UI-SPEC Destructive role)
   - 加载状态使用 v-loading 指令
   - 空状态: 当 active_count=0 且 joined=0 且 left=0 时显示 el-empty，文案 "暂无员工数据 -- 请先添加员工"
   - 错误状态: 加载失败 ElMessage.error("加载看板数据失败，请刷新页面重试")

3. 在 `frontend/src/router/index.ts` 的 employee 子路由区域新增：
```typescript
{
  path: '/employee/dashboard',
  name: 'employee-dashboard',
  component: () => import('@/views/employee/EmployeeDashboard.vue'),
},
```
放在 `/employee` 路由之前，确保不会被 `:id` 参数路由捕获。
  </action>
  <verify>
    <automated>cd /Users/wencai/github/EasyHR/frontend && npx vue-tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - EmployeeDashboard.vue 组件创建完成，4张卡片正确显示
    - API 方法 getDashboard 返回 EmployeeDashboard 类型数据
    - 路由 /employee/dashboard 可访问
    - 离职率分母为0时卡片显示 0.00%
    - 卡片样式与首页数据概览一致（#FAFAFA 背景, #1677FF 数值色）
  </done>
  <acceptance_criteria>
    - `frontend/src/api/employee.ts` contains `getDashboard`
    - `frontend/src/views/employee/EmployeeDashboard.vue` exists
    - `frontend/src/views/employee/EmployeeDashboard.vue` contains `active_count`
    - `frontend/src/views/employee/EmployeeDashboard.vue` contains `turnover_rate`
    - `frontend/src/router/index.ts` contains `employee-dashboard`
    - `npx vue-tsc --noEmit` passes (no new type errors)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> Dashboard API | 认证用户请求数据看板，需验证 JWT + org_id 权限 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | Dashboard API | accept | 看板数据为聚合统计，不含 PII，且需 JWT 认证 |
| T-05-02 | Tampering | turnover_rate 计算 | mitigate | 后端计算离职率（不接受前端输入），分母为0时返回0%（避免 NaN/Inf） |
</threat_model>

<verification>
1. `go test ./internal/dashboard/... -v` 全部通过
2. `cd frontend && npx vue-tsc --noEmit` 无新增类型错误
3. API `/api/v1/dashboard/employee-dashboard` 返回正确 JSON 结构
</verification>

<success_criteria>
- Dashboard API 返回 active_count/joined_this_month/left_this_month/turnover_rate 四个字段
- 离职率公式: left/(left+active)*100, 分母为0返回0%, 保留2位小数
- 前端4张卡片展示，样式与首页一致
- 路由 /employee/dashboard 可访问
</success_criteria>

<output>
After completion, create `.planning/phases/05-员工管理增强-组织架构基础/05-01-SUMMARY.md`
</output>
