---
phase: 05
plan: 04
type: execute
wave: 2
depends_on:
  - 05-02
files_modified:
  - internal/employee/offboarding_model.go
  - internal/employee/offboarding_service.go
  - internal/employee/offboarding_handler.go
  - internal/employee/offboarding_dto.go
  - frontend/src/views/employee/OffboardingList.vue
  - frontend/src/api/employee.ts
autonomous: true
requirements:
  - EMP-09
  - EMP-10
  - EMP-11
  - EMP-12

must_haves:
  truths:
    - "管理员可在离职列表中直接点击同意或驳回按钮审批离职申请"
    - "审批通过后行内显示'去减员'按钮，点击跳转社保减员页面"
    - "离职列表包含 rejected 状态筛选"
    - "驳回操作需要确认弹窗，可选填驳回原因"
    - "社保减员完成后，系统通过 SocialInsuranceEventHandler.OnEmployeeResigned 回调自动更新离职状态"
  artifacts:
    - path: "internal/employee/offboarding_model.go"
      provides: "新增 rejected 状态常量"
      contains: "OffboardingStatusRejected"
    - path: "internal/employee/offboarding_service.go"
      provides: "RejectResign 方法"
      contains: "RejectResign"
    - path: "frontend/src/views/employee/OffboardingList.vue"
      provides: "扩展的离职列表，含行内审批按钮"
      min_lines: 30
  key_links:
    - from: "frontend/src/views/employee/OffboardingList.vue"
      to: "/tool/socialinsurance"
      via: "router.push 携带 employee_id + employee_name"
      pattern: "goToSIRegister"
    - from: "SocialInsuranceEventHandler.OnEmployeeResigned"
      to: "OffboardingService.CompleteOffboarding"
      via: "减员完成后回调自动更新离职状态为 completed"
      pattern: "OnEmployeeResigned"
---

<objective>
优化离职审批流程：新增驳回功能 + 行内审批按钮 + 审批通过后"去减员"跳转社保减员页面 + 减员完成后自动更新离职状态

Purpose: 让管理员在离职列表中一步完成审批操作，通过后直接跳转社保减员，减员完成后系统自动更新离职状态，无需手动操作
Output: 后端 RejectResign API + 前端 OffboardingList 行内审批扩展
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-员工管理增强-组织架构基础/05-RESEARCH.md
@.planning/phases/05-员工管理增强-组织架构基础/05-UI-SPEC.md
@.planning/phases/05-员工管理增强-组织架构基础/05-PATTERNS.md

<interfaces>
<!-- Offboarding 现有模型 -->

From internal/employee/offboarding_model.go:
```go
type Offboarding struct {
    model.BaseModel
    EmployeeID      int64          `gorm:"column:employee_id;index;not null" json:"employee_id"`
    Type            string         `gorm:"column:type;type:varchar(20);not null" json:"type"`
    ResignationDate time.Time      `gorm:"column:resignation_date;type:date;not null" json:"resignation_date"`
    Reason          string         `gorm:"column:reason;type:varchar(500)" json:"reason"`
    Status          string         `gorm:"column:status;type:varchar(20);not null;default:pending" json:"status"`
    ChecklistItems  datatypes.JSON `gorm:"column:checklist_items;type:jsonb;not null" json:"checklist_items"`
    CompletedAt     *time.Time     `gorm:"column:completed_at" json:"completed_at"`
    ApprovedBy      *int64         `gorm:"column:approved_by" json:"approved_by"`
    ApprovedAt      *time.Time     `gorm:"column:approved_at" json:"approved_at"`
}
const (
    OffboardingStatusPending   = "pending"
    OffboardingStatusApproved  = "approved"
    OffboardingStatusCompleted = "completed"
)
```

<!-- ApproveResign 模式（RejectResign 需遵循） -->

From internal/employee/offboarding_service.go:
```go
func (s *OffboardingService) ApproveResign(orgID, approverID, offboardingID int64) error {
    ob, err := s.obRepo.FindByID(orgID, offboardingID)
    // ... 验证状态为 pending
    updates := map[string]interface{}{
        "status":      OffboardingStatusApproved,
        "approved_by": approverID,
        "approved_at": now,
        "updated_by":  approverID,
    }
    s.obRepo.Update(orgID, offboardingID, updates)
    // ... 更新 Employee 状态为 resigned
}
```

<!-- OffboardingHandler 路由模式 -->

From internal/employee/offboarding_handler.go:
```go
func (h *OffboardingHandler) RegisterRoutes(rg *gin.RouterGroup, authMiddleware gin.HandlerFunc) {
    authGroup := rg.Group("")
    authGroup.Use(authMiddleware)
    authGroup.POST("/offboardings/:id/approve", h.ApproveResign)
    authGroup.POST("/offboardings/:id/complete", h.CompleteOffboarding)
    // ...
}
```

<!-- OffboardingDetailResponse -->

From internal/employee/offboarding_dto.go:
```go
type OffboardingDetailResponse struct {
    ID              int64          `json:"id"`
    EmployeeID      int64          `json:"employee_id"`
    EmployeeName    string         `json:"employee_name"`
    Type            string         `json:"type"`
    ResignationDate time.Time      `json:"resignation_date"`
    Reason          string         `json:"reason"`
    Status          string         `json:"status"`
    // ...
}
```

<!-- EMP-09: 离职列表现有字段确认 -->

OffboardingDetailResponse 已包含 EmployeeName（作为"发起人"显示）、CreatedAt（作为"时间"显示）、Status（作为"状态"显示）。
列表默认按 created_at DESC 排序（最新的离职申请排在前面），符合 EMP-09 的排序要求。
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 后端 - RejectResign API + rejected 状态 + 减员完成回调确认</name>
  <files>internal/employee/offboarding_model.go, internal/employee/offboarding_service.go, internal/employee/offboarding_handler.go, internal/employee/offboarding_dto.go</files>
  <read_first>
    - internal/employee/offboarding_model.go
    - internal/employee/offboarding_service.go
    - internal/employee/offboarding_handler.go
    - internal/employee/offboarding_dto.go
    - internal/employee/offboarding_repository.go
  </read_first>
  <action>
1. 在 `internal/employee/offboarding_model.go` 状态常量块中新增:
```go
OffboardingStatusRejected = "rejected"
```

2. 在 `internal/employee/offboarding_service.go` 新增 RejectResign 方法:
```go
func (s *OffboardingService) RejectResign(orgID, rejectorID, offboardingID int64, reason string) error {
    ob, err := s.obRepo.FindByID(orgID, offboardingID)
    if err != nil {
        return fmt.Errorf("离职记录不存在")
    }
    if ob.Status != OffboardingStatusPending {
        return fmt.Errorf("当前状态不可驳回（状态: %s）", ob.Status)
    }
    updates := map[string]interface{}{
        "status":     OffboardingStatusRejected,
        "reason":     reason,
        "updated_by": rejectorID,
    }
    if err := s.obRepo.Update(orgID, offboardingID, updates); err != nil {
        return fmt.Errorf("驳回离职失败: %w", err)
    }
    // 注意: 驳回不更新 Employee 状态（撤销离职流程）
    return nil
}
```

3. 在 `internal/employee/offboarding_dto.go` 新增:
```go
type RejectResignRequest struct {
    Reason string `json:"reason"` // 驳回原因（选填）
}
```

4. 在 `internal/employee/offboarding_handler.go` RegisterRoutes 中新增路由:
```go
authGroup.POST("/offboardings/:id/reject", h.RejectResign)
```
新增 handler 方法:
```go
func (h *OffboardingHandler) RejectResign(c *gin.Context) {
    orgID := c.GetInt64("org_id")
    userID := c.GetInt64("user_id")
    idStr := c.Param("id")
    id, err := strconv.ParseInt(idStr, 10, 64)
    if err != nil {
        response.BadRequest(c, "无效的ID")
        return
    }
    var req RejectResignRequest
    if err := c.ShouldBindJSON(&req); err != nil {
        // reason 是选填的，不需要严格绑定
    }
    if err := h.svc.RejectResign(orgID, userID, id, req.Reason); err != nil {
        response.Error(c, http.StatusBadRequest, 20210, err.Error())
        return
    }
    response.Success(c, nil)
}
```

5. 在 `internal/employee/offboarding_service.go` 的 CompleteOffboarding 方法中更新状态检查:
   - 将 `ob.Status != OffboardingStatusPending && ob.Status != OffboardingStatusApproved` 改为也排除 rejected:
   ```go
   if ob.Status != OffboardingStatusPending && ob.Status != OffboardingStatusApproved {
       return fmt.Errorf("当前状态不可完成交接（状态: %s）", ob.Status)
   }
   ```
   (现有代码已经是这个逻辑，rejected 状态自然被排除)

6. **EMP-12: 减员完成后离职状态自动更新**
   确认社保模块 `SocialInsuranceEventHandler.OnEmployeeResigned` 接口是否已存在:
   a. 读取 `internal/socialinsurance/` 目录查找 `OnEmployeeResigned` 或类似回调接口
   b. 如果接口已存在: 在 `OffboardingService` 中注册监听，或在 `CompleteOffboarding` 方法中检查是否被社保模块调用
   c. 如果接口不存在: 在 `internal/employee/offboarding_service.go` 新增 `CompleteOffboardingFromSI(orgID, employeeID int64) error` 方法供社保模块调用，内部查找该 employee_id 对应的 approved 状态 Offboarding 记录，自动更新为 completed
   d. 在代码注释中标注: "此方法由社保减员完成时调用，实现 EMP-12 自动更新离职状态"
  </action>
  <verify>
    <automated>cd /Users/wencai/github/EasyHR && go test ./internal/employee/... -run TestReject -v 2>&1 | head -20; go build ./... 2>&1 | head -10</automated>
  </verify>
  <done>
    - OffboardingStatusRejected = "rejected" 常量已添加
    - RejectResign 方法验证状态为 pending 后更新为 rejected
    - 驳回不更新 Employee 状态
    - RejectResignRequest DTO 包含 reason 字段（选填）
    - 路由 POST /offboardings/:id/reject 已注册
    - EMP-12: 社保减员完成后自动更新离职状态的回调机制已确认/实现
    - 编译通过
  </done>
  <acceptance_criteria>
    - `internal/employee/offboarding_model.go` contains `OffboardingStatusRejected`
    - `internal/employee/offboarding_service.go` contains `func (s *OffboardingService) RejectResign`
    - `internal/employee/registration_service.go` contains `CompleteOffboardingFromSI` 或 OnEmployeeResigned 回调已确认
    - `internal/employee/offboarding_dto.go` contains `RejectResignRequest`
    - `internal/employee/offboarding_handler.go` contains `/offboardings/:id/reject`
    - `go build ./...` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 前端 - OffboardingList 行内审批扩展</name>
  <files>frontend/src/views/employee/OffboardingList.vue, frontend/src/api/employee.ts</files>
  <read_first>
    - frontend/src/views/employee/OffboardingList.vue
    - frontend/src/api/employee.ts
  </read_first>
  <action>
1. 在 `frontend/src/api/employee.ts` 的 employeeApi 中新增:
```typescript
rejectOffboarding: (id: number, reason?: string) =>
  request.post(`/offboardings/${id}/reject`, { reason }),
```

2. 修改 `frontend/src/views/employee/OffboardingList.vue`:
   a. 操作列宽度改为 260px (per UI-SPEC Layout Patterns)
   b. 操作按钮按状态显示 (per UI-SPEC Interaction Patterns):
      - pending: "同意"(primary) + "驳回"(danger) -- 同意使用 el-popconfirm 确认
      - approved: "去减员"(warning) + "完成离职"(success)
      - rejected/completed: 无操作按钮
   c. "同意" 按钮: el-popconfirm 确认 "确认同意 {employee_name} 的离职申请？", 确认后调用 approveOffboarding API
   d. "驳回" 按钮: 弹出 el-dialog, 包含:
      - 标题: "驳回离职申请"
      - el-input textarea, label "驳回原因（选填）", maxlength 500
      - 确认按钮调用 rejectOffboarding API
   e. "去减员" 按钮:
      ```typescript
      function goToSIRegister(employeeId: number, employeeName: string) {
        router.push({
          path: '/tool/socialinsurance',
          query: { action: 'reduce', employee_id: String(employeeId), employee_name: employeeName }
        })
      }
      ```
   f. "完成离职" 按钮: el-popconfirm 确认后调用 completeOffboarding API
   g. 状态筛选下拉新增 "已驳回" (rejected) 选项
   h. 状态显示: rejected 使用 el-tag type="danger", 文字 "已驳回"
   i. API 调用成功后刷新列表 (load(currentPage))

3. 在 statusMap 中新增 rejected 映射 (如果项目使用集中式 statusMap):
   - offboardingStatusMap.rejected = '已驳回'
   - offboardingStatusTagType.rejected = 'danger'

4. **EMP-09 确认:**
   - 现有 OffboardingList 已包含 "事项"(type)、"发起人"(employee_name)、"时间"(created_at)、"状态"(status) 列
   - 列表默认按 created_at DESC 排序（最新申请在前）
   - 确认这些列在 el-table 中已正确绑定 prop，如缺少则补充
  </action>
  <verify>
    <automated>cd /Users/wencai/github/EasyHR/frontend && npx vue-tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - 离职列表操作列包含行内同意/驳回按钮 (pending 状态)
    - 驳回弹窗包含原因输入框（选填）
    - approved 状态显示"去减员"按钮，点击跳转社保减员页面
    - 状态筛选下拉包含 "已驳回" 选项
    - rejected 状态显示红色 "已驳回" 标签
    - EMP-09: 列表已包含事项/发起人/时间/状态列，按时间降序排序
  </done>
  <acceptance_criteria>
    - `frontend/src/api/employee.ts` contains `rejectOffboarding`
    - `frontend/src/views/employee/OffboardingList.vue` contains `rejected` (rejected 状态处理)
    - `frontend/src/views/employee/OffboardingList.vue` contains `goToSIRegister` (或 router.push 社保减员)
    - `frontend/src/views/employee/OffboardingList.vue` contains `el-popconfirm` (同意确认弹窗)
    - `npx vue-tsc --noEmit` passes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 管理员 -> 审批 API | 需要 JWT + OWNER/ADMIN 权限 |
| 状态转换 | pending -> approved/rejected, 不允许从 rejected 再转换 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | Tampering | 状态转换绕过 | mitigate | RejectResign 仅允许 pending -> rejected, 服务端严格校验当前状态 |
| T-05-11 | Elevation | 非 ADMIN 调用审批 API | mitigate | RequireRole(owner, admin) 中间件校验 |
</threat_model>

<verification>
1. `go build ./...` 编译通过
2. `cd frontend && npx vue-tsc --noEmit` 无新增类型错误
3. POST /api/v1/offboardings/:id/reject 返回 200 成功
4. 重复驳回同一记录返回错误
5. EMP-12: 确认社保减员完成后可自动更新离职状态（通过回调或直接调用）
</verification>

<success_criteria>
- Offboarding 新增 rejected 状态常量
- RejectResign 方法: 验证 pending -> rejected, 不更新 Employee 状态
- 前端行内同意/驳回按钮，el-popconfirm 确认
- "去减员" 按钮跳转社保减员页面（携带 employee_id + name）
- 状态筛选下拉包含 "已驳回"
- EMP-12: 减员完成后离职状态自动更新的回调机制已确认/实现
</success_criteria>

<output>
After completion, create `.planning/phases/05-员工管理增强-组织架构基础/05-04-SUMMARY.md`
</output>
