# P02: H5 Employee + Tool Tab 完整实现

---
phase: "09"
plan: "02"
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - frontend/src/api/employee.ts
  - frontend/src/api/socialinsurance.ts
  - frontend/src/api/salary.ts
  - frontend/src/api/tax.ts
  - frontend/src/views/employee/EmployeeList.vue
  - frontend/src/views/employee/EmployeeDetail.vue
  - frontend/src/views/employee/EmployeeCreate.vue
  - frontend/src/views/employee/InvitationList.vue
  - frontend/src/views/employee/OffboardingList.vue
  - frontend/src/views/tool/SalaryTool.vue
  - frontend/src/views/tool/SITool.vue
  - frontend/src/views/tool/TaxTool.vue
  - frontend/src/views/layout/AppLayout.vue (update active tab)
autonomous: false
requirements:
  - EMPL-01
  - EMPL-02
  - EMPL-03
  - EMPL-04
  - EMPL-05
  - EMPL-06
  - EMPL-07
  - EMPL-08
  - PAYR-01
  - HOME-02
must_haves:
  truths:
    - "Employee list page calls GET /api/v1/employees with pagination"
    - "Employee create/edit form with all fields (name, phone, id_number, position, entry_date)"
    - "Invitation list page calls GET /api/v1/invitations"
    - "Offboarding list shows pending/approved/completed status"
    - "Salary tool: template config + payroll calculate + payroll list"
    - "SI tool: policy list + calculate + enroll/stop buttons"
    - "Tax tool: deduction CRUD + tax calculate + records"
  artifacts:
    - path: frontend/src/views/employee/EmployeeList.vue
      provides: Employee CRUD list with search
    - path: frontend/src/views/tool/SalaryTool.vue
      provides: Salary template + calculate + export

---
## Task 1: API Layer

### frontend/src/api/employee.ts

```typescript
import request from './request'

export interface Employee {
  id: number
  name: string
  phone: string
  id_number: string
  position: string
  entry_date: string
  status: string
}

export interface Invitation {
  id: number
  name: string
  phone: string
  status: string
  created_at: string
  invite_url: string
}

export interface Offboarding {
  id: number
  employee_name: string
  status: string
  resign_reason: string
  last_workday: string
  created_at: string
}

export const employeeApi = {
  list: (params: { page: number; page_size: number; search?: string }) =>
    request.get<{ list: Employee[]; total: number }>('/employees', { params }),
  get: (id: number) => request.get<Employee>(`/employees/${id}`),
  create: (data: Partial<Employee>) => request.post<Employee>('/employees', data),
  update: (id: number, data: Partial<Employee>) => request.put<Employee>(`/employees/${id}`, data),
  delete: (id: number) => request.delete(`/employees/${id}`),
  invitations: (params?: { page: number; page_size: number }) =>
    request.get<{ list: Invitation[]; total: number }>('/invitations', { params }),
  createInvitation: (data: { name: string; phone: string }) =>
    request.post<{ invite_url: string }>('/invitations', data),
  cancelInvitation: (id: number) => request.delete(`/invitations/${id}`),
  offboardings: (params?: { page: number; page_size: number; status?: string }) =>
    request.get<{ list: Offboarding[]; total: number }>('/offboardings', { params }),
  approveOffboarding: (id: number) => request.post(`/offboardings/${id}/approve`, {}),
  completeOffboarding: (id: number) => request.post(`/offboardings/${id}/complete`, {}),
  exportExcel: () => request.get('/employees/export', { responseType: 'blob' }),
}
```

### frontend/src/api/socialinsurance.ts

```typescript
import request from './request'

export const siApi = {
  policies: () => request.get('/social-insurance/policies'),
  calculate: (data: { city: string; year: number; salary_base: number }) =>
    request.post('/social-insurance/calculate', data),
  records: (params?: { page: number; employee_id?: number }) =>
    request.get('/social-insurance/records', { params }),
  enroll: (data: { employee_ids: number[]; policy_id: number; start_month: string }) =>
    request.post('/social-insurance/enroll', data),
  stop: (data: { record_ids: number[]; stop_month: string }) =>
    request.post('/social-insurance/stop', data),
  changeHistory: (recordId: number) =>
    request.get(`/social-insurance/records/${recordId}/history`),
}
```

### frontend/src/api/salary.ts

```typescript
import request from './request'

export const salaryApi = {
  template: () => request.get('/salary/template'),
  updateTemplate: (data: { items: { id: number; is_enabled: boolean }[] }) =>
    request.put('/salary/template', data),
  employeeItems: (employeeId: number, month: string) =>
    request.get('/salary/items', { params: { employee_id: employeeId, month } }),
  setEmployeeItems: (employeeId: number, month: string, items: any[]) =>
    request.put(`/salary/items/${employeeId}`, { month, items }),
  createPayroll: (data: { year: number; month: number; copy_from_month?: string }) =>
    request.post('/salary/payroll', data),
  calculatePayroll: (data: { year: number; month: number }) =>
    request.post('/salary/payroll/calculate', data),
  confirmPayroll: (data: { year: number; month: number }) =>
    request.put('/salary/payroll/confirm', data),
  list: (params: { year: number; month: number; page: number; page_size: number }) =>
    request.get('/salary/payroll', { params }),
  detail: (id: number) => request.get(`/salary/payroll/${id}`),
  recordPayment: (id: number, data: { method: string; paid_at: string }) =>
    request.put(`/salary/payroll/${id}/pay`, data),
  export: (year: number, month: number) =>
    request.get('/salary/payroll/export', { params: { year, month }, responseType: 'blob' }),
}
```

### frontend/src/api/tax.ts

```typescript
import request from './request'

export const taxApi = {
  brackets: () => request.get('/tax/brackets'),
  deductions: () => request.get('/tax/deductions'),
  createDeduction: (data: any) => request.post('/tax/deductions', data),
  updateDeduction: (id: number, data: any) => request.put(`/tax/deductions/${id}`, data),
  deleteDeduction: (id: number) => request.delete(`/tax/deductions/${id}`),
  calculate: (data: { gross_income: number; year: number; month: number }) =>
    request.post('/tax/calculate', data),
  records: (params?: { page: number; year?: number }) =>
    request.get('/tax/records', { params }),
}
```

---

## Task 2: Employee Tab Pages

### frontend/src/views/employee/EmployeeList.vue

Full employee list with:
- Search bar (name/phone/position)
- el-table with columns: 姓名, 手机号, 岗位, 入职日期, 状态, 操作
- Pagination
- Actions: 查看, 编辑, 删除
- "新增员工" button → EmployeeCreate.vue
- "入职邀请" button → InvitationList.vue
- "离职管理" button → OffboardingList.vue

```vue
<template>
  <div class="employee-list">
    <el-card>
      <template #header>
        <div class="flex justify-between items-center">
          <span>员工管理</span>
          <div>
            <el-button @click="$router.push('/employee/invitations')">入职邀请</el-button>
            <el-button @click="$router.push('/employee/offboardings')">离职管理</el-button>
            <el-button type="primary" @click="$router.push('/employee/create')">新增员工</el-button>
          </div>
        </div>
      </template>
      <el-form inline @submit.prevent="load">
        <el-form-item><el-input v-model="search" placeholder="搜索姓名/手机号/岗位" clearable /></el-form-item>
        <el-form-item><el-button type="primary" @click="load">搜索</el-button></el-form-item>
        <el-form-item><el-button @click="handleExport">导出Excel</el-button></el-form-item>
      </el-form>
      <el-table :data="list" stripe v-loading="loading">
        <el-table-column prop="name" label="姓名" />
        <el-table-column prop="phone" label="手机号" />
        <el-table-column prop="position" label="岗位" />
        <el-table-column prop="entry_date" label="入职日期" />
        <el-table-column prop="status" label="状态">
          <template #default="{ row }">{{ statusMap[row.status] }}</template>
        </el-table-column>
        <el-table-column label="操作" width="180">
          <template #default="{ row }">
            <el-button size="small" @click="$router.push(`/employee/${row.id}`)">查看</el-button>
            <el-button size="small" type="primary" @click="$router.push(`/employee/${row.id}/edit`)">编辑</el-button>
          </template>
        </el-table-column>
      </el-table>
      <el-pagination class="mt-4" layout="total,prev,pager,next"
        :total="total" :page="page" :page-size="pageSize" @current-change="load" />
    </el-card>
  </div>
</template>
<script setup lang="ts">
import { ref, onMounted } from 'vue'
import { employeeApi } from '@/api/employee'
import { ElMessage } from 'element-plus'

const search = ref('')
const list = ref([])
const total = ref(0)
const page = ref(1)
const pageSize = ref(20)
const loading = ref(false)

const statusMap: Record<string, string> = {
  active: '在职', probation: '试用期', resigned: '离职', archived: '归档'
}

async function load(p = 1) {
  page.value = p
  loading.value = true
  try {
    const res = await employeeApi.list({ page: p, page_size: pageSize.value, search: search.value })
    list.value = res.list
    total.value = res.total
  } finally {
    loading.value = false
  }
}

function handleExport() {
  employeeApi.exportExcel().then(blob => {
    const url = URL.createObjectURL(blob)
    const a = document.createElement('a'); a.href = url; a.click(); URL.revokeObjectURL(url)
  })
}

onMounted(() => load())
</script>
```

### frontend/src/views/employee/InvitationList.vue

- List invitations with status badges (pending/used/expired/cancelled)
- "发送邀请" button → dialog with name + phone fields
- Copy invite URL button
- Cancel invitation action

### frontend/src/views/employee/OffboardingList.vue

- List offboardings with status (pending_review/approved/completed)
- Approve/complete buttons (OWNER only)
- Show last workday, reason, checklist progress

### frontend/src/views/employee/EmployeeCreate.vue

Form with: 姓名, 手机号, 身份证号, 岗位, 入职日期, 薪资（可选）
POST to /employees. Redirect on success.

---

## Task 3: Tool Tab Pages

### frontend/src/views/tool/SalaryTool.vue

Tab 1 - 薪资模板: GET /salary/template, toggle is_enabled per item, PUT /salary/template

Tab 2 - 工资核算:
- Year/month picker + "复制上月" checkbox
- "创建工资表" button → POST /salary/payroll
- "一键核算" button → POST /salary/payroll/calculate
- "确认锁定" button → PUT /salary/payroll/confirm
- Payroll list table (status badge: draft/calculated/confirmed/paid)

Tab 3 - 导出:
- Select year/month → GET /salary/payroll/export → download .xlsx

### frontend/src/views/tool/SITool.vue

Tab 1 - 政策库: List policies (city, year, effective date)
Tab 2 - 参保操作:
- Employee multi-select
- Policy selector
- "参保预览" → POST /social-insurance/enroll/preview
- "确认参保" → POST /social-insurance/enroll
- "批量停缴" → POST /social-insurance/stop

### frontend/src/views/tool/TaxTool.vue

Tab 1 - 专项附加扣除:
- List deductions with type + amount
- CRUD operations (CREATE/DELETE via API)

Tab 2 - 个税计算:
- Salary input + year/month + deduction IDs
- "计算" → POST /tax/calculate
- Show: 应纳税所得额, 税率, 速算扣除数, 应缴税额

Tab 3 - 申报记录: List /tax/records

---

## Task 4: AppLayout Tab Update

Update router/index.ts to load actual views instead of PlaceholderView:

```typescript
children: [
  { path: '/home', name: 'home', component: () => import('@/views/home/HomeView.vue') },
  { path: '/employee', name: 'employee', component: () => import('@/views/employee/EmployeeList.vue') },
  { path: '/employee/create', name: 'employee-create', component: () => import('@/views/employee/EmployeeCreate.vue') },
  { path: '/employee/invitations', name: 'employee-invitations', component: () => import('@/views/employee/InvitationList.vue') },
  { path: '/employee/offboardings', name: 'employee-offboardings', component: () => import('@/views/employee/OffboardingList.vue') },
  { path: '/employee/:id', name: 'employee-detail', component: () => import('@/views/employee/EmployeeDetail.vue') },
  { path: '/tool', name: 'tool', component: () => import('@/views/tool/ToolHome.vue') },
  { path: '/finance', name: 'finance', component: () => import('@/views/finance/FinanceHome.vue') },
  { path: '/mine', name: 'mine', component: () => import('@/views/mine/MineView.vue') },
],
```
