# P03: H5 Finance + Mine Tab + AUTH-03 城市定位

---
phase: "09"
plan: "03"
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - frontend/src/api/finance.ts
  - frontend/src/views/finance/FinanceHome.vue
  - frontend/src/views/finance/AccountTree.vue
  - frontend/src/views/finance/VoucherList.vue
  - frontend/src/views/finance/VoucherCreate.vue
  - frontend/src/views/finance/InvoiceList.vue
  - frontend/src/views/finance/ExpenseApproval.vue
  - frontend/src/views/mine/MineView.vue
  - frontend/src/views/onboarding/OrgSetup.vue
  - frontend/src/api/finance.ts
autonomous: false
requirements:
  - FINC-01
  - FINC-02
  - FINC-03
  - FINC-04
  - FINC-05
  - FINC-06
  - FINC-07
  - FINC-08
  - FINC-09
  - FINC-10
  - FINC-11
  - FINC-12
  - FINC-13
  - FINC-14
  - FINC-15
  - FINC-16
  - FINC-17
  - FINC-18
  - FINC-19
  - FINC-20
  - FINC-21
  - FINC-22
  - HOME-04
  - AUTH-03
must_haves:
  truths:
    - "Finance tab: account tree + voucher CRUD + invoice list + expense approval"
    - "Mine tab: org info + user info + logout"
    - "AUTH-03: city auto-detection via IP geolocation on onboarding"
  artifacts:
    - path: frontend/src/views/finance/FinanceHome.vue
      provides: Finance tab with 5 sub-pages
    - path: frontend/src/views/onboarding/OrgSetup.vue
      provides: City auto-location via ipapi.co

---
## Task 1: Finance API Layer

### frontend/src/api/finance.ts

```typescript
import request from './request'

export interface Voucher {
  id: number
  period: string
  voucher_no: string
  status: string
  total_debit: string
  total_credit: string
  creator_name: string
  created_at: string
}

export interface Invoice {
  id: number
  invoice_no: string
  type: string
  amount: string
  tax_amount: string
  status: string
  created_at: string
}

export const financeApi = {
  // Accounts
  accountTree: () => request.get('/accounts'),
  createAccount: (data: any) => request.post('/accounts', data),

  // Vouchers
  vouchers: (params: { page: number; period_id?: number; account_id?: number; keyword?: string }) =>
    request.get('/vouchers', { params }),
  createVoucher: (data: { period_id: number; entries: { account_id: number; dc: string; amount: string; summary: string }[] }) =>
    request.post('/vouchers', data),
  submitVoucher: (id: number) => request.post('/vouchers/submit', { id }),
  auditVoucher: (id: number) => request.post('/vouchers/audit', { id }),
  reverseVoucher: (id: number) => request.post('/vouchers/reverse', { id }),

  // Invoices
  invoices: (params?: { page: number; type?: string }) =>
    request.get('/invoices', { params }),
  createInvoice: (data: any) => request.post('/invoices', data),

  // Expenses
  expenses: (params?: { page: number; status?: string }) =>
    request.get('/expenses', { params }),
  approveExpense: (id: number) => request.post(`/expenses/${id}/approve`, {}),
  rejectExpense: (id: number) => request.post(`/expenses/${id}/reject`, {}),

  // Books
  trialBalance: (periodId: number) => request.get('/books/trial-balance', { params: { period_id: periodId } }),
  ledger: (periodId: number, accountId?: number) =>
    request.get('/books/ledger', { params: { period_id: periodId, account_id: accountId } }),
  bookExport: (periodId: number) =>
    request.get('/books/export', { params: { period_id: periodId }, responseType: 'blob' }),

  // Reports
  balanceSheet: (periodId: number) => request.get('/reports/balance-sheet', { params: { period_id: periodId } }),
  incomeStatement: (periodId: number) => request.get('/reports/income-statement', { params: { period_id: periodId } }),
  taxDeclaration: (year: number, month: number) =>
    request.get('/reports/tax-declaration', { params: { year, month } }),
  taxExport: (year: number, month: number) =>
    request.get('/reports/tax-declaration/export', { params: { year, month }, responseType: 'blob' }),

  // Periods
  periods: () => request.get('/periods'),
  closePeriod: (id: number) => request.post(`/periods/${id}/close`, {}),
  revertPeriod: (id: number) => request.post(`/periods/${id}/revert`, { confirm: true }),
}
```

---

## Task 2: Finance Tab Pages

### frontend/src/views/finance/FinanceHome.vue

5-tab tab inside Finance page:

1. **凭证管理** - VoucherList.vue
2. **科目管理** - AccountTree.vue
3. **发票管理** - InvoiceList.vue
4. **报销审批** - ExpenseApproval.vue
5. **账簿报表** - BookReport.vue

```vue
<template>
  <div class="finance-home">
    <el-tabs v-model="activeTab" type="border-card">
      <el-tab-pane label="凭证管理" name="voucher">
        <VoucherList />
      </el-tab-pane>
      <el-tab-pane label="科目管理" name="account">
        <AccountTree />
      </el-tab-pane>
      <el-tab-pane label="发票管理" name="invoice">
        <InvoiceList />
      </el-tab-pane>
      <el-tab-pane label="报销审批" name="expense">
        <ExpenseApproval />
      </el-tab-pane>
      <el-tab-pane label="账簿报表" name="book">
        <BookReport />
      </el-tab-pane>
    </el-tabs>
  </div>
</template>
<script setup lang="ts">
import { ref } from 'vue'
import VoucherList from './VoucherList.vue'
import AccountTree from './AccountTree.vue'
import InvoiceList from './InvoiceList.vue'
import ExpenseApproval from './ExpenseApproval.vue'
import BookReport from './BookReport.vue'

const activeTab = ref('voucher')
</script>
```

### frontend/src/views/finance/VoucherList.vue

- Period selector (get from /periods)
- Search: keyword filter
- Voucher table: 凭证号, 日期, 状态, 借方合计, 贷方合计, 操作
- "新增凭证" button → VoucherCreate.vue (dialog)
- Actions: 查看详情, 提交, 审核, 红冲
- Status badge: draft(草稿)/submitted(待审核)/audited(已审核)/reversed(已红冲)

VoucherCreate dialog:
- Period selector
- Dynamic rows: 科目(级联选择) + 借贷方向(借/贷) + 金额 + 摘要
- Real-time balance display: 借方合计 vs 贷方合计
- Submit button disabled if unbalanced

### frontend/src/views/finance/AccountTree.vue

- GET /accounts → el-tree component
- Show category badge (资产/负债/权益/成本/损益)
- Expand/collapse all
- "新增科目" button (for custom accounts)

### frontend/src/views/finance/InvoiceList.vue

- Type filter (全部/进项/销项)
- Table: 发票号, 类型, 金额, 税率, 税额, 关联凭证, 日期
- "登记发票" button (dialog form)

### frontend/src/views/finance/ExpenseApproval.vue

- Status tab: 全部/待审批/已通过/已支付/已驳回
- Table: 员工姓名, 类型, 金额, 状态, 提交时间
- Actions: 审批通过, 驳回 (show reject reason dialog)
- Link to voucher if approved

### frontend/src/views/finance/BookReport.vue

Tabs: 科目余额表 | 总账 | 报表 | 期间管理

**科目余额表 tab:**
- Period selector → GET /books/trial-balance
- el-table with: 编码, 名称, 期初, 借方, 贷方, 期末
- "导出Excel" → GET /books/export

**报表 tab:**
- Period selector
- "资产负债表" + "利润表" buttons
- GET /reports/balance-sheet and GET /reports/income-statement
- Display data in formatted tables

**期间管理 tab:**
- Period list with status badges
- "结账" button (POST /periods/:id/close)
- "反结账" button (POST /periods/:id/revert, requires confirm=true)

---

## Task 3: Mine Tab

### frontend/src/views/mine/MineView.vue

```vue
<template>
  <div class="mine-view">
    <el-card class="mb-4">
      <div class="flex items-center gap-4">
        <el-avatar :size="60" style="background: #409EFF">{{ user?.name?.[0] }}</el-avatar>
        <div>
          <div class="text-lg font-bold">{{ user?.name }}</div>
          <div class="text-gray-500 text-sm">{{ user?.phone }}</div>
          <div class="text-gray-400 text-xs">{{ org?.name }}</div>
        </div>
      </div>
    </el-card>

    <el-card class="mb-4">
      <el-descriptions :column="1" border>
        <el-descriptions-item label="企业名称">{{ org?.name }}</el-descriptions-item>
        <el-descriptions-item label="统一社会信用代码">{{ org?.credit_code }}</el-descriptions-item>
        <el-descriptions-item label="城市">{{ org?.city }}</el-descriptions-item>
        <el-descriptions-item label="联系人">{{ org?.contact_name }}</el-descriptions-item>
        <el-descriptions-item label="联系电话">{{ org?.contact_phone }}</el-descriptions-item>
      </el-descriptions>
      <el-button type="primary" class="mt-4" @click="handleEditOrg">编辑企业信息</el-button>
    </el-card>

    <el-card>
      <el-space direction="vertical" fill style="width:100%">
        <el-button @click="handleChangePassword">修改密码</el-button>
        <el-button @click="handleLogout" type="danger" plain>退出登录</el-button>
      </el-space>
    </el-card>
  </div>
</template>
<script setup lang="ts">
import { ref, onMounted } from 'vue'
import { useRouter } from 'vue-router'
import { useUserStore } from '@/stores/user'

const router = useRouter()
const userStore = useUserStore()
const user = ref(userStore.user)
const org = ref(userStore.org)

function handleLogout() {
  userStore.logout()
  router.push('/login')
}

function handleEditOrg() {
  // Navigate to org edit page
}

function handleChangePassword() {
  // Show change password dialog
}
</script>
```

---

## Task 4: AUTH-03 城市自动定位

In the onboarding/org setup flow:

### frontend/src/views/onboarding/OrgSetup.vue

```typescript
async function detectCity() {
  try {
    const res = await fetch('https://ipapi.co/json/')
    const data = await res.json()
    if (data.city && data.country_code === 'CN') {
      // Match city from our city list
      const matched = cityList.value.find(c => c.name.includes(data.city) || data.city.includes(c.name))
      if (matched) {
        form.city_id = matched.id
        cityAutoDetected.value = true
      }
    }
  } catch {
    // Silently fail, user can select manually
  }
}
```

Show a "自动定位" button next to city selector. If auto-detected, show a small tag "已定位: {city_name}".
