---
phase: "01-login-boss"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/user/dto.go
  - internal/user/handler.go
  - internal/user/service.go
  - internal/user/repository.go
autonomous: false
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04

must_haves:
  truths:
    - 手机号+密码登录返回 JWT token
    - OWNER/ADMIN 登录成功，MEMBER 登录返回 403 Forbidden
    - MEMBER 403 响应包含清晰的拒绝文案
    - 登录成功后返回 onboarding_required 字段
    - GET /api/v1/auth/me 返回当前用户信息+企业信息
  artifacts:
    - path: internal/user/dto.go
      provides: 密码登录请求/响应 DTO
      min_lines: 10
    - path: internal/user/handler.go
      provides: POST /auth/login/password + GET /auth/me handler
      exports: LoginPassword, GetMe
    - path: internal/user/service.go
      provides: 密码验证 + MEMBER 403 + onboarding 判断逻辑
      exports: LoginPassword, GetMe
    - path: internal/user/repository.go
      provides: 密码哈希存储和校验查询
      exports: FindByPhoneHash, FindByIDWithOrg
  key_links:
    - from: handler.go
      to: service.go
      via: h.svc.LoginPassword(ctx, phone, password)
      pattern: LoginPassword
    - from: service.go
      to: repository.go
      via: s.repo.FindByPhoneHash + bcrypt.CompareHashAndPassword
      pattern: CompareHashAndPassword
    - from: handler.go
      to: service.go
      via: h.svc.GetMe(ctx, userID, orgID)
      pattern: GetMe
---

<objective>
扩展后端登录接口：新增手机号+密码登录（`POST /api/v1/auth/login/password`），MEMBER 角色返回 403，新增 `GET /api/v1/auth/me` 返回用户+企业信息。密码使用 bcrypt 哈希存储。

Purpose: 为前端 LoginView.vue 提供密码登录接口和 MEMBER 角色过滤能力
Output: 密码登录 handler + service + repository + DTO，/auth/me 接口
</objective>

<context>
@internal/user/handler.go
@internal/user/service.go
@internal/user/repository.go
@internal/user/dto.go
@internal/common/model/user.go
@internal/common/model/org.go
@pkg/jwt/jwt.go

# Interface contracts

From internal/user/dto.go:
```go
type LoginRequest struct {
  Phone string `json:"phone" binding:"required,len=11"`
  Code  string `json:"code" binding:"required,len=6"`
}
type LoginResponse struct {
  AccessToken        string `json:"access_token"`
  RefreshToken       string `json:"refresh_token"`
  OnboardingRequired bool   `json:"onboarding_required"`
}
```

From internal/common/model/user.go:
```go
type User struct {
  BaseModel
  Phone     string  // AES-256-GCM encrypted, stored as base64
  PhoneHash string  // SHA-256 of plaintext phone
  Name      string
  Role      string  // "owner" | "admin" | "member"
  Status    string  // "active" | "inactive"
  OrgID     int64   // gorm会自动注入 via BaseModel
}
```

From pkg/jwt/jwt.go:
```go
func GenerateAccessToken(userID, orgID int64, role, secret string, ttl time.Duration) (string, error)
func GenerateRefreshToken(userID int64, secret string, ttl time.Duration) (string, error)
```

# bcrypt availability

`golang.org/x/crypto` 已作为 indirect 依赖存在于 go.mod，无需新增依赖。
直接使用：
```go
import "golang.org/x/crypto/bcrypt"
bcrypt.GenerateFromPassword([]byte(password), bcrypt.DefaultCost)
bcrypt.CompareHashAndPassword(hash, []byte(password))
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: 添加密码登录 DTO</name>
  <files>internal/user/dto.go</files>
  <read_first>
    internal/user/dto.go
  </read_first>
  <action>
在 internal/user/dto.go 末尾添加以下类型定义：

```go
// PasswordLoginRequest 密码登录请求
type PasswordLoginRequest struct {
  Phone    string `json:"phone" binding:"required,len=11"`
  Password string `json:"password" binding:"required,min=6,max=20"`
}

// PasswordLoginResponse 密码登录响应（与 LoginResponse 结构一致）
type PasswordLoginResponse = LoginResponse

// MeResponse /auth/me 响应
type MeResponse struct {
  ID       int64        `json:"id"`
  Name     string       `json:"name"`
  Phone    string       `json:"phone"`     // 明文手机号
  Role     string       `json:"role"`     // "owner" | "admin" | "member"
  Org      *OrgInfo     `json:"org,omitempty"`
  OnboardingRequired bool `json:"onboarding_required"`
}

// OrgInfo 企业基本信息（用于 /auth/me 响应）
type OrgInfo struct {
  ID         int64  `json:"id"`
  Name       string `json:"name"`
  CreditCode string `json:"credit_code"`
  City       string `json:"city"`
}
```

注意：
- PasswordLoginResponse 使用类型别名 = LoginResponse（避免重复字段定义）
- MeResponse.Org 是指针，MEMBER 账号可能没有 org（nil）
- Phone 字段在 MeResponse 中返回明文（前端需要，handler 层负责解密）
</action>
  <verify>
    <automated>grep -c "PasswordLoginRequest\|PasswordLoginResponse\|MeResponse\|OrgInfo" internal/user/dto.go</automated>
  </verify>
  <done>
    DTO 中包含 PasswordLoginRequest（phone + password 字段）、PasswordLoginResponse（LoginResponse 别名）、MeResponse（用户+企业信息）、OrgInfo（企业基本信息）
  </done>
</task>

<task type="auto">
  <name>Task 2: 添加密码登录 Service 逻辑</name>
  <files>internal/user/service.go</files>
  <read_first>
    internal/user/service.go
    internal/common/crypto/crypto.go
  </read_first>
  <action>
在 internal/user/service.go 中做以下扩展：

**步骤1: 添加 bcrypt import：**
```go
import (
  "golang.org/x/crypto/bcrypt"
  // ... 其他 imports
)
```

**步骤2: 在 Service 结构体中添加密码哈希列（用于存储用户设置密码时的哈希）：**
User model 中需要添加 PasswordHash 字段。检查 internal/common/model/user.go：
如果 BaseModel 中没有预留 PasswordHash 字段，需要在 User struct 中添加：
```go
PasswordHash string `gorm:"column:password_hash;type:varchar(200)" json:"-"`
```

**步骤3: 添加 RegisterPassword 方法（设置/重置密码）：**
```go
// RegisterPassword 设置用户密码（bcrypt 哈希）
func (s *Service) RegisterPassword(ctx context.Context, phone, password string) error {
  phoneHash := crypto.HashSHA256(phone)
  user, err := s.repo.FindByPhoneHash(phoneHash)
  if err != nil {
    return fmt.Errorf("用户不存在: %w", err)
  }

  hashed, err := bcrypt.GenerateFromPassword([]byte(password), bcrypt.DefaultCost)
  if err != nil {
    return fmt.Errorf("密码哈希失败: %w", err)
  }

  return s.repo.UpdateUserPassword(user.ID, string(hashed))
}
```

**步骤4: 在 Repository 中添加 UpdateUserPassword 方法（见 Task 4）。**

**步骤5: 添加 LoginPassword 方法（per D-06, D-20）：**
```go
// LoginPassword 手机号+密码登录
func (s *Service) LoginPassword(ctx context.Context, phone, password string) (*LoginResponse, error) {
  phoneHash := crypto.HashSHA256(phone)
  user, err := s.repo.FindByPhoneHash(phoneHash)
  if err != nil {
    return nil, fmt.Errorf("手机号或密码错误")
  }

  // MEMBER 角色拒绝登录（per D-06, D-20）
  if user.Role == "member" {
    return nil, fmt.Errorf("MEMBER_ROLE_FORBIDDEN")
  }

  // 密码校验
  if user.PasswordHash == "" {
    return nil, fmt.Errorf("该账号未设置密码，请使用手机验证码登录")
  }
  if err := bcrypt.CompareHashAndPassword([]byte(user.PasswordHash), []byte(password)); err != nil {
    return nil, fmt.Errorf("手机号或密码错误")
  }

  // 生成 token
  accessToken, _ := jwt.GenerateAccessToken(user.ID, user.OrgID, user.Role, s.jwtCfg.Secret, s.jwtCfg.AccessTTL)
  refreshToken, _ := jwt.GenerateRefreshToken(user.ID, s.jwtCfg.Secret, s.jwtCfg.RefreshTTL)

  // 判断 onboarding 状态
  onboardingRequired := false
  org, _ := s.repo.FindByOrgID(user.OrgID)
  if org != nil && org.Status == "inactive" {
    onboardingRequired = true
  }

  return &LoginResponse{
    AccessToken:        accessToken,
    RefreshToken:       refreshToken,
    OnboardingRequired: onboardingRequired,
  }, nil
}
```

**步骤6: 修改 Login（短信验证码登录）方法，在找到用户后增加 MEMBER 角色检查：**
在 Login 方法中，找到用户后（第112行附近）添加：
```go
// MEMBER 角色拒绝登录（per D-06, D-20）
if user.Role == "member" {
  return nil, fmt.Errorf("MEMBER_ROLE_FORBIDDEN")
}
```
在 handler.go 的 Login handler 中，当 error 包含 "MEMBER_ROLE_FORBIDDEN" 时返回 403（见 Task 3）。

**步骤7: 添加 GetMe 方法：**
```go
// GetMe 获取当前用户信息（含企业信息）
func (s *Service) GetMe(ctx context.Context, userID, orgID int64) (*MeResponse, error) {
  user, err := s.repo.FindUserByID(orgID, userID)
  if err != nil {
    return nil, fmt.Errorf("用户不存在")
  }

  // 解密手机号
  aesKey := []byte(s.crypto.AESKey)
  decryptedPhone, _ := crypto.Decrypt(user.Phone, aesKey)

  resp := &MeResponse{
    ID:       user.ID,
    Name:     user.Name,
    Phone:    decryptedPhone,
    Role:     user.Role,
    OnboardingRequired: false,
  }

  if orgID > 0 {
    org, err := s.repo.FindByOrgID(orgID)
    if err == nil && org != nil {
      resp.Org = &OrgInfo{
        ID:         org.ID,
        Name:       org.Name,
        CreditCode: org.CreditCode,
        City:       org.City,
      }
      if org.Status == "inactive" {
        resp.OnboardingRequired = true
      }
    }
  }

  return resp, nil
}
```
</action>
  <verify>
  <automated>grep -c "LoginPassword\|GetMe\|RegisterPassword\|MEMBER_ROLE_FORBIDDEN\|bcrypt\|PasswordHash\|FindUserByID" internal/user/service.go</automated>
  </verify>
  <done>
    service.go 包含 LoginPassword 方法（手机号+密码登录），包含 bcrypt 密码校验，包含 MEMBER 角色检查（返回 MEMBER_ROLE_FORBIDDEN 错误），包含 GetMe 方法（返回用户+企业信息），Login 方法也包含 MEMBER 检查。
  </done>
</task>

<task type="auto">
  <name>Task 3: 添加密码登录和 GetMe Handler</name>
  <files>internal/user/handler.go</files>
  <read_first>
    internal/user/handler.go
    internal/common/response/response.go
  </read_first>
  <action>
修改 internal/user/handler.go：

**步骤1: 在 RegisterRoutes 中添加新路由：**
```go
func (h *Handler) RegisterRoutes(rg *gin.RouterGroup, authMiddleware gin.HandlerFunc) {
  authGroup := rg.Group("")
  authGroup.Use(authMiddleware)

  rg.POST("/auth/send-code", h.SendCode)
  rg.POST("/auth/login", h.Login)
  rg.POST("/auth/login/password", h.LoginPassword)       // 新增：密码登录
  rg.POST("/auth/refresh", h.Refresh)
  authGroup.POST("/auth/logout", h.Logout)
  authGroup.GET("/auth/me", h.GetMe)                      // 新增：获取当前用户信息
  authGroup.PUT("/org/onboarding", h.CompleteOnboarding)
  // ... 子账号路由保持不变
}
```

注意：密码登录不需要 authMiddleware（用户还没登录），所以用 `rg.POST` 而不是 `authGroup.POST`。

**步骤2: 添加 LoginPassword Handler（per D-06, D-20, D-19）：**
```go
func (h *Handler) LoginPassword(c *gin.Context) {
  var req PasswordLoginRequest
  if err := c.ShouldBindJSON(&req); err != nil {
    response.BadRequest(c, "参数错误: "+err.Error())
    return
  }

  resp, err := h.svc.LoginPassword(c.Request.Context(), req.Phone, req.Password)
  if err != nil {
    if strings.Contains(err.Error(), "MEMBER_ROLE_FORBIDDEN") {
      // MEMBER 角色返回 403（per D-06, D-19, D-20）
      response.Error(c, http.StatusForbidden, 10010, "您的账号为员工账号，请使用员工端微信小程序登录")
      return
    }
    if strings.Contains(err.Error(), "该账号未设置密码") {
      response.Error(c, http.StatusUnauthorized, 10011, err.Error())
      return
    }
    response.Error(c, http.StatusUnauthorized, 10012, "手机号或密码错误")
    return
  }
  response.Success(c, resp)
}
```

**步骤3: 添加 GetMe Handler：**
```go
func (h *Handler) GetMe(c *gin.Context) {
  userID := c.GetInt64("user_id")
  orgID := c.GetInt64("org_id")

  resp, err := h.svc.GetMe(c.Request.Context(), userID, orgID)
  if err != nil {
    response.Error(c, http.StatusInternalServerError, 10013, "获取用户信息失败")
    return
  }
  response.Success(c, resp)
}
```

**步骤4: 修改 Login Handler，增加 MEMBER 403 处理：**
```go
func (h *Handler) Login(c *gin.Context) {
  var req LoginRequest
  if err := c.ShouldBindJSON(&req); err != nil {
    response.BadRequest(c, "参数错误: "+err.Error())
    return
  }
  resp, err := h.svc.Login(c.Request.Context(), req.Phone, req.Code)
  if err != nil {
    if strings.Contains(err.Error(), "MEMBER_ROLE_FORBIDDEN") {
      response.Error(c, http.StatusForbidden, 10010, "您的账号为员工账号，请使用员工端微信小程序登录")
      return
    }
    response.Error(c, http.StatusUnauthorized, 10003, err.Error())
    return
  }
  response.Success(c, resp)
}
```

**步骤5: 添加 strings import：**
```go
import (
  "net/http"
  "strconv"
  "strings"  // 新增

  "github.com/gin-gonic/gin"
  "github.com/wencai/easyhr/internal/common/middleware"
  "github.com/wencai/easyhr/internal/common/response"
)
```
</action>
  <verify>
  <automated>grep -c "LoginPassword\|GetMe\|StatusForbidden\|MEMBER_ROLE_FORBIDDEN\|/auth/login/password\|/auth/me\|strings.Contains" internal/user/handler.go</automated>
  </verify>
  <done>
    handler.go 中 POST /auth/login/password 路由已注册，GET /auth/me 路由已注册，LoginPassword handler 包含 MEMBER 403 返回（status=403，文案="您的账号为员工账号，请使用员工端微信小程序登录"），Login handler 也包含 MEMBER 403 处理。
  </done>
</task>

<task type="auto">
  <name>Task 4: 添加 Repository 密码相关方法</name>
  <files>internal/user/repository.go</files>
  <read_first>
    internal/user/repository.go
    internal/common/model/user.go
  </read_first>
  <action>
修改 internal/user/repository.go：

**步骤1: 添加 UpdateUserPassword 方法（在 DeleteUser 方法之前）：**
```go
// UpdateUserPassword 更新用户密码哈希
func (r *Repository) UpdateUserPassword(userID int64, passwordHash string) error {
  return r.db.Model(&model.User{}).Where("id = ?", userID).Update("password_hash", passwordHash).Error
}
```

**步骤2: 确认 User model 包含 PasswordHash 字段。**
如果 internal/common/model/user.go 中的 User struct 没有 password_hash 列：
在 User struct 中添加：
```go
PasswordHash string `gorm:"column:password_hash;type:varchar(200)" json:"-"`
```

**步骤3: 确认 FindUserByID 方法存在（已在 repository.go 第71行）：**
```go
func (r *Repository) FindUserByID(orgID, userID int64) (*model.User, error) {
  var user model.User
  err := r.db.Scopes(middleware.TenantScope(orgID)).Where("id = ?", userID).First(&user).Error
  if err != nil {
    return nil, err
  }
  return &user, nil
}
```

注意：FindUserByID 使用 TenantScope（需 orgID），而 GetMe handler 传入的 orgID 来自 JWT token 一定有效，所以可以这样用。
</action>
  <verify>
  <automated>grep -c "UpdateUserPassword\|FindUserByID\|password_hash\|PasswordHash" internal/user/repository.go internal/common/model/user.go</automated>
  </verify>
  <done>
    repository.go 包含 UpdateUserPassword 方法，model/user.go 包含 PasswordHash 字段，GORM 会在下次迁移时添加该列。
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 5: 人工验证后端接口</name>
  <what-built>完整的后端密码登录接口 + /auth/me 接口</what-built>
  <how-to-verify>
    1. 启动后端服务：cd /Users/wencai/github/EasyHR && make run（或 go run ./cmd/server）
    2. 验证密码登录（需要先用验证码登录设置密码）：
       a. 先用 POST /auth/send-code + POST /auth/login 登录（已有账号）
       b. 调用 PUT /auth/me 或其他方式设置密码（目前无设置密码接口，可直接通过 DB 设置 bcrypt 哈希）
       c. POST /api/v1/auth/login/password {"phone": "13800138000", "password": "xxx"}
       d. 期望：返回 access_token + refresh_token + onboarding_required
    3. 验证 MEMBER 403：
       a. 用一个 role=member 的账号尝试 POST /auth/login 或 POST /auth/login/password
       b. 期望：返回 403，body: {"code": 10010, "message": "您的账号为员工账号，请使用员工端微信小程序登录"}
    4. 验证 /auth/me：
       a. GET /api/v1/auth/me（带上 Authorization: Bearer token）
       b. 期望：返回 {id, name, phone, role, org{id, name, credit_code, city}, onboarding_required}
  </how-to-verify>
  <resume-signal>Type "approved" 或描述问题</resume-signal>
</task>

</tasks>

<verification>
## 整体验证

1. `go build ./cmd/server` 编译通过
2. `go test ./internal/user/...` 测试通过
3. POST /auth/login/password 正常返回 JWT
4. MEMBER 账号登录返回 403
5. GET /auth/me 返回完整用户+企业信息
</verification>

<success_criteria>
- POST /api/v1/auth/login/password 实现：手机号+密码登录，返回 JWT
- OWNER/ADMIN 登录成功，MEMBER 返回 HTTP 403（状态码）+ 拒绝文案
- 拒绝文案："您的账号为员工账号，请使用员工端微信小程序登录"
- GET /api/v1/auth/me 实现：返回用户信息（id/name/phone/role）+ 企业信息（id/name/credit_code/city）+ onboarding_required
- 密码使用 bcrypt 哈希存储（不存储明文）
- Login（验证码登录）也包含 MEMBER 403 检查
</success_criteria>

<output>
After completion, create `.planning/phases/01-login-boss/01-02-SUMMARY.md`
</output>
