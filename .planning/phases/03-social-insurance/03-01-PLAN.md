---
phase: 03-social-insurance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/socialinsurance/model.go
  - internal/socialinsurance/dto.go
  - internal/socialinsurance/repository.go
  - internal/socialinsurance/service.go
  - internal/socialinsurance/handler.go
  - internal/socialinsurance/service_test.go
  - internal/socialinsurance/repository_test.go
  - cmd/server/main.go
  - go.mod
  - go.sum
autonomous: true
requirements:
  - SOCL-01

must_haves:
  truths:
    - "管理员可在H5后台创建/编辑/删除30+城市的社保政策（五险一金比例+基数上下限）"
    - "系统根据城市ID+年度查询最新有效政策记录"
    - "根据员工薪资和城市政策自动计算各险种企业和个人缴费金额（基数 clamp 到上下限范围内）"
    - "工伤保险和生育保险的个人缴费比例为0（企业全额承担）"
  artifacts:
    - path: "internal/socialinsurance/model.go"
      provides: "SocialInsurancePolicy, InsuranceItem, FiveInsurances 数据模型"
      exports: ["SocialInsurancePolicy", "InsuranceItem", "FiveInsurances"]
    - path: "internal/socialinsurance/repository.go"
      provides: "政策库 CRUD + 按城市+年度查询"
      exports: ["Repository", "NewRepository"]
    - path: "internal/socialinsurance/service.go"
      provides: "基数计算引擎 CalculateInsuranceAmounts"
      exports: ["Service", "NewService", "CalculateInsuranceAmounts"]
    - path: "internal/socialinsurance/handler.go"
      provides: "政策管理 HTTP 端点"
      exports: ["Handler", "NewHandler", "RegisterRoutes"]
  key_links:
    - from: "internal/socialinsurance/model.go"
      to: "internal/common/model/base.go"
      via: "嵌入 BaseModel（但政策库 org_id=0 表示全局共享）"
      pattern: "model\\.BaseModel"
    - from: "internal/socialinsurance/repository.go"
      to: "gorm.io/datatypes"
      via: "JSONType[FiveInsurances] JSONB 字段"
      pattern: "datatypes\\.JSONType"
    - from: "internal/socialinsurance/handler.go"
      to: "cmd/server/main.go"
      via: "路由注册 + AutoMigrate"
      pattern: "socialinsurance.*RegisterRoutes"
    - from: "internal/socialinsurance/service.go"
      to: "internal/city/model.go"
      via: "city_id 关联城市名称"
      pattern: "city\\.Cities"
---

<objective>
创建社保政策库数据模型和基数计算引擎。管理员可在H5后台维护30+城市的五险一金政策（企业/个人缴费比例、基数上下限），系统根据员工城市+薪资自动匹配政策并计算各险种缴费金额。

Purpose: 社保政策库是整个社保模块的基础（SOCL-01），所有参保、停缴、缴费提醒功能都依赖政策数据。
Output: 社保政策库 CRUD API、基数计算引擎、单元测试
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-social-insurance/03-CONTEXT.md
@.planning/phases/03-social-insurance/03-RESEARCH.md
@.planning/phases/03-social-insurance/03-VALIDATION.md

<interfaces>
<!-- Phase 1-2 已有接口，社保模块需要对接 -->

From internal/common/model/base.go:
```go
type BaseModel struct {
    ID        int64          `gorm:"primaryKey;autoIncrement" json:"id"`
    OrgID     int64          `gorm:"column:org_id;index;not null" json:"org_id"`
    CreatedBy int64          `gorm:"column:created_by" json:"created_by"`
    CreatedAt time.Time      `gorm:"column:created_at;autoCreateTime" json:"created_at"`
    UpdatedBy int64          `gorm:"column:updated_by" json:"updated_by"`
    UpdatedAt time.Time      `gorm:"column:updated_at;autoUpdateTime" json:"updated_at"`
    DeletedAt gorm.DeletedAt `gorm:"column:deleted_at;index" json:"-"`
}
```

From internal/city/model.go:
```go
type City struct {
    ID       int    `json:"id"`
    Name     string `json:"name"`
    Province string `json:"province"`
}
// 共37个城市，ID 1-37
```

From internal/common/response/:
```go
func Success(c *gin.Context, data interface{})
func Error(c *gin.Context, httpStatus, code int, msg string)
func BadRequest(c *gin.Context, msg string)
func PageSuccess(c *gin.Context, list interface{}, total int64, page, pageSize int)
```

From internal/common/middleware/:
```go
func RequireRole(roles ...string) gin.HandlerFunc  // RBAC 中间件
func TenantScope(orgID int64) func(db *gorm.DB) *gorm.DB  // 多租户 Scope
```

From internal/employee/model.go:
```go
type Employee struct {
    model.BaseModel
    Name     string  `gorm:"column:name;type:varchar(50);not null;index"`
    Position string  `gorm:"column:position;type:varchar(100);not null;index"`
    Status   string  `gorm:"column:status;type:varchar(20);not null;default:pending;index"`
    // ... 其他字段
}
```

From cmd/server/main.go 路由注册模式:
```go
empRepo := employee.NewRepository(db)
empSvc := employee.NewService(empRepo, cfg.Crypto)
empHandler := employee.NewHandler(empSvc)
empHandler.RegisterRoutes(v1, authMiddleware)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 社保政策库模型 + Repository + 基数计算 Service</name>
  <files>
    internal/socialinsurance/model.go
    internal/socialinsurance/dto.go
    internal/socialinsurance/repository.go
    internal/socialinsurance/service.go
    internal/socialinsurance/service_test.go
    internal/socialinsurance/repository_test.go
  </files>
  <read_first>
    internal/common/model/base.go
    internal/common/middleware/tenant.go
    internal/city/model.go
    internal/employee/model.go
    internal/employee/repository.go
    .planning/phases/03-social-insurance/03-CONTEXT.md
    .planning/phases/03-social-insurance/03-RESEARCH.md
  </read_first>
  <behavior>
    - TestCreatePolicy: 创建北京2025年社保政策，验证JSONB字段正确存储和读取
    - TestFindPolicyByCityAndYear: 查询城市ID=1（北京）且 effective_year<=2025 的最新政策
    - TestCalculateInsuranceAmounts: 给定薪资10000和北京2025政策，验证各险种基数正确clamp到上下限，企业和个人金额计算正确
    - TestCalculateInsuranceAmounts_BelowLower: 资薪3000低于下限7162时，基数应使用下限7162
    - TestCalculateInsuranceAmounts_AboveUpper: 资薪50000高于上限35811时，基数应使用上限35811
    - TestWorkInjuryAndMaternityPersonalRateZero: 工伤保险和生育保险的个人缴费金额必须为0
    - TestListPolicies: 政策列表分页查询
    - TestUpdatePolicy: 更新政策JSONB配置
    - TestDeletePolicy: 软删除政策记录
  </behavior>
  <action>
    1. 创建 `internal/socialinsurance/model.go`:
       - 定义 InsuranceItem 结构体: CompanyRate float64, PersonalRate float64, BaseLower float64, BaseUpper float64（per D-03）
       - 定义 FiveInsurances 结构体: Pension, Medical, Unemployment, WorkInjury, Maternity, HousingFund 各为 InsuranceItem（per D-03）
       - 定义 SocialInsurancePolicy 结构体: 嵌入 BaseModel，CityID int（索引），EffectiveYear int（索引），Config datatypes.JSONType[FiveInsurances]（gorm type:jsonb）（per D-01/D-04）
       - **重要：政策库为全局共享数据，不使用 TenantScope**。Repository 方法中不注入 org_id。BaseModel.OrgID 设为 0 表示全局数据（per RESEARCH Open Question 1）
       - TableName() 返回 "social_insurance_policies"

    2. 创建 `internal/socialinsurance/dto.go`:
       - CreatePolicyRequest: CityID int `binding:"required"`, EffectiveYear int `binding:"required"`, Config FiveInsurances `binding:"required"`
       - UpdatePolicyRequest: Config FiveInsurances `binding:"required"`
       - PolicyResponse: ID, CityID, CityName（从 city.Cities 查）, EffectiveYear, Config, CreatedAt, UpdatedAt
       - CalculateRequest: CityID int `binding:"required"`, Salary float64 `binding:"required,gt=0"`, Year int `binding:"required"`
       - InsuranceAmountDetail: Name string, Base float64, CompanyRate float64, CompanyAmount float64, PersonalRate float64, PersonalAmount float64
       - CalculateResponse: CityName string, Salary float64, BaseAmount float64, TotalCompany float64, TotalPersonal float64, Items []InsuranceAmountDetail

    3. 创建 `internal/socialinsurance/repository.go`:
       - Repository struct { db *gorm.DB }
       - NewRepository(db *gorm.DB) *Repository
       - Create(policy *SocialInsurancePolicy) error
       - FindByID(id int64) (*SocialInsurancePolicy, error) — 无需 org_id，全局数据
       - FindByCityAndYear(cityID int, year int) (*SocialInsurancePolicy, error) — 查询条件 `city_id = ? AND effective_year <= ? ORDER BY effective_year DESC LIMIT 1`（per D-04 和 Pitfall 3）
       - List(cityID int, page, pageSize int) ([]SocialInsurancePolicy, int64, error) — 支持按城市筛选
       - Update(id int64, updates map[string]interface{}) error
       - Delete(id int64) error — 软删除（BaseModel 有 DeletedAt）
       - **不使用 TenantScope** — 政策库是全局数据，org_id=0

    4. 创建 `internal/socialinsurance/service.go`:
       - Service struct { repo *Repository }
       - NewService(repo *Repository) *Service
       - CreatePolicy(policy *SocialInsurancePolicy) error — 设置 OrgID=0
       - GetPolicy(id int64) (*PolicyResponse, error)
       - ListPolicies(cityID int, page, pageSize int) ([]PolicyResponse, int64, error) — 查询后关联 city.Cities 获取城市名
       - UpdatePolicy(id int64, req *UpdatePolicyRequest) error
       - DeletePolicy(id int64) error
       - CalculateInsuranceAmounts(cityID int, salary float64, year int) (*CalculateResponse, error):
         a. 调用 repo.FindByCityAndYear(cityID, year) 获取政策
         b. 对每个险种: base = clamp(salary, item.BaseLower, item.BaseUpper)
         c. companyAmount = base * item.CompanyRate
         d. personalAmount = base * item.PersonalRate
         e. 汇总 totalCompany 和 totalPersonal
         f. 返回 CalculateResponse
       - GetSocialInsuranceDeduction(orgID, employeeID int64, month string) — 预留接口（D-12），本 Plan 只定义空方法签名，Plan 02 实现

    5. 创建 `internal/socialinsurance/service_test.go`:
       - 使用 SQLite 内存数据库（与 Phase 1-2 模式一致）
       - SetupTestDB 函数: 自动迁移 SocialInsurancePolicy
       - TestCreatePolicy: 创建政策记录，验证 JSONB 存取
       - TestFindPolicyByCityAndYear: 插入多条年度记录，验证取最新
       - TestCalculateInsuranceAmounts: 使用北京2025模拟数据（养老16%/8%、医疗9.8%/2%、失业0.5%/0.5%、工伤0.2%/0%、生育0.8%/0%、公积金12%/12%，下限7162，上限35811），验证各金额计算
       - TestCalculateInsuranceAmounts_BelowLower: 薪资3000，验证基数=7162
       - TestCalculateInsuranceAmounts_AboveUpper: 薪资50000，验证基数=35811
       - TestWorkInjuryAndMaternityPersonalRateZero: 验证工伤/生育 personal_amount=0

    6. 创建 `internal/socialinsurance/repository_test.go`:
       - TestPolicyCRUD: 创建、查询、更新、删除政策
       - TestFindByCityAndYear_MultipleYears: 验证取 effective_year 最大的记录
       - TestListPolicies_Pagination: 验证分页查询
       - TestSoftDelete: 验证软删除后查询不到

    7. 安装依赖: `go get github.com/go-co-op/gocron/v2@v2.19.1 && go get github.com/go-co-op/gocron-redis-lock/v2@v2.2.1`（为 Plan 03 提前安装，避免后续 go.mod 冲突）
  </action>
  <verify>
    <automated>cd /Users/wencai/github/EasyHR && go test ./internal/socialinsurance/... -count=1 -v</automated>
  </verify>
  <done>
    - SocialInsurancePolicy 模型使用 datatypes.JSONType[FiveInsurances] 存储 JSONB
    - Repository 支持 CRUD + FindByCityAndYear（取 effective_year<=year 的最新记录）
    - Service.CalculateInsuranceAmounts 正确计算各险种金额，基数 clamp 到上下限
    - 工伤/生育保险个人缴费金额为0
    - 所有单元测试 PASS
  </done>
</task>

<task type="auto">
  <name>Task 2: 政策管理 Handler + 路由注册 + main.go 集成</name>
  <files>
    internal/socialinsurance/handler.go
    cmd/server/main.go
  </files>
  <read_first>
    internal/employee/handler.go
    internal/common/response/response.go
    internal/common/middleware/auth.go
    cmd/server/main.go
    internal/socialinsurance/model.go
    internal/socialinsurance/dto.go
    internal/socialinsurance/service.go
  </read_first>
  <acceptance_criteria>
    - handler.go 包含 "type Handler struct" 和 "func NewHandler"
    - handler.go 包含 "func (h *Handler) RegisterRoutes"
    - handler.go 包含 "CreatePolicy", "GetPolicy", "ListPolicies", "UpdatePolicy", "DeletePolicy", "CalculateInsurance" 方法
    - main.go 包含 "socialinsurance" 相关的 import 和 AutoMigrate 条目
    - main.go 包含 socialinsurance.NewRepository, socialinsurance.NewService, socialinsurance.NewHandler 初始化
    - main.go 包含 socialinsuranceHandler.RegisterRoutes(v1, authMiddleware) 调用
    - "go build ./cmd/server/" 编译通过
    - "go test ./... -count=1" 全部 PASS
  </acceptance_criteria>
  <action>
    1. 创建 `internal/socialinsurance/handler.go`:
       - Handler struct { svc *Service }
       - NewHandler(svc *Service) *Handler
       - RegisterRoutes(rg *gin.RouterGroup, authMiddleware gin.HandlerFunc):
         ```
         authGroup := rg.Group("")
         authGroup.Use(authMiddleware)
         authGroup.POST("/social-insurance/policies", middleware.RequireRole("owner"), h.CreatePolicy)
         authGroup.GET("/social-insurance/policies", h.ListPolicies)
         authGroup.GET("/social-insurance/policies/:id", h.GetPolicy)
         authGroup.PUT("/social-insurance/policies/:id", middleware.RequireRole("owner"), h.UpdatePolicy)
         authGroup.DELETE("/social-insurance/policies/:id", middleware.RequireRole("owner"), h.DeletePolicy)
         authGroup.POST("/social-insurance/calculate", h.CalculateInsurance)
         ```
         注意：政策管理仅 OWNER 可操作（D-14），查询和计算所有角色可访问

       - CreatePolicy: 绑定 CreatePolicyRequest，设置 OrgID=0 和 CreatedBy/UpdatedBy，调用 svc.CreatePolicy
       - GetPolicy: 路径参数 id，调用 svc.GetPolicy，返回 PolicyResponse
       - ListPolicies: query 参数 city_id（可选）、page、page_size，调用 svc.ListPolicies
       - UpdatePolicy: 路径参数 id，绑定 UpdatePolicyRequest，调用 svc.UpdatePolicy
       - DeletePolicy: 路径参数 id，调用 svc.DeletePolicy
       - CalculateInsurance: 绑定 CalculateRequest（city_id, salary, year），调用 svc.CalculateInsuranceAmounts

    2. 修改 `cmd/server/main.go`:
       - import 添加 `"github.com/wencai/easyhr/internal/socialinsurance"
       - AutoMigrate 添加 `&socialinsurance.SocialInsurancePolicy{}`
       - 依赖注入区域添加:
         ```go
         siRepo := socialinsurance.NewRepository(db)
         siSvc := socialinsurance.NewService(siRepo)
         siHandler := socialinsurance.NewHandler(siSvc)
         ```
       - 路由注册区域添加: `siHandler.RegisterRoutes(v1, authMiddleware)`
  </action>
  <verify>
    <automated>cd /Users/wencai/github/EasyHR && go build ./cmd/server/ && go test ./... -count=1</automated>
  </verify>
  <done>
    - 6个政策管理API端点已注册（POST/GET/PUT/DELETE + 计算端点）
    - OWNER权限控制政策增删改，所有角色可查询和计算
    - main.go AutoMigrate 包含 SocialInsurancePolicy
    - go build 和 go test 全部通过
  </done>
</task>

</tasks>

<verification>
1. `go test ./internal/socialinsurance/... -count=1 -v` — 所有单元测试 PASS
2. `go build ./cmd/server/` — 编译通过
3. `go test ./... -count=1` — 全项目测试无回归
4. grep "socialinsurance" cmd/server/main.go — 验证集成
</verification>

<success_criteria>
- SocialInsurancePolicy 模型使用 JSONB 存储 FiveInsurances 配置（datatypes.JSONType[T]）
- 政策查询取 effective_year<=year 的最新记录（D-04）
- CalculateInsuranceAmounts 正确计算6个险种金额，基数 clamp 到上下限
- 工伤/生育个人比例为0（企业全额承担）
- 6个 HTTP 端点在 main.go 注册，RBAC 权限设置正确
- 全部单元测试 PASS，编译通过
</success_criteria>

<output>
After completion, create `.planning/phases/03-social-insurance/03-01-SUMMARY.md`
</output>
