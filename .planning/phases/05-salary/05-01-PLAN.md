---
phase: 05-salary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/salary/model.go
  - internal/salary/repository.go
  - internal/salary/service.go
  - internal/salary/handler.go
  - internal/salary/dto.go
  - internal/salary/adapter.go
  - internal/salary/errors.go
  - cmd/server/main.go
autonomous: true
requirements: [PAYR-01, PAYR-02]

must_haves:
  truths:
    - "企业可启用/禁用预置薪资项模板（基本工资、绩效、补贴、扣款等）"
    - "每个员工可独立填写各项薪资金额"
    - "工资核算主表和明细表模型就绪，支持状态流转 draft→calculated→confirmed→paid"
    - "工资单模型就绪，含唯一 token 用于 H5 查看链接"
  artifacts:
    - path: "internal/salary/model.go"
      provides: "SalaryTemplateItem, SalaryItem, PayrollRecord, PayrollItem, PayrollSlip 模型定义"
      contains: "type SalaryTemplateItem struct"
    - path: "internal/salary/repository.go"
      provides: "数据访问层，TenantScope 自动注入"
      exports: ["NewRepository"]
    - path: "internal/salary/service.go"
      provides: "薪资结构配置 CRUD、员工薪资项 CRUD"
      contains: "func (s *Service) GetTemplate"
    - path: "internal/salary/handler.go"
      provides: "HTTP Handler + RegisterRoutes"
      contains: "func (h *Handler) RegisterRoutes"
    - path: "internal/salary/adapter.go"
      provides: "TaxProvider, SIDeductionProvider, EmployeeProvider 接口定义"
      contains: "type TaxProvider interface"
    - path: "internal/salary/errors.go"
      provides: "错误码 50xxx 定义"
      contains: "50001"
    - path: "cmd/server/main.go"
      provides: "AutoMigrate + 路由注册 + DI 注入"
      contains: "salary"
  key_links:
    - from: "internal/salary/adapter.go"
      to: "internal/tax/calculator.go"
      via: "TaxProvider interface wraps TaxCalculator"
      pattern: "CalculateTax"
    - from: "internal/salary/adapter.go"
      to: "internal/socialinsurance/service.go"
      via: "SIDeductionProvider interface wraps GetSocialInsuranceDeduction"
      pattern: "GetPersonalDeduction"
    - from: "cmd/server/main.go"
      to: "internal/salary/"
      via: "DI + AutoMigrate + RegisterRoutes"
      pattern: "salary"
---

<objective>
建立工资核算模块的基础数据层：定义 SalaryTemplateItem/SalaryItem/PayrollRecord/PayrollItem/PayrollSlip 五大核心模型，实现薪资结构配置 CRUD（预置模板启用/禁用）和员工薪资项管理 CRUD，定义跨模块接口（TaxProvider/SIDeductionProvider/EmployeeProvider），集成到 main.go。

Purpose: 为工资核算引擎（Plan 02）和工资单/导出功能（Plan 03）提供完整的数据模型和基础设施。
Output: 8 个文件（7 个 salary 包文件 + main.go 修改），覆盖 PAYR-01（薪资结构自定义）和 PAYR-02 的数据准备。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-salary/05-CONTEXT.md
@.planning/phases/05-salary/05-RESEARCH.md

<interfaces>
<!-- 关键接口和类型，executor 无需探索代码库 -->

From internal/common/model/base.go:
```go
type BaseModel struct {
    ID        int64          `gorm:"primaryKey;autoIncrement" json:"id"`
    OrgID     int64          `gorm:"column:org_id;index;not null" json:"org_id"`
    CreatedBy int64          `gorm:"column:created_by" json:"created_by"`
    CreatedAt time.Time      `gorm:"column:created_at;autoCreateTime" json:"created_at"`
    UpdatedBy int64          `gorm:"column:updated_by" json:"updated_by"`
    UpdatedAt time.Time      `gorm:"column:updated_at;autoUpdateTime" json:"updated_at"`
    DeletedAt gorm.DeletedAt `gorm:"column:deleted_at;index" json:"-"`
}
```

From internal/tax/calculator.go:
```go
type TaxCalculator interface {
    CalculateTax(orgID, employeeID int64, year, month int, grossIncome float64) (*TaxResult, error)
}
```

From internal/tax/dto.go:
```go
type TaxResult struct {
    MonthlyTax                float64 `json:"monthly_tax"`
    CumulativeIncome          float64 `json:"cumulative_income"`
    // ... 其他字段
}
```

From internal/socialinsurance/dto.go:
```go
type DeductionResponse struct {
    Items         []DeductionItem `json:"items"`
    TotalPersonal float64         `json:"total_personal"`
}
```

From internal/socialinsurance/service.go:
```go
func (s *Service) GetSocialInsuranceDeduction(orgID, employeeID int64, month string) (*DeductionResponse, error)
func (s *Service) SuggestBaseAdjustment(orgID, employeeID int64, newSalary float64)
```

From internal/employee/invitation_service.go (token 生成模式):
```go
func generateToken() (string, error) {
    bytes := make([]byte, 32)
    if _, err := rand.Read(bytes); err != nil {
        return "", fmt.Errorf("生成 token 失败: %w", err)
    }
    return hex.EncodeToString(bytes), nil
}
```

From cmd/server/main.go (DI 模式):
```go
// 三步模式: repo -> svc (注入 adapter) -> handler
taxRepo := tax.NewRepository(db)
taxEmpAdapter := tax.NewEmployeeAdapter(contractRepo, empRepo)
taxSIAdapter := tax.NewSocialInsuranceAdapter(siSvc)
taxSvc := tax.NewService(taxRepo, taxEmpAdapter, taxSIAdapter)
taxHandler := tax.NewHandler(taxSvc)
```

From internal/employee/repository.go (Repository 模式):
```go
type Repository struct { db *gorm.DB }
func NewRepository(db *gorm.DB) *Repository { return &Repository{db: db} }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 数据模型 + Repository + 薪资结构 Service</name>
  <files>internal/salary/model.go, internal/salary/repository.go, internal/salary/service.go, internal/salary/errors.go, internal/salary/adapter.go</files>
  <read_first>
    internal/common/model/base.go
    internal/tax/calculator.go
    internal/tax/dto.go
    internal/tax/employee_adapter.go
    internal/tax/si_adapter.go
    internal/socialinsurance/service.go (lines 440-467, 656-695)
    internal/socialinsurance/dto.go (lines 159-172)
    internal/employee/invitation_model.go
    internal/employee/invitation_service.go (lines 46-52)
    cmd/server/main.go
  </read_first>
  <behavior>
    - Test SalaryTemplateItem seed: 10 preset items created (7 income + 3 deduction), base_salary is_required=true
    - Test SalaryTemplate CRUD: GetTemplate returns org-specific enabled items; UpdateTemplate enables/disables items
    - Test SalaryItem CRUD: SetItems creates/updates employee salary amounts; GetItemsByEmployee returns correct amounts
    - Test PayrollRecord status constants: draft/calculated/confirmed/paid defined
    - Test PayrollSlip token generation: 64-char hex string, unique
    - Test adapter interfaces: TaxProvider/SIDeductionProvider/EmployeeProvider compile correctly
  </behavior>
  <action>
    **1. 创建 internal/salary/model.go** — 定义 5 个核心模型：

    ```go
    // SalaryTemplateItem 薪资项模板（全局预置 + 企业启用状态）
    // OrgID=0 为全局预置项，OrgID=企业ID 为企业启用/禁用覆盖
    type SalaryTemplateItem struct {
        model.BaseModel
        Name      string  `gorm:"column:name;type:varchar(50);not null" json:"name"`
        Type      string  `gorm:"column:type;type:varchar(20);not null" json:"type"` // income/deduction
        SortOrder int     `gorm:"column:sort_order;not null;default:0" json:"sort_order"`
        IsRequired bool   `gorm:"column:is_required;not null;default:false" json:"is_required"`
        IsEnabled  bool   `gorm:"column:is_enabled;not null;default:true" json:"is_enabled"`
    }
    ```

    预置 10 个全局模板项（OrgID=0）：
    - 收入项：基本工资(required,sort=1)、绩效工资(sort=2)、岗位补贴(sort=3)、餐补(sort=4)、交通补(sort=5)、通讯补(sort=6)、其他补贴(sort=7)
    - 扣款项：事假扣款(sort=8)、病假扣款(sort=9)、其他扣款(sort=10)

    ```go
    // SalaryItem 员工薪资项金额（每员工每启用的薪资项一条记录）
    type SalaryItem struct {
        model.BaseModel
        EmployeeID      int64   `gorm:"column:employee_id;index;not null" json:"employee_id"`
        TemplateItemID  int64   `gorm:"column:template_item_id;index;not null" json:"template_item_id"`
        Amount          float64 `gorm:"column:amount;type:decimal(10,2);not null;default:0" json:"amount"`
        EffectiveMonth  string  `gorm:"column:effective_month;type:varchar(7);not null" json:"effective_month"` // "2026-04"
    }
    ```

    ```go
    // PayrollRecord 月度工资核算主表（每员工每月一条）
    type PayrollRecord struct {
        model.BaseModel
        EmployeeID      int64   `gorm:"column:employee_id;index;not null" json:"employee_id"`
        EmployeeName    string  `gorm:"column:employee_name;type:varchar(50);not null" json:"employee_name"`
        Year            int     `gorm:"column:year;not null" json:"year"`
        Month           int     `gorm:"column:month;not null" json:"month"`
        Status          string  `gorm:"column:status;type:varchar(20);not null;default:draft" json:"status"` // draft/calculated/confirmed/paid
        GrossIncome     float64 `gorm:"column:gross_income;type:decimal(12,2);not null;default:0" json:"gross_income"`
        SIDeduction     float64 `gorm:"column:si_deduction;type:decimal(12,2);not null;default:0" json:"si_deduction"`
        Tax             float64 `gorm:"column:tax;type:decimal(12,2);not null;default:0" json:"tax"`
        TotalDeductions float64 `gorm:"column:total_deductions;type:decimal(12,2);not null;default:0" json:"total_deductions"`
        NetIncome       float64 `gorm:"column:net_income;type:decimal(12,2);not null;default:0" json:"net_income"`
        PayMethod       string  `gorm:"column:pay_method;type:varchar(20)" json:"pay_method"` // bank_transfer/cash/other
        PayDate         *time.Time `gorm:"column:pay_date" json:"pay_date"`
        PayNote         string  `gorm:"column:pay_note;type:varchar(200)" json:"pay_note"`
    }
    // 唯一索引: employee_id + year + month (WHERE deleted_at IS NULL)
    ```

    ```go
    // PayrollItem 工资核算明细（每员工多条，快照薪资项名称和金额）
    type PayrollItem struct {
        model.BaseModel
        PayrollRecordID int64   `gorm:"column:payroll_record_id;index;not null" json:"payroll_record_id"`
        ItemName        string  `gorm:"column:item_name;type:varchar(50);not null" json:"item_name"`
        ItemType        string  `gorm:"column:item_type;type:varchar(20);not null" json:"item_type"` // income/deduction
        Amount          float64 `gorm:"column:amount;type:decimal(12,2);not null;default:0" json:"amount"`
    }
    ```

    ```go
    // PayrollSlip 工资单（含唯一 token 用于 H5 查看链接）
    type PayrollSlip struct {
        model.BaseModel
        PayrollRecordID int64      `gorm:"column:payroll_record_id;index;not null" json:"payroll_record_id"`
        EmployeeID      int64      `gorm:"column:employee_id;index;not null" json:"employee_id"`
        Token           string     `gorm:"column:token;type:varchar(64);uniqueIndex;not null" json:"token"`
        PhoneEncrypted  string     `gorm:"column:phone_encrypted;type:varchar(200)" json:"-"`
        PhoneHash       string     `gorm:"column:phone_hash;type:varchar(64);index" json:"-"`
        Status          string     `gorm:"column:status;type:varchar(20);not null;default:pending" json:"status"` // pending/sent/viewed/signed
        SentAt          *time.Time `gorm:"column:sent_at" json:"sent_at"`
        ViewedAt        *time.Time `gorm:"column:viewed_at" json:"viewed_at"`
        SignedAt        *time.Time `gorm:"column:signed_at" json:"signed_at"`
        ExpiresAt       time.Time  `gorm:"column:expires_at;not null" json:"expires_at"`
    }
    ```

    状态常量：
    - PayrollStatusDraft / PayrollStatusCalculated / PayrollStatusConfirmed / PayrollStatusPaid
    - SlipStatusPending / SlipStatusSent / SlipStatusViewed / SlipStatusSigned

    **2. 创建 internal/salary/errors.go** — 错误码 50xxx：
    - 50001: 薪资模板配置错误
    - 50002: 工资核算失败
    - 50003: 工资表状态不允许操作
    - 50004: 考勤导入失败
    - 50005: 工资单 token 无效或过期
    - 50006: 短信验证失败
    - 50007: 员工匹配失败

    **3. 创建 internal/salary/adapter.go** — 跨模块接口定义：
    ```go
    // TaxProvider 个税计算接口
    type TaxProvider interface {
        CalculateTax(orgID, employeeID int64, year, month int, grossIncome float64) (*tax.TaxResult, error)
    }

    // SIDeductionProvider 社保扣款接口
    type SIDeductionProvider interface {
        GetPersonalDeduction(orgID, employeeID int64, month string) (float64, error)
    }

    // EmployeeProvider 员工信息接口
    type EmployeeProvider interface {
        GetActiveEmployees(orgID int64) ([]EmployeeInfo, error)
        GetEmployeeByID(orgID, employeeID int64) (*EmployeeInfo, error)
    }

    // EmployeeInfo 员工简要信息（跨模块传输）
    type EmployeeInfo struct {
        ID         int64
        Name       string
        Phone      string
        HireDate   time.Time
        BaseSalary float64 // 从 Contract.Salary 获取
    }

    // BaseAdjustmentProvider 社保基数调整建议接口
    type BaseAdjustmentProvider interface {
        SuggestBaseAdjustment(orgID, employeeID int64, newSalary float64)
    }
    ```

    **4. 创建 internal/salary/repository.go** — 数据访问层，遵循 TenantScope 模式：
    - SalaryTemplateRepository（独立，处理全局+企业模板）
      - SeedGlobalTemplateItems() — 创建 OrgID=0 的 10 个预置项
      - GetOrgTemplate(orgID) — 返回全局模板 + 企业启用覆盖
      - UpdateOrgTemplateItem(orgID, templateItemID, isEnabled) — 企业级启用/禁用
    - Repository（主 Repository，含 SalaryItem/PayrollRecord/PayrollItem/PayrollSlip CRUD）
      - 遵循 `func NewRepository(db *gorm.DB) *Repository` 模式
      - 所有查询自动注入 TenantScope（org_id）
      - PayrollRecord 按员工+年月唯一（部分唯一索引 WHERE deleted_at IS NULL）
      - PayrollSlip 按 token 唯一查询（FindSlipByToken）
      - SalaryItem 按员工+月份查询

    **5. 创建 internal/salary/service.go** — 业务逻辑层：
    - `NewService(repo *Repository, templateRepo *SalaryTemplateRepository, taxProvider TaxProvider, siProvider SIDeductionProvider, empProvider EmployeeProvider, baseAdjustProvider BaseAdjustmentProvider) *Service`
    - GetTemplate(orgID) — 获取企业薪资模板（含启用状态）
    - UpdateTemplate(orgID, userID, items []TemplateItemUpdate) — 批量更新薪资项启用/禁用
    - GetEmployeeItems(orgID, employeeID, month string) — 获取员工某月各项金额
    - SetEmployeeItems(orgID, userID, employeeID, month string, items []SalaryItemInput) — 设置员工各项金额
    - SeedTemplateItems() — 启动时调用，创建全局预置薪资项

    SalaryItem 设置逻辑：
    - 查询该员工已启用的薪资项列表
    - 对于每个 SalaryItemInput，upert（存在则更新金额，不存在则创建）
    - amount 必须 >= 0（验证）
    - effective_month 为 "YYYY-MM" 格式
  </action>
  <verify>
    <automated>cd /Users/wencai/github/EasyHR && go build ./internal/salary/... && go test -race -count=1 ./internal/salary/... -run "TestSalaryTemplate|TestSalaryItem"</automated>
  </verify>
  <done>
    - model.go 包含 5 个模型：SalaryTemplateItem, SalaryItem, PayrollRecord, PayrollItem, PayrollSlip
    - errors.go 包含错误码 50001-50007
    - adapter.go 定义 TaxProvider/SIDeductionProvider/EmployeeProvider/BaseAdjustmentProvider 接口
    - repository.go 实现 TenantScope 自动注入的 CRUD
    - service.go 实现薪资结构配置 CRUD 和员工薪资项 CRUD
    - 10 个预置薪资项种子数据函数可用
    - go build ./internal/salary/... 编译通过
  </done>
</task>

<task type="auto">
  <name>Task 2: Handler + DTO + Adapter 实现 + main.go 集成</name>
  <files>internal/salary/handler.go, internal/salary/dto.go, internal/salary/tax_adapter.go, internal/salary/si_adapter.go, internal/salary/employee_adapter.go, cmd/server/main.go</files>
  <read_first>
    internal/tax/handler.go
    internal/tax/employee_adapter.go
    internal/tax/si_adapter.go
    internal/socialinsurance/service.go (lines 440-467, 656-695)
    internal/employee/repository.go
    internal/employee/contract_repository.go
    cmd/server/main.go
  </read_first>
  <action>
    **1. 创建 internal/salary/dto.go** — 请求/响应结构体：

    薪资模板 DTO：
    ```go
    type TemplateItemResponse struct {
        ID        int64  `json:"id"`
        Name      string `json:"name"`
        Type      string `json:"type"`      // income/deduction
        SortOrder int    `json:"sort_order"`
        IsRequired bool  `json:"is_required"`
        IsEnabled  bool  `json:"is_enabled"`
    }

    type TemplateResponse struct {
        Items []TemplateItemResponse `json:"items"`
    }

    type UpdateTemplateRequest struct {
        Items []TemplateItemUpdate `json:"items" binding:"required"`
    }

    type TemplateItemUpdate struct {
        TemplateItemID int64 `json:"template_item_id" binding:"required"`
        IsEnabled      bool  `json:"is_enabled"`
    }
    ```

    员工薪资项 DTO：
    ```go
    type EmployeeItemResponse struct {
        TemplateItemID int64   `json:"template_item_id"`
        ItemName       string  `json:"item_name"`
        ItemType       string  `json:"item_type"`
        Amount         float64 `json:"amount"`
    }

    type EmployeeItemsResponse struct {
        EmployeeID int64                   `json:"employee_id"`
        Month      string                  `json:"month"`
        Items      []EmployeeItemResponse  `json:"items"`
    }

    type SetEmployeeItemsRequest struct {
        Month string              `json:"month" binding:"required"` // "YYYY-MM"
        Items []SalaryItemInput   `json:"items" binding:"required"`
    }

    type SalaryItemInput struct {
        TemplateItemID int64   `json:"template_item_id" binding:"required"`
        Amount         float64 `json:"amount" binding:"min=0"`
    }
    ```

    **2. 创建 adapter 实现文件：**

    internal/salary/tax_adapter.go:
    ```go
    type TaxAdapter struct { taxSvc *tax.Service }
    func NewTaxAdapter(taxSvc *tax.Service) *TaxAdapter
    func (a *TaxAdapter) CalculateTax(orgID, employeeID int64, year, month int, grossIncome float64) (*tax.TaxResult, error)
    ```

    internal/salary/si_adapter.go:
    ```go
    type SIAdapter struct { siSvc *socialinsurance.Service }
    func NewSIAdapter(siSvc *socialinsurance.Service) *SIAdapter
    func (a *SIAdapter) GetPersonalDeduction(orgID, employeeID int64, month string) (float64, error)
    func (a *SIAdapter) SuggestBaseAdjustment(orgID, employeeID int64, newSalary float64)
    ```

    internal/salary/employee_adapter.go:
    ```go
    type EmployeeAdapter struct {
        empRepo      *employee.Repository
        contractRepo *employee.ContractRepository
    }
    func NewEmployeeAdapter(empRepo *employee.Repository, contractRepo *employee.ContractRepository) *EmployeeAdapter
    func (a *EmployeeAdapter) GetActiveEmployees(orgID int64) ([]EmployeeInfo, error)
    func (a *EmployeeAdapter) GetEmployeeByID(orgID, employeeID int64) (*EmployeeInfo, error)
    ```
    - GetActiveEmployees 查询 status=active 的员工列表，通过 contractRepo 获取每个员工的 baseSalary
    - GetEmployeeByID 查询单个员工 + 合同薪资

    **3. 创建 internal/salary/handler.go** — HTTP Handler:

    API 端点（per RESEARCH section 6）：
    ```
    GET    /salary/template           → GetTemplate
    PUT    /salary/template           → UpdateTemplate (OWNER/ADMIN)
    GET    /salary/items              → GetEmployeeItems (query: employee_id, month)
    PUT    /salary/items/:employee_id → SetEmployeeItems (OWNER/ADMIN)
    ```

    RegisterRoutes 方法遵循项目模式：
    ```go
    func (h *Handler) RegisterRoutes(rg *gin.RouterGroup, authMiddleware gin.HandlerFunc) {
        salary := rg.Group("/salary", authMiddleware)
        {
            salary.GET("/template", h.GetTemplate)
            salary.PUT("/template", middleware.RequireRole("OWNER", "ADMIN"), h.UpdateTemplate)
            salary.GET("/items", h.GetEmployeeItems)
            salary.PUT("/items/:employee_id", middleware.RequireRole("OWNER", "ADMIN"), h.SetEmployeeItems)
        }
    }
    ```

    Handler 从 gin.Context 获取 orgID 和 userID（通过 middleware.GetOrgID(c), middleware.GetUserID(c)）。

    **4. 修改 cmd/server/main.go** — 在 tax 模块 DI 之后添加 salary 模块：

    AutoMigrate 新增：
    ```go
    &salary.SalaryTemplateItem{},
    &salary.SalaryItem{},
    &salary.PayrollRecord{},
    &salary.PayrollItem{},
    &salary.PayrollSlip{},
    ```

    DI 注入（在 tax 模块之后）：
    ```go
    // 工资模块依赖注入
    salaryRepo := salary.NewRepository(db)
    salaryTemplateRepo := salary.NewSalaryTemplateRepository(db)
    salaryTaxAdapter := salary.NewTaxAdapter(taxSvc)
    salarySIAdapter := salary.NewSIAdapter(siSvc)
    salaryEmpAdapter := salary.NewEmployeeAdapter(empRepo, contractRepo)
    salarySvc := salary.NewService(salaryRepo, salaryTemplateRepo, salaryTaxAdapter, salarySIAdapter, salaryEmpAdapter, salarySIAdapter)
    salaryHandler := salary.NewHandler(salarySvc)
    ```

    路由注册：
    ```go
    salaryHandler.RegisterRoutes(v1, authMiddleware)
    ```

    种子数据初始化：
    ```go
    if err := salarySvc.SeedTemplateItems(); err != nil {
        logger.Logger.Warn("salary template seed failed", zap.Error(err))
    }
    ```
  </action>
  <verify>
    <automated>cd /Users/wencai/github/EasyHR && go build ./cmd/server/ && go test -race -count=1 ./internal/salary/...</automated>
  </verify>
  <done>
    - handler.go 注册 4 个 API 端点（GET/PUT template, GET/PUT items）
    - dto.go 包含完整请求/响应结构体，带 binding tag 校验
    - tax_adapter.go/si_adapter.go/employee_adapter.go 分别实现对应接口
    - main.go 完成工资模块的 AutoMigrate + DI + 路由注册 + 种子数据
    - go build ./cmd/server/ 编译通过
    - go test ./internal/salary/... 全部通过
  </done>
</task>

</tasks>

<verification>
```bash
go build ./cmd/server/
go test -race -count=1 ./internal/salary/...
go test -race -count=1 ./internal/...
```
</verification>

<success_criteria>
- 5 个模型（SalaryTemplateItem, SalaryItem, PayrollRecord, PayrollItem, PayrollSlip）定义完整
- 4 个 API 端点（template GET/PUT, items GET/PUT）注册到 /api/v1/salary/
- 跨模块接口（TaxProvider, SIDeductionProvider, EmployeeProvider, BaseAdjustmentProvider）定义并实现
- 10 个预置薪资项种子数据可用
- main.go 集成完成：AutoMigrate + DI + 路由 + 种子
- go build ./cmd/server/ && go test -race ./internal/salary/... 全部通过
</success_criteria>

<output>
After completion, create `.planning/phases/05-salary/05-01-SUMMARY.md`
</output>
