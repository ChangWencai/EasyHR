# Plan 07-04: 调薪管理 UI + 薪资列表导出增强 + 解锁

## Overview

**Phase:** 07 薪资管理增强
**Plan:** 07-04 of 04
**Wave:** 2 (depends on 07-01)
**Type:** execute
**Autonomous:** true
**Requirements Addressed:** SAL-05, SAL-06, SAL-07, SAL-11, SAL-12, SAL-16, SAL-19

## Objective

前端：SalaryTool.vue 扩展新 Tab（调薪管理/个税上传/绩效系数/发工资条/薪资列表）+ SalaryAdjustment.vue + PerformanceCoefficient.vue + SalaryList.vue。后端：调薪预览、薪资列表 API、导出增强、confirmed/paid 解锁服务。

## Context

**Read first:**
- `frontend/src/views/tool/SalaryTool.vue` — existing 3-tab structure (template/payroll/export)
- `frontend/src/views/tool/SalaryTool.vue` — current implementation (336 lines)
- `07-01-PLAN.md` — adjustment/performance backend APIs
- `07-UI-SPEC.md` — detailed UI copy, layout, and interaction patterns for all 6 pages
- `frontend/src/views/attendance/AttendanceRule.vue` — multi-tab pattern (el-tabs with content below)
- `frontend/src/api/employee.ts` — employee API (for search/department filter)

## Frontend Files Created/Modified

### New files:

```
frontend/src/views/tool/SalaryAdjustment.vue   — 调薪/普调 Tab 表单
frontend/src/views/tool/PerformanceCoefficient.vue — 绩效系数设置 Tab
frontend/src/views/tool/SalaryList.vue         — 薪资列表 Tab（含导出）
```

### Modified files:

```
frontend/src/views/tool/SalaryTool.vue — extend with 6 tabs (dashboard/template/payroll/adjustment/export/list)
frontend/src/router/index.ts — add /salary/slip/:token route
frontend/src/api/salary.ts — already extended in 07-01
```

### Task 1: SalaryTool.vue — Extend to 6 Tabs

**Files:** `frontend/src/views/tool/SalaryTool.vue`

**Action:**
- Replace existing 3-tab structure with 6 tabs:
  ```
  name="dashboard" label="数据看板"
  name="template" label="薪资模板"
  name="payroll" label="工资核算"
  name="adjustment" label="调薪管理"
  name="export" label="导出"
  name="list" label="薪资列表"
  ```
- Import and embed:
  ```typescript
  import SalaryDashboard from './SalaryDashboard.vue'   // from 07-01
  import SalaryAdjustment from './SalaryAdjustment.vue'
  import PerformanceCoefficient from './PerformanceCoefficient.vue'
  import SalaryList from './SalaryList.vue'
  ```
- Tab content: wrap imported components directly
  ```vue
  <el-tab-pane label="数据看板" name="dashboard">
    <SalaryDashboard />
  </el-tab-pane>
  <el-tab-pane label="调薪管理" name="adjustment">
    <SalaryAdjustment />
  </el-tab-pane>
  <el-tab-pane label="薪资列表" name="list">
    <SalaryList />
  </el-tab-pane>
  ```
- Default active tab: "dashboard"

**Verify:** `grep -n "SalaryDashboard\|SalaryAdjustment\|SalaryList" frontend/src/views/tool/SalaryTool.vue`
**Done:** SalaryTool.vue has 6 tabs, all new components embedded

### Task 2: SalaryAdjustment.vue

**Files:** `frontend/src/views/tool/SalaryAdjustment.vue`

**Action:**
- Two tabs: "员工调薪" / "部门普调"
- **员工调薪 Tab:**
  - el-select for employee (searchable, remote filter by name)
  - Adjustment type selector: 岗位工资/补贴/奖金/年终奖/其他扣除
  - Radio: 金额 / 比例
  - Input: 调整金额 (元) or 调整比例 (%)
  - Month picker: 生效月份
  - "预览影响" button → call `POST /api/v1/salary/adjustment/preview`
  - Preview panel (el-card): 显示影响人数、月度影响、年度影响、生效月份
  - "提交调薪" button → call `POST /api/v1/salary/adjustment`
- **部门普调 Tab:**
  - el-select multiple for departments (with "全选" button)
  - Same adjustment form as above
  - Preview shows: 影响部门数、影响员工数、月度影响、年度影响、生效月份
  - "提交调薪" button → call `POST /api/v1/salary/mass-adjustment`
- On success: show ElMessage.success + reset form
- On error: show error in preview area or dialog

**Verify:** `grep -n "SalaryAdjustment" frontend/src/views/tool/`
**Done:** Both employee and department adjustment forms work, preview and submit functional

### Task 3: PerformanceCoefficient.vue

**Files:** `frontend/src/views/tool/PerformanceCoefficient.vue`

**Action:**
- Page header: "绩效系数设置" + 薪资模板月份选择 + 保存按钮
- Filter bar: search input (员工姓名), department filter dropdown
- Employee table with columns:
  - 姓名
  - 部门
  - 绩效工资标准
  - 绩效系数 (el-slider, min=0, max=100, step=5, with label showing "{n}%")
  - 实际绩效工资 (computed: standard × coefficient, read-only)
  - 重置按钮
- Dirty tracking: only modified rows saved
- "保存系数" button: batch submit all dirty coefficients via `PUT /api/v1/salary/performance`
- Default coefficient (100%) shown with grey tag "默认"
- "重置为默认" per row: reset slider to 100

**Verify:** `grep -n "PerformanceCoefficient" frontend/src/views/tool/`
**Done:** Slider updates show real-time coefficient value and computed actual performance salary

### Task 4: SalaryList.vue

**Files:** `frontend/src/views/tool/SalaryList.vue`

**Action:**
- Page header: "薪资列表" + "导出 Excel" button
- Filter bar: month selector, department dropdown, employee search input
- el-table with columns: 姓名/部门/应发合计/扣除/个税/社保公积金/实发/状态
- Status tags: draft=info/calculated=warning/confirmed=primary/paid=success
- Lock icon (el-icon: Lock) on confirmed/paid rows (per D-SAL-DATA-01)
- Click lock icon → unlock dialog:
  - "该月工资已确认锁定，如需修改请输入解锁码"
  - Input: 企业主手机验证码
  - After unlock: row becomes editable, needs recalculate + reconfirm
- Pagination
- "导出 Excel" button → open el-dialog with options:
  - "当前页" (radio)
  - "全部数据（含税前明细）" (radio, with checkboxes for detail fields)
  - Confirm → call export API

**Verify:** `grep -n "SalaryList" frontend/src/views/tool/`
**Done:** Salary list loads, filters work, export dialog offers both options

## Backend Files Created/Modified

### New files (internal/salary/):

```
salary_unlock_service.go  — confirmed/paid unlock with SMS verification
salary_unlock_handler.go — POST /api/v1/salary/unlock
salary_list_service.go    — salary list with filters
salary_list_handler.go   — GET /api/v1/salary/list
excel_enhanced.go        — export with tax detail option
```

### Modified files:

```
internal/salary/excel.go         — extend ExportPayrollExcel with include_tax_detail option
internal/salary/repository.go   — add ListPayrollRecordsWithFilters, UpdatePayrollRecord methods
internal/salary/adjustment_handler.go — add adjustment/preview endpoint (part of 07-01)
```

### Task 5: SalaryList Backend + Export

**Files:** `internal/salary/salary_list_service.go`, `internal/salary/salary_list_handler.go`, `internal/salary/excel.go`

**Action:**

**salary_list_service.go:**
```go
type SalaryListFilter struct {
    OrgID        int64
    Year         int
    Month        int
    DepartmentID *int64
    Keyword      string
    Page         int
    PageSize     int
}
```

**salary_list_handler.go:**
- `GET /api/v1/salary/list` — list payroll records with filters:
  - `?year=&month=&department_id=&keyword=&page=&page_size=`
  - Join with employees table to get department name
  - Return list + total count
- `GET /api/v1/salary/export` — extend existing export:
  - `?year=&month=&include_details=true` → add PayrollItem breakdown columns to Excel
  - PayrollItem columns: 基本工资/岗位工资/绩效工资/加班费/补贴/奖金/其他/... (all income items) + personal_si/公积金/个税/其他扣除 (all deduction items)

**excel.go — extend ExportPayrollExcel:**
- Add parameter `includeDetails bool`
- When `includeDetails=true`: add PayrollItem rows as additional columns per employee
- Group income items, deduction items with subtotals

**Verify:** `grep -n "SalaryList\|ExportPayrollExcel" internal/salary/`
**Done:** Salary list API returns filtered records with dept name; export Excel with optional detail columns

### Task 6: Unlock Service

**Files:** `internal/salary/salary_unlock_service.go`, `internal/salary/salary_unlock_handler.go`

**Action:**

**salary_unlock_service.go:**
```go
// UnlockPayroll 解锁 confirmed/paid 月份（per D-SAL-DATA-01）
// Requires: 企业主手机验证码
func (s *Service) UnlockPayroll(orgID, userID, recordID int64, smsCode string) error {
    // 1. Verify SMS code from Redis (or temporary fallback)
    // 2. Check payroll record status (must be confirmed or paid)
    // 3. Set status back to calculated (allow editing)
    // 4. Write audit log (unlock who/unlock when/why)
    // 5. Note: must recalculate and re-confirm after unlock
}

type UnlockRequest struct {
    RecordID int64  `json:"record_id" binding:"required"`
    SMSCode  string `json:"sms_code" binding:"required,len=6"`
}
```

**salary_unlock_handler.go:**
- `POST /api/v1/salary/unlock` — unlock payroll record
  - Request: `{ record_id, sms_code }`
  - Verify SMS code (send code via existing VerifySlipPhone pattern)
  - After unlock: record status = calculated, admin can edit and re-confirm

**Frontend unlock dialog (in SalaryList.vue):**
- Click lock icon → el-dialog
- Input SMS code (6 digits)
- "发送验证码" button → call `POST /api/v1/salary/unlock/send-code`
- Submit → unlock API

**Verify:** `grep -n "UnlockPayroll\|UnlockRequest" internal/salary/`
**Done:** confirmed/paid rows can be unlocked with SMS verification, audit log written

## Output

After completion, create `.planning/phases/07-薪资管理增强/07-04-SUMMARY.md`
