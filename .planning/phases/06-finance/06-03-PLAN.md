---
phase: 06-finance
plan: '03'
type: execute
wave: '4'
depends_on:
  - '06-00'
  - '06-01'
  - '06-02'
files_modified:
  - internal/finance/model_report.go
  - internal/finance/repository.go
  - internal/finance/dto_book.go
  - internal/finance/dto_report.go
  - internal/finance/dto_period.go
  - internal/finance/service_book.go
  - internal/finance/service_report.go
  - internal/finance/service_period.go
  - internal/finance/handler_book.go
  - internal/finance/handler_report.go
  - internal/finance/handler.go
  - cmd/server/main.go
autonomous: true
requirements:
  - FINC-11
  - FINC-12
  - FINC-13
  - FINC-14
  - FINC-15
  - FINC-16
  - FINC-17
  - FINC-18
  - FINC-21
  - FINC-22
user_setup: []
gap_closure: false

must_haves:
  truths:
    - "实时生成总账、明细账、科目余额表（凭证量<500时实时SUM够用）"
    - "账簿查询支持按期间+科目筛选，支持导出Excel"
    - "月末结账后生成资产负债表和利润表快照存储"
    - "财务报表支持多期对比（选2-4期并排展示）"
    - "结账后当期凭证禁止修改，只能红冲"
    - "反结账需OWNER权限+二次确认"
    - "月末自动计算增值税（销项-进项）和企业所得税（季度预缴）"
    - "纳税申报辅助数据导出Excel"
  artifacts:
    - path: internal/finance/model_report.go
      provides: "报表快照模型"
      exports: ["ReportSnapshot", "ReportType"]
    - path: internal/finance/service_book.go
      provides: "实时账簿计算（总账/明细账/科目余额表）"
      exports: ["GetLedger", "GetAccountBalance", "GetTrialBalance"]
    - path: internal/finance/service_report.go
      provides: "财务报表+纳税申报"
      exports: ["GenerateBalanceSheet", "GenerateIncomeStatement", "CalculateVAT", "CalculateCIT"]
    - path: internal/finance/service_period.go
      provides: "结账/反结账"
      exports: ["ClosePeriod", "RevertClosing", "ValidateClosing"]
  key_links:
    - from: internal/finance/service_report.go
      to: internal/finance/model_report.go
      via: ReportSnapshot create on close
      pattern: "reportSnapshotRepo.Create"
    - from: internal/finance/service_book.go
      to: internal/finance/repository.go
      via: JournalEntryRepo queries with SUM
      pattern: "journalRepo.SumByAccount"
    - from: internal/finance/service_period.go
      to: internal/finance/service_report.go
      via: GenerateBalanceSheet called during ClosePeriod
      pattern: "reportSvc.GenerateBalanceSheet"
---

<objective>
实现账簿实时查询（FINC-11/12）、财务报表生成+快照存储（FINC-13/14/15）、期间结账+反结账（FINC-16/17/18）、纳税申报辅助数据（FINC-21/22）。实时账簿按期间+科目SUM借贷分录（V1.0不维护中间表），结账时生成ReportSnapshot。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-finance/06-01-SUMMARY.md
@.planning/phases/06-finance/06-02-SUMMARY.md
@internal/finance/model_voucher.go
@internal/finance/service_voucher.go
@internal/employee/model.go

<interfaces>
<!-- Key types from Plan 01 -->
```go
// From model_voucher.go
type Voucher struct {
    ID, OrgID int64
    VoucherNo string
    PeriodID int64
    VoucherDate time.Time
    Status string  // draft/submitted/audited/closed
    SourceType string
    SourceID int64
    Description string
    ReversalOf int64
    Attachments string  // JSON
    JournalEntries []JournalEntry
}

type JournalEntry struct {
    ID, OrgID int64
    VoucherID int64
    AccountID int64
    DC string  // "DEBIT"/"CREDIT"
    Amount decimal.Decimal  // stored as string in DB
    Summary string
}
```

<!-- Account categories and normal balance (from Plan 01) -->
```go
// From model_account.go
// ASSET, COST: normal_balance = DEBIT
// LIABILITY, EQUITY, PROFIT: normal_balance = CREDIT
// Balance formula:
//   ASSET/COST:  ending = SUM(debit) - SUM(credit)
//   LIABILITY/EQUITY/PROFIT: ending = SUM(credit) - SUM(debit)
```

<!-- Balance sheet formula (D-14 in 06-CONTEXT) -->
```go
// Simplified V1.0 balance sheet structure:
// Assets:
//   1001 库存现金
//   1002 银行存款
//   1122 应收账款
//   1221 其他应收款
//   1405 原材料
//   1601 固定资产
//   1602 累计折旧 (subtract)
// Liabilities:
//   2202 应付账款
//   2241 其他应付款
//   2801 应付职工薪酬
//   2221 应交税费
// Equity:
//   4001 实收资本
//   4103 本年利润
//   4104 利润分配
// Balance equation: Assets = Liabilities + Equity
```

<!-- Income statement formula (D-15 in 06-CONTEXT) -->
```go
// Simplified V1.0 income statement:
// Revenue: 6001 主营业务收入 + 6051 其他业务收入
// COGS: 6401 主营业务成本 + 6402 其他业务成本
// SGA: 6601 销售费用 + 6602 管理费用 + 6603 财务费用
// Tax: 6403 税金及附加
// Non-op: 6901 营业外收入 - 6911 营业外支出
// Tax: 6901 所得税费用
// Net = Revenue - COGS - SGA - Tax + NonOp - Tax
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Implement ReportSnapshot model + Book service (real-time ledger/trial balance)</name>
  <files>
    internal/finance/model_report.go
    internal/finance/repository.go
    internal/finance/dto_book.go
    internal/finance/service_book.go
    internal/finance/handler_book.go
  </files>
  <read_first>
    internal/finance/model_report.go
    internal/finance/repository.go
    internal/finance/dto_book.go
    internal/finance/service_book.go
    internal/finance/handler_book.go
    internal/finance/model_voucher.go
  </read_first>
  <action>
    Create the following files:

    1. internal/finance/model_report.go: ReportSnapshot model embedding model.BaseModel. Fields: PeriodID (int64), ReportType ("balance_sheet"/"income_statement"), Data (jsonb, stored as string, holds computed report data), GeneratedBy (int64), GeneratedAt (time.Time), IsValid (bool, true by default). TableName: "report_snapshots". Unique index on (org_id, period_id, report_type). Add SnapshotRepository to repository.go.

    2. internal/finance/dto_book.go: AccountBalanceResponse (account_id, code, name, category, debit_sum, credit_sum, balance, is_leaf). TrialBalanceResponse (items []AccountBalanceResponse, total_debit, total_credit, is_balanced bool). LedgerEntryResponse (voucher_no, voucher_date, description, account_name, dc, amount, balance_after). LedgerResponse (account_id, account_code, account_name, period_id, year, month, entries []LedgerEntryResponse, period_debit_sum, period_credit_sum, ending_balance). BookExportRequest (period_id, account_id optional, format "excel").

    3. internal/finance/service_book.go: BookService. GetTrialBalance(ctx, orgID, periodID): real-time query. SQL: JOIN accounts with journal_entries filtered by period_id, GROUP BY account_id, compute SUM(debit)/SUM(credit), calculate balance by category. GetAccountBalance(ctx, orgID, periodID, accountID): all entries for account, running balance. GetLedger(ctx, orgID, periodID, accountID, page, limit): paginated entries for a specific account, compute period debit/credit sums and ending balance. ExportToExcel(ctx, orgID, periodID, accountID, format): use excelize to write trial balance or ledger to xlsx. Return OSS URL.

    4. internal/finance/handler_book.go: BookHandler. Routes: GET /books/trial-balance (GetTrialBalance, OWNER+ADMIN), GET /books/ledger (GetLedger, OWNER+ADMIN), GET /books/account-balance (GetAccountBalance, OWNER+ADMIN), GET /books/export (ExportToExcel, OWNER+ADMIN). Apply TenantScope for org_id.

    After implementation, update service_test.go:
    - TestTrialBalance_CalculatesCorrectly: verify SUM(debit) = SUM(credit) across all entries
  </action>
  <acceptance_criteria>
    <grep>grep -q 'type ReportSnapshot struct' internal/finance/model_report.go</grep>
    <grep>grep -q 'func.*GetTrialBalance' internal/finance/service_book.go</grep>
    <grep>grep -q 'func.*GetLedger' internal/finance/service_book.go</grep>
    <grep>grep -q 'decimal.Decimal' internal/finance/service_book.go</grep>
    <grep>grep -q 'func.*RegisterRoutes' internal/finance/handler_book.go</grep>
    <grep>grep -q 'SnapshotRepository' internal/finance/repository.go</grep>
  </acceptance_criteria>
  <done>
    Trial balance returns all accounts with real-time debit/credit sums and computed balances. Ledger shows entries for one account with running balance. Excel export generates .xlsx file. All amounts use decimal.Decimal.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Financial Report service (balance sheet + income statement + snapshot)</name>
  <files>
    internal/finance/dto_report.go
    internal/finance/service_report.go
  </files>
  <read_first>
    internal/finance/dto_report.go
    internal/finance/service_report.go
    internal/finance/model_report.go
  </read_first>
  <action>
    Create the following files:

    1. internal/finance/dto_report.go: BalanceSheetResponse (period_id, year, month, assets, liabilities, equity, asset_total, liability_total, equity_total, generated_at). IncomeStatementResponse (period_id, year, month, revenue, cogs, sga, tax, non_op_income, non_op_expense, income_tax, net_profit, generated_at). MultiPeriodReportRequest (period_ids []int64). MultiPeriodBalanceSheetResponse (periods []PeriodSummary, items []MultiPeriodItem). VATCalculationResponse (period_id, year, month, output_tax, input_tax, net_vat, input_invoices []InvoiceRef, output_invoices []InvoiceRef). CITCalculationResponse (year, quarter, revenue, costs, expenses, profit_before_tax, tax_rate, estimated_cit). TaxDeclarationExport (vat VATCalculationResponse, cit CITCalculationResponse, generated_at).

    2. internal/finance/service_report.go: ReportService. GenerateBalanceSheet(ctx, orgID, periodID): (a) query all ASSET accounts with SUM(journal_entries.amount) where DC=DEBIT minus DC=CREDIT for ending balance; (b) query LIABILITY + EQUITY accounts similarly; (c) compute totals; (d) return BalanceSheetResponse; (e) also save to ReportSnapshot with is_valid=true. GenerateIncomeStatement(ctx, orgID, periodID): similar approach, SUM revenue accounts (6xxx), SUM cost accounts (5xxx), compute net = revenue - costs - expenses. GetBalanceSheet(ctx, orgID, periodID): first check ReportSnapshot for this period/type (is_valid=true), return snapshot data. If no valid snapshot, generate fresh and save snapshot. GetMultiPeriodBalanceSheet(ctx, orgID, periodIDs): call GetBalanceSheet for each, combine into multi-period response with diff and pct change. CalculateVAT(ctx, orgID, year, month): sum OUTPUT invoice tax_amount where date in month, sum verified INPUT invoice tax_amount, net_vat = output - input. CalculateCIT(ctx, orgID, year, quarter): quarterly estimate. CIT = (annual_revenue_ytd - annual_costs_ytd - annual_expenses_ytd) * 0.05 (simplified, V1.0). ExportTaxDeclaration(ctx, orgID, year, month): combine VAT + CIT into TaxDeclarationExport, use excelize to write xlsx.

    Balance sheet key account mappings (D-14, simplified V1.0):
    - Assets: 1001, 1002, 1122, 1123, 1221, 1405, 1407, 1601 (minus 1602), 1701, 1901
    - Liabilities: 2001, 2201, 2202, 2203, 2241, 2501, 2801, 2221
    - Equity: 4001, 4002, 4101, 4103, 4104

    After implementation, update service_test.go:
    - TestBalanceSheet_EquationHolds: verify Assets == Liabilities + Equity
  </action>
  <acceptance_criteria>
    <grep>grep -q 'func.*GenerateBalanceSheet' internal/finance/service_report.go</grep>
    <grep>grep -q 'func.*GenerateIncomeStatement' internal/finance/service_report.go</grep>
    <grep>grep -q 'func.*CalculateVAT' internal/finance/service_report.go</grep>
    <grep>grep -q 'func.*CalculateCIT' internal/finance/service_report.go</grep>
    <grep>grep -q 'decimal.Decimal' internal/finance/service_report.go</grep>
    <grep>grep -q 'ReportSnapshot' internal/finance/service_report.go</grep>
    <grep>grep -q 'func.*GetMultiPeriod' internal/finance/service_report.go</grep>
  </acceptance_criteria>
  <done>
    Balance sheet generates with Assets = Liabilities + Equity. Income statement computes net profit correctly. ReportSnapshot saved on generation. Multi-period response includes period-over-period diff. VAT monthly calculation works. CIT quarterly calculation works.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement Period closing/reopening + Report handler + main.go integration</name>
  <files>
    internal/finance/dto_period.go
    internal/finance/service_period.go
    internal/finance/handler_report.go
    internal/finance/handler.go
    cmd/server/main.go
  </files>
  <read_first>
    internal/finance/dto_period.go
    internal/finance/service_period.go
    internal/finance/handler_report.go
    internal/finance/handler.go
    cmd/server/main.go
    internal/finance/service_report.go
  </read_first>
  <action>
    Create the following files:

    1. internal/finance/dto_period.go: ClosingValidationResponse (can_close bool, errors []string). ClosePeriodRequest (period_id). RevertPeriodRequest (period_id, confirm bool).

    2. internal/finance/service_period.go: PeriodService. ValidateClosing(ctx, orgID, periodID) (ClosingValidationResponse): checks: (a) no draft/submitted vouchers in period; (b) period total debit SUM = period total credit SUM; (c) ASSET and COST account balances are non-negative. Return list of validation errors. ClosePeriod(ctx, orgID, periodID, userID): (a) call ValidateClosing, if !can_close return errors; (b) update period status to CLOSED; (c) update all audited vouchers in period to status=closed; (d) call reportSvc.GenerateBalanceSheet + GenerateIncomeStatement (saves snapshots); (e) return success. RevertClosing(ctx, orgID, periodID): (a) require OWNER role; (b) check no vouchers in period with status=paid (can't revert if payroll/expense vouchers already resulted in payment - simplified: just check if there's a paid expense with voucher in this period); (c) update period status to OPEN; (d) update voucher status=audited; (e) mark ReportSnapshot.is_valid = false (don't delete). GetPeriods(ctx, orgID): list all periods with status.

    3. internal/finance/handler_report.go: ReportHandler. Routes: GET /reports/balance-sheet (GetBalanceSheet, OWNER+ADMIN), GET /reports/income-statement (GetIncomeStatement, OWNER+ADMIN), GET /reports/multi-period (GetMultiPeriod, OWNER+ADMIN), GET /reports/vat (CalculateVAT, OWNER+ADMIN), GET /reports/cit (CalculateCIT, OWNER+ADMIN), GET /reports/tax-declaration/export (ExportTaxDeclaration, OWNER+ADMIN). PeriodHandler: POST /periods/validate (ValidateClosing, OWNER+ADMIN), POST /periods/:id/close (ClosePeriod, OWNER only), POST /periods/:id/revert (RevertClosing, OWNER only), GET /periods (ListPeriods, OWNER+ADMIN). Add periodHandler, reportHandler to handler.go.

    4. Update cmd/server/main.go: Add AutoMigrate for Invoice, ExpenseReimbursement, ReportSnapshot, ExpenseAttachment (if any). Wire FinanceService containing both VoucherService, AccountService, InvoiceService, ExpenseService, BookService, ReportService, PeriodService.
  </action>
  <acceptance_criteria>
    <grep>grep -q 'func.*ValidateClosing' internal/finance/service_period.go</grep>
    <grep>grep -q 'func.*ClosePeriod' internal/finance/service_period.go</grep>
    <grep>grep -q 'func.*RevertClosing' internal/finance/service_period.go</grep>
    <grep>grep -q 'func.*RegisterRoutes' internal/finance/handler_report.go</grep>
    <grep>grep -q 'func.*RegisterRoutes' internal/finance/handler.go</grep>
    <grep>grep -q 'AutoMigrate.*Invoice\|AutoMigrate.*Expense' cmd/server/main.go</grep>
  </acceptance_criteria>
  <done>
    Server compiles with all finance routes. ValidateClosing returns detailed errors. ClosePeriod generates report snapshots and locks period. RevertClosing requires OWNER and invalidates snapshots. Period list shows status. VAT/CIT calculation returns correct numbers. Tax declaration Excel export works.
  </done>
</task>

</tasks>

<verification>
go build ./internal/finance/... 2>&1
go build ./cmd/server/... 2>&1
go test ./internal/finance/service_test.go 2>&1
</verification>

<success_criteria>
- [x] Trial balance real-time query (SUM of journal entries per account per period)
- [x] Ledger by account with running balance
- [x] Balance sheet: Assets = Liabilities + Equity formula holds
- [x] Income statement: net profit = revenue - costs - expenses - tax + non-op
- [x] Closing: all validation checks pass before closing; period status = CLOSED; vouchers locked
- [x] Reverting closing: OWNER only; period back to OPEN; vouchers back to audited; snapshots marked invalid
- [x] Multi-period balance sheet shows period-over-period changes
- [x] VAT: net_vat = SUM(OUTPUT tax_amount) - SUM(INPUT verified tax_amount)
- [x] CIT: quarterly estimate based on accumulated P&L
- [x] Tax declaration Excel export contains VAT + CIT data
- [x] Book export to Excel (.xlsx) for trial balance and ledger
</success_criteria>

<output>
After completion, create `.planning/phases/06-finance/06-03-SUMMARY.md`
</output>
