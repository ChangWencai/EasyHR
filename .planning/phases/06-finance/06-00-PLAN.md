---
phase: 06-finance
plan: '00'
type: execute
wave: '1'
depends_on: []
files_modified:
  - go.mod
  - internal/finance/model_test.go
  - internal/finance/voucher_service_test.go
  - internal/finance/service_test.go
autonomous: true
requirements: []
user_setup: []
gap_closure: false

must_haves:
  truths:
    - "Test scaffold exists before implementation tasks run"
    - "decimal.Decimal dependency available before models reference it"
  artifacts:
    - path: go.mod
      provides: "shopspring/decimal dependency declared"
    - path: internal/finance/model_test.go
      provides: "Model unit test scaffold"
    - path: internal/finance/voucher_service_test.go
      provides: "Voucher service test scaffold"
    - path: internal/finance/service_test.go
      provides: "General service test scaffold"
  key_links:
    - from: internal/finance/model_test.go
      to: go.mod
      via: shopspring/decimal import
      pattern: "github.com/shopspring/decimal"
---

<objective>
Set up test infrastructure and dependencies for Phase 6 finance module. All subsequent plans depend on this to run actual tests (not just `go build`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@go.mod
@.planning/phases/06-finance/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install decimal dependency + create test scaffold files</name>
  <files>
    go.mod
    internal/finance/model_test.go
    internal/finance/voucher_service_test.go
    internal/finance/service_test.go
  </files>
  <read_first>
    go.mod
    test/testutil/testutil.go
  </read_first>
  <action>
    1. Run: `go get github.com/shopspring/decimal` to install the decimal library. Verify go.mod includes `github.com/shopspring/decimal`.

    2. Create test/testutil/finance_helper.go if not exists: Helper functions for finance tests:
       - `TestDB(t *testing.T) *gorm.DB` — returns in-memory SQLite test DB, runs migrations for Account/Period/Voucher/JournalEntry models
       - `CreateTestAccount(db, orgID, code, name, category, normalBalance)` — helper
       - `CreateTestPeriod(db, orgID, year, month)` — helper
       - `CreateTestVoucher(db, orgID, periodID, status)` — helper

    3. Create internal/finance/model_test.go with these placeholder test functions (TDD: tests MUST fail before implementation):
       ```go
       func TestAccountModel_NormalBalance(t *testing.T) {
           // t.Errorf("Account.NormalBalance not yet defined")
       }
       func TestVoucherModel_StatusTransitions(t *testing.T) {
           // t.Errorf("Voucher.Status not yet defined")
       }
       func TestJournalEntry_AmountPrecision(t *testing.T) {
           // t.Errorf("JournalEntry.Amount not yet defined")
       }
       ```

    4. Create internal/finance/voucher_service_test.go with placeholder tests:
       ```go
       func TestCreateVoucher_BalancedEntries(t *testing.T) {
           // t.Errorf("VoucherService.CreateVoucher not yet implemented")
       }
       func TestCreateVoucher_UnbalancedEntries_ReturnsError(t *testing.T) {
           // t.Errorf("VoucherService.CreateVoucher not yet implemented")
       }
       func TestReverseVoucher_FlipsDC(t *testing.T) {
           // t.Errorf("VoucherService.ReverseVoucher not yet implemented")
       }
       ```

    5. Create internal/finance/service_test.go with placeholder tests:
       ```go
       func TestTrialBalance_CalculatesCorrectly(t *testing.T) {
           // t.Errorf("BookService not yet implemented")
       }
       func TestBalanceSheet_EquationHolds(t *testing.T) {
           // t.Errorf("ReportService not yet implemented")
       }
       ```

    Run `go test ./internal/finance/... 2>&1` — tests should FAIL with "not yet implemented" messages.
  </action>
  <verify>
    <automated>go get github.com/shopspring/decimal && go test ./internal/finance/... 2>&1 | grep -q "not yet implemented"</automated>
  </verify>
  <done>
    decimal library in go.mod. Test files exist and compile. Tests fail with "not yet implemented" (ready for RED phase of TDD).
  </done>
</task>

</tasks>

<verification>
go test ./internal/finance/... 2>&1 | head -20
</verification>

<success_criteria>
- [x] go.mod contains shopspring/decimal
- [x] internal/finance/model_test.go exists with failing tests
- [x] internal/finance/voucher_service_test.go exists with failing tests
- [x] internal/finance/service_test.go exists with failing tests
- [x] All subsequent plans can update their verify to run `go test ./internal/finance/...`
</success_criteria>

<output>
After completion, create `.planning/phases/06-finance/06-00-SUMMARY.md`
</output>
