---
phase: 06-finance
plan: '02'
type: execute
wave: '2'
depends_on:
  - '06-01'
files_modified:
  - internal/finance/model_invoice.go
  - internal/finance/model_expense.go
  - internal/finance/dto_invoice.go
  - internal/finance/dto_expense.go
  - internal/finance/service_invoice.go
  - internal/finance/service_expense.go
  - internal/finance/handler_invoice.go
  - internal/finance/handler_expense.go
  - cmd/server/main.go
autonomous: true
requirements:
  - FINC-06
  - FINC-07
  - FINC-08
  - FINC-09
  - FINC-10
user_setup: []
gap_closure: false

must_haves:
  truths:
    - "老板可以手动登记进项/销项发票"
    - "发票可关联至凭证"
    - "员工提交费用报销，老板在线审批"
    - "报销审批通过后自动生成费用凭证"
    - "报销状态可追踪（pending/approved/rejected/paid）"
  artifacts:
    - path: internal/finance/model_invoice.go
      provides: "发票模型"
      exports: ["Invoice", "InvoiceType", "InvoiceStatus"]
    - path: internal/finance/model_expense.go
      provides: "费用报销模型"
      exports: ["ExpenseReimbursement", "ExpenseStatus", "ExpenseType"]
    - path: internal/finance/service_expense.go
      provides: "报销审批+自动凭证生成"
      exports: ["SubmitExpense", "ApproveExpense", "RejectExpense", "MarkExpensePaid"]
  key_links:
    - from: internal/finance/service_expense.go
      to: internal/finance/service_voucher.go
      via: CreateVoucher call on approval
      pattern: "financeSvc.CreateVoucher"
    - from: internal/finance/model_invoice.go
      to: internal/finance/model_voucher.go
      via: Invoice.VoucherID FK
      pattern: "voucher_id.*index"
---

<objective>
实现发票管理（FINC-06/07）和费用报销审批流程（FINC-08/09/10），包含：发票CRUD、发票与凭证关联、费用报销提交/审批/驳回、审批通过后自动生成费用凭证。依赖 Plan 01 的 VoucherService.CreateVoucher。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-finance/06-01-SUMMARY.md
@internal/finance/model_voucher.go
@internal/finance/service_voucher.go
@internal/salary/model.go

<interfaces>
<!-- Key types consumed from Plan 01 -->
```go
// From Plan 01 - VoucherService interface (reference, actual in service_voucher.go)
type VoucherServiceInterface interface {
    CreateVoucher(ctx context.Context, req *CreateVoucherRequest, orgID, userID int64) (*Voucher, error)
}

// From Plan 01 - VoucherService struct fields needed for auto-voucher
type VoucherService struct {
    voucherRepo *VoucherRepository
    periodRepo  *PeriodRepository
    accountRepo *AccountRepository
    db          *gorm.DB
}
```

<!-- Expense auto-voucher mapping (D-25 in 06-CONTEXT) -->
```go
// When expense approved, generate voucher entries:
// expense_type "差旅费" -> debit Account "6602 管理费用-差旅费" (COST)
// expense_type "交通费" -> debit Account "6602 管理费用-交通费" (COST)
// expense_type "招待费" -> debit Account "6602 管理费用-业务招待费" (COST)
// expense_type "办公费" -> debit Account "6602 管理费用-办公费" (COST)
// expense_type "其他"   -> debit Account "6602 管理费用-其他" (COST)
// Credit Account "2241 其他应付款-员工借款" (LIABILITY)
// Find accounts by code pattern: LIKE "6602%" for management expenses
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Implement Invoice model + repository + service + handler</name>
  <files>
    internal/finance/model_invoice.go
    internal/finance/repository.go
    internal/finance/dto_invoice.go
    internal/finance/service_invoice.go
    internal/finance/handler_invoice.go
  </files>
  <action>
    Create the following files following Plan 01 patterns:

    1. internal/finance/model_invoice.go: Invoice model embedding model.BaseModel. Fields: InvoiceType ("INPUT"/"OUTPUT"), Code (varchar 50, 发票代码), Number (varchar 50, 发票号码), Date (date), Amount (decimal.Decimal, 含税金额), TaxRate (decimal.Decimal, 税率如 0.13), TaxAmount (decimal.Decimal, 税额), Status ("unverified"/"verified"/"deducted"), VoucherID (int64 nullable FK to vouchers), Remark (varchar 500). TableName: "invoices". Index on (org_id, invoice_type), (voucher_id).

    2. internal/finance/dto_invoice.go: CreateInvoiceRequest (invoice_type, code, number, date, amount, tax_rate, status, remark). LinkVoucherRequest (invoice_id, voucher_id). Response: InvoiceResponse.

    3. internal/finance/service_invoice.go: InvoiceService. CreateInvoice: validates amount and tax_rate via decimal, saves invoice. LinkToVoucher: sets voucher_id on invoice. GetMonthlySummary(orgID, year, month): returns {output_tax_sum, input_tax_sum_verified} for VAT calculation (FINC-07). ListInvoices: paginated with filters (type, status, year, month).

    4. internal/finance/handler_invoice.go: InvoiceHandler. Routes: GET /invoices (List, OWNER+ADMIN), POST /invoices (Create, OWNER+ADMIN), PUT /invoices/:id (Update, OWNER+ADMIN), POST /invoices/:id/link-voucher (Link, OWNER+ADMIN), GET /invoices/monthly-summary (GetMonthlySummary, OWNER+ADMIN). Also add InvoiceRepository to repository.go.
  </action>
  <verify>
    <automated>go build ./internal/finance/... 2>&1 | head -20</automated>
  </verify>
  <done>
    Invoice model created with Amount/TaxAmount as decimal.Decimal. Monthly summary groups by invoice_type and sums tax_amount for verified invoices. VoucherID linking works.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement ExpenseReimbursement model + repository + service + handler</name>
  <files>
    internal/finance/model_expense.go
    internal/finance/repository.go
    internal/finance/dto_expense.go
    internal/finance/service_expense.go
    internal/finance/handler_expense.go
    internal/finance/errors.go
  </files>
  <action>
    Create the following files:

    1. internal/finance/model_expense.go: ExpenseReimbursement model embedding model.BaseModel. Fields: EmployeeID (int64, FK to employees), Amount (decimal.Decimal, stored as string), ExpenseType (varchar 20: "travel"/"transport"/"entertainment"/"office"/"other"), Description (varchar 500), Attachments (varchar 1000, JSON array of OSS URLs max 9), Status (varchar 20: "pending"/"approved"/"rejected"/"paid"), ApproverID (int64 nullable), ApprovedAt (nullable timestamp), ApprovedNote (varchar 500), RejectedAt (nullable timestamp), RejectedNote (varchar 500), PaidAt (nullable timestamp), PaidNote (varchar 500), VoucherID (int64 nullable FK). TableName: "expense_reimbursements". Index on (org_id, status), (employee_id).

    2. internal/finance/dto_expense.go: CreateExpenseRequest (employee_id, amount string, expense_type, description, attachments []string). ApproveExpenseRequest (expense_id, note). RejectExpenseRequest (expense_id, note). MarkPaidRequest (expense_id, note). Response: ExpenseResponse with status_history. MonthlyExpenseSummary (for dashboard).

    3. internal/finance/service_expense.go: ExpenseService. IMPORTANT: Add voucherSvc VoucherServiceInterface field to ExpenseService struct. CreateExpense: MEMBER role creates expense (org_id from context). SubmitExpense: MEMBER submits own expense (employee_id from token). ApproveExpense (orgID, approverID, expenseID, note): (a) validate status=pending; (b) update status=approved; (c) find management expense account by name pattern (D-25: "管理费用-%费" based on expense_type); (d) call voucherSvc.CreateVoucher with: entries=[{account: 管理费用-XXX, DC: DEBIT, amount: expense.Amount, summary: "费用报销-员工XXX"}, {account: 其他应付款-员工借款, DC: CREDIT, amount: expense.Amount, summary: "待支付"}]; (e) update expense.VoucherID = created_voucher.ID; (f) return voucher info in response. RejectExpense: validate status=pending, update to rejected. MarkExpensePaid: (a) validate status=approved; (b) find bank account (code "1002"); (c) call voucherSvc.CreateVoucher: entries=[{account: 其他应付款-员工借款, DC: DEBIT}, {account: 银行存款, DC: CREDIT, amount: expense.Amount}]; (d) update status=paid. ListExpenses: paginated filter by status + employee_id.

    4. internal/finance/handler_expense.go: ExpenseHandler. Routes: POST /expenses (Submit, OWNER+ADMIN+MEMBER), GET /expenses (List, OWNER+ADMIN), GET /expenses/:id (Detail, OWNER+ADMIN), POST /expenses/:id/approve (Approve, OWNER+ADMIN), POST /expenses/:id/reject (Reject, OWNER+ADMIN), POST /expenses/:id/mark-paid (MarkPaid, OWNER+ADMIN). For MEMBER submit: extract employee_id from employee-user mapping. Add ExpenseRepository to repository.go.
  </action>
  <verify>
    <automated>go build ./internal/finance/... 2>&1 | head -20</automated>
  </verify>
  <done>
    Expense approval auto-generates voucher with correct debit (management expense account) and credit (other payables). VoucherID stored on expense record. Status transitions: pending -> approved -> paid (or pending -> rejected). Amount uses decimal.Decimal throughout.
  </done>
</task>

</tasks>

<verification>
go build ./cmd/server/... 2>&1
go vet ./internal/finance/... 2>&1
</verification>

<success_criteria>
- [x] Invoice: create/list/update, monthly VAT summary by summing verified INPUT/OUTPUT tax amounts
- [x] Invoice can be linked to a Voucher (VoucherID FK set)
- [x] Expense: MEMBER can submit, OWNER+ADMIN can approve/reject/mark-paid
- [x] Approve expense auto-generates voucher: debit 管理费用-XXX, credit 其他应付款
- [x] MarkPaid auto-generates payment voucher: debit 其他应付款, credit 银行存款
- [x] Expense status history tracked (approved_at, rejected_at, paid_at with notes)
- [x] Amount field on both Invoice and Expense uses decimal.Decimal (not float64)
</success_criteria>

<output>
After completion, create `.planning/phases/06-finance/06-02-SUMMARY.md`
</output>
