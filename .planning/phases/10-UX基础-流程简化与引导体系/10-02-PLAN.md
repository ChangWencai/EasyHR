---
phase: "10"
plan: "02"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "frontend/src/components/common/ExcelImportWizard.vue"
  - "frontend/src/views/employee/EmployeeList.vue"
autonomous: true
requirements:
  - "UX-02"

must_haves:
  truths:
    - "老板可在员工列表页通过Excel批量导入员工，支持50人+"
    - "Excel预览页标注合格行（绿色）和错误行（红色），支持部分导入"
    - "导入流程3步：下载模板→上传预览→确认导入"
  artifacts:
    - path: "frontend/src/components/common/ExcelImportWizard.vue"
      provides: "通用Excel导入向导，含上传/预览/确认3步"
      min_lines: 200
    - path: "frontend/src/views/employee/EmployeeList.vue"
      provides: "员工列表页顶部新增「批量入职」按钮入口"
      exports: ["批量入职"]
  key_links:
    - from: "frontend/src/views/employee/EmployeeList.vue"
      to: "frontend/src/components/common/ExcelImportWizard.vue"
      via: "el-button @click opens dialog containing ExcelImportWizard"
    - from: "ExcelImportWizard.vue"
      to: "frontend/src/api/employee.ts"
      via: "calls batch import API"
    - from: "ExcelImportWizard.vue"
      to: "xlsx (SheetJS)"
      via: "import * as XLSX from 'xlsx' for Excel parsing"
---

<objective>
构建通用ExcelImportWizard组件（3步：下载模板→上传预览→确认导入），并在员工列表页集成批量入职入口，实现批量操作(UX-02)。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-UX基础-流程简化与引导体系/10-CONTEXT.md
@.planning/phases/10-UX基础-流程简化与引导体系/10-UI-SPEC.md

## UI-SPEC Layout (Excel Import Wizard)

Step 1: Upload
```
┌──────────────────────────────────────────┐
│  [拖拽上传区域]                          │
│  拖拽Excel文件到此处，或  点击上传       │
│  支持 .xlsx .xls 格式                    │
└──────────────────────────────────────────┘
```

Step 2: Preview
```
┌──────────────────────────────────────────┐
│  预览：{n}条记录  ✓{n}合格  ✗{n}有误    │
│  ┌──────────────────────────────────────┐ │
│  │ ▌合格行  - green border + bg tint   │ │
│  │ ▌错误行  - red border + bg tint     │ │
│  └──────────────────────────────────────┘ │
│  [仅导入合格项]          [重新上传]       │
└──────────────────────────────────────────┘
```

Step 3: Confirm
```
┌──────────────────────────────────────────┐
│  ✓ 确认导入 {n} 条员工信息？             │
│  [取消]              [确认导入]           │
└──────────────────────────────────────────┘
```

## UI-SPEC Row Color Coding (D-10-05, D-10-06):
- Qualified row: left border `4px solid #10B981`, row background `rgba(16, 185, 129, 0.05)`
- Error row: left border `4px solid #EF4444`, row background `rgba(239, 68, 68, 0.05)`, error cell text `#EF4444`
- Error badge: `background: #EF4444`, text white
- Qualified badge: `background: #10B981`, text white

## UI-SPEC Copywriting:
- Step 1 button: `下载模板` / `上传Excel`
- Step 2 qualified label: `{n} 条合格`
- Step 2 error label: `{n} 条有误`
- Step 2 import qualified only: `仅导入合格项`
- Destructive confirm: `确定要将 {n} 名员工标记为导入吗？`

## Employee Fields for Template (per D-10-04 template):
Columns: 姓名, 手机号, 身份证号, 入职日期(YYYY-MM-DD), 岗位, 薪资

## EmployeeList.vue expected structure:
- Located at: frontend/src/views/employee/EmployeeList.vue
- Has existing employee table/list
- Add "批量入职" el-button in the page header or toolbar
- Opens dialog with ExcelImportWizard

## Key libraries:
- xlsx (SheetJS): already in package.json — import as `import * as XLSX from 'xlsx'`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ExcelImportWizard.vue component</name>
  <files>frontend/src/components/common/ExcelImportWizard.vue</files>
  <read_first>frontend/src/api/employee.ts</read_first>
  <action>
Create `frontend/src/components/common/ExcelImportWizard.vue` — a generic Excel import wizard component that accepts props for template configuration and import API.

**Props interface:**
```typescript
interface ExcelImportWizardProps {
  templateLabel: string       // e.g. "员工" — used in copy: "下载{templateLabel}模板"
  templateFields: string[]    // column headers expected in the Excel
  importApi: (rows: Record<string, unknown>[]) => Promise<{ success: number; failed: number }>
  // dialogVisible: passed from parent to control show/hide
}
```

**Internal types:**
```typescript
interface ParsedRow {
  _raw: Record<string, unknown>   // original Excel row
  _qualified: boolean              // passes validation
  _errors: Record<string, string>  // field → error message
  // Dynamic fields from template (name, phone, id_number, entry_date, position, salary)
}
```

**State:**
```typescript
const currentStep = ref(0)           // 0=upload, 1=preview, 2=confirm
const fileList = ref<File[]>([])
const loading = ref(false)
const parsedRows = ref<ParsedRow[]>([])
const templateFields = ['姓名', '手机号', '身份证号', '入职日期', '岗位', '薪资']  // default
```

**Step 0 — Upload:**
```vue
<el-upload
  ref="uploadRef"
  class="excel-upload-area"
  drag
  :auto-upload="false"
  :limit="1"
  accept=".xlsx,.xls"
  :on-change="handleFileChange"
>
  <el-icon class="upload-icon"><UploadFilled /></el-icon>
  <div class="upload-text">拖拽Excel文件到此处，或 <em>点击上传</em></div>
  <div class="upload-hint">支持 .xlsx .xls 格式</div>
</el-upload>
<div class="step-actions">
  <el-button @click="downloadTemplate">下载模板</el-button>
  <el-button type="primary" :disabled="fileList.length === 0" @click="parseFile">
    下一步
  </el-button>
</div>
```

**Step 1 — Preview:**
```vue
<div class="preview-header">
  <span>预览：{{ parsedRows.length }}条记录</span>
  <span class="qualified-badge">✓{{ qualifiedCount }}条合格</span>
  <span class="error-badge">✗{{ errorCount }}条有误</span>
</div>
<el-table :data="parsedRows" max-height="400" row-class-name="excel-row">
  <!-- Dynamic columns from templateFields -->
  <el-table-column v-for="field in templateFields" :key="field"
    :prop="field" :label="field">
    <template #default="{ row }">
      <span :class="{ 'cell-error': row._errors[field] }">
        {{ row._raw[field] }}
        <span v-if="row._errors[field]" class="field-error">{{ row._errors[field] }}</span>
      </span>
    </template>
  </el-table-column>
</el-table>
<div class="step-actions">
  <el-button @click="currentStep = 0">重新上传</el-button>
  <el-button type="primary" :disabled="qualifiedCount === 0" @click="currentStep = 2">
    仅导入合格项 ({{ qualifiedCount }}条)
  </el-button>
</div>
```

**Step 2 — Confirm:**
```vue
<div class="confirm-box">
  <el-icon color="var(--success)" :size="48"><SuccessFilled /></el-icon>
  <p class="confirm-text">确认导入 {{ qualifiedCount }} 条{{ templateLabel }}信息？</p>
</div>
<div class="step-actions">
  <el-button @click="currentStep = 1">上一步</el-button>
  <el-button type="primary" :loading="importing" @click="confirmImport">确认导入</el-button>
</div>
```

**Key functions:**

1. `downloadTemplate()` — use xlsx to create and download a template:
```typescript
import * as XLSX from 'xlsx'
function downloadTemplate() {
  const ws = XLSX.utils.json_to_sheet([])  // empty sheet
  XLSX.utils.sheet_add_aoa(ws, [templateFields])  // add header row
  const wb = XLSX.utils.book_new()
  XLSX.utils.book_append_sheet(wb, ws, 'Sheet1')
  XLSX.writeFile(wb, `${props.templateLabel}导入模板.xlsx`)
}
```

2. `handleFileChange(file: File)` — read file and store:
```typescript
const excelFile = ref<File | null>(null)
function handleFileChange(file: any) {
  excelFile.value = file.raw
  fileList.value = [file.raw]
}
```

3. `parseFile()` — parse Excel, validate each row:
```typescript
async function parseFile() {
  if (!excelFile.value) return
  loading.value = true
  try {
    const buffer = await excelFile.value.arrayBuffer()
    const wb = XLSX.read(buffer, { type: 'array' })
    const ws = wb.Sheets[wb.SheetNames[0]]
    const rawData = XLSX.utils.sheet_to_json<Record<string, unknown>>(ws)

    parsedRows.value = rawData.map((row) => {
      const errors: Record<string, string> = {}
      // Required field: 姓名
      if (!row['姓名']) errors['姓名'] = '姓名不能为空'
      // Phone: 11 digits starting with 1
      const phone = String(row['手机号'] || '')
      if (!/^1[3-9]\d{9}$/.test(phone)) errors['手机号'] = '手机号格式错误，请输入11位数字'
      // ID number: 18 digits
      const id = String(row['身份证号'] || '')
      if (!/^[1-9]\d{5}(18|19|20)\d{2}(0[1-9]|1[0-2])(0[1-9]|[12]\d|3[01])\d{3}[\dXx]$/.test(id))
        errors['身份证号'] = '身份证号格式不正确'
      // entry_date: YYYY-MM-DD format
      const date = String(row['入职日期'] || '')
      if (!/^\d{4}-\d{2}-\d{2}$/.test(date)) errors['入职日期'] = '日期格式需为YYYY-MM-DD'
      return { _raw: row, _qualified: Object.keys(errors).length === 0, _errors: errors }
    })
    currentStep.value = 1
  } finally {
    loading.value = false
  }
}
```

4. `confirmImport()` — call API with qualified rows only:
```typescript
const importing = ref(false)
async function confirmImport() {
  importing.value = true
  try {
    const qualified = parsedRows.value.filter(r => r._qualified).map(r => r._raw)
    const result = await props.importApi(qualified)
    ElMessage.success(`导入完成：成功${result.success}条，失败${result.failed}条`)
    emit('complete', result)
    emit('update:dialogVisible', false)
  } catch {
    ElMessage.error('导入失败，请稍后重试')
  } finally {
    importing.value = false
  }
}
```

**CSS (add to component `<style scoped>`):**
```css
.excel-upload-area {
  width: 100%;
  border: 2px dashed var(--el-border-color);
  border-radius: 12px;
  padding: 48px;
  text-align: center;
  transition: border-color 0.2s;
}
.excel-upload-area:hover { border-color: var(--primary) }
.upload-icon { font-size: 48px; color: var(--el-color-primary); margin-bottom: 16px }
.upload-text { font-size: 16px; color: var(--text-primary); margin-bottom: 8px }
.upload-hint { font-size: 12px; color: var(--text-secondary) }
.step-actions { display: flex; justify-content: flex-end; gap: 12px; margin-top: 24px }
.preview-header { display: flex; align-items: center; gap: 16px; margin-bottom: 16px; font-size: 14px }
.qualified-badge { background: #10B981; color: white; padding: 2px 8px; border-radius: 4px; font-size: 12px }
.error-badge { background: #EF4444; color: white; padding: 2px 8px; border-radius: 4px; font-size: 12px }
.cell-error { color: #EF4444 }
.field-error { display: block; font-size: 12px }
.confirm-box { display: flex; flex-direction: column; align-items: center; gap: 16px; padding: 32px; text-align: center }
.confirm-text { font-size: 16px; font-weight: 500 }
```

**el-table row class for coloring (D-10-05):**
```typescript
// Add to template: :row-class-name="getRowClass"
function getRowClass({ row }: { row: ParsedRow }) {
  if (!row._qualified) return 'error-row'
  return 'qualified-row'
}
```
Add CSS:
```css
:deep(.qualified-row) { border-left: 4px solid #10B981; background: rgba(16, 185, 129, 0.05) }
:deep(.error-row) { border-left: 4px solid #EF4444; background: rgba(239, 68, 68, 0.05) }
```

**Emit events:**
```typescript
const emit = defineEmits<{
  complete: [{ success: number; failed: number }]
  'update:dialogVisible': [visible: boolean]
}>()
```
</action>
  <verify>
<automated>
grep -c "ExcelImportWizard\|parseFile\|downloadTemplate" frontend/src/components/common/ExcelImportWizard.vue 2>/dev/null | awk -F: '{sum+=$2} END{print sum >= 3 ? "PASS" : "FAIL"}'
</automated>
  </verify>
  <acceptance_criteria>
- File exists at frontend/src/components/common/ExcelImportWizard.vue
- Uses `<script setup lang="ts">` with props for templateLabel/templateFields/importApi
- Has 3-step flow: upload → preview → confirm
- Uses xlsx (SheetJS) for Excel parsing
- Validates phone (11 digits), ID number (18 digits), entry_date (YYYY-MM-DD)
- qualifiedCount and errorCount computed from parsed rows
- el-table with row-class-name for colored rows
- "仅导入合格项" button on Step 1
- confirmImport calls props.importApi with qualified rows only
</acceptance_criteria>
  <done>ExcelImportWizard.vue created with all required features</done>
</task>

<task type="auto">
  <name>Task 2: Integrate batch import into EmployeeList.vue</name>
  <files>frontend/src/views/employee/EmployeeList.vue</files>
  <read_first>frontend/src/views/employee/EmployeeList.vue, frontend/src/components/common/ExcelImportWizard.vue</read_first>
  <action>
Modify `frontend/src/views/employee/EmployeeList.vue` to add a "批量入职" button in the page header/toolbar and wire it to the ExcelImportWizard.

**Steps:**

1. **Add imports** in the script section:
```typescript
import ExcelImportWizard from '@/components/common/ExcelImportWizard.vue'
import { batchImportEmployees } from '@/api/employee'
```

2. **Add dialog state**:
```typescript
const showBatchImport = ref(false)
```

3. **Add "批量入职" button** to the page header or toolbar (find the existing action buttons section, add before or after the existing "新增员工" button):
```vue
<el-button @click="showBatchImport = true">
  <el-icon><Upload /></el-icon>
  批量入职
</el-button>
```

4. **Add dialog** wrapping ExcelImportWizard (place after the page template's closing div):
```vue
<el-dialog
  v-model="showBatchImport"
  title="批量入职"
  width="680px"
  :close-on-click-modal="false"
>
  <ExcelImportWizard
    template-label="员工"
    :template-fields="['姓名', '手机号', '身份证号', '入职日期', '岗位', '薪资']"
    :import-api="batchImportEmployees"
    dialog-visible
    @complete="handleBatchComplete"
    @update:dialog-visible="showBatchImport = $event"
  />
</el-dialog>
```

5. **Add handler**:
```typescript
async function handleBatchComplete(result: { success: number; failed: number }) {
  showBatchImport.value = false
  // Refresh employee list
  await loadEmployees()
}
```

**Verify EmployeeList.vue exists** before modifying — if not found, create stub:
```bash
ls frontend/src/views/employee/EmployeeList.vue 2>/dev/null || echo "NOT_FOUND"
```
If NOT_FOUND, create a minimal EmployeeList.vue that contains the batch import dialog (the list can be added later — for now focus on the wizard integration point).

**If EmployeeList.vue does not exist:** Create it as a minimal list page with the ExcelImportWizard dialog:
- Import useDashboardStore to get org employees
- Table showing employee name/phone/status
- "新增员工" button → router.push('/employee/create')
- "批量入职" button → showBatchImport dialog
- loadEmployees() fetches from employee API

**Backend API needed:** Ensure `batchImportEmployees` exists in `frontend/src/api/employee.ts`. If not, add:
```typescript
export async function batchImportEmployees(rows: Record<string, unknown>[]): Promise<{ success: number; failed: number }> {
  const res = await request.post('/employees/batch-import', { employees: rows })
  return res.data
}
```
</action>
  <verify>
<automated>
grep -c "批量入职\|ExcelImportWizard" frontend/src/views/employee/EmployeeList.vue 2>/dev/null | awk -F: '{sum+=$2} END{print sum >= 2 ? "PASS" : "FAIL"}'
</automated>
  </verify>
  <acceptance_criteria>
- grep confirms "批量入职" in EmployeeList.vue
- grep confirms "ExcelImportWizard" import in EmployeeList.vue
- grep confirms "showBatchImport" ref added
- el-dialog wraps ExcelImportWizard with dialogVisible
- batchImportEmployees API call wired
- handleBatchComplete refreshes list on complete
</acceptance_criteria>
  <done>EmployeeList.vue updated with batch import button and dialog</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user→ExcelParse | Excel file content crosses from user to JS parser |
| client→API | Parsed Excel rows sent to backend for import |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Injection | ExcelImportWizard parseFile | mitigate | All Excel cell values treated as string, validated with strict patterns — no raw string interpolation |
| T-10-05 | Information Disclosure | ExcelImportWizard | mitigate | Parsed data held only in component state, not persisted or logged |
| T-10-06 | Denial of Service | ExcelImportWizard | accept | Large file (5000+ rows) — el-table uses virtual scroll via max-height=400, memory is bounded |
</threat_model>

<verification>
grep "ExcelImportWizard" frontend/src/components/common/ExcelImportWizard.vue
grep "批量入职" frontend/src/views/employee/EmployeeList.vue
grep "xlsx\|XLSX" frontend/src/components/common/ExcelImportWizard.vue
grep "qualified-row\|error-row" frontend/src/components/common/ExcelImportWizard.vue
</verification>

<success_criteria>
ExcelImportWizard.vue created with 3-step flow (upload→preview→confirm), xlsx parsing, row validation, color-coded preview, and partial import support. EmployeeList.vue updated with batch import button and dialog. batchImportEmployees API wired.
</success_criteria>

<output>
After completion, create `.planning/phases/10-UX基础-流程简化与引导体系/10-02-SUMMARY.md`
</output>
