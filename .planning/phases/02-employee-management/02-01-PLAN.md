---
phase: 02-employee-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/employee/model.go
  - internal/employee/dto.go
  - internal/employee/repository.go
  - internal/employee/service.go
  - internal/employee/handler.go
  - internal/employee/repository_test.go
  - internal/employee/service_test.go
  - test/testutil/db.go
  - cmd/server/main.go
  - go.mod
autonomous: true
requirements:
  - EMPL-02
  - EMPL-03
  - EMPL-04

must_haves:
  truths:
    - "老板可手动录入员工信息（姓名、手机号、身份证号、岗位、入职日期），敏感字段自动加密存储"
    - "员工列表支持按姓名/岗位模糊搜索、手机号精确搜索、状态筛选"
    - "员工档案可导出 Excel（敏感字段脱敏），仅 OWNER/ADMIN 可导出"
    - "MEMBER 只能查看脱敏数据，不可导出、不可查看完整敏感信息"
  artifacts:
    - path: "internal/employee/model.go"
      provides: "Employee 数据模型（含加密双列字段）"
      contains: "type Employee struct"
    - path: "internal/employee/repository.go"
      provides: "GORM CRUD + 搜索 + 分页 + 多租户隔离"
      exports: ["NewRepository"]
    - path: "internal/employee/service.go"
      provides: "创建/更新/搜索/导出/敏感信息查看 业务逻辑"
      exports: ["NewService"]
    - path: "internal/employee/handler.go"
      provides: "HTTP 端点 + RBAC 路由注册"
      exports: ["NewHandler"]
    - path: "internal/employee/repository_test.go"
      provides: "Repository 层单元测试"
    - path: "internal/employee/service_test.go"
      provides: "Service 层单元测试"
  key_links:
    - from: "internal/employee/handler.go"
      to: "/api/v1/employees"
      via: "RegisterRoutes"
      pattern: "func.*RegisterRoutes"
    - from: "internal/employee/service.go"
      to: "internal/common/crypto"
      via: "Encrypt/Decrypt/HashSHA256/MaskPhone/MaskIDCard"
      pattern: "crypto\\.(Encrypt|Decrypt|HashSHA256)"
    - from: "internal/employee/repository.go"
      to: "internal/common/middleware"
      via: "TenantScope(orgID)"
      pattern: "middleware\\.TenantScope"
    - from: "cmd/server/main.go"
      to: "internal/employee"
      via: "AutoMigrate + RegisterRoutes + 依赖注入"
      pattern: "employee\\."

---

# Plan 02-01: Employee Model + CRUD + Search + Excel Export

<objective>
建立员工档案管理的完整三层架构（model/repository/service/handler），包含 Employee 数据模型、加密双列存储、CRUD 操作、多维度搜索（姓名/岗位 ILIKE + 手机号 hash 精确 + 状态筛选）、分页列表、Excel 导出（excelize）、RBAC 权限控制（OWNER/ADMIN 全权限，MEMBER 只读脱敏）、以及完整的单元测试。

Purpose: 这是 Phase 2 的基础模块，后续入职邀请（Plan 02）、离职管理（Plan 03）、合同管理（Plan 04）都依赖 Employee 模型和 Service。
Output: 完整的 internal/employee/ 包，注册到 main.go，Repository + Service 单元测试通过。
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</execution_context>

<context>
@.planning/phases/02-employee-management/02-CONTEXT.md
@.planning/phases/02-employee-management/02-RESEARCH.md

<interfaces>
<!-- Phase 1 已有接口 — executor 直接使用 -->

From internal/common/model/base.go:
```go
type BaseModel struct {
    ID        int64          `gorm:"primaryKey;autoIncrement" json:"id"`
    OrgID     int64          `gorm:"column:org_id;index;not null" json:"org_id"`
    CreatedBy int64          `gorm:"column:created_by" json:"created_by"`
    CreatedAt time.Time      `gorm:"column:created_at;autoCreateTime" json:"created_at"`
    UpdatedBy int64          `gorm:"column:updated_by" json:"updated_by"`
    UpdatedAt time.Time      `gorm:"column:updated_at;autoUpdateTime" json:"updated_at"`
    DeletedAt gorm.DeletedAt `gorm:"column:deleted_at;index" json:"-"`
}
```

From internal/common/model/org.go:
```go
type Organization struct {
    BaseModel
    Name         string `gorm:"column:name;type:varchar(200);not null" json:"name"`
    CreditCode   string `gorm:"column:credit_code;type:varchar(18);uniqueIndex:...,where:deleted_at IS NULL;not null" json:"credit_code"`
    City         string `gorm:"column:city;type:varchar(50);not null" json:"city"`
    ContactName  string `gorm:"column:contact_name;type:varchar(50)" json:"contact_name"`
    ContactPhone string `gorm:"column:contact_phone;type:varchar(200)" json:"contact_phone"`
    Status       string `gorm:"column:status;type:varchar(20);default:active" json:"status"`
}
```

From internal/common/crypto/aes.go:
```go
func Encrypt(plaintext string, key []byte) (string, error)
func Decrypt(encoded string, key []byte) (string, error)
```

From internal/common/crypto/hash.go:
```go
func HashSHA256(input string) string
```

From internal/common/crypto/mask.go:
```go
func MaskPhone(phone string) string   // 138****8000
func MaskIDCard(idCard string) string  // 110108****1234
```

From internal/common/middleware/tenant.go:
```go
func TenantScope(orgID int64) func(db *gorm.DB) *gorm.DB
```

From internal/common/middleware/rbac.go:
```go
func RequireRole(roles ...string) gin.HandlerFunc
```

From internal/common/response/response.go:
```go
func Success(c *gin.Context, data interface{})
func Error(c *gin.Context, httpStatus, code int, message string)
func PageSuccess(c *gin.Context, data interface{}, total int64, page, pageSize int)
func BadRequest(c *gin.Context, message string)
func Unauthorized(c *gin.Context, message string)
func Forbidden(c *gin.Context, message string)
```

From internal/common/config/config.go:
```go
type CryptoConfig struct {
    AESKey string
}
```

From test/testutil/db.go:
```go
func SetupTestDB() (*gorm.DB, error)
func CreateTestOrg(db *gorm.DB, name, creditCode, city string) (*model.Organization, error)
func CreateTestUser(db *gorm.DB, orgID int64, name, phoneHash, role string) (*model.User, error)
func CleanupTestDB(db *gorm.DB) error
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Employee Model + Repository + 单元测试</name>
  <files>
    internal/employee/model.go,
    internal/employee/repository.go,
    internal/employee/repository_test.go,
    test/testutil/db.go
  </files>
  <behavior>
    - Test: 创建 Employee 记录后，数据库中 phone_encrypted/id_card_encrypted/bank_account_encrypted 非空，phone_hash/id_card_hash/bank_account_hash 为 SHA-256 值
    - Test: 同一 org_id 下重复 phone_hash 插入返回错误（部分唯一索引）
    - Test: 不同 org_id 下相同 phone_hash 可正常创建（多租户隔离）
    - Test: 搜索姓名 ILIKE 返回匹配结果，不匹配返回空
    - Test: 搜索手机号通过 phone_hash 精确匹配
    - Test: 按状态筛选返回正确结果
    - Test: 分页查询返回正确的 total、数据量
    - Test: 软删除后的记录不出现在查询结果中
    - Test: 从身份证号提取的 gender 和 birth_date 正确保存
  </behavior>
  <read_first>
    - internal/common/model/base.go
    - internal/common/model/user.go
    - internal/user/repository.go
    - internal/common/crypto/hash.go
    - internal/common/crypto/aes.go
    - internal/common/crypto/mask.go
    - internal/common/middleware/tenant.go
    - test/testutil/db.go
  </read_first>
  <action>
    1. **安装 excelize 依赖**: `go get github.com/xuri/excelize/v2@v2.10.1`

    2. **创建 internal/employee/model.go**:
       ```go
       package employee

       import (
           "time"
           "github.com/wencai/easyhr/internal/common/model"
           "gorm.io/gorm"
       )

       type Employee struct {
           model.BaseModel
           Name                     string         `gorm:"column:name;type:varchar(50);not null" json:"name"`
           PhoneEncrypted           string         `gorm:"column:phone_encrypted;type:varchar(200);not null" json:"-"`
           PhoneHash                string         `gorm:"column:phone_hash;type:varchar(64);uniqueIndex:idx_emp_phone_hash,where:deleted_at IS NULL;not null" json:"-"`
           IDCardEncrypted          string         `gorm:"column:id_card_encrypted;type:varchar(200)" json:"-"`
           IDCardHash               string         `gorm:"column:id_card_hash;type:varchar(64);uniqueIndex:idx_emp_idcard_hash,where:deleted_at IS NULL" json:"-"`
           Gender                   string         `gorm:"column:gender;type:varchar(10)" json:"gender"`
           BirthDate                *time.Time     `gorm:"column:birth_date;type:date" json:"birth_date"`
           Position                 string         `gorm:"column:position;type:varchar(100);not null" json:"position"`
           HireDate                 time.Time      `gorm:"column:hire_date;type:date;not null" json:"hire_date"`
           Status                   string         `gorm:"column:status;type:varchar(20);not null;default:pending" json:"status"`
           UserID                   *int64         `gorm:"column:user_id" json:"user_id"`
           BankName                 string         `gorm:"column:bank_name;type:varchar(100)" json:"bank_name"`
           BankAccountEncrypted     string         `gorm:"column:bank_account_encrypted;type:varchar(200)" json:"-"`
           BankAccountHash          string         `gorm:"column:bank_account_hash;type:varchar(64);uniqueIndex:idx_emp_bank_hash,where:deleted_at IS NULL" json:"-"`
           EmergencyContact         string         `gorm:"column:emergency_contact;type:varchar(50)" json:"emergency_contact"`
           EmergencyPhoneEncrypted  string         `gorm:"column:emergency_phone_encrypted;type:varchar(200)" json:"-"`
           EmergencyPhoneHash       string         `gorm:"column:emergency_phone_hash;type:varchar(64)" json:"-"`
           Address                  string         `gorm:"column:address;type:varchar(500)" json:"address"`
           Remark                   string         `gorm:"column:remark;type:text" json:"remark"`
           ResignationDate          *time.Time     `gorm:"column:resignation_date;type:date" json:"resignation_date"`
           ResignationReason        string         `gorm:"column:resignation_reason;type:varchar(500)" json:"resignation_reason"`
       }

       func (Employee) TableName() string {
           return "employees"
       }
       ```

    3. **创建 internal/employee/repository.go** — 参照 internal/user/repository.go 模式:
       - `NewRepository(db *gorm.DB) *Repository`
       - `Create(emp *Employee) error`
       - `FindByID(orgID, id int64) (*Employee, error)` — 使用 TenantScope
       - `Update(orgID, id int64, updates map[string]interface{}) error` — 使用 TenantScope
       - `Delete(orgID, id int64) error` — 软删除，使用 TenantScope
       - `List(orgID int64, params SearchParams, page, pageSize int) ([]Employee, int64, error)` — 搜索+分页
       - `FindByPhoneHash(orgID int64, phoneHash string) (*Employee, error)` — 手机号唯一性检查
       - `FindByIDCardHash(orgID int64, idCardHash string) (*Employee, error)` — 身份证号唯一性检查
       - `FindAllForExport(orgID int64, params SearchParams) ([]Employee, error)` — 导出（不分页）

       SearchParams 结构体:
       ```go
       type SearchParams struct {
           Name     string
           Position string
           Phone    string  // 明文，repository 内部转 hash
           Status   string
       }
       ```

       List 方法搜索逻辑（per D-11）:
       - Name 非空: `WHERE name ILIKE '%name%'`
       - Position 非空: `WHERE position ILIKE '%position%'`
       - Phone 非空: `crypto.HashSHA256(phone)` → `WHERE phone_hash = hash`
       - Status 非空: `WHERE status = status`
       - 所有查询带 `org_id = ?` (TenantScope)

    4. **添加辅助函数 extractFromIDCard(idCard string) (gender string, birthDate time.Time, err error)**:
       - 18位身份证：第 16-17 位奇数="男"，偶数="女"
       - 第 7-14 位 = YYYYMMDD，`time.Parse("20060102", ...)`
       - 返回 gender（"男"/"女"）和 birthDate

    5. **更新 test/testutil/db.go** — 在 SetupTestDB 的 AutoMigrate 中添加 `&employee.Employee{}`，添加 `CreateTestEmployee` helper:
       ```go
       func CreateTestEmployee(db *gorm.DB, orgID int64, name, phoneHash, position, status string) (*employee.Employee, error)
       ```

    6. **创建 internal/employee/repository_test.go** — 使用 SQLite 内存数据库测试:
       - TestCreateEmployee: 创建员工，验证 ID > 0 且 org_id 正确
       - TestDuplicatePhoneHash: 同 org_id 下重复 phone_hash 返回错误
       - TestCrossOrgSamePhone: 不同 org_id 下相同 phone_hash 可创建
       - TestSearchByName: ILIKE 模糊匹配
       - TestSearchByPhoneHash: hash 精确匹配
       - TestSearchByStatus: 状态筛选
       - TestPagination: 分页 total 和数据量正确
       - TestSoftDelete: 删除后查询不到
       - TestExtractFromIDCard: 验证身份证提取 gender/birthDate 正确
  </action>
  <verify>
    <automated>cd /Users/wencai/github/EasyHR && go test ./internal/employee/... -run TestRepository -v -count=1 2>&1 | tail -30</automated>
  </verify>
  <done>
    - Employee 模型定义完整，包含所有 CONTEXT.md D-06~D-10 指定的字段
    - Repository 层 CRUD + 搜索 + 分页 + 多租户隔离全部实现
    - 所有 9 个 repository 测试通过
    - extractFromIDCard 函数正确提取性别和出生日期
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Employee Service + Handler + Excel 导出 + 路由注册</name>
  <files>
    internal/employee/dto.go,
    internal/employee/service.go,
    internal/employee/service_test.go,
    internal/employee/handler.go,
    cmd/server/main.go
  </files>
  <behavior>
    - Test: CreateEmployee 明文手机号/身份证号加密存储，hash 列正确
    - Test: CreateEmployee 重复手机号返回错误 "该手机号已存在"
    - Test: CreateEmployee 自动从身份证号提取 gender 和 birth_date
    - Test: ListEmployees 返回脱敏数据（手机号 138****8000 格式）
    - Test: ExportExcel 返回有效的 xlsx 二进制数据（以 PK 开头）
    - Test: MEMBER 角色调用 ExportExcel 被 RequireRole 中间件拦截返回 403
    - Test: GetSensitiveInfo 返回完整解密手机号/身份证号
  </behavior>
  <read_first>
    - internal/employee/model.go
    - internal/employee/repository.go
    - internal/user/service.go
    - internal/user/handler.go
    - internal/user/dto.go
    - internal/common/response/response.go
    - internal/common/config/config.go
    - cmd/server/main.go
  </read_first>
  <action>
    1. **创建 internal/employee/dto.go** — 请求/响应结构体:
       ```go
       // 创建员工请求（手动录入 per D-08）
       type CreateEmployeeRequest struct {
           Name             string `json:"name" binding:"required,min=2,max=50"`
           Phone            string `json:"phone" binding:"required,len=11"`
           IDCard           string `json:"id_card" binding:"required,len=18"`
           Position         string `json:"position" binding:"required,min=1,max=100"`
           HireDate         string `json:"hire_date" binding:"required"` // YYYY-MM-DD
           BankName         string `json:"bank_name" binding:"omitempty,max=100"`
           BankAccount      string `json:"bank_account" binding:"omitempty"`
           EmergencyContact string `json:"emergency_contact" binding:"omitempty,max=50"`
           EmergencyPhone   string `json:"emergency_phone" binding:"omitempty,len=11"`
           Address          string `json:"address" binding:"omitempty,max=500"`
           Remark           string `json:"remark" binding:"omitempty"`
       }

       // 更新员工请求
       type UpdateEmployeeRequest struct {
           Name             *string `json:"name" binding:"omitempty,min=2,max=50"`
           Phone            *string `json:"phone" binding:"omitempty,len=11"`
           IDCard           *string `json:"id_card" binding:"omitempty,len=18"`
           Position         *string `json:"position" binding:"omitempty,min=1,max=100"`
           HireDate         *string `json:"hire_date" binding:"omitempty"`
           BankName         *string `json:"bank_name" binding:"omitempty,max=100"`
           BankAccount      *string `json:"bank_account" binding:"omitempty"`
           EmergencyContact *string `json:"emergency_contact" binding:"omitempty,max=50"`
           EmergencyPhone   *string `json:"emergency_phone" binding:"omitempty,len=11"`
           Address          *string `json:"address" binding:"omitempty,max=500"`
           Remark           *string `json:"remark" binding:"omitempty"`
       }

       // 员工列表响应（脱敏 per D-10）
       type EmployeeResponse struct {
           ID           int64      `json:"id"`
           Name         string     `json:"name"`
           Phone        string     `json:"phone"`         // 脱敏: 138****8000
           IDCard       string     `json:"id_card"`       // 脱敏: 110108****1234
           Gender       string     `json:"gender"`
           BirthDate    *time.Time `json:"birth_date"`
           Position     string     `json:"position"`
           HireDate     time.Time  `json:"hire_date"`
           Status       string     `json:"status"`
           BankName     string     `json:"bank_name,omitempty"`
           BankAccount  string     `json:"bank_account,omitempty"` // 脱敏: ****5678
           EmergencyContact string `json:"emergency_contact,omitempty"`
           Address      string     `json:"address,omitempty"`
           Remark       string     `json:"remark,omitempty"`
           CreatedAt    time.Time  `json:"created_at"`
       }

       // 敏感信息响应（完整解密 per D-13）
       type SensitiveInfoResponse struct {
           Phone           string `json:"phone"`
           IDCard          string `json:"id_card"`
           BankAccount     string `json:"bank_account,omitempty"`
           EmergencyPhone  string `json:"emergency_phone,omitempty"`
       }

       // 搜索参数
       type ListQueryParams struct {
           Name     string `form:"name"`
           Position string `form:"position"`
           Phone    string `form:"phone"`
           Status   string `form:"status"`
           Page     int    `form:"page,default=1"`
           PageSize int    `form:"page_size,default=20"`
       }
       ```

    2. **创建 internal/employee/service.go**:
       ```go
       type Service struct {
           repo     *Repository
           cryptoCfg config.CryptoConfig
       }
       func NewService(repo *Repository, cryptoCfg config.CryptoConfig) *Service
       ```

       关键方法:
       - `CreateEmployee(ctx, orgID, userID int64, req *CreateEmployeeRequest) (*EmployeeResponse, error)`:
         1. 明文手机号 → `crypto.HashSHA256(phone)` → 检查 repo.FindByPhoneHash 唯一性
         2. 身份证号同理检查唯一性
         3. 调用 extractFromIDCard 提取 gender + birthDate
         4. 加密: phone_encrypted, id_card_encrypted, bank_account_encrypted, emergency_phone_encrypted
         5. 哈希: phone_hash, id_card_hash, bank_account_hash, emergency_phone_hash
         6. repo.Create 保存
         7. 返回脱敏 EmployeeResponse
       - `ListEmployees(ctx, orgID int64, query ListQueryParams) ([]EmployeeResponse, int64, error)`:
         1. 构建 SearchParams（phone 明文转 hash）
         2. repo.List 查询
         3. 遍历结果解密 → 脱敏 → 返回 EmployeeResponse 列表
       - `GetEmployee(ctx, orgID, id int64) (*EmployeeResponse, error)`: 解密+脱敏返回
       - `UpdateEmployee(ctx, orgID, id int64, req *UpdateEmployeeRequest) (*EmployeeResponse, error)`: 部分更新（仅更新非 nil 字段），敏感字段更新需重新加密+hash
       - `DeleteEmployee(ctx, orgID, id int64) error`: 软删除
       - `GetSensitiveInfo(ctx, orgID, id int64) (*SensitiveInfoResponse, error)`: 解密返回完整敏感数据（per D-13，仅 OWNER/ADMIN 调用）
       - `ExportExcel(ctx, orgID int64, query ListQueryParams) ([]byte, error)`:
         1. repo.FindAllForExport 获取全部匹配数据（不分页）
         2. 解密+脱敏
         3. excelize 创建 workbook: 表头行（姓名、手机号、身份证号、岗位、入职日期、状态），冻结首行
         4. `excelize.WriteToBuffer()` → `[]byte`
       - `toResponse(emp *Employee) (*EmployeeResponse, error)`: 内部辅助方法，解密+脱敏
       - `maskBankAccount(account string) string`: 保留后4位，前面用 `****` 替代

    3. **创建 internal/employee/handler.go** — 参照 internal/user/handler.go:
       ```go
       type Handler struct {
           svc *Service
       }
       func NewHandler(svc *Service) *Handler
       func (h *Handler) RegisterRoutes(rg *gin.RouterGroup, authMiddleware gin.HandlerFunc)
       ```

       路由注册（per D-22 RBAC 权限）:
       ```go
       func (h *Handler) RegisterRoutes(rg *gin.RouterGroup, authMiddleware gin.HandlerFunc) {
           authGroup := rg.Group("")
           authGroup.Use(authMiddleware)

           // 员工 CRUD — OWNER/ADMIN 可写，MEMBER 可读
           authGroup.POST("/employees", middleware.RequireRole("owner", "admin"), h.CreateEmployee)
           authGroup.GET("/employees", h.ListEmployees)       // 所有角色可查看（脱敏数据）
           authGroup.GET("/employees/export", middleware.RequireRole("owner", "admin"), h.ExportExcel)
           authGroup.GET("/employees/:id", h.GetEmployee)
           authGroup.PUT("/employees/:id", middleware.RequireRole("owner", "admin"), h.UpdateEmployee)
           authGroup.DELETE("/employees/:id", middleware.RequireRole("owner", "admin"), h.DeleteEmployee)
           authGroup.POST("/employees/:id/sensitive", middleware.RequireRole("owner", "admin"), h.GetSensitiveInfo)
       }
       ```

       Handler 方法实现:
       - CreateEmployee: ShouldBindJSON → c.GetInt64("org_id") + c.GetInt64("user_id") → svc.CreateEmployee
       - ListEmployees: ShouldBindQuery → svc.ListEmployees → PageSuccess
       - GetEmployee: strconv.ParseInt(c.Param("id")) → svc.GetEmployee → Success
       - UpdateEmployee: ShouldBindJSON → svc.UpdateEmployee
       - DeleteEmployee: svc.DeleteEmployee
       - ExportExcel: ShouldBindQuery → svc.ExportExcel → c.Data(http.StatusOK, "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet", data) + Content-Disposition header
       - GetSensitiveInfo: svc.GetSensitiveInfo → Success

    4. **修改 cmd/server/main.go**:
       - import `"github.com/wencai/easyhr/internal/employee"`
       - 在 AutoMigrate 中添加 `&employee.Employee{}`
       - 在依赖注入部分添加:
         ```go
         empRepo := employee.NewRepository(db)
         empSvc := employee.NewService(empRepo, cfg.Crypto)
         empHandler := employee.NewHandler(empSvc)
         ```
       - 在路由注册部分添加: `empHandler.RegisterRoutes(v1, authMiddleware)`

    5. **创建 internal/employee/service_test.go** — 使用 mock Repository 或直接用 SQLite:
       - TestCreateEmployee_Success: 验证创建后 phone_hash 正确，通过 GetSensitiveInfo 验证解密后明文匹配
       - TestCreateEmployee_DuplicatePhone: 返回 "该手机号已存在" 错误
       - TestCreateEmployee_ExtractIDCard: 验证 gender="男"/"女"，birthDate 正确
       - TestListEmployees_MaskedData: 验证响应中手机号为 138****8000 格式
       - TestExportExcel_ValidXLSX: 验证返回的 []byte 以 PK\x03\x04 开头（xlsx 文件头）
       - TestGetSensitiveInfo: 验证返回完整解密手机号
  </action>
  <verify>
    <automated>cd /Users/wencai/github/EasyHR && go test ./internal/employee/... -v -count=1 2>&1 | tail -40</automated>
  </verify>
  <done>
    - Service 层完整实现创建/列表/详情/更新/删除/搜索/导出/敏感信息查看
    - 所有敏感字段创建时加密存储，响应时脱敏（per D-09, D-10）
    - Excel 导出使用 excelize 生成有效 xlsx 文件
    - RBAC 权限通过 RequireRole 中间件控制（per D-22）
    - 所有 service 测试通过
    - main.go 注册了 employee 路由，`go build ./cmd/server` 编译通过
  </done>
</task>

</tasks>

<verification>
1. 所有单元测试通过: `go test ./internal/employee/... -v`
2. 编译通过: `go build ./cmd/server`
3. Employee 模型字段覆盖 CONTEXT.md D-06~D-10 所有要求
4. 搜索支持姓名/岗位 ILIKE + 手机号 hash 精确 + 状态筛选（per D-11）
5. Excel 导出敏感字段脱敏（per D-12）
6. RBAC: MEMBER 只读脱敏，OWNER/ADMIN 全权限（per D-22）
</verification>

<success_criteria>
- `go test ./internal/employee/... -v` 全部 PASS
- `go build ./cmd/server` 编译成功
- Employee 表包含所有指定字段（name, phone_encrypted/hash, id_card_encrypted/hash, gender, birth_date, position, hire_date, status, user_id, bank_*, emergency_*, address, remark, resignation_*）
- 搜索功能支持 4 个维度（姓名/岗位/手机号/状态）
- Excel 导出生成有效 xlsx 文件
- RBAC 权限正确配置
</success_criteria>

<output>
After completion, create `.planning/phases/02-employee-management/02-01-SUMMARY.md`
</output>
