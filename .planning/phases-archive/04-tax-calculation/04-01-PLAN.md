---
phase: 04-tax-calculation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/tax/model.go
  - internal/tax/dto.go
  - internal/tax/repository.go
  - internal/tax/calculator.go
  - internal/tax/calculator_test.go
  - internal/tax/service.go
  - internal/tax/adapter.go
  - internal/tax/errors.go
autonomous: true
requirements:
  - TAX-01
  - TAX-02

must_haves:
  truths:
    - "老板可为员工录入7种专项附加扣除项，选择类型+人数后系统自动计算月度扣除金额"
    - "按中国累计预扣预缴法精准计算个税，正确处理税率跳档边界值（36000元等）"
    - "税率表七级超额累进税率存储于数据库，OrgID=0 全局共享"
    - "1月累计数据自动清零，年中入职从入职月份起算"
    - "Phase 5 可通过 TaxCalculator 接口调用个税计算，传入 grossIncome 作为参数"
  artifacts:
    - path: "internal/tax/model.go"
      provides: "TaxBracket, SpecialDeduction, TaxRecord, TaxDeclaration 数据模型"
      contains: "type TaxBracket struct"
    - path: "internal/tax/calculator.go"
      provides: "累计预扣预缴计算引擎 + TaxCalculator 接口定义"
      exports: ["CalculateTax", "TaxCalculator"]
    - path: "internal/tax/calculator_test.go"
      provides: "计算引擎完整测试覆盖（基础场景、跳档、边界值、年中入职）"
      min_lines: 150
    - path: "internal/tax/service.go"
      provides: "TaxService 业务逻辑层"
      exports: ["NewService"]
    - path: "internal/tax/repository.go"
      provides: "Repository 数据访问层（税率表+扣除+记录 CRUD）"
    - path: "internal/tax/adapter.go"
      provides: "EmployeeInfoProvider 和 SIDeductionProvider 接口定义"
      exports: ["EmployeeInfoProvider", "SIDeductionProvider"]
    - path: "internal/tax/errors.go"
      provides: "错误定义 + 错误码 40xxx"
  key_links:
    - from: "internal/tax/calculator.go"
      to: "internal/tax/repository.go"
      via: "查询 TaxBracket 税率表 + 累计 TaxRecord"
      pattern: "FindTaxBrackets|FindTaxRecordsByEmployee"
    - from: "internal/tax/service.go"
      to: "internal/tax/calculator.go"
      via: "调用 CalculateTax 计算引擎"
      pattern: "CalculateTax"
    - from: "internal/tax/service.go"
      to: "internal/tax/adapter.go"
      via: "通过 EmployeeInfoProvider 获取薪资，通过 SIDeductionProvider 获取社保扣款"
      pattern: "EmployeeInfoProvider|SIDeductionProvider"
---

<objective>
建立个税模块核心：数据模型、专项附加扣除管理、累计预扣预缴计算引擎。

Purpose: 个税计算引擎是 Phase 4 的核心，必须在 API 层之前完成并充分测试。算法正确性直接影响所有下游功能（申报表、提醒、Phase 5 调用）。专项附加扣除管理为计算引擎提供输入数据。

Output: 可独立运行并通过完整测试的计算引擎，以及专项附加扣除的 CRUD 管理。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-tax-calculation/04-CONTEXT.md
@.planning/phases/04-tax-calculation/04-RESEARCH.md
</context>

<interfaces>
<!-- Executor 需要了解的已有代码接口 -->

From internal/common/model/base.go:
```go
type BaseModel struct {
    ID        int64          `gorm:"primaryKey;autoIncrement" json:"id"`
    OrgID     int64          `gorm:"column:org_id;index;not null" json:"org_id"`
    CreatedBy int64          `gorm:"column:created_by" json:"created_by"`
    CreatedAt time.Time      `gorm:"column:created_at;autoCreateTime" json:"created_at"`
    UpdatedBy int64          `gorm:"column:updated_by" json:"updated_by"`
    UpdatedAt time.Time      `gorm:"column:updated_at;autoUpdateTime" json:"updated_at"`
    DeletedAt gorm.DeletedAt `gorm:"column:deleted_at;index" json:"-"`
}
```

From internal/common/middleware/ (TenantScope):
```go
func TenantScope(orgID int64) func(db *gorm.DB) *gorm.DB
func RequireRole(roles ...string) gin.HandlerFunc
```

From internal/common/response/:
```go
func Success(c *gin.Context, data interface{})
func Error(c *gin.Context, httpStatus, code int, msg string)
func BadRequest(c *gin.Context, msg string)
func PageSuccess(c *gin.Context, list interface{}, total int64, page, pageSize int)
```

From internal/socialinsurance/service.go (GetSocialInsuranceDeduction 供 Phase 4 调用):
```go
func (s *Service) GetSocialInsuranceDeduction(orgID, employeeID int64, month string) (*DeductionResponse, error)
// DeductionResponse.TotalPersonal float64 — 社保个人扣款总额
```

From internal/employee/contract_model.go:
```go
type Contract struct {
    model.BaseModel
    EmployeeID   int64   `gorm:"column:employee_id;index;not null"`
    Salary       float64 `gorm:"column:salary;type:decimal(10,2)"`
    Status       string  `gorm:"column:status;type:varchar(20);not null;default:draft"`
}
const ContractStatusActive = "active"
```

From internal/socialinsurance/employee_adapter.go (adapter 模式参考):
```go
type EmployeeAdapter struct {
    empRepo *employee.Repository
}
func NewEmployeeAdapter(empRepo *employee.Repository) *EmployeeAdapter
```

From internal/socialinsurance/repository.go (Repository 模式参考):
```go
type Repository struct { db *gorm.DB }
func NewRepository(db *gorm.DB) *Repository
// 税率表全局数据用 Where("org_id = 0")，租户数据用 Scopes(middleware.TenantScope(orgID))
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 个税数据模型 + 专项附加扣除 CRUD + 税率表种子数据</name>
  <files>internal/tax/model.go, internal/tax/dto.go, internal/tax/repository.go, internal/tax/errors.go</files>
  <read_first>
    internal/tax/model.go (new file)
    internal/tax/dto.go (new file)
    internal/tax/repository.go (new file)
    internal/tax/errors.go (new file)
    internal/socialinsurance/model.go (model pattern reference)
    internal/socialinsurance/repository.go (repository pattern reference)
    internal/common/model/base.go (BaseModel)
  </read_first>
  <behavior>
    - Test: CreateSpecialDeduction inserts a child_education deduction for employee, monthly_amount = 2000 * count
    - Test: CreateSpecialDeduction rejects housing_loan when housing_rent already active for same employee (mutual exclusion)
    - Test: ListDeductionsByEmployee returns only active deductions for given month (effective_start <= month <= effective_end)
    - Test: SeedTaxBrackets creates exactly 7 records with correct rates (3%/10%/20%/25%/30%/35%/45%)
    - Test: FindTaxBracketForAmount returns 10% rate and 2520 quick_deduction for cumulative taxable income = 50000
  </behavior>
  <action>
    Per D-23/D-24/D-25, create 4 files in internal/tax/:

    **internal/tax/errors.go** — 错误定义:
    - ErrBracketNotFound = fmt.Errorf("税率表未找到")
    - ErrDeductionNotFound = fmt.Errorf("专项附加扣除记录未找到")
    - ErrTaxRecordNotFound = fmt.Errorf("个税计算记录未找到")
    - ErrDeclarationNotFound = fmt.Errorf("个税申报记录未找到")
    - ErrDuplicateDeduction = fmt.Errorf("该员工已有同类型的专项附加扣除记录")
    - ErrMutuallyExclusiveDeduction = fmt.Errorf("住房贷款利息和住房租金不可同时享受")
    - Error code range: 40xxx (per D-25)

    **internal/tax/model.go** — 数据模型 (全部嵌入 model.BaseModel):

    1. TaxBracket (OrgID=0, 全局共享, per D-01/D-02):
       - Level int `gorm:"column:level;not null;index"`
       - LowerBound float64 `gorm:"column:lower_bound;not null"`
       - UpperBound float64 `gorm:"column:upper_bound"` (nullable, 顶级无上限)
       - Rate float64 `gorm:"column:rate;not null"` (如 0.03 = 3%)
       - QuickDeduction float64 `gorm:"column:quick_deduction;not null;default:0"`
       - EffectiveYear int `gorm:"column:effective_year;not null;index"`
       - TableName() = "tax_brackets"
       - 常量 BasicDeductionMonthly = 5000.0 (per D-03)

    2. SpecialDeduction (租户隔离, per D-04/D-05/D-06):
       - EmployeeID int64 `gorm:"column:employee_id;not null;index"`
       - DeductionType string `gorm:"column:deduction_type;type:varchar(30);not null;index"`
       - MonthlyAmount float64 `gorm:"column:monthly_amount;not null"`
       - Count int `gorm:"column:count;default:1"` (子女数/老人数等)
       - EffectiveStart string `gorm:"column:effective_start;type:varchar(7);not null"` (YYYY-MM)
       - EffectiveEnd *string `gorm:"column:effective_end;type:varchar(7)"` (nullable, ongoing)
       - Remark string `gorm:"column:remark;type:varchar(200)"`
       - TableName() = "special_deductions"
       - 扣除类型常量: DeductionTypeChildEducation="child_education", DeductionTypeContinuingEducation="continuing_education", DeductionTypeHousingLoan="housing_loan", DeductionTypeHousingRent="housing_rent", DeductionTypeElderlyCare="elderly_care", DeductionTypeInfantCare="infant_care" (6种按月扣除, 不含 serious_illness 按 D-07 per research pitfall #5)
       - 预置扣除标准映射: DeductionStandard = map[string]float64 { child_education: 2000, continuing_education: 400, housing_loan: 1000, housing_rent: 1500, elderly_care: 3000, infant_care: 2000 } (per D-06 per research section 3)
       - 互斥组: housing_loan 和 housing_rent 互斥 (per research pitfall #6)

    3. TaxRecord (租户隔离, 每员工每月一条, per D-24):
       - EmployeeID int64 `gorm:"column:employee_id;not null;index"`
       - EmployeeName string `gorm:"column:employee_name;type:varchar(50);not null"`
       - Year int `gorm:"column:year;not null;index"`
       - Month int `gorm:"column:month;not null"`
       - GrossIncome float64 `gorm:"column:gross_income;not null"`
       - BasicDeduction float64 `gorm:"column:basic_deduction;not null"`
       - SIDeduction float64 `gorm:"column:si_deduction;not null;default:0"`
       - SpecialDeduction float64 `gorm:"column:special_deduction;not null;default:0"`
       - TotalDeduction float64 `gorm:"column:total_deduction;not null"`
       - CumulativeIncome float64 `gorm:"column:cumulative_income;not null"`
       - CumulativeBasicDeduction float64 `gorm:"column:cumulative_basic_deduction;not null"`
       - CumulativeSIDeduction float64 `gorm:"column:cumulative_si_deduction;not null;default:0"`
       - CumulativeSpecialDeduction float64 `gorm:"column:cumulative_special_deduction;not null;default:0"`
       - CumulativeTaxableIncome float64 `gorm:"column:cumulative_taxable_income;not null"`
       - TaxRate float64 `gorm:"column:tax_rate;not null"`
       - QuickDeduction float64 `gorm:"column:quick_deduction;not null;default:0"`
       - CumulativeTax float64 `gorm:"column:cumulative_tax;not null"`
       - MonthlyTax float64 `gorm:"column:monthly_tax;not null"`
       - Source string `gorm:"column:source;type:varchar(20);not null;default:contract"` (contract/salary per D-12)
       - TableName() = "tax_records"
       - 唯一索引: idx_tax_records_employee_month UNIQUE(employee_id, year, month) WHERE deleted_at IS NULL

    4. TaxDeclaration (租户隔离, 每企业每月一条, per D-22):
       - Year int `gorm:"column:year;not null;index"`
       - Month int `gorm:"column:month;not null"`
       - Status string `gorm:"column:status;type:varchar(20);not null;default:pending"` (pending/declared)
       - TotalEmployees int `gorm:"column:total_employees;not null;default:0"`
       - TotalIncome float64 `gorm:"column:total_income;not null;default:0"`
       - TotalTax float64 `gorm:"column:total_tax;not null;default:0"`
       - DeclaredAt *time.Time `gorm:"column:declared_at"`
       - DeclaredBy int64 `gorm:"column:declared_by"`
       - TableName() = "tax_declarations"

    **internal/tax/dto.go** — 请求/响应 DTO:

    专项附加扣除 DTO:
    - CreateDeductionRequest: EmployeeID int64 `binding:required`, DeductionType string `binding:required`, Count int `binding:required,min=1`, EffectiveStart string `binding:required`, EffectiveEnd *string, Remark string
    - UpdateDeductionRequest: Count int `binding:required,min=1`, EffectiveEnd *string, Remark string
    - DeductionResponse: ID, EmployeeID, EmployeeName, DeductionType, MonthlyAmount, Count, EffectiveStart, EffectiveEnd, Remark, CreatedAt
    - DeductionListQuery: EmployeeID int64 `form:employee_id`, Page, PageSize

    税率表 DTO:
    - TaxBracketResponse: ID, Level, LowerBound, UpperBound, Rate, QuickDeduction, EffectiveYear
    - TaxBracketListQuery: EffectiveYear int `form:effective_year`, Page, PageSize

    个税计算结果 DTO:
    - TaxResult: MonthlyTax, CumulativeIncome, CumulativeDeduction, CumulativeTaxableIncome, TaxRate, QuickDeduction, CumulativeTax, GrossIncome, BasicDeduction, SIDeduction, SpecialDeduction, TotalDeduction (per D-08)
    - TaxRecordResponse: ID, EmployeeID, EmployeeName, Year, Month, GrossIncome, BasicDeduction, SIDeduction, SpecialDeduction, TotalDeduction, CumulativeIncome, CumulativeBasicDeduction, CumulativeSIDeduction, CumulativeSpecialDeduction, CumulativeTaxableIncome, TaxRate, QuickDeduction, CumulativeTax, MonthlyTax, Source, CreatedAt
    - TaxRecordListQuery: EmployeeID int64 `form:employee_id`, Year int `form:year`, Month int `form:month`, Page, PageSize

    **internal/tax/repository.go** — Repository 数据访问层:

    type Repository struct { db *gorm.DB }
    func NewRepository(db *gorm.DB) *Repository

    税率表方法 (全局数据, OrgID=0):
    - SeedTaxBrackets(year int) error — 插入7级税率种子数据（per research section 1），先检查是否已有该年份数据
    - FindTaxBrackets(year int) ([]TaxBracket, error) — 查询指定年份全部税率，按 level ASC 排序
    - FindTaxBracketForAmount(brackets []TaxBracket, amount float64) *TaxBracket — 纯函数，从税率列表中找到 amount 所在区间。边界值规则: lower_bound <= amount < upper_bound (top bracket: amount >= lower_bound) (per research pitfall #3)

    专项附加扣除方法 (租户隔离):
    - CreateDeduction(orgID int64, deduction *SpecialDeduction) error
    - FindDeductionByID(orgID, id int64) (*SpecialDeduction, error)
    - FindDeductionByEmployeeAndType(orgID, employeeID int64, deductionType string) (*SpecialDeduction, error) — 查找同类型有效扣除（用于去重检查）
    - ListDeductionsByEmployee(orgID, employeeID int64, month string, page, pageSize int) ([]SpecialDeduction, int64, error) — 当月生效的扣除项：WHERE employee_id=? AND effective_start<=? AND (effective_end IS NULL OR effective_end>=?)
    - ListAllActiveDeductionsByEmployee(orgID, employeeID int64, month string) ([]SpecialDeduction, error) — 不分页，获取当月全部有效扣除（计算引擎用）
    - UpdateDeduction(orgID, id int64, updates map[string]interface{}) error
    - DeleteDeduction(orgID, id int64) error

    TaxRecord 方法 (租户隔离):
    - CreateTaxRecord(record *TaxRecord) error
    - FindTaxRecordByEmployeeMonth(orgID, employeeID int64, year, month int) (*TaxRecord, error) — 查找指定月份记录
    - FindTaxRecordsByEmployeeYear(orgID, employeeID int64, year int) ([]TaxRecord, error) — 查询指定年份全部记录（用于累计计算）
    - ListTaxRecords(orgID int64, params TaxRecordListQuery) ([]TaxRecord, int64, error) — 分页查询
    - FindTaxRecordByID(orgID, id int64) (*TaxRecord, error)

    TaxDeclaration 方法 (租户隔离):
    - CreateDeclaration(decl *TaxDeclaration) error
    - FindDeclarationByMonth(orgID int64, year, month int) (*TaxDeclaration, error)
    - UpdateDeclaration(orgID, id int64, updates map[string]interface{}) error
    - ListDeclarations(orgID int64, year int, page, pageSize int) ([]TaxDeclaration, int64, error)

    所有租户隔离方法使用 db.Scopes(middleware.TenantScope(orgID))。
  </action>
  <verify>
    <automated>cd /Users/wencai/github/EasyHR && go vet ./internal/tax/... 2>&1 | head -20</automated>
  </verify>
  <done>
    - internal/tax/model.go 包含 TaxBracket, SpecialDeduction, TaxRecord, TaxDeclaration 四个模型
    - internal/tax/dto.go 包含所有请求/响应 DTO
    - internal/tax/repository.go 包含 Repository 及所有 CRUD 方法
    - internal/tax/errors.go 包含错误定义
    - TaxBracket 使用 OrgID=0 全局数据模式（与社保政策一致）
    - SpecialDeduction 包含 housing_loan/housing_rent 互斥校验逻辑
    - DeductionStandard 包含6种按月扣除类型的预置标准金额
    - go vet ./internal/tax/... 无错误
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 累计预扣预缴计算引擎 + Service 层 + 跨模块接口</name>
  <files>internal/tax/calculator.go, internal/tax/calculator_test.go, internal/tax/service.go, internal/tax/adapter.go</files>
  <read_first>
    internal/tax/calculator.go (new file)
    internal/tax/calculator_test.go (new file)
    internal/tax/service.go (new file)
    internal/tax/adapter.go (new file)
    internal/tax/model.go (created in Task 1)
    internal/tax/repository.go (created in Task 1)
    internal/tax/dto.go (created in Task 1)
    internal/tax/errors.go (created in Task 1)
    internal/socialinsurance/service.go (service pattern reference)
    internal/socialinsurance/employee_adapter.go (adapter pattern reference)
  </read_first>
  <behavior>
    - Test: 月薪10000无扣除，1月个税 = (10000-5000)*0.03-0 = 150
    - Test: 月薪10000无扣除，2月累计个税 = (20000-10000)*0.03-0 = 300，当月个税 = 300-150 = 150
    - Test: 月薪30000无扣除，1月个税 = (30000-5000)*0.03 = 750；6月累计应纳税所得额 = 150000，跨过144000边界，使用20%税率，速算扣除数16920
    - Test: 月薪20000，社保个人扣款2000，子女教育扣除2000，计算正确扣除专项+专项附加
    - Test: 年中入职（3月入职），1-2月无记录，累计从3月起算
    - Test: 当月应扣税额为负数时（后期扣除大于收入），monthly_tax = max(cumulativeTax - previousCumulativeTax, 0)，不退不扣
    - Test: 1月清零，跨年后累计数据从零重新计算
    - Test: CalculateTax 接口传入 grossIncome=15000 正确计算，不依赖工资模块
    - Test: 独立查询模式使用 Contract.Salary 作为 grossIncome 来源
  </behavior>
  <action>
    Per D-08/D-09/D-10/D-11/D-13/D-14/D-15/D-16, 创建4个文件:

    **internal/tax/adapter.go** — 跨模块接口定义 (per D-09/D-10):

    ```go
    // EmployeeInfoProvider 员工信息接口（由 employee adapter 在 main.go 实现, per D-09）
    type EmployeeInfoProvider interface {
        GetActiveSalary(orgID, employeeID int64) (float64, error)
        GetEmployeeHireMonth(orgID, employeeID int64) (string, error) // "2025-03" 格式
    }

    // SIDeductionProvider 社保扣款接口（由 socialinsurance adapter 在 main.go 实现, per D-10）
    type SIDeductionProvider interface {
        GetPersonalDeduction(orgID, employeeID int64, month string) (float64, error)
    }
    ```

    **internal/tax/calculator.go** — 累计预扣预缴计算引擎:

    ```go
    // TaxCalculator 个税计算接口（Phase 5 调用, per D-08）
    type TaxCalculator interface {
        CalculateTax(orgID, employeeID int64, year, month int, grossIncome float64) (*TaxResult, error)
    }
    ```

    核心函数 calculateCumulativeTax (纯函数式, per CONTEXT specifics):
    - 输入: brackets []TaxBracket, records []TaxRecord (本年已有记录), currentMonth int, grossIncome float64, basicDeduction float64, siDeduction float64, specialDeduction float64
    - 输出: *TaxResult, error

    算法步骤 (per research section 2, per D-13):
    1. 累计收入 = SUM(records的gross_income) + 当月grossIncome
    2. 累计基本减除 = (len(records)+1) * 5000.0 (per D-03)
    3. 累计社保扣款 = SUM(records的si_deduction) + 当月siDeduction
    4. 累计专项附加扣除 = SUM(records的special_deduction) + 当月specialDeduction
    5. 累计应纳税所得额 = (1) - (2) - (3) - (4)
    6. 如果 累计应纳税所得额 <= 0, 当月个税 = 0
    7. 查税率表找到对应区间 (per D-15)
    8. 累计应扣税额 = 累计应纳税所得额 * 税率 - 速算扣除数
    9. 累计已扣税额 = SUM(records的monthly_tax)
    10. 当月应扣税额 = 累计应扣税额 - 累计已扣税额
    11. 如果 当月应扣税额 < 0, 设为 0 (不退税, per research pitfall #1)

    所有中间结果 math.Round(val*100)/100 保留两位小数 (per research pitfall #1)。

    函数 roundTo2(val float64) float64 { return math.Round(val*100) / 100 }

    **internal/tax/calculator_test.go** — 计算引擎完整测试:

    使用 table-driven tests，测试场景:
    1. TestCalculateTax_BasicScenario: 月薪10000无扣除，验证1月个税=150
    2. TestCalculateTax_CumulativeMonth2: 月薪10000连续2月，验证累计正确
    3. TestCalculateTax_TaxBracketJump: 月薪30000，连续6月后累计应纳税所得额跨过36000边界（跳至10%税率）
    4. TestCalculateTax_WithDeductions: 月薪20000 + 社保个人2000 + 子女教育2000，验证各项扣除正确
    5. TestCalculateTax_NegativeMonthlyTax: 当月应扣为负时返回0
    6. TestCalculateTax_JanuaryReset: 1月记录无前序记录，累计从零开始
    7. TestCalculateTax_MidYearHire: 年中入职（如3月），累计从入职月起算
    8. TestCalculateTax_HighIncomeBracketJump: 高收入跨多档（如月入80000跨至25%甚至30%）
    9. TestFindTaxBracketForAmount_BoundaryValues: 验证36000边界（应落在10%档），36000-1=35999（3%档）

    测试设置: setupTestDB(t) 用 SQLite 内存数据库 + AutoMigrate, 创建标准7级税率种子数据。

    种子数据（per research section 1）:
    ```
    Level 1: 0 ~ 36000, rate=0.03, quick_deduction=0
    Level 2: 36000 ~ 144000, rate=0.10, quick_deduction=2520
    Level 3: 144000 ~ 300000, rate=0.20, quick_deduction=16920
    Level 4: 300000 ~ 420000, rate=0.25, quick_deduction=31920
    Level 5: 420000 ~ 660000, rate=0.30, quick_deduction=52920
    Level 6: 660000 ~ 960000, rate=0.35, quick_deduction=85920
    Level 7: 960000 ~ 无上限(nil), rate=0.45, quick_deduction=181920
    ```

    **internal/tax/service.go** — Service 业务逻辑层:

    type Service struct { repo *Repository; empProvider EmployeeInfoProvider; siProvider SIDeductionProvider }
    func NewService(repo *Repository, empProvider EmployeeInfoProvider, siProvider SIDeductionProvider) *Service

    专项附加扣除管理:
    - CreateDeduction(orgID, userID int64, req *CreateDeductionRequest) (*DeductionResponse, error):
      1. 校验 DeductionType 合法性
      2. 检查互斥: 如果是 housing_loan/housing_rent，查询另一个类型是否已存在 (per research pitfall #6)
      3. 检查去重: 同员工同类型不能重复
      4. MonthlyAmount = DeductionStandard[req.DeductionType] * req.Count (per D-06)
      5. 创建记录
    - UpdateDeduction(orgID, userID, id int64, req *UpdateDeductionRequest) error: 更新 count/remark/effective_end，重新计算 MonthlyAmount
    - DeleteDeduction(orgID, id int64) error: 软删除
    - ListDeductions(orgID int64, params DeductionListQuery) ([]DeductionResponse, int64, error)
    - GetDeduction(orgID, id int64) (*DeductionResponse, error)

    税率表管理:
    - ListTaxBrackets(year, page, pageSize int) ([]TaxBracketResponse, int64, error)
    - GetTaxBracket(id int64) (*TaxBracketResponse, error)
    - SeedDefaultBrackets(year int) error — 调用 repo.SeedTaxBrackets

    个税计算 (per D-08/D-12):
    - CalculateTax(orgID, employeeID int64, year, month int, grossIncome float64) (*TaxResult, error):
      实现 TaxCalculator 接口。步骤:
      1. 查询本年已有 TaxRecord 列表 (repo.FindTaxRecordsByEmployeeYear)
      2. 查询当月专项附加扣除总额 (repo.ListAllActiveDeductionsByEmployee, SUM monthly_amount)
      3. 查询当月社保个人扣款 (siProvider.GetPersonalDeduction, 如果 siProvider 为 nil 则为 0)
      4. 调用 calculateCumulativeTax 纯函数计算
      5. 创建 TaxRecord 存储完整快照
      6. 返回 TaxResult

    - CalculateTaxFromContract(orgID, employeeID int64, year, month int) (*TaxResult, error):
      独立查询模式 (per D-12)，从 Contract.Salary 获取 grossIncome:
      salary, err := empProvider.GetActiveSalary(orgID, employeeID)
      然后调用 CalculateTax(orgID, employeeID, year, month, salary)

    - GetTaxRecord(orgID, id int64) (*TaxRecordResponse, error)
    - ListTaxRecords(orgID int64, params TaxRecordListQuery) ([]TaxRecordResponse, int64, error)
    - GetMyTaxRecords(orgID, userID int64) ([]TaxRecordResponse, error): MEMBER 角色通过 user_id -> employee_id 查询 (per D-26)

    申报管理:
    - GetOrCreateDeclaration(orgID int64, year, month int) (*TaxDeclaration, error): 获取或创建当月申报记录
    - ListDeclarations(orgID int64, year, page, pageSize int) ([]TaxDeclaration, int64, error)
    - MarkAsDeclared(orgID, userID, id int64) error: 标记为已申报 (per D-22)
  </action>
  <verify>
    <automated>cd /Users/wencai/github/EasyHR && go test -race -count=1 ./internal/tax/... -v 2>&1 | tail -30</automated>
  </verify>
  <done>
    - internal/tax/calculator.go 包含 TaxCalculator 接口 + calculateCumulativeTax 纯函数
    - internal/tax/calculator_test.go 包含至少8个测试用例覆盖基础、跳档、边界值、年中入职
    - internal/tax/adapter.go 定义 EmployeeInfoProvider 和 SIDeductionProvider 接口
    - internal/tax/service.go 包含 Service 完整业务逻辑（扣除管理 + 计算调用 + 申报管理）
    - 所有测试 PASS
    - 累计预扣预缴法正确处理税率跳档（36000边界值）
    - 当月应扣为负时返回0（不退税）
    - math.Round(val*100)/100 在所有中间步骤保证两位小数精度
  </done>
</task>

</tasks>

<verification>
1. `go vet ./internal/tax/...` — 无错误
2. `go test -race -count=1 ./internal/tax/...` — 全部测试 PASS
3. 模型定义与 CONTEXT.md D-23/D-24 一致
4. 计算引擎实现累计预扣预缴法（D-13/D-14/D-15/D-16）
5. TaxCalculator 接口解耦循环依赖（D-08/D-11）
</verification>

<success_criteria>
- 个税模块核心代码通过 go vet 和全部测试
- 累计预扣预缴计算引擎正确处理7级税率和跳档
- 专项附加扣除 CRUD 支持6种按月扣除类型 + 住房互斥校验
- 跨模块接口定义清晰（EmployeeInfoProvider, SIDeductionProvider, TaxCalculator）
- TaxRecord 存储完整计算快照确保历史可追溯
</success_criteria>

<output>
After completion, create `.planning/phases/04-tax-calculation/04-01-SUMMARY.md`
</output>
