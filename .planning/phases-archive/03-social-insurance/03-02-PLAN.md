---
phase: 03-social-insurance
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - internal/socialinsurance/model.go
  - internal/socialinsurance/dto.go
  - internal/socialinsurance/repository.go
  - internal/socialinsurance/service.go
  - internal/socialinsurance/handler.go
  - internal/socialinsurance/service_test.go
  - cmd/server/main.go
autonomous: true
requirements:
  - SOCL-02
  - SOCL-04
  - SOCL-07

must_haves:
  truths:
    - "老板可勾选多个员工，系统根据城市自动匹配基数并显示各险种金额预览，确认后批量参保"
    - "老板可对参保中员工手动停缴，停缴后记录结束月份"
    - "可按月查询参保记录和缴费明细，支持按员工/状态筛选"
    - "所有参保/停缴/基数变更操作自动记录变更历史（操作类型、操作前/后值、操作人、时间）"
    - "MEMBER 角色只能查看自己的社保记录（通过 user_id -> employee_id 关联）"
  artifacts:
    - path: "internal/socialinsurance/model.go"
      provides: "SocialInsuranceRecord, PaymentDetail, ChangeHistory 模型（追加到现有 model.go）"
      contains: "SocialInsuranceRecord"
    - path: "internal/socialinsurance/service.go"
      provides: "批量参保、停缴、变更历史记录、缴费明细查询"
      exports: ["EnrollEmployees", "StopEnrollment", "ListRecords", "GetChangeHistory"]
    - path: "internal/socialinsurance/handler.go"
      provides: "参保/停缴/明细查询 HTTP 端点"
      contains: "EnrollEmployees"
  key_links:
    - from: "internal/socialinsurance/service.go"
      to: "internal/socialinsurance/repository.go"
      via: "调用 FindByCityAndYear 获取政策 → CalculateInsuranceAmounts → 批量创建参保记录"
      pattern: "CalculateInsuranceAmounts|FindByCityAndYear"
    - from: "internal/socialinsurance/model.go"
      to: "internal/employee/model.go"
      via: "employee_id 关联员工档案"
      pattern: "employee_id"
    - from: "internal/socialinsurance/service.go"
      to: "internal/socialinsurance/model.go"
      via: "变更历史 ChangeHistory 记录"
      pattern: "ChangeHistory"
    - from: "internal/socialinsurance/handler.go"
      to: "cmd/server/main.go"
      via: "新增参保/停缴/明细路由"
      pattern: "EnrollEmployees|StopEnrollment"
---

<objective>
实现社保参保/停缴核心操作、缴费明细查询和变更历史记录。老板可批量选择员工一键参保（3步操作），手动停缴，查看缴费明细，所有操作自动记录变更历史。

Purpose: 这是社保模块的核心业务逻辑，覆盖 SOCL-02（参保）、SOCL-04（明细查询）、SOCL-07（变更历史）三个需求。
Output: 参保/停缴 API、缴费明细查询 API、变更历史 API
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-social-insurance/03-CONTEXT.md
@.planning/phases/03-social-insurance/03-RESEARCH.md
@.planning/phases/03-social-insurance/03-01-SUMMARY.md

<interfaces>
<!-- Plan 01 创建的接口 -->

From internal/socialinsurance/model.go (Plan 01):
```go
type SocialInsurancePolicy struct {
    model.BaseModel
    CityID        int                                `gorm:"column:city_id;not null;index"`
    EffectiveYear int                                `gorm:"column:effective_year;not null;index"`
    Config        datatypes.JSONType[FiveInsurances]  `gorm:"column:config;type:jsonb"`
}
type InsuranceItem struct {
    CompanyRate  float64 `json:"company_rate"`
    PersonalRate float64 `json:"personal_rate"`
    BaseLower    float64 `json:"base_lower"`
    BaseUpper    float64 `json:"base_upper"`
}
type FiveInsurances struct {
    Pension      InsuranceItem `json:"pension"`
    Medical      InsuranceItem `json:"medical"`
    Unemployment InsuranceItem `json:"unemployment"`
    WorkInjury   InsuranceItem `json:"work_injury"`
    Maternity    InsuranceItem `json:"maternity"`
    HousingFund  InsuranceItem `json:"housing_fund"`
}
```

From internal/socialinsurance/repository.go (Plan 01):
```go
type Repository struct { db *gorm.DB }
func NewRepository(db *gorm.DB) *Repository
func (r *Repository) FindByCityAndYear(cityID int, year int) (*SocialInsurancePolicy, error)
```

From internal/socialinsurance/service.go (Plan 01):
```go
type Service struct { repo *Repository }
func NewService(repo *Repository) *Service
func (s *Service) CalculateInsuranceAmounts(cityID int, salary float64, year int) (*CalculateResponse, error)
```

<!-- Employee 模型（参保操作需要查询员工信息）-->

From internal/employee/model.go:
```go
type Employee struct {
    model.BaseModel
    Name     string `gorm:"column:name;type:varchar(50);not null;index"`
    Position string `gorm:"column:position;type:varchar(100);not null;index"`
    Status   string `gorm:"column:status;type:varchar(20);not null;default:pending;index"`
    UserID   *int64 `gorm:"column:user_id"`
}
const (
    StatusPending   = "pending"
    StatusProbation = "probation"
    StatusActive    = "active"
    StatusResigned  = "resigned"
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 参保/停缴模型 + Repository + Service 核心业务</name>
  <files>
    internal/socialinsurance/model.go
    internal/socialinsurance/dto.go
    internal/socialinsurance/repository.go
    internal/socialinsurance/service.go
    internal/socialinsurance/service_test.go
  </files>
  <read_first>
    internal/socialinsurance/model.go
    internal/socialinsurance/repository.go
    internal/socialinsurance/service.go
    internal/socialinsurance/dto.go
    internal/employee/model.go
    internal/common/model/base.go
    .planning/phases/03-social-insurance/03-CONTEXT.md
  </read_first>
  <behavior>
    - TestEnrollEmployees: 批量参保3个员工，验证生成3条 active 记录，各险种金额正确
    - TestEnrollEmployees_PartialFailure: 3个员工中1个城市无政策，验证2成功1失败，返回部分成功报告
    - TestStopEnrollment: 停缴1个 active 员工，验证状态变为 stopped，end_month 设置
    - TestStopEnrollment_AlreadyStopped: 对已停缴员工再次停缴返回错误
    - TestBatchStopEnrollment: 批量停缴3个员工，验证全部成功
    - TestListRecords: 查询参保记录列表，支持按状态筛选和分页
    - TestGetRecordByEmployee_MemberRole: MEMBER 角色通过 user_id 关联查询自己的记录
    - TestChangeHistory: 参保后生成 enroll 类型变更记录，停缴后生成 stop 类型变更记录
    - TestGetChangeHistory: 按员工查询变更历史时间线
  </behavior>
  <action>
    1. 在 `internal/socialinsurance/model.go` 追加以下模型:

       **SocialInsuranceRecord 参保记录（一条记录存所有险种明细）:**
       - BaseModel（多租户 org_id 隔离）
       - EmployeeID int64 `gorm:"column:employee_id;not null;index"` — 关联员工
       - EmployeeName string `gorm:"column:employee_name;type:varchar(50);not null"` — 冗余存储避免关联查询
       - CityID int `gorm:"column:city_id;not null"` — 参保城市
       - PolicyID int64 `gorm:"column:policy_id;not null"` — 使用的政策记录ID
       - BaseAmount float64 `gorm:"column:base_amount;not null"` — 参保基数
       - Status string `gorm:"column:status;type:varchar(20);not null;default:pending"` — pending/active/stopped（D-08）
       - StartMonth string `gorm:"column:start_month;type:varchar(7);not null"` — 参保起始月 YYYY-MM
       - EndMonth *string `gorm:"column:end_month;type:varchar(7)"` — 停缴月份 YYYY-MM（nullable）
       - Details datatypes.JSON `gorm:"column:details;type:jsonb"` — 各险种金额快照 JSON（per RESEARCH Open Question 2）
       - Details JSON 结构: `[{"name":"养老保险","base":7162,"company_rate":0.16,"company_amount":1145.92,"personal_rate":0.08,"personal_amount":572.96}, ...]`
       - TotalCompany float64 `gorm:"column:total_company;not null"` — 企业合计
       - TotalPersonal float64 `gorm:"column:total_personal;not null"` — 个人合计
       - TableName() = "social_insurance_records"

       **ChangeHistory 变更历史:**
       - BaseModel（多租户）
       - RecordID int64 `gorm:"column:record_id;not null;index"` — 关联参保记录
       - EmployeeID int64 `gorm:"column:employee_id;not null;index"`
       - ChangeType string `gorm:"column:change_type;type:varchar(20);not null"` — enroll/base_adjust/stop
       - BeforeValue datatypes.JSON `gorm:"column:before_value;type:jsonb"` — 变更前快照（nullable，enroll时为null）
       - AfterValue datatypes.JSON `gorm:"column:after_value;type:jsonb"` — 变更后快照
       - Remark string `gorm:"column:remark;type:varchar(500)"` — 备注
       - TableName() = "social_insurance_change_histories"

       **状态常量:**
       ```go
       const (
           SIStatusPending = "pending"
           SIStatusActive  = "active"
           SIStatusStopped = "stopped"
       )
       const (
           SIChangeEnroll      = "enroll"
           SIChangeBaseAdjust  = "base_adjust"
           SIChangeStop        = "stop"
       )
       ```

    2. 在 `internal/socialinsurance/dto.go` 追加:

       **参保操作:**
       - BatchEnrollRequest: EmployeeIDs []int64 `binding:"required,min=1"`, CityID int `binding:"required"`, StartMonth string `binding:"required"` （YYYY-MM 格式）
       - EnrollPreviewRequest: EmployeeIDs []int64 `binding:"required,min=1"`, CityID int `binding:"required"`
       - EnrollPreviewItem: EmployeeID, EmployeeName, BaseAmount, TotalCompany, TotalPersonal, Items []InsuranceAmountDetail
       - EnrollPreviewResponse: Items []EnrollPreviewItem
       - BatchEnrollResult: SuccessCount int, FailCount int, Failures []EnrollFailure
       - EnrollFailure: EmployeeID int64, EmployeeName string, Reason string

       **停缴操作:**
       - BatchStopRequest: RecordIDs []int64 `binding:"required,min=1"`, EndMonth string `binding:"required"` （YYYY-MM）
       - BatchStopResult: SuccessCount int, FailCount int, Failures []StopFailure

       **查询:**
       - RecordListQueryParams: Status string, EmployeeName string, Page int, PageSize int
       - RecordResponse: ID, EmployeeID, EmployeeName, CityID, CityName, BaseAmount, Status, StartMonth, EndMonth, Details, TotalCompany, TotalPersonal, CreatedAt
       - ChangeHistoryResponse: ID, RecordID, EmployeeID, EmployeeName, ChangeType, BeforeValue, AfterValue, Remark, CreatedAt

    3. 扩展 `internal/socialinsurance/repository.go`:

       - **新增 EmployeeRepository 依赖**（Service 通过接口注入，不直接 import employee 包）:
         ```go
         // EmployeeInfo 员工信息（从 employee 模块查询）
         type EmployeeInfo struct {
             ID     int64
             Name   string
             OrgID  int64
             UserID *int64
         }
         // EmployeeQuerier 员工查询接口（解耦社保和员工模块）
         type EmployeeQuerier interface {
             FindByIDs(orgID int64, ids []int64) ([]EmployeeInfo, error)
             FindByUserID(orgID int64, userID int64) (*EmployeeInfo, error)
         }
         ```

       - Record CRUD:
         - CreateRecord(record *SocialInsuranceRecord) error
         - FindRecordByID(orgID, id int64) (*SocialInsuranceRecord, error)
         - FindActiveRecordByEmployee(orgID, employeeID int64) (*SocialInsuranceRecord, error) — 查询员工当前 active 记录
         - ListRecords(orgID int64, status string, page, pageSize int) ([]SocialInsuranceRecord, int64, error)
         - UpdateRecord(orgID, id int64, updates map[string]interface{}) error

       - ChangeHistory CRUD:
         - CreateChangeHistory(history *ChangeHistory) error
         - ListChangeHistories(orgID int64, employeeID int64, page, pageSize int) ([]ChangeHistory, int64, error)

    4. 重构并扩展 `internal/socialinsurance/service.go`:

       - Service struct 增加 empQuerier EmployeeQuerier 字段
       - 修改 NewService 签名: `NewService(repo *Repository, empQuerier EmployeeQuerier) *Service`

       **EnrollPreview（参保预览，D-05 Step 2）:**
       - 输入: orgID int64, req *EnrollPreviewRequest
       - 逻辑: 查询员工列表 → 按城市获取政策 → 对每个员工计算金额
       - 返回: EnrollPreviewResponse（per UI-SPEC Enrollment Preview shape）

       **BatchEnroll（批量参保，D-05 Step 3 + D-06）:**
       - 输入: orgID, userID int64, req *BatchEnrollRequest
       - 逻辑:
         a. 查询员工列表（empQuerier.FindByIDs）
         b. 逐个处理（per Pitfall 5，不用整体事务）:
            - 查询员工当前是否有 active 参保记录（有则跳过，记入 failures）
            - 查询城市政策
            - 调用 CalculateInsuranceAmounts 计算金额
            - 创建 SocialInsuranceRecord（status=active, 存 details 快照）
            - 创建 ChangeHistory（change_type=enroll）
         c. 返回 BatchEnrollResult（成功/失败计数）

       **BatchStopEnrollment（批量停缴，D-06/D-07/D-08）:**
       - 输入: orgID, userID int64, req *BatchStopRequest
       - 逻辑:
           a. 逐个处理:
            - 查询参保记录，验证状态为 active
            - 更新 status=stopped, end_month
            - 创建 ChangeHistory（change_type=stop）
         b. 返回 BatchStopResult

       **ListRecords（参保记录列表，SOCL-04）:**
       - 输入: orgID int64, params RecordListQueryParams
       - 逻辑: 调用 repo.ListRecords，关联 city.Cities 获取城市名
       - 返回: []RecordResponse, total, page, pageSize

       **GetMyRecords（MEMBER 查询自己记录，D-14）:**
       - 输入: orgID, userID int64
       - 逻辑: empQuerier.FindByUserID → 查询该员工参保记录

       **GetChangeHistory（变更历史，SOCL-07）:**
       - 输入: orgID, employeeID int64, page, pageSize int
       - 返回: []ChangeHistoryResponse, total

       **GetSocialInsuranceDeduction（D-12 接口，供 Phase 5 调用）:**
       - 输入: orgID, employeeID int64, month string
       - 逻辑: 查询该员工 active 记录，返回该月份各险种个人扣款金额
       - 返回: DeductionResponse { Items []DeductionItem, TotalPersonal float64 }

    5. 在 `internal/socialinsurance/service_test.go` 追加测试:
       - 创建 mock EmployeeQuerier 实现
       - TestEnrollEmployees: mock 3个员工 → 批量参保 → 验证记录和变更历史
       - TestEnrollEmployees_DuplicateEnrollment: 已参保员工再次参保返回失败
       - TestEnrollEmployees_NoPolicy: 城市无政策时返回失败
       - TestStopEnrollment: 停缴 → 验证状态和 end_month
       - TestStopEnrollment_NotActive: 非 active 状态停缴返回错误
       - TestBatchStopEnrollment: 批量停缴
       - TestListRecords_FilterByStatus: 按状态筛选
       - TestGetChangeHistory: 验证 enroll 和 stop 类型的历史记录
       - TestGetSocialInsuranceDeduction: 查询指定月份个人扣款
  </action>
  <verify>
    <automated>cd /Users/wencai/github/EasyHR && go test ./internal/socialinsurance/... -count=1 -v</automated>
  </verify>
  <done>
    - SocialInsuranceRecord 模型支持 pending/active/stopped 状态（D-08）
    - 批量参保 EnrollEmployees 支持多员工同时参保，返回部分成功报告（D-06）
    - 参保预览 EnrollPreview 显示各险种金额明细（D-05 Step 2）
    - 批量停缴 BatchStopEnrollment 更新状态和结束月份
    - ChangeHistory 自动记录所有变更操作（SOCL-07）
    - GetSocialInsuranceDeduction 预留接口供 Phase 5 调用（D-12）
    - 所有测试 PASS
  </done>
</task>

<task type="auto">
  <name>Task 2: 参保/停缴 Handler + 员工查询适配器 + main.go 集成</name>
  <files>
    internal/socialinsurance/handler.go
    internal/socialinsurance/employee_adapter.go
    cmd/server/main.go
  </files>
  <read_first>
    internal/socialinsurance/handler.go
    internal/socialinsurance/service.go
    internal/socialinsurance/dto.go
    internal/employee/repository.go
    internal/employee/handler.go
    cmd/server/main.go
  </read_first>
  <acceptance_criteria>
    - handler.go 包含 "EnrollPreview", "BatchEnroll", "BatchStopEnrollment", "ListRecords", "GetMyRecords", "GetChangeHistory", "GetDeduction" 方法
    - handler.go RegisterRoutes 包含新路由:
      - "POST /social-insurance/enroll/preview" (owner, admin)
      - "POST /social-insurance/enroll" (owner, admin)
      - "POST /social-insurance/stop" (owner, admin)
      - "GET /social-insurance/records" (所有角色)
      - "GET /social-insurance/my-records" (所有角色 — MEMBER 只看自己)
      - "GET /social-insurance/records/:id/history" (所有角色)
      - "GET /social-insurance/deduction" (所有角色 — Phase 5 调用)
    - employee_adapter.go 包含 "EmployeeAdapter" struct 实现 "EmployeeQuerier" 接口
    - main.go 包含 SocialInsuranceRecord, ChangeHistory 的 AutoMigrate
    - main.go 包含 employeeAdapter 初始化和 Service 依赖注入
    - "go build ./cmd/server/" 编译通过
    - "go test ./... -count=1" 全部 PASS
  </acceptance_criteria>
  <action>
    1. 创建 `internal/socialinsurance/employee_adapter.go`:
       - EmployeeAdapter struct { empRepo *employee.Repository }
       - NewEmployeeAdapter(empRepo *employee.Repository) *EmployeeAdapter
       - func (a *EmployeeAdapter) FindByIDs(orgID int64, ids []int64) ([]EmployeeInfo, error):
         - 调用 empRepo 的查询方法获取员工列表
         - 由于 employee.Repository 没有批量查询方法，需要逐个查询或添加新方法
         - **最佳方案: 在 employee 包中添加一个公开的批量查询方法**:
           在 internal/employee/repository.go 中添加:
           ```go
           func (r *Repository) FindByIDs(orgID int64, ids []int64) ([]Employee, error) {
               var employees []Employee
               err := r.db.Scopes(middleware.TenantScope(orgID)).Where("id IN ?", ids).Find(&employees).Error
               return employees, err
           }
           ```
         - 适配器将 Employee 转换为 EmployeeInfo
       - func (a *EmployeeAdapter) FindByUserID(orgID int64, userID int64) (*EmployeeInfo, error):
         - 查询 user_id = userID 的员工记录
         - 需要在 employee.Repository 添加:
           ```go
           func (r *Repository) FindByUserID(orgID int64, userID int64) (*Employee, error) {
               var emp Employee
               err := r.db.Scopes(middleware.TenantScope(orgID)).Where("user_id = ?", userID).First(&emp).Error
               return &emp, err
           }
           ```

    2. 在 `internal/employee/repository.go` 添加两个公开方法:
       - FindByIDs(orgID int64, ids []int64) ([]Employee, error)
       - FindByUserID(orgID int64, userID int64) (*Employee, error)

    3. 扩展 `internal/socialinsurance/handler.go` RegisterRoutes:
       ```
       // 参保操作（OWNER/ADMIN）
       authGroup.POST("/social-insurance/enroll/preview", middleware.RequireRole("owner", "admin"), h.EnrollPreview)
       authGroup.POST("/social-insurance/enroll", middleware.RequireRole("owner", "admin"), h.BatchEnroll)
       authGroup.POST("/social-insurance/stop", middleware.RequireRole("owner", "admin"), h.BatchStopEnrollment)
       // 查询（所有角色，MEMBER 通过 GetMyRecords 只看自己）
       authGroup.GET("/social-insurance/records", h.ListRecords)
       authGroup.GET("/social-insurance/my-records", h.GetMyRecords)
       authGroup.GET("/social-insurance/records/:id/history", h.GetChangeHistory)
       authGroup.GET("/social-insurance/deduction", h.GetDeduction)
       ```

       Handler 方法:
       - EnrollPreview: 绑定 EnrollPreviewRequest，调用 svc.EnrollPreview
       - BatchEnroll: 绑定 BatchEnrollRequest，调用 svc.BatchEnroll
       - BatchStopEnrollment: 绑定 BatchStopRequest，调用 svc.BatchStopEnrollment
       - ListRecords: query 参数 status, employee_name, page, page_size
       - GetMyRecords: 从 context 获取 user_id，调用 svc.GetMyRecords
       - GetChangeHistory: query 参数 employee_id, page, page_size
       - GetDeduction: query 参数 employee_id, month

    4. 修改 `cmd/server/main.go`:
       - AutoMigrate 添加:
         ```go
         &socialinsurance.SocialInsuranceRecord{},
         &socialinsurance.ChangeHistory{},
         ```
       - 依赖注入修改:
         ```go
         empAdapter := socialinsurance.NewEmployeeAdapter(empRepo)
         siSvc := socialinsurance.NewService(siRepo, empAdapter)
         ```
       - 注意：社保模块的 Service 现在需要 EmployeeAdapter 参数，需要修改 Plan 01 中 NewService 的调用

  </action>
  <verify>
    <automated>cd /Users/wencai/github/EasyHR && go build ./cmd/server/ && go test ./... -count=1</automated>
  </verify>
  <done>
    - 7个新的参保/停缴/查询 HTTP 端点已注册
    - OWNER/ADMIN 可操作参保/停缴，MEMBER 只能查看自己记录（D-14）
    - EmployeeAdapter 解耦社保和员工模块
    - main.go AutoMigrate 包含新模型
    - 编译通过，全部测试 PASS
  </done>
</task>

</tasks>

<verification>
1. `go test ./internal/socialinsurance/... -count=1 -v` — 所有测试 PASS
2. `go build ./cmd/server/` — 编译通过
3. `go test ./... -count=1` — 全项目测试无回归
4. 验证路由注册：grep "social-insurance/enroll" internal/socialinsurance/handler.go
5. 验证适配器：grep "EmployeeAdapter" internal/socialinsurance/employee_adapter.go
</verification>

<success_criteria>
- 批量参保支持多员工同时操作，返回部分成功报告（D-05/D-06）
- 参保预览显示各险种企业和个人金额明细（符合 UI-SPEC shape）
- 停缴操作更新状态和结束月份（D-08）
- 变更历史自动记录 enroll/stop 操作（SOCL-07）
- GetSocialInsuranceDeduction 接口可供 Phase 5 调用（D-12）
- RBAC 权限正确（OWNER/ADMIN 操作，MEMBER 只查自己）
- 全部测试 PASS
</success_criteria>

<output>
After completion, create `.planning/phases/03-social-insurance/03-02-SUMMARY.md`
</output>
