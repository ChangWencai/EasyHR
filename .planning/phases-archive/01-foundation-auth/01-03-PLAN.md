---
phase: 01-foundation-auth
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - internal/user/model.go
  - internal/user/dto.go
  - internal/user/repository.go
  - internal/user/repository_test.go
  - internal/user/service.go
  - internal/user/service_test.go
  - internal/user/handler.go
  - internal/user/handler_test.go
  - internal/common/middleware/rbac.go
  - internal/common/middleware/rbac_test.go
  - internal/audit/model.go
  - internal/audit/handler.go
  - internal/audit/handler_test.go
  - internal/audit/repository.go
  - internal/city/model.go
  - internal/city/handler.go
  - internal/city/handler_test.go
  - cmd/server/main.go
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - PLAT-01
  - PLAT-02

must_haves:
  truths:
    - "用户通过手机号+验证码登录，手机号不存在则自动创建用户和企业（OWNER角色）"
    - "验证码6位数字，Redis存储，5分钟有效，60秒发送间隔限制"
    - "验证码错误5次后需重新发送"
    - "首次登录返回 onboarding_required=true，引导录入企业信息"
    - "用户可刷新 Token，旧 Refresh Token 加入黑名单"
    - "用户可退出登录（当前设备 Token 加入黑名单）"
    - "OWNER 可创建/删除子账号并分配角色（ADMIN 不可管理子账号 per D-30）"
    - "OWNER 角色不可被删除或降级"
    - "RequireRole 中间件阻止无权限角色访问路由"
    - "MEMBER 角色无法访问用户管理接口"
    - "城市列表 API 返回完整的城市数据（id, name, province）"
    - "所有写操作自动记录审计日志（who/when/what）"
    - "审计日志只能查询，不能删除或修改"
  artifacts:
    - path: "internal/user/handler.go"
      provides: "用户认证 HTTP Handler（发送验证码/登录/刷新/退出/企业引导/子账号管理）"
      exports: ["Handler", "NewHandler", "RegisterRoutes"]
    - path: "internal/user/service.go"
      provides: "用户认证业务逻辑"
      exports: ["Service", "NewService"]
    - path: "internal/user/repository.go"
      provides: "用户数据访问（含多租户 Scope）"
      exports: ["Repository", "NewRepository"]
    - path: "internal/city/handler.go"
      provides: "城市列表 API（AUTH-03 后端支持）"
      exports: ["Handler", "NewHandler", "RegisterRoutes"]
    - path: "internal/common/middleware/rbac.go"
      provides: "RBAC 角色检查中间件"
      exports: ["RequireRole"]
    - path: "internal/audit/model.go"
      provides: "审计日志数据模型"
      contains: "AuditLog"
    - path: "internal/audit/handler.go"
      provides: "审计日志查询接口"
      exports: ["Handler", "NewHandler"]
  key_links:
    - from: "internal/user/handler.go"
      to: "internal/user/service.go"
      via: "Handler 调用 Service 方法"
      pattern: "s\\.service\\."
    - from: "internal/user/service.go"
      to: "pkg/jwt/jwt.go"
      via: "生成/刷新 Token"
      pattern: "jwt\\.Generate"
    - from: "internal/user/service.go"
      to: "pkg/sms/client.go"
      via: "发送验证码"
      pattern: "smsClient\\.SendCode"
    - from: "internal/user/repository.go"
      to: "internal/common/middleware/tenant.go"
      via: "TenantScope 自动注入 org_id"
      pattern: "TenantScope"
    - from: "internal/common/middleware/rbac.go"
      to: "gin.Context"
      via: "读取 role 并校验"
      pattern: "c\\.GetString\\(\"role\"\\)"
    - from: "internal/audit/model.go"
      to: "gorm.io/gorm"
      via: "GORM Hook 自动记录"
      pattern: "gorm:\\create|gorm:\\update|gorm:\\delete"
    - from: "internal/city/handler.go"
      to: "internal/city/model.go"
      via: "读取城市种子数据"
      pattern: "cities|Cities"
---

<objective>
实现核心用户认证业务流程、城市列表 API 和 RBAC 权限系统：手机号+验证码登录/自动注册、企业信息引导、Token 刷新/退出、城市列表 API（AUTH-03）、子账号管理（OWNER only per D-30）、审计日志自动记录。

Purpose: 这是 Phase 1 的核心业务逻辑层。将 Plan 01 的基础设施（响应/加密/多租户）和 Plan 02 的工具包（JWT/SMS/OSS）组装为完整的用户认证和权限管理系统。同时提供城市列表 API 支持前端城市选择器。

Output: 完整的认证 API（登录/注册/引导/刷新/退出/子账号管理）、城市列表 API、RBAC 中间件、审计日志系统，所有集成测试通过。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-auth/1-CONTEXT.md
@.planning/phases/01-foundation-auth/1-RESEARCH.md
@.planning/phases/01-foundation-auth/01-01-SUMMARY.md
@.planning/phases/01-foundation-auth/01-02-SUMMARY.md
</context>

<interfaces>
<!-- Plan 01 产出 -->

From internal/common/response/response.go:
```go
func Success(c *gin.Context, data interface{})
func Error(c *gin.Context, httpStatus int, code int, msg string)
func Unauthorized(c *gin.Context, msg string)
func Forbidden(c *gin.Context, msg string)
func BadRequest(c *gin.Context, msg string)
func PageSuccess(c *gin.Context, list interface{}, total int64, page, pageSize int)
```

From internal/common/crypto/aes.go:
```go
func Encrypt(plaintext string, key []byte) (string, error)
func Decrypt(encoded string, key []byte) (string, error)
```

From internal/common/crypto/hash.go:
```go
func HashSHA256(input string) string
```

From internal/common/crypto/mask.go:
```go
func MaskPhone(phone string) string
func MaskIDCard(idCard string) string
```

From internal/common/middleware/tenant.go:
```go
func TenantScope(orgID int64) func(db *gorm.DB) *gorm.DB
func TenantMiddleware() gin.HandlerFunc
```

From internal/common/model/base.go:
```go
type BaseModel struct {
    ID        int64
    OrgID     int64
    CreatedBy int64
    CreatedAt time.Time
    UpdatedBy int64
    UpdatedAt time.Time
    DeletedAt gorm.DeletedAt
}
```

From internal/common/model/org.go:
```go
type Organization struct { /* 嵌入 BaseModel, Name, CreditCode, City, ContactName, ContactPhone, Status */ }
```

From internal/common/model/user.go:
```go
type User struct { /* 嵌入 BaseModel, Phone, PhoneHash, Name, Role, Status */ }
```

<!-- Plan 02 产出 -->

From pkg/jwt/jwt.go:
```go
type Claims struct { UserID int64; OrgID int64; Role string; jwt.RegisteredClaims }
type TokenPair struct { AccessToken string; RefreshToken string }
func GenerateAccessToken(userID, orgID int64, role, secret string, ttl time.Duration) (string, error)
func GenerateRefreshToken(userID int64, secret string, ttl time.Duration) (string, error)
func ParseToken(tokenStr, secret string) (*Claims, error)
func RefreshTokens(refreshTokenStr, secret string, accessTTL, refreshTTL time.Duration, blacklistFunc func(jti string, ttl time.Duration) error) (*TokenPair, error)
```

From pkg/sms/client.go:
```go
type Client struct { /* ... */ }
func NewClient(cfg Config) (*Client, error)
func (c *Client) SendCode(ctx context.Context, phone, code string) error
```

From internal/common/middleware/auth.go:
```go
func Auth(jwtSecret string, rdb *redis.Client) gin.HandlerFunc
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 用户数据层（model/dto/repository）</name>
  <files>
    internal/user/model.go,
    internal/user/dto.go,
    internal/user/repository.go,
    internal/user/repository_test.go
  </files>
  <read_first>
    .planning/phases/01-foundation-auth/1-CONTEXT.md (D-02 三层架构, D-09 多租户Scope, D-15 加密存储)
    .planning/phases/01-foundation-auth/1-RESEARCH.md (Pattern 1: GORM Scope, Pattern 2: 加密双列)
  </read_first>
  <behavior>
    - Test 1: FindByPhoneHash 使用 SHA-256 哈希精确查找用户，不使用 TenantScope（登录时不知道 org_id）
    - Test 2: CreateUser 使用 AES 加密手机号 + SHA-256 哈希双列存储
    - Test 3: ListUsers(orgID=1) 只返回 org_id=1 的用户（TenantScope 隔离）
    - Test 4: ListUsers(orgID=1) 和 ListUsers(orgID=2) 数据完全隔离——创建两个 org 的用户，查询互不可见
    - Test 5: DeleteUser 尝试删除 OWNER 角色用户返回错误 per D-14
    - Test 6: UpdateUserRole 尝试将 OWNER 角色改为其他角色返回错误 per D-14
  </behavior>
  <action>
    1. 创建 `internal/user/model.go`：
       - 复用 `internal/common/model/user.go` 和 `internal/common/model/org.go` 的模型定义
       - 如果已有模型可直接 import common/model

    2. 创建 `internal/user/dto.go`：
       ```go
       // 请求 DTO
       type SendCodeRequest struct { Phone string `json:"phone" binding:"required,len=11"` }
       type LoginRequest struct { Phone string `json:"phone" binding:"required,len=11"` Code string `json:"code" binding:"required,len=6"` }
       type RefreshRequest struct { RefreshToken string `json:"refresh_token" binding:"required"` }
       type CompleteOnboardingRequest struct {
           Name        string `json:"name" binding:"required,min=2,max=100"`
           CreditCode  string `json:"credit_code" binding:"required,len=18"`
           City        string `json:"city" binding:"required"`
           ContactName string `json:"contact_name" binding:"required"`
           ContactPhone string `json:"contact_phone" binding:"required,len=11"`
       }
       type CreateSubAccountRequest struct {
           Phone string `json:"phone" binding:"required,len=11"`
           Name  string `json:"name" binding:"required,min=2,max=50"`
           Role  string `json:"role" binding:"required,oneof=admin member"`
       }
       type UpdateRoleRequest struct {
           Role string `json:"role" binding:"required,oneof=admin member"`
       }

       // 响应 DTO
       type LoginResponse struct {
           AccessToken      string `json:"access_token"`
           RefreshToken     string `json:"refresh_token"`
           OnboardingRequired bool  `json:"onboarding_required"`
       }
       type UserInfoResponse struct {
           ID    int64  `json:"id"`
           Name  string `json:"name"`
           Phone string `json:"phone"`  // 脱敏
           Role  string `json:"role"`
           OrgID int64  `json:"org_id"`
           OrgName string `json:"org_name"`
       }
       ```

    3. 创建 `internal/user/repository.go` per D-02（handler->service->repository 三层）：
       ```go
       type Repository interface {
           FindByPhoneHash(phoneHash string) (*model.User, error)
           FindByOrgID(orgID int64) (*model.Organization, error)
           CreateUser(user *model.User) error
           CreateOrg(org *model.Organization) error
           UpdateOrg(orgID int64, updates map[string]interface{}) error
           ListUsers(orgID int64, page, pageSize int) ([]model.User, int64, error)
           FindUserByID(orgID, userID int64) (*model.User, error)
           UpdateUserRole(orgID, userID int64, role string) error
           DeleteUser(orgID, userID int64) error
       }
       ```
       - 所有方法必须使用 `db.Scopes(common.TenantScope(orgID))` 自动注入 org_id per D-09
       - FindByPhoneHash 不使用 TenantScope（登录时不知道 org_id，用全局查询）
       - DeleteUser 内部检查：如果目标用户角色为 owner，返回错误 per D-14
       - UpdateUserRole 内部检查：如果目标用户角色为 owner，返回错误 per D-14

    4. 创建 `internal/user/repository_test.go`：使用 SQLite 内存数据库（GORM 兼容）测试 Repository 方法。验证 TenantScope 隔离效果（创建两个 org 的用户，查询互不可见）。验证 OWNER 不可删除和不可降级。
  </action>
  <verify>
    <automated>cd /Users/wencai/github/EasyHR && go test ./internal/user/... -run TestRepository -v</automated>
  </verify>
  <done>
    - DTO 定义完整（请求/响应结构体 + binding 标签校验）
    - Repository 接口定义完整 per D-02
    - 所有查询自动注入 TenantScope per D-09
    - FindByPhoneHash 全局查询（无 TenantScope）
    - OWNER 不可删除/不可降级 per D-14
    - `go test ./internal/user/... -run TestRepository -v` 全部通过
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 用户业务逻辑层（service）</name>
  <files>
    internal/user/service.go,
    internal/user/service_test.go
  </files>
  <read_first>
    .planning/phases/01-foundation-auth/1-CONTEXT.md (D-05 登录方式, D-06 JWT, D-07 验证码Redis, D-08 首次登录引导, D-27 多设备, D-28 refresh轮转, D-29 退出策略, D-30 权限规则)
    .planning/phases/01-foundation-auth/1-RESEARCH.md (Pitfall 2: 验证码暴力破解)
  </read_first>
  <behavior>
    - Test 1: SendCode 对同一手机号 60 秒内第二次发送返回限流错误
    - Test 2: Login 验证码正确+手机号已存在 -> 返回 token（onboarding_required=false）
    - Test 3: Login 验证码正确+手机号不存在 -> 自动创建用户+企业，返回 token（onboarding_required=true）
    - Test 4: Login 验证码错误 -> 返回 401，错误计数+1
    - Test 5: Login 验证码错误5次 -> Redis key 被删除，需重新发送
    - Test 6: RefreshToken 成功 -> 返回新 token pair，旧 refresh token jti 在黑名单
    - Test 7: Logout -> 当前 access token jti 加入黑名单
    - Test 8: CompleteOnboarding -> 更新企业名称/信用代码/城市，返回成功
    - Test 9: CreateSubAccount 成功创建 admin/member 角色用户
    - Test 10: CreateSubAccount 角色为 owner 时返回错误（OWNER 唯一 per D-14）
  </behavior>
  <action>
    1. 创建 `internal/user/service.go`：
       ```go
       type Service interface {
           SendCode(ctx context.Context, phone string) error
           Login(ctx context.Context, phone, code string) (*LoginResponse, error)
           RefreshToken(ctx context.Context, refreshToken string) (*jwt.TokenPair, error)
           Logout(ctx context.Context, accessToken, refreshToken string) error
           CompleteOnboarding(ctx context.Context, orgID int64, req *CompleteOnboardingRequest) error
           CreateSubAccount(ctx context.Context, orgID, creatorID int64, req *CreateSubAccountRequest) error
           ListSubAccounts(ctx context.Context, orgID int64, page, pageSize int) ([]UserInfoResponse, int64, error)
           UpdateSubAccountRole(ctx context.Context, orgID, targetUserID int64, role string) error
           DeleteSubAccount(ctx context.Context, orgID, targetUserID int64) error
       }
       ```

       SendCode 实现 per D-07：
       - 检查 Redis `sms:limit:{phone}` 是否存在（60秒限制），存在则返回限流错误
       - 生成 6 位随机数字验证码
       - 调用 SMS 客户端发送
       - 存储到 Redis `sms:code:{phone}`，TTL 5 分钟
       - 设置限流 key `sms:limit:{phone}`，TTL 60 秒

       Login 实现 per D-05/D-08：
       - 从 Redis 获取验证码 `sms:code:{phone}`
       - 不存在 -> 返回"验证码已过期"
       - 递增错误计数 `sms:errors:{phone}`，检查是否 >= 5 -> 删除验证码 key，要求重新发送 per RESEARCH Pitfall 2
       - 验证码不匹配 -> 返回错误
       - 验证码匹配 -> 删除验证码 key + 错误计数 key
       - 用 phone 的 SHA-256 哈希查找用户
       - 用户不存在 -> 自动注册：创建 Organization（status=inactive）+ 创建 User（role=owner），返回 token（onboarding_required=true）per D-08
       - 用户存在 -> 返回 token（onboarding_required=false）
       - 生成 access token（7天）+ refresh token（30天）

       RefreshToken 实现 per D-28：
       - 调用 `jwt.RefreshTokens()`，传入 blacklistFunc 将旧 jti 加入 Redis（TTL = 剩余有效期）

       Logout 实现 per D-29：
       - 将当前 access token 的 jti 加入 Redis 黑名单 `token:blacklist:{jti}`，TTL = token 剩余有效期
       - 如果提供了 refresh token，同样加入黑名单

       CompleteOnboarding 实现：
       - 更新 Organization 的 name/credit_code/city/contact_name/contact_phone
       - 设置 Organization status = active
       - 加密存储 contact_phone（AES + SHA-256 双列）

       CreateSubAccount 实现 per D-12/D-14/D-30：
       - 注意：此方法不做角色权限检查（由 Handler 层 RBAC 中间件控制路由，仅 OWNER 可调用）
       - 检查手机号是否已存在于同一企业
       - 创建用户，加密手机号（AES + SHA-256 双列）
       - 角色只能是 admin 或 member per D-12（OWNER 唯一 per D-14，CreateSubAccountRequest 的 binding 已限制 oneof=admin member）

    2. 创建 `internal/user/service_test.go`：使用 mock Repository 和 mock SMS 客户端测试业务逻辑。覆盖：验证码发送限流、登录成功（已有用户）、自动注册（新用户）、验证码错误5次、Token 刷新、退出登录、企业引导完成、子账号创建成功、子账号创建角色校验。
  </action>
  <verify>
    <automated>cd /Users/wencai/github/EasyHR && go test ./internal/user/... -run TestService -v</automated>
  </verify>
  <done>
    - SendCode 验证码发送限流（60秒间隔）per D-07
    - Login 验证码校验 + 自动注册 per D-05/D-08
    - 验证码错误5次后需重新发送 per RESEARCH Pitfall 2
    - RefreshToken 轮转返回新 token pair per D-28
    - Logout 当前设备 token 黑名单 per D-29
    - CompleteOnboarding 企业信息录入 per D-08
    - CreateSubAccount 子账号创建（仅 admin/member 角色）per D-12/D-14
    - `go test ./internal/user/... -run TestService -v` 全部通过
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 用户 API 层（handler/routes）+ RBAC 中间件 + 城市列表 API + 审计日志</name>
  <files>
    internal/user/handler.go,
    internal/user/handler_test.go,
    internal/common/middleware/rbac.go,
    internal/common/middleware/rbac_test.go,
    internal/city/model.go,
    internal/city/handler.go,
    internal/city/handler_test.go,
    internal/audit/model.go,
    internal/audit/repository.go,
    internal/audit/handler.go,
    internal/audit/handler_test.go,
    cmd/server/main.go
  </files>
  <read_first>
    .planning/phases/01-foundation-auth/1-CONTEXT.md (D-12 三级角色, D-13 中间件角色检查, D-14 OWNER不可删, D-18 统一响应, D-20 分页参数, D-21 API前缀, D-22 GORM Hook审计, D-23 审计只增不改, D-30 通用权限规则)
    .planning/phases/01-foundation-auth/1-RESEARCH.md (Pattern 3: Gin中间件RBAC, Pattern 5: 审计日志GORM Hook)
  </read_first>
  <behavior>
    - Test 1: RequireRole("owner", "admin") 对 role=admin 的请求放行
    - Test 2: RequireRole("owner") 对 role=member 的请求返回 403
    - Test 3: RequireRole("owner") 对无 role 的请求返回 403
    - Test 4: POST /api/v1/auth/send-code 限流保护，参数校验（手机号11位）
    - Test 5: POST /api/v1/auth/login 参数校验，响应包含 access_token + onboarding_required
    - Test 6: GET /api/v1/cities 返回城市列表（id, name, province），无需认证
    - Test 7: POST /api/v1/users（创建子账号）仅 OWNER 可访问，ADMIN 返回 403 per D-30
    - Test 8: PUT /api/v1/users/:id/role（修改角色）仅 OWNER 可访问 per D-30
    - Test 9: DELETE /api/v1/users/:id（删除用户）仅 OWNER 可访问 per D-30
    - Test 10: GET /api/v1/users（查看用户列表）OWNER 和 ADMIN 可访问 per D-30
    - Test 11: 审计日志在 GORM Create/Update/Delete 操作后自动插入 audit_logs 表
    - Test 12: 审计日志查询接口支持分页，不支持删除
  </behavior>
  <action>
    1. 创建 `internal/user/handler.go` per D-02/D-21：
       ```go
       type Handler struct { service Service }
       func NewHandler(svc Service) *Handler
       func (h *Handler) RegisterRoutes(rg *gin.RouterGroup, authMiddleware, rbacMiddleware gin.HandlerFunc)
       ```
       RegisterRoutes 注册以下路由 per D-21（/api/v1 前缀）：
       - POST /api/v1/auth/send-code -> SendCode（限流中间件保护）
       - POST /api/v1/auth/login -> Login
       - POST /api/v1/auth/refresh -> RefreshToken
       - POST /api/v1/auth/logout -> Logout（需认证）
       - PUT /api/v1/org/onboarding -> CompleteOnboarding（需认证）
       - GET /api/v1/users -> ListSubAccounts（需认证，RequireRole("owner", "admin") per D-30: ADMIN 可查看用户列表）
       - POST /api/v1/users -> CreateSubAccount（需认证，RequireRole("owner") ONLY per D-30: ADMIN 不可管理子账号）
       - PUT /api/v1/users/:id/role -> UpdateSubAccountRole（需认证，RequireRole("owner") ONLY per D-30）
       - DELETE /api/v1/users/:id -> DeleteSubAccount（需认证，RequireRole("owner") ONLY per D-30，不可删 OWNER per D-14）

    2. 创建 `internal/user/handler_test.go`：使用 httptest + gin test router 测试 HTTP 层。测试路由注册、请求参数校验、响应格式。

    3. 创建 `internal/common/middleware/rbac.go` per D-12/D-13/D-30：
       ```go
       // RequireRole 检查当前用户角色是否在允许列表中
       func RequireRole(roles ...string) gin.HandlerFunc {
           return func(c *gin.Context) {
               userRole := c.GetString("role")
               for _, r := range roles {
                   if strings.EqualFold(userRole, r) {
                       c.Next()
                       return
                   }
               }
               response.Forbidden(c, "权限不足")
               c.Abort()
           }
       }
       ```

    4. 创建 `internal/common/middleware/rbac_test.go`：
       - 测试允许的角色放行
       - 测试不允许的角色返回 403
       - 测试无角色信息返回 403
       - 测试 OWNER/ADMIN/MEMBER 不同组合

    5. 创建 `internal/city/model.go`（AUTH-03 后端支持）：
       ```go
       type City struct {
           ID       int64  `json:"id"`
           Name     string `json:"name"`
           Province string `json:"province"`
       }
       ```
       - 不使用数据库表，使用硬编码的种子数据（中国主要城市列表，覆盖所有省份）
       - 数据以 Go 变量形式存储在 model.go 或单独的 seed.go 文件中
       - 包含至少 300+ 城市，覆盖全部 34 个省级行政区

    6. 创建 `internal/city/handler.go`（AUTH-03 后端支持）：
       ```go
       type Handler struct { cities []City }
       func NewHandler() *Handler
       func (h *Handler) RegisterRoutes(rg *gin.RouterGroup)
       ```
       - GET /api/v1/cities -> 返回城市列表，无需认证
       - 支持 province 查询参数筛选（可选）
       - 返回格式：`{"code":0,"message":"success","data":[...]}`

    7. 创建 `internal/city/handler_test.go`：
       - 测试 GET /api/v1/cities 返回非空城市列表
       - 测试城市数据包含 id, name, province 字段
       - 测试 province 筛选功能

    8. 创建 `internal/audit/model.go` per D-22/D-23：
       ```go
       type AuditLog struct {
           ID         int64          `gorm:"primaryKey;autoIncrement" json:"id"`
           OrgID      int64          `gorm:"column:org_id;index;not null" json:"org_id"`
           UserID     int64          `gorm:"column:user_id;index;not null" json:"user_id"`
           Module     string         `gorm:"column:module;type:varchar(50);not null" json:"module"`
           Action     string         `gorm:"column:action;type:varchar(20);not null" json:"action"`  // create/update/delete
           TargetType string         `gorm:"column:target_type;type:varchar(50);not null" json:"target_type"`
           TargetID   int64          `gorm:"column:target_id;not null" json:"target_id"`
           Detail     datatypes.JSON `gorm:"column:detail;type:jsonb" json:"detail"`
           IPAddress  string         `gorm:"column:ip_address;type:varchar(45)" json:"ip_address"`
           CreatedAt  time.Time      `gorm:"column:created_at;autoCreateTime" json:"created_at"`
       }
       ```
       - 表名 `audit_logs`
       - 无软删除（INSERT ONLY per D-23）
       - 索引：org_id, user_id, module, action, created_at

    9. 创建 `internal/audit/repository.go`：
       ```go
       type Repository interface {
           List(orgID int64, module, action string, page, pageSize int) ([]AuditLog, int64, error)
       }
       ```
       - List 方法使用 TenantScope(orgID) 过滤
       - 支持按 module、action 筛选
       - 按 created_at DESC 排序

    10. 创建 `internal/audit/handler.go`：
        - GET /api/v1/audit-logs -> 查询审计日志（需认证，RequireRole("owner", "admin") per D-30）
        - 分页参数：page, page_size, module, action

    11. 创建 `internal/audit/handler_test.go`：测试查询接口的参数校验和分页。

    12. 在 `cmd/server/main.go` 中注册审计日志 GORM Callback per D-22：
        ```go
        // 注册 GORM Hook
        db.Callback().Create().After("gorm:create").Register("audit:log", auditCallback)
        db.Callback().Update().After("gorm:update").Register("audit:log", auditCallback)
        db.Callback().Delete().After("gorm:delete").Register("audit:log", auditCallback)
        ```
        auditCallback 函数从 `*gorm.DB` 的 Context 中获取 user_id, org_id，解析表名为 target_type，提取操作类型，写入 audit_logs 表。

    13. 更新 `cmd/server/main.go`：
        - 注册 Auth 中间件到需要认证的路由组
        - 注册 RBAC 中间件到需要角色控制的路由
        - 注册 user.Handler 的路由
        - 注册 city.Handler 的路由（无需认证）
        - 注册 audit handler 的路由
        - 添加 User, Organization, AuditLog 模型的 AutoMigrate

    14. 注意：审计日志的 GORM Hook 不应在审计日志自身的写操作上触发（防止递归）。实现时检查 target_type 不为 `audit_logs`。
  </action>
  <verify>
    <automated>cd /Users/wencai/github/EasyHR && go test ./internal/user/... ./internal/common/middleware/... ./internal/city/... ./internal/audit/... -v -short</automated>
  </verify>
  <done>
    - 用户认证路由完整注册 per D-21
    - 子账号管理路由（POST/PUT/DELETE users）仅 OWNER 可访问 per D-30
    - 用户列表查看路由（GET users）OWNER 和 ADMIN 可访问 per D-30
    - RequireRole 中间件正确放行/拒绝角色 per D-12/D-13
    - 城市列表 API 返回完整城市数据 per AUTH-03
    - 审计日志在 Create/Update/Delete 后自动记录 per D-22
    - 审计日志字段完整 per D-22
    - 审计日志只能查询不能删除 per D-23
    - `go test ./internal/... -v` 全部通过
  </done>
</task>

</tasks>

<verification>
go build ./cmd/server
go test ./internal/user/... ./internal/common/middleware/... ./internal/city/... ./internal/audit/... -v -short
</verification>

<success_criteria>
1. 手机号+验证码登录/自动注册流程完整 per AUTH-01/D-05/D-08
2. 首次登录引导企业信息录入 per AUTH-02
3. 城市列表 API 返回完整城市数据 per AUTH-03
4. Token 刷新轮转和退出登录 per AUTH-04/D-28/D-29
5. OWNER 可管理子账号，ADMIN 不可管理子账号（仅可查看列表）per PLAT-01/D-30
6. RequireRole 中间件正确执行 RBAC per D-13/D-30
7. 审计日志自动记录所有写操作 per PLAT-02/D-22
8. Repository 层所有查询使用 TenantScope per PLAT-06/D-09
9. `go test ./internal/... -v` 全部通过
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-03-SUMMARY.md`
</output>
